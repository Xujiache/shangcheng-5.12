.doc__updated {
  font-size: 12px;
  color: var(--faint);
  padding: 4px 2px 10px;
}
.doc__sec {
  margin-top: 18px;
}
.doc__h {
  font-size: 15.5px;
  font-weight: 700;
  color: var(--text);
  margin-bottom: 8px;
}
.doc__p {
  font-size: 13.5px;
  color: var(--muted);
  line-height: 1.75;
  margin-bottom: 8px;
}
.doc__foot {
  font-size: 12px;
  color: var(--faint);
  line-height: 1.6;
  text-align: center;
  padding: 26px 10px 4px;
}
