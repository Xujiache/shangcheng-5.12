diff --git a/native/harmony-merchant/entry/src/main/ets/core/navigation/AppRouter.ets b/native/harmony-merchant/entry/src/main/ets/core/navigation/AppRouter.ets
index 222fab83..a7aa2a84 100644
--- a/native/harmony-merchant/entry/src/main/ets/core/navigation/AppRouter.ets
+++ b/native/harmony-merchant/entry/src/main/ets/core/navigation/AppRouter.ets
@@ -1,11 +1,18 @@
 export class AppRouter {
   private static stack?: NavPathStack;
+  private static lastPush: string = '';
+  private static lastPushAt: number = 0;
 
   static attach(stack: NavPathStack): void {
     AppRouter.stack = stack;
   }
 
   static push(name: string, param?: Object): void {
+    if (!AppRouter.stack) return;
+    const key = `${name}:${JSON.stringify(param || {})}`;
+    const now = Date.now();
+    if (key === AppRouter.lastPush && now - AppRouter.lastPushAt < 400) return;
+    AppRouter.lastPush = key; AppRouter.lastPushAt = now;
     AppRouter.stack?.pushPathByName(name, param, true);
   }
 
@@ -14,13 +21,14 @@ export class AppRouter {
   }
 
   static back(result?: Object): void {
+    AppRouter.lastPush = '';
     if (!AppRouter.stack) return;
     if (result) AppRouter.stack.pop(result, true);
     else AppRouter.stack.pop(true);
   }
 
   static clear(): void {
+    AppRouter.lastPush = '';
     AppRouter.stack?.clear(true);
   }
 }
-
diff --git a/native/harmony-merchant/entry/src/main/ets/features/auth/ChangePhonePage.ets b/native/harmony-merchant/entry/src/main/ets/features/auth/ChangePhonePage.ets
index 82e640ff..ba19f2f1 100644
--- a/native/harmony-merchant/entry/src/main/ets/features/auth/ChangePhonePage.ets
+++ b/native/harmony-merchant/entry/src/main/ets/features/auth/ChangePhonePage.ets
@@ -1,4 +1,7 @@
-import { IBestButton, IBestField, IBestNavBar, IBestToast } from '@ibestservices/ibest-ui';
+import { LeaveGuard } from '../../core/ui/LeaveGuard';
+import { PageReadScope, SecondaryPolicy } from '../../core/ui/SecondaryPolicy';
+import { SecondaryFeedback, SecondaryHeader } from '../../core/ui/SecondaryPage';
+import { IBestButton, IBestField, IBestToast } from '@ibestservices/ibest-ui';
 import { AppRouter } from '../../core/navigation/AppRouter';
 import { ApiError } from '../../core/network/ApiClient';
 import { SessionStore } from '../../core/storage/SessionStore';
@@ -13,6 +16,16 @@ export function ChangePhoneBuilder(_: string) { ChangePhonePage(); }
 
 @Component
 struct ChangePhonePage {
+  private handleBack(): void {
+
+
+    this.leaveGuard.request(this.language, this.saving || this.sendingNew || this.sendingOld, Boolean(this.newPhone || this.newCode || this.oldCode), () => AppRouter.back());
+  }
+
+  private leaveGuard: LeaveGuard = new LeaveGuard();
+  private pageScope: PageReadScope = new PageReadScope();
+  @State private pageError: string = '';
+
   @StorageProp('safeBottom') private safeBottom: number = 16;
   @StorageLink('language') private language: string = 'zh-CN';
   @StorageLink('darkMode') private darkMode: boolean = false;
@@ -29,24 +42,28 @@ struct ChangePhonePage {
   private oldTimer: number = -1;
   private newTimer: number = -1;
 
-  aboutToAppear(): void {
+  aboutToAppear(): void { this.pageScope.open();
     this.currentPhone = SessionStore.shared().user?.phone || '';
     this.step = this.currentPhone ? 0 : 1;
     if (!this.currentPhone) this.loadUser();
   }
 
-  aboutToDisappear(): void {
+  aboutToDisappear(): void { this.pageScope.dispose();
     if (this.oldTimer >= 0) clearInterval(this.oldTimer);
     if (this.newTimer >= 0) clearInterval(this.newTimer);
   }
 
   private async loadUser(): Promise<void> {
+    const pageTicket = this.pageScope.begin('loadUser');
+    this.pageError = '';
     try {
-      const user = await AuthRepository.userInfo();
-      await SessionStore.shared().updateUser(user);
+      const user = await this.pageScope.read(AuthRepository.userInfo(), pageTicket);
+      await this.pageScope.read(SessionStore.shared().updateUser(user), pageTicket);
       this.currentPhone = user.phone || '';
       this.step = this.currentPhone ? 0 : 1;
-    } catch (_) {}
+    } catch (_) {
+      if (!this.pageScope.accepts(pageTicket)) return;
+      this.pageError = SecondaryPolicy.errorKind(_);}
   }
 
   private continueToNewPhone(): void {
@@ -83,7 +100,7 @@ struct ChangePhonePage {
       IBestToast.show(I18n.text(this.language, '验证码已发送到原手机号', 'Verification code sent to your current phone'));
       this.startOldCountdown();
     } catch (error) {
-      IBestToast.show(error instanceof ApiError ? error.message : I18n.text(this.language, '验证码发送失败', 'Failed to send verification code'));
+      IBestToast.show(error instanceof ApiError ? SecondaryPolicy.message(error, this.language) : I18n.text(this.language, '验证码发送失败', 'Failed to send verification code'));
     } finally { this.sendingOld = false; }
   }
 
@@ -96,7 +113,7 @@ struct ChangePhonePage {
       IBestToast.show(I18n.text(this.language, '验证码已发送到新手机号', 'Verification code sent to your new phone'));
       this.startNewCountdown();
     } catch (error) {
-      IBestToast.show(error instanceof ApiError ? error.message : I18n.text(this.language, '验证码发送失败', 'Failed to send verification code'));
+      IBestToast.show(error instanceof ApiError ? SecondaryPolicy.message(error, this.language) : I18n.text(this.language, '验证码发送失败', 'Failed to send verification code'));
     } finally { this.sendingNew = false; }
   }
 
@@ -117,24 +134,25 @@ struct ChangePhonePage {
       IBestToast.show(I18n.text(this.language, '手机号修改成功', 'Phone number changed'));
       AppRouter.back();
     } catch (error) {
-      IBestToast.show(error instanceof ApiError ? error.message : I18n.text(this.language, '手机号修改失败', 'Failed to change phone number'));
+      IBestToast.show(error instanceof ApiError ? SecondaryPolicy.message(error, this.language) : I18n.text(this.language, '手机号修改失败', 'Failed to change phone number'));
     } finally { this.saving = false; }
   }
 
   build() {
     NavDestination() {
       Column() {
-        IBestNavBar({
+        SecondaryHeader({
           title: I18n.text(this.language, '修改手机号', 'Change phone number'), isShowStatusBar: true,
-          navBarBgColor: DesignTokens.surface(this.darkMode), onLeftClick: () => AppRouter.back()
+          navBarBgColor: DesignTokens.surface(this.darkMode), onLeftClick: () => this.handleBack()
         });
+          SecondaryFeedback({ errorKind: this.pageError, onRetry: () => this.loadUser() });
         Scroll() {
           Column({ space: 16 }) {
             Row() {
               Column({ space: 5 }) {
                 Text('1').fontSize(12).fontWeight(FontWeight.Bold)
                   .fontColor(this.step === 0 ? DesignTokens.onBrand(this.darkMode) : DesignTokens.brand(this.darkMode));
-                Text(I18n.text(this.language, '验证原手机', 'Verify current phone')).fontSize(10)
+                Text(I18n.text(this.language, '验证原手机', 'Verify current phone')).fontSize(12)
                   .fontColor(this.step === 0 ? DesignTokens.text(this.darkMode) : DesignTokens.muted(this.darkMode));
               }.layoutWeight(1).height(48).justifyContent(FlexAlign.Center)
                 .backgroundColor(this.step === 0 ? DesignTokens.brand(this.darkMode) : DesignTokens.selectedSurface(this.darkMode))
@@ -142,7 +160,7 @@ struct ChangePhonePage {
               Column({ space: 5 }) {
                 Text('2').fontSize(12).fontWeight(FontWeight.Bold)
                   .fontColor(this.step === 1 ? DesignTokens.onBrand(this.darkMode) : DesignTokens.muted(this.darkMode));
-                Text(I18n.text(this.language, '绑定新手机', 'Link new phone')).fontSize(10)
+                Text(I18n.text(this.language, '绑定新手机', 'Link new phone')).fontSize(12)
                   .fontColor(this.step === 1 ? DesignTokens.text(this.darkMode) : DesignTokens.muted(this.darkMode));
               }.layoutWeight(1).height(48).justifyContent(FlexAlign.Center)
                 .backgroundColor(this.step === 1 ? DesignTokens.brand(this.darkMode) : DesignTokens.subtle(this.darkMode))
@@ -160,33 +178,33 @@ struct ChangePhonePage {
                 ? I18n.text(this.language, `验证码将发送至 ${this.currentPhone}`, `A code will be sent to ${this.currentPhone}`)
                 : I18n.text(this.language, '新手机号将用于后续登录与安全验证', 'The new number will be used for sign-in and verification'))
                 .fontSize(12).fontColor(DesignTokens.muted(this.darkMode)).textAlign(TextAlign.Center);
-            }.width('100%').padding(20).backgroundColor(DesignTokens.card(this.darkMode))
+            }.width('100%').padding(16).backgroundColor(DesignTokens.card(this.darkMode))
               .borderRadius(DesignTokens.RADIUS_XL).border({ width: 1, color: DesignTokens.border(this.darkMode) });
             if (this.step === 0 && this.currentPhone) {
               Column({ space: 14 }) {
               Row({ space: 8 }) {
                 IBestField({ value: $oldCode, label: I18n.text(this.language, '原手机验证码', 'Current phone code'), placeholder: I18n.text(this.language, '4–6位验证码', '4–6 digit code'), type: 'number' });
-                IBestButton({ text: this.oldCountdown > 0 ? I18n.text(this.language, `${this.oldCountdown}秒`, `${this.oldCountdown}s`) : I18n.text(this.language, '发送', 'Send'), plain: true, color: DesignTokens.brand(this.darkMode), loading: this.sendingOld, disabled: this.oldCountdown > 0, onBtnClick: () => this.sendOldCode() });
+                IBestButton({ btnHeight: 44, text: this.oldCountdown > 0 ? I18n.text(this.language, `${this.oldCountdown}秒`, `${this.oldCountdown}s`) : I18n.text(this.language, '发送', 'Send'), plain: true, color: DesignTokens.brand(this.darkMode), loading: this.sendingOld, disabled: this.oldCountdown > 0, onBtnClick: () => this.sendOldCode() });
               }.width('100%');
-                IBestButton({ text: I18n.text(this.language, '下一步', 'Continue'), type: 'primary',
+                IBestButton({ btnHeight: 44, text: I18n.text(this.language, '下一步', 'Continue'), type: 'primary',
                   color: DesignTokens.brand(this.darkMode), fontColor: DesignTokens.onBrand(this.darkMode),
                   btnWidth: '100%', onBtnClick: () => this.continueToNewPhone() });
               }.width('100%').padding(16).backgroundColor(DesignTokens.card(this.darkMode))
-                .borderRadius(DesignTokens.RADIUS_LARGE).border({ width: 1, color: DesignTokens.border(this.darkMode) });
+                .borderRadius(16).border({ width: 1, color: DesignTokens.border(this.darkMode) });
             } else {
               Column({ space: 14 }) {
                 IBestField({ value: $newPhone, label: I18n.text(this.language, '新手机号', 'New phone number'), placeholder: I18n.text(this.language, '请输入11位手机号', 'Enter an 11-digit mobile number'), type: 'number' });
                 Row({ space: 8 }) {
                   IBestField({ value: $newCode, label: I18n.text(this.language, '新手机验证码', 'New phone code'), placeholder: I18n.text(this.language, '4–6位验证码', '4–6 digit code'), type: 'number' });
-                  IBestButton({ text: this.newCountdown > 0 ? I18n.text(this.language, `${this.newCountdown}秒`, `${this.newCountdown}s`) : I18n.text(this.language, '发送', 'Send'), plain: true, color: DesignTokens.brand(this.darkMode), loading: this.sendingNew, disabled: this.newCountdown > 0, onBtnClick: () => this.sendNewCode() });
+                  IBestButton({ btnHeight: 44, text: this.newCountdown > 0 ? I18n.text(this.language, `${this.newCountdown}秒`, `${this.newCountdown}s`) : I18n.text(this.language, '发送', 'Send'), plain: true, color: DesignTokens.brand(this.darkMode), loading: this.sendingNew, disabled: this.newCountdown > 0, onBtnClick: () => this.sendNewCode() });
                 }.width('100%');
-                IBestButton({ text: I18n.text(this.language, '确认更换', 'Confirm change'), type: 'primary', color: DesignTokens.brand(this.darkMode), fontColor: DesignTokens.onBrand(this.darkMode), btnWidth: '100%', loading: this.saving, disabled: this.saving, onBtnClick: () => this.submit() });
+                IBestButton({ btnHeight: 44, text: I18n.text(this.language, '确认更换', 'Confirm change'), type: 'primary', color: DesignTokens.brand(this.darkMode), fontColor: DesignTokens.onBrand(this.darkMode), btnWidth: '100%', loading: this.saving, disabled: this.saving, onBtnClick: () => this.submit() });
                 if (this.currentPhone) {
                   Text(I18n.text(this.language, '返回上一步', 'Back to previous step')).fontSize(13)
                     .fontColor(DesignTokens.brand(this.darkMode)).onClick(() => this.step = 0);
                 }
               }.width('100%').padding(16).backgroundColor(DesignTokens.card(this.darkMode))
-                .borderRadius(DesignTokens.RADIUS_LARGE).border({ width: 1, color: DesignTokens.border(this.darkMode) });
+                .borderRadius(16).border({ width: 1, color: DesignTokens.border(this.darkMode) });
             }
             Text(I18n.text(this.language,
               '为保护账号安全，已绑定手机号时必须同时验证原手机号与新手机号。',
@@ -196,6 +214,6 @@ struct ChangePhonePage {
             .padding({ left: 18, right: 18, top: 18, bottom: this.safeBottom + 24 });
         }.layoutWeight(1).align(Alignment.Top);
       }.width('100%').height('100%').backgroundColor(DesignTokens.page(this.darkMode));
-    }.hideTitleBar(true);
+    }.hideTitleBar(true).onBackPressed((): boolean => { this.handleBack(); return true; });
   }
 }
diff --git a/native/harmony-merchant/entry/src/main/ets/features/auth/MerchantApplyPage.ets b/native/harmony-merchant/entry/src/main/ets/features/auth/MerchantApplyPage.ets
index c13db42f..ad34f242 100644
--- a/native/harmony-merchant/entry/src/main/ets/features/auth/MerchantApplyPage.ets
+++ b/native/harmony-merchant/entry/src/main/ets/features/auth/MerchantApplyPage.ets
@@ -1,7 +1,9 @@
+import { SecondaryPolicy } from '../../core/ui/SecondaryPolicy';
+import { LeaveGuard } from '../../core/ui/LeaveGuard';
+import { SecondaryHeader } from '../../core/ui/SecondaryPage';
 import {
   IBestButton,
   IBestField,
-  IBestNavBar,
   IBestSteps,
   IBestStep,
   IBestToast
@@ -25,6 +27,13 @@ export function MerchantApplyBuilder(_: string) {
 
 @Component
 struct MerchantApplyPage {
+  private handleBack(): void {
+
+
+    this.leaveGuard.request(this.language, this.submitting || this.sendingCode, !this.applyId && Boolean(this.phone || this.merchantName || this.smsCode), () => AppRouter.back());
+  }
+
+  private leaveGuard: LeaveGuard = new LeaveGuard();
   @StorageProp('safeBottom') private safeBottom: number = 16;
   @StorageLink('language') private language: string = 'zh-CN';
   @StorageLink('darkMode') private darkMode: boolean = false;
@@ -90,7 +99,7 @@ struct MerchantApplyPage {
         }
       }, 1000);
     } catch (error) {
-      IBestToast.show(error instanceof ApiError ? error.message : I18n.text(this.language, '验证码发送失败', 'Failed to send verification code'));
+      IBestToast.show(error instanceof ApiError ? SecondaryPolicy.message(error, this.language) : I18n.text(this.language, '验证码发送失败', 'Failed to send verification code'));
     } finally {
       this.sendingCode = false;
     }
@@ -108,7 +117,7 @@ struct MerchantApplyPage {
       this.accountHasPassword = session.user.hasPassword === true;
       this.step = this.accountHasPassword ? 2 : 1;
     } catch (error) {
-      IBestToast.show(error instanceof ApiError ? error.message : I18n.text(this.language, '手机号验证失败', 'Phone verification failed'));
+      IBestToast.show(error instanceof ApiError ? SecondaryPolicy.message(error, this.language) : I18n.text(this.language, '手机号验证失败', 'Phone verification failed'));
     } finally {
       this.submitting = false;
     }
@@ -160,7 +169,7 @@ struct MerchantApplyPage {
       this.confirmPassword = '';
       this.step = 3;
     } catch (error) {
-      const message = error instanceof ApiError ? error.message
+      const message = error instanceof ApiError ? SecondaryPolicy.message(error, this.language)
         : I18n.text(this.language, '入驻申请提交失败', 'Failed to submit merchant application');
       if (error instanceof ApiError && error.code === 2001) {
         AuthRepository.discardApplicationSession();
@@ -190,7 +199,7 @@ struct MerchantApplyPage {
       IBestField({ value: $smsCode, label: I18n.text(this.language, '验证码', 'Verification code'), placeholder: I18n.text(this.language, '请输入短信验证码', 'Enter the SMS code'), type: 'number' });
       Row() {
         Blank();
-        IBestButton({
+        IBestButton({ btnHeight: 44,
           text: this.countdown > 0
             ? I18n.text(this.language, `${this.countdown}秒后重发`, `Resend in ${this.countdown}s`)
             : (this.sendingCode
@@ -204,7 +213,7 @@ struct MerchantApplyPage {
           onBtnClick: () => this.sendCode()
         });
       }
-      IBestButton({
+      IBestButton({ btnHeight: 44,
         text: I18n.text(this.language, '验证手机号', 'Verify phone'), type: 'primary', color: DesignTokens.brand(this.darkMode), fontColor: DesignTokens.onBrand(this.darkMode),
         btnWidth: '100%', loading: this.submitting, disabled: this.submitting, onBtnClick: () => this.verifyPhone()
       });
@@ -218,7 +227,7 @@ struct MerchantApplyPage {
         .fontSize(14).fontColor(DesignTokens.muted(this.darkMode)).width('100%');
       IBestField({ value: $password, label: I18n.text(this.language, '新密码', 'New password'), placeholder: I18n.text(this.language, '请输入6至32位密码', 'Enter a 6–32 character password'), type: 'password' });
       IBestField({ value: $confirmPassword, label: I18n.text(this.language, '确认密码', 'Confirm password'), placeholder: I18n.text(this.language, '请再次输入密码', 'Enter the password again'), type: 'password' });
-      IBestButton({
+      IBestButton({ btnHeight: 44,
         text: I18n.text(this.language, '下一步', 'Next'), type: 'primary', color: DesignTokens.brand(this.darkMode), fontColor: DesignTokens.onBrand(this.darkMode),
         btnWidth: '100%', onBtnClick: () => this.continuePassword()
       });
@@ -229,13 +238,13 @@ struct MerchantApplyPage {
   private profileStep() {
     Column({ space: 12 }) {
       Row({ space: 10 }) {
-        IBestButton({
+        IBestButton({ btnHeight: 44,
           text: I18n.text(this.language, '厂家', 'Factory'), buttonSize: 'small',
           type: this.merchantType === 'factory' ? 'primary' : 'default',
           color: this.merchantType === 'factory' ? DesignTokens.brand(this.darkMode) : '',
           onBtnClick: () => this.merchantType = 'factory'
         });
-        IBestButton({
+        IBestButton({ btnHeight: 44,
           text: I18n.text(this.language, '门店', 'Store'), buttonSize: 'small',
           type: this.merchantType === 'store' ? 'primary' : 'default',
           color: this.merchantType === 'store' ? DesignTokens.brand(this.darkMode) : '',
@@ -260,7 +269,7 @@ struct MerchantApplyPage {
           }, (item: string) => item);
         }.width('100%');
       }.width('100%').alignItems(HorizontalAlign.Start);
-      IBestButton({
+      IBestButton({ btnHeight: 44,
         text: I18n.text(this.language, '提交入驻申请', 'Submit application'), type: 'primary', color: DesignTokens.brand(this.darkMode), fontColor: DesignTokens.onBrand(this.darkMode),
         btnWidth: '100%', loading: this.submitting, disabled: this.submitting, onBtnClick: () => this.submitApplication()
       });
@@ -277,7 +286,7 @@ struct MerchantApplyPage {
       if (this.applyId) {
         Text(I18n.text(this.language, `申请编号：${this.applyId}`, `Application ID: ${this.applyId}`)).fontSize(12).fontColor(DesignTokens.muted(this.darkMode));
       }
-      IBestButton({
+      IBestButton({ btnHeight: 44,
         text: I18n.text(this.language, '返回登录', 'Back to sign in'), type: 'primary', color: DesignTokens.brand(this.darkMode), fontColor: DesignTokens.onBrand(this.darkMode),
         onBtnClick: () => AppRouter.back()
       });
@@ -289,11 +298,11 @@ struct MerchantApplyPage {
   build() {
     NavDestination() {
       Column() {
-        IBestNavBar({
+        SecondaryHeader({
           title: I18n.text(this.language, '商家入驻', 'Merchant application'),
           isShowStatusBar: true,
           navBarBgColor: DesignTokens.surface(this.darkMode),
-          onLeftClick: () => AppRouter.back()
+          onLeftClick: () => this.handleBack()
         });
         Scroll() {
           Column({ space: 22 }) {
@@ -310,9 +319,9 @@ struct MerchantApplyPage {
               else this.successStep();
             }
             .width('100%')
-            .padding(18)
+            .padding(16)
             .backgroundColor(DesignTokens.card(this.darkMode))
-            .borderRadius(DesignTokens.RADIUS_LARGE);
+            .borderRadius(16);
           }
           .width('100%')
           .padding({ left: 16, right: 16, top: 18, bottom: this.safeBottom + 24 });
@@ -325,6 +334,6 @@ struct MerchantApplyPage {
       .height('100%')
       .backgroundColor(DesignTokens.page(this.darkMode));
     }
-    .hideTitleBar(true);
+    .hideTitleBar(true).onBackPressed((): boolean => { this.handleBack(); return true; });
   }
 }
diff --git a/native/harmony-merchant/entry/src/main/ets/features/auth/SetPasswordPage.ets b/native/harmony-merchant/entry/src/main/ets/features/auth/SetPasswordPage.ets
index 01538e17..6f4d0371 100644
--- a/native/harmony-merchant/entry/src/main/ets/features/auth/SetPasswordPage.ets
+++ b/native/harmony-merchant/entry/src/main/ets/features/auth/SetPasswordPage.ets
@@ -1,4 +1,6 @@
-import { IBestButton, IBestField, IBestNavBar, IBestToast } from '@ibestservices/ibest-ui';
+import { SecondaryPolicy } from '../../core/ui/SecondaryPolicy';
+import { SecondaryHeader } from '../../core/ui/SecondaryPage';
+import { IBestButton, IBestField, IBestToast } from '@ibestservices/ibest-ui';
 import { AppRouter } from '../../core/navigation/AppRouter';
 import { ApiError } from '../../core/network/ApiClient';
 import { DesignTokens } from '../../core/theme/DesignTokens';
@@ -76,7 +78,7 @@ struct SetPasswordPage {
         AppStorage.setOrCreate<number>('sessionEpoch', epoch + 1);
       }
     } catch (error) {
-      IBestToast.show(error instanceof ApiError ? error.message : I18n.text(this.language, '密码设置失败', 'Failed to save password'));
+      IBestToast.show(error instanceof ApiError ? SecondaryPolicy.message(error, this.language) : I18n.text(this.language, '密码设置失败', 'Failed to save password'));
     } finally {
       this.submitting = false;
     }
@@ -85,7 +87,7 @@ struct SetPasswordPage {
   build() {
     NavDestination() {
       Column() {
-        IBestNavBar({
+        SecondaryHeader({
           title: this.hasPassword
             ? I18n.text(this.language, '修改登录密码', 'Change sign-in password')
             : I18n.text(this.language, '设置登录密码', 'Set sign-in password'), isShowStatusBar: true,
@@ -106,7 +108,7 @@ struct SetPasswordPage {
                 ? I18n.text(this.language, '修改密码需要验证当前密码', 'Verify your current password before changing it')
                 : I18n.text(this.language, '首次设置后即可使用手机号和密码登录', 'After setting a password, you can sign in with your phone number and password'))
                 .fontSize(12).fontColor(DesignTokens.muted(this.darkMode)).textAlign(TextAlign.Center);
-            }.width('100%').padding(20).backgroundColor(DesignTokens.card(this.darkMode))
+            }.width('100%').padding(16).backgroundColor(DesignTokens.card(this.darkMode))
               .borderRadius(DesignTokens.RADIUS_XL).border({ width: 1, color: DesignTokens.border(this.darkMode) });
             Column({ space: 4 }) {
               if (this.hasPassword) {
@@ -115,15 +117,15 @@ struct SetPasswordPage {
               IBestField({ value: $password, label: I18n.text(this.language, '新密码', 'New password'), placeholder: I18n.text(this.language, '请输入6至32位密码', 'Enter a 6–32 character password'), type: 'password' });
               IBestField({ value: $confirmPassword, label: I18n.text(this.language, '确认密码', 'Confirm password'), placeholder: I18n.text(this.language, '请再次输入密码', 'Enter the password again'), type: 'password', hasBorder: false });
             }.width('100%').padding(6).backgroundColor(DesignTokens.card(this.darkMode))
-              .borderRadius(DesignTokens.RADIUS_LARGE).border({ width: 1, color: DesignTokens.border(this.darkMode) });
+              .borderRadius(16).border({ width: 1, color: DesignTokens.border(this.darkMode) });
             Row({ space: 8 }) {
               MerchantIcon({ name: 'info-o', color: DesignTokens.brand(this.darkMode), iconSize: 16 });
               Text(I18n.text(this.language,
                 '密码长度为6至32位，请勿与其他平台使用相同密码。',
                 'Use 6–32 characters and avoid reusing a password from another service.'))
-                .fontSize(11).fontColor(DesignTokens.muted(this.darkMode)).layoutWeight(1).lineHeight(18);
+                .fontSize(12).fontColor(DesignTokens.muted(this.darkMode)).layoutWeight(1).lineHeight(18);
             }.width('100%').padding(12).backgroundColor(DesignTokens.subtle(this.darkMode)).borderRadius(14);
-            IBestButton({
+            IBestButton({ btnHeight: 44,
               text: I18n.text(this.language, '保存密码', 'Save password'), type: 'primary', color: DesignTokens.brand(this.darkMode), fontColor: DesignTokens.onBrand(this.darkMode),
               btnWidth: '100%', loading: this.submitting, disabled: this.submitting, onBtnClick: () => this.submit()
             });
diff --git a/native/harmony-merchant/entry/src/main/ets/features/business/CommissionPage.ets b/native/harmony-merchant/entry/src/main/ets/features/business/CommissionPage.ets
index 294048db..2ff4a16b 100644
--- a/native/harmony-merchant/entry/src/main/ets/features/business/CommissionPage.ets
+++ b/native/harmony-merchant/entry/src/main/ets/features/business/CommissionPage.ets
@@ -1,5 +1,9 @@
+import { ResilientRemoteImage } from '../../core/ui/ResilientRemoteImage';
+import { LeaveGuard } from '../../core/ui/LeaveGuard';
+import { PageReadScope, SecondaryPolicy } from '../../core/ui/SecondaryPolicy';
+import { SecondaryFeedback, SecondaryHeader, SecondarySaveBar } from '../../core/ui/SecondaryPage';
 import {
-  IBestButton, IBestEmpty, IBestField, IBestNavBar, IBestSwitch, IBestToast
+  IBestButton, IBestEmpty, IBestField, IBestSwitch, IBestToast
 } from '@ibestservices/ibest-ui';
 import { AppRouter } from '../../core/navigation/AppRouter';
 import { ApiError } from '../../core/network/ApiClient';
@@ -15,6 +19,24 @@ export function CommissionBuilder(_: string) { CommissionPage(); }
 
 @Component
 struct CommissionPage {
+  @StorageProp('windowWidth') private windowWidth: number = 390;
+  @State private verifying: boolean = false;
+  private handleBack(): void {
+    if (this.editorVisible) { this.closeLocalEditor(); return; }
+
+    this.leaveGuard.request(this.language, this.saving, this.dirty, () => AppRouter.back());
+  }
+
+  private editStamp: string = '';
+  private localStamp(): string { return JSON.stringify([this.editProductId, this.ruleFirst, this.ruleSecond]); }
+  private closeLocalEditor(): void {
+    this.leaveGuard.request(this.language, this.saving, this.localStamp() !== this.editStamp, () => { this.editorVisible = false; });
+  }
+
+  private leaveGuard: LeaveGuard = new LeaveGuard();
+  private pageScope: PageReadScope = new PageReadScope();
+  @State private pageError: string = '';
+
   @StorageProp('safeBottom') private safeBottom: number = 16;
   @StorageLink('language') private language: string = 'zh-CN';
   @StorageLink('darkMode') private darkMode: boolean = false;
@@ -31,40 +53,42 @@ struct CommissionPage {
   @State private editProductId: string = '';
   @State private ruleFirst: string = '0';
   @State private ruleSecond: string = '0';
-  private loaded: boolean = false;
-  private dirty: boolean = false;
-  private saveTimer: number = -1;
+  @State private loaded: boolean = false;
+  @State private dirty: boolean = false;
+  private savedRules: string = '';
 
-  aboutToAppear(): void { this.load(); }
+  aboutToAppear(): void { this.pageScope.open(); this.load(); }
 
-  aboutToDisappear(): void {
-    if (this.saveTimer >= 0) {
-      clearTimeout(this.saveTimer);
-      this.saveTimer = -1;
-    }
-    if (this.dirty) this.save(false);
-  }
+  aboutToDisappear(): void { this.pageScope.dispose();}
 
   private async load(): Promise<void> {
-    this.loaded = false;
+    if (!this.pageScope.isCurrent()) return;
+    const pageTicket = this.pageScope.begin('load');
+    this.pageError = '';
     this.loading = true;
     try {
-      const result = await BusinessRepository.commissionRules();
-      this.first = `${result.default.level1Percent}`;
-      this.second = `${result.default.level2Percent}`;
-      this.visible = result.default.visibleToPromoter;
-      this.allowOffline = result.default.allowOffline;
-      this.commissionEnabled = result.default.enabled;
-      this.productRules = result.productRules;
-      const productPage = await ProductRepository.list('', '', 1, 100);
+      const result = await this.pageScope.read(BusinessRepository.commissionRules(), pageTicket);
+      if (!result.default || !Array.isArray(result.productRules)) throw new Error('invalid-commission');
+      this.savedRules = JSON.stringify([`${result.default.level1Percent}`, `${result.default.level2Percent}`,
+        result.default.visibleToPromoter, result.default.allowOffline, result.default.enabled, result.productRules]);
+      if (!this.dirty) {
+        this.first = `${result.default.level1Percent}`; this.second = `${result.default.level2Percent}`;
+        this.visible = result.default.visibleToPromoter; this.allowOffline = result.default.allowOffline;
+        this.commissionEnabled = result.default.enabled; this.productRules = result.productRules;
+      }
+      this.loaded = true; this.verifying = false; this.dirty = this.fingerprint() !== this.savedRules;
+      const productPage = await this.pageScope.read(ProductRepository.list('', '', 1, 100), pageTicket);
+      if (!Array.isArray(productPage.list)) throw new Error('invalid-products');
       this.products = productPage.list;
-      this.dirty = false;
-      this.loaded = true;
     } catch (error) {
-      IBestToast.show(error instanceof ApiError ? error.message : I18n.text(this.language, '佣金规则加载失败', 'Failed to load commission rules'));
+      if (!this.pageScope.accepts(pageTicket)) return;
+      this.pageError = SecondaryPolicy.errorKind(error);
+      IBestToast.show(error instanceof ApiError ? SecondaryPolicy.message(error, this.language) : I18n.text(this.language, '佣金规则加载失败', 'Failed to load commission rules'));
     } finally {
+      if (this.pageScope.accepts(pageTicket)) {
       this.loading = false;
     }
+    }
   }
 
   private validateRates(level1: string, level2: string): boolean {
@@ -76,51 +100,45 @@ struct CommissionPage {
 
   private scheduleSave(): void {
     if (!this.loaded) return;
-    this.dirty = true;
-    if (this.saveTimer >= 0) clearTimeout(this.saveTimer);
-    this.saveTimer = setTimeout((): void => {
-      this.saveTimer = -1;
-      this.save(false);
-    }, 300);
+    this.dirty = this.fingerprint() !== this.savedRules;
+  }
+  private fingerprint(): string {
+    return JSON.stringify([this.first, this.second, this.visible, this.allowOffline, this.commissionEnabled, this.productRules]);
   }
 
   private async save(showFeedback: boolean = true): Promise<void> {
-    if (this.saving) {
-      this.dirty = true;
-      return;
-    }
+    if (this.saving) { return; }
+    if (!this.loaded || !this.dirty || this.verifying || this.loading) return;
+    if (!this.pageScope.isCurrent()) return;
+    const writeTicket = this.pageScope.begin('save');
     if (!this.validateRates(this.first, this.second)) {
-      if (showFeedback) IBestToast.show(I18n.text(this.language, '佣金比例必须为0至100，两级合计不得超过100', 'Commission rates must be 0–100 and the two levels cannot exceed 100 in total'));
+      IBestToast.show(I18n.text(this.language, '佣金比例须为0至100，两级合计不超过100', 'Rates must be 0–100; combined rates cannot exceed 100'));
       return;
     }
+    const submitted = this.fingerprint();
     const body: CommissionRuleBundle = {
-      default: {
-        level1Percent: Number(this.first), level2Percent: Number(this.second),
-        visibleToPromoter: this.visible, allowOffline: this.allowOffline, enabled: this.commissionEnabled
-      },
-      productRules: this.productRules
+      default: { level1Percent: Number(this.first), level2Percent: Number(this.second), visibleToPromoter: this.visible, allowOffline: this.allowOffline, enabled: this.commissionEnabled },
+      productRules: this.productRules.map((rule: ProductCommissionRule): ProductCommissionRule => ({ productId: rule.productId, productName: rule.productName, productImage: rule.productImage, level1Percent: rule.level1Percent, level2Percent: rule.level2Percent }))
     };
-    this.dirty = false;
     this.saving = true;
-    let failed = false;
     try {
       await BusinessRepository.saveCommissionRules(body);
+      if (!this.pageScope.accepts(writeTicket)) return;
+      this.savedRules = submitted; this.dirty = this.fingerprint() !== submitted;
       if (showFeedback) IBestToast.show(I18n.text(this.language, '佣金规则已保存', 'Commission rules saved'));
     } catch (error) {
-      this.dirty = true;
-      failed = true;
-      IBestToast.show(error instanceof ApiError ? error.message : I18n.text(this.language, '保存失败', 'Failed to save'));
-    } finally {
-      this.saving = false;
-      if (this.dirty && this.loaded && !failed) this.scheduleSave();
-    }
+      if (!this.pageScope.accepts(writeTicket)) return;
+      this.verifying = true;
+      await this.load();
+      IBestToast.show(I18n.text(this.language, '保存未确认，草稿已保留，请重新读取核对。', 'Save was not confirmed. The draft is kept; reload to verify.'));
+    } finally { if (this.pageScope.accepts(writeTicket)) this.saving = false; }
   }
 
   private openRule(item?: ProductCommissionRule): void {
     this.editProductId = item?.productId || '';
     this.ruleFirst = `${item?.level1Percent ?? Number(this.first)}`;
     this.ruleSecond = `${item?.level2Percent ?? Number(this.second)}`;
-    this.editorVisible = true;
+    this.editStamp = this.localStamp(); this.editorVisible = true;
   }
 
   private chooseProduct(id: string): void {
@@ -170,7 +188,7 @@ struct CommissionPage {
   private ruleCard(item: ProductCommissionRule) {
     Row({ space: 10 }) {
       if (item.productImage) {
-        Image(item.productImage).width(48).height(48).objectFit(ImageFit.Cover).borderRadius(9);
+        ResilientRemoteImage({ originalUrl: item.productImage, widthValue: 48, heightValue: 48, radius: 9 });
       } else {
         Column().width(48).height(48).backgroundColor(DesignTokens.subtle(this.darkMode)).borderRadius(9);
       }
@@ -180,8 +198,8 @@ struct CommissionPage {
           `Level 1 ${item.level1Percent}% · Level 2 ${item.level2Percent}%`))
           .fontSize(12).fontColor(DesignTokens.brand(this.darkMode));
       }.layoutWeight(1).alignItems(HorizontalAlign.Start);
-      IBestButton({ text: I18n.text(this.language, '编辑', 'Edit'), plain: true, color: DesignTokens.brand(this.darkMode), onBtnClick: () => this.openRule(item) });
-      IBestButton({ text: I18n.text(this.language, '删除', 'Delete'), plain: true, type: 'danger', onBtnClick: () => this.removeRule(item.productId) });
+      IBestButton({ btnHeight: 44, text: I18n.text(this.language, '编辑', 'Edit'), plain: true, color: DesignTokens.brand(this.darkMode), onBtnClick: () => this.openRule(item) });
+      IBestButton({ btnHeight: 44, text: I18n.text(this.language, '删除', 'Delete'), plain: true, type: 'danger', onBtnClick: () => this.removeRule(item.productId) });
     }.width('100%').padding({ top: 10, bottom: 10 }).border({ width: { bottom: 1 }, color: DesignTokens.border(this.darkMode) });
   }
 
@@ -190,7 +208,7 @@ struct CommissionPage {
     Column({ space: 13 }) {
       Row() {
         Text(this.editProductId ? I18n.text(this.language, '商品独立佣金', 'Product commission') : I18n.text(this.language, '添加商品规则', 'Add product rule')).fontSize(20).fontWeight(FontWeight.Bold).layoutWeight(1);
-        Text(I18n.text(this.language, '关闭', 'Close')).fontSize(14).fontColor(DesignTokens.muted(this.darkMode)).onClick(() => this.editorVisible = false);
+        Text(I18n.text(this.language, '关闭', 'Close')).fontSize(14).fontColor(DesignTokens.muted(this.darkMode)).onClick(() => this.closeLocalEditor());
       }.width('100%');
       if (!this.editProductId) {
         Text(I18n.text(this.language, '选择商品', 'Select a product')).fontSize(14).fontWeight(FontWeight.Medium).width('100%');
@@ -201,7 +219,7 @@ struct CommissionPage {
             Column({ space: 8 }) {
               ForEach(this.products, (item: ProductSummary) => {
                 Row({ space: 9 }) {
-                  if (item.images.length > 0) Image(item.images[0]).width(40).height(40).borderRadius(8).objectFit(ImageFit.Cover);
+                  if ((item.images || []).length > 0) ResilientRemoteImage({ originalUrl: item.images[0], widthValue: 40, heightValue: 40, radius: 8 });
                   Text(item.name).fontSize(13).maxLines(1).layoutWeight(1);
                   Text(this.productRules.some((rule: ProductCommissionRule): boolean => rule.productId === item.id)
                     ? I18n.text(this.language, '已配置', 'Configured') : I18n.text(this.language, '选择', 'Select'))
@@ -217,56 +235,60 @@ struct CommissionPage {
       }
       IBestField({ value: $ruleFirst, label: I18n.text(this.language, '一级佣金', 'Level 1 commission'), suffix: '%', type: 'decimal' });
       IBestField({ value: $ruleSecond, label: I18n.text(this.language, '二级佣金', 'Level 2 commission'), suffix: '%', type: 'decimal' });
-      IBestButton({ text: I18n.text(this.language, '应用规则', 'Apply rule'), type: 'primary', color: DesignTokens.brand(this.darkMode), fontColor: DesignTokens.onBrand(this.darkMode), btnWidth: '100%', onBtnClick: () => this.saveRule() });
-    }.width('100%').padding({ left: 20, right: 20, top: 20, bottom: this.safeBottom + 20 });
+      IBestButton({ btnHeight: 44, text: I18n.text(this.language, '应用规则', 'Apply rule'), type: 'primary', color: DesignTokens.brand(this.darkMode), fontColor: DesignTokens.onBrand(this.darkMode), btnWidth: '100%', onBtnClick: () => this.saveRule() });
+    }.width('100%').padding({ left: 16, right: 16, top: 20, bottom: 12 });
   }
 
   build() {
     NavDestination() {
       Column() {
-        IBestNavBar({
+        SecondaryHeader({
           title: I18n.text(this.language, '佣金设置', 'Commission settings'), isShowStatusBar: true, navBarBgColor: DesignTokens.surface(this.darkMode),
-          onLeftClick: () => AppRouter.back()
+          onLeftClick: () => this.handleBack()
         });
-        if (this.loading) {
+          SecondaryFeedback({ errorKind: this.pageError, updating: this.loading, onRetry: () => this.load() });
+        if (this.loading && !this.loaded) {
           Column() { LoadingProgress().width(34).height(34).color(DesignTokens.brand(this.darkMode)); }
             .layoutWeight(1).justifyContent(FlexAlign.Center);
         } else {
           Scroll() {
-            Column({ space: 14 }) {
-              Column({ space: 12 }) {
-                Text(I18n.text(this.language, '默认分佣规则', 'Default commission rule')).fontSize(18).fontWeight(FontWeight.Bold).width('100%');
-                IBestField({ value: $first, label: I18n.text(this.language, '一级佣金', 'Level 1 commission'), suffix: '%', type: 'decimal', onChange: () => this.scheduleSave() });
-                IBestField({ value: $second, label: I18n.text(this.language, '二级佣金', 'Level 2 commission'), suffix: '%', type: 'decimal', onChange: () => this.scheduleSave() });
-                Row() { Text(I18n.text(this.language, '启用分佣', 'Enable commission')).layoutWeight(1); IBestSwitch({ value: $commissionEnabled, activeColor: DesignTokens.brand(this.darkMode), onChange: () => this.scheduleSave() }); }.width('100%');
-                Row() { Text(I18n.text(this.language, '向推广员显示比例', 'Show rates to promoters')).layoutWeight(1); IBestSwitch({ value: $visible, activeColor: DesignTokens.brand(this.darkMode), onChange: () => this.scheduleSave() }); }.width('100%');
-                Row() { Text(I18n.text(this.language, '允许线下结算', 'Allow offline settlement')).layoutWeight(1); IBestSwitch({ value: $allowOffline, activeColor: DesignTokens.brand(this.darkMode), onChange: () => this.scheduleSave() }); }.width('100%');
-              }.width('100%').padding(16).backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(DesignTokens.RADIUS_LARGE);
-              Column({ space: 8 }) {
-                Row() {
-                  Text(I18n.text(this.language, `商品独立规则 · ${this.productRules.length}`, `Product rules · ${this.productRules.length}`)).fontSize(18).fontWeight(FontWeight.Bold).layoutWeight(1);
-                  IBestButton({ text: I18n.text(this.language, '添加', 'Add'), plain: true, color: DesignTokens.brand(this.darkMode), onBtnClick: () => this.openRule() });
-                }.width('100%');
-                if (this.productRules.length === 0) {
-                  Text(I18n.text(this.language, '暂无独立规则，全部使用默认比例', 'No custom rules; all products use the default rates')).fontSize(13).fontColor(DesignTokens.muted(this.darkMode)).width('100%');
-                }
-                ForEach(this.productRules, (item: ProductCommissionRule) => { this.ruleCard(item); }, (item: ProductCommissionRule) => item.productId);
-              }.width('100%').padding(16).backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(DesignTokens.RADIUS_LARGE);
-              Text(I18n.text(this.language, '修改后会自动保存到服务端，也可点击下方按钮立即同步。',
-                'Changes are saved automatically. You can also sync immediately below.'))
-                .fontSize(11).fontColor(DesignTokens.muted(this.darkMode)).width('100%');
-              IBestButton({
-                text: I18n.text(this.language, '保存设置', 'Save settings'), type: 'primary', color: DesignTokens.brand(this.darkMode), fontColor: DesignTokens.onBrand(this.darkMode),
-                btnWidth: '100%', loading: this.saving, onBtnClick: () => this.save(true)
-              });
-            }.width('100%').padding({ left: 16, right: 16, top: 12, bottom: this.safeBottom + 20 });
-          }.layoutWeight(1).align(Alignment.Top).scrollBar(BarState.Off);
+            GridRow({ columns: this.windowWidth >= 720 ? 2 : 1, gutter: 12 }) {
+              GridCol() {
+                Column({ space: 12 }) {
+                  Text(I18n.text(this.language, '默认分佣规则', 'Default commission rule')).fontSize(18).fontWeight(FontWeight.Bold).width('100%');
+                  IBestField({ value: $first, label: I18n.text(this.language, '一级佣金', 'Level 1 commission'), suffix: '%', type: 'decimal', onChange: () => this.scheduleSave() });
+                  IBestField({ value: $second, label: I18n.text(this.language, '二级佣金', 'Level 2 commission'), suffix: '%', type: 'decimal', onChange: () => this.scheduleSave() });
+                  Row() { Text(I18n.text(this.language, '启用分佣', 'Enable commission')).layoutWeight(1); IBestSwitch({ value: $commissionEnabled, activeColor: DesignTokens.brand(this.darkMode), onChange: () => this.scheduleSave() }); }.width('100%');
+                  Row() { Text(I18n.text(this.language, '向推广员显示比例', 'Show rates to promoters')).layoutWeight(1); IBestSwitch({ value: $visible, activeColor: DesignTokens.brand(this.darkMode), onChange: () => this.scheduleSave() }); }.width('100%');
+                  Row() { Text(I18n.text(this.language, '允许线下结算', 'Allow offline settlement')).layoutWeight(1); IBestSwitch({ value: $allowOffline, activeColor: DesignTokens.brand(this.darkMode), onChange: () => this.scheduleSave() }); }.width('100%');
+                }.width('100%').padding(16).backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(16);
+              }
+              GridCol() {
+                Column({ space: 8 }) {
+                  Row() {
+                    Text(I18n.text(this.language, `商品独立规则 · ${this.productRules.length}`, `Product rules · ${this.productRules.length}`)).fontSize(18).fontWeight(FontWeight.Bold).layoutWeight(1);
+                    IBestButton({ btnHeight: 44, text: I18n.text(this.language, '添加', 'Add'), plain: true, color: DesignTokens.brand(this.darkMode), onBtnClick: () => this.openRule() });
+                  }.width('100%');
+                  if (this.productRules.length === 0) {
+                    Text(I18n.text(this.language, '暂无独立规则，全部使用默认比例', 'No custom rules; all products use the default rates')).fontSize(13).fontColor(DesignTokens.muted(this.darkMode)).width('100%');
+                  }
+                  ForEach(this.productRules, (item: ProductCommissionRule) => { this.ruleCard(item); }, (item: ProductCommissionRule) => item.productId);
+                }.width('100%').padding(16).backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(16);
+              }
+              GridCol() {
+                Text(I18n.text(this.language, '修改仅保留在当前草稿，点击下方保存后生效。',
+                  'Edits remain in this draft until you save.'))
+                  .fontSize(12).fontColor(DesignTokens.muted(this.darkMode)).width('100%');
+              }
+            }.width('100%').padding({ left: 16, right: 16, top: 12, bottom: 12 });
+          }.enabled(this.loaded && !this.saving && !this.loading).layoutWeight(1).align(Alignment.Top).scrollBar(BarState.Off);
+          SecondarySaveBar({ saving: this.saving, canSave: this.loaded && this.dirty && !this.verifying && !this.loading, changed: this.dirty, onSave: () => this.save() });
         }
       }.width('100%').height('100%').backgroundColor(DesignTokens.page(this.darkMode))
         .bindSheet(this.editorVisible, this.ruleEditor(), {
-          height: SheetSize.MEDIUM,
-          onWillDismiss: () => { this.editorVisible = false; }
+          height: '85%',
+          onWillDismiss: () => { this.closeLocalEditor(); }
         });
-    }.hideTitleBar(true);
+    }.hideTitleBar(true).onBackPressed((): boolean => { this.handleBack(); return true; });
   }
 }
diff --git a/native/harmony-merchant/entry/src/main/ets/features/business/CustomerPage.ets b/native/harmony-merchant/entry/src/main/ets/features/business/CustomerPage.ets
index 890b80ad..e4dd6028 100644
--- a/native/harmony-merchant/entry/src/main/ets/features/business/CustomerPage.ets
+++ b/native/harmony-merchant/entry/src/main/ets/features/business/CustomerPage.ets
@@ -1,5 +1,8 @@
+import { ResilientRemoteImage } from '../../core/ui/ResilientRemoteImage';
+import { PageReadScope, PageSearch, SecondaryPolicy } from '../../core/ui/SecondaryPolicy';
+import { SecondaryFeedback, SecondaryHeader } from '../../core/ui/SecondaryPage';
 import { common } from '@kit.AbilityKit';
-import { IBestButton, IBestDialogUtil, IBestEmpty, IBestNavBar, IBestSearch, IBestTag, IBestToast } from '@ibestservices/ibest-ui';
+import { IBestButton, IBestDialogUtil, IBestEmpty, IBestSearch, IBestTag, IBestToast } from '@ibestservices/ibest-ui';
 import { PhoneService } from '../../core/device/PhoneService';
 import { AppRouter } from '../../core/navigation/AppRouter';
 import { ApiError } from '../../core/network/ApiClient';
@@ -15,6 +18,30 @@ export function CustomersBuilder(_: string) { CustomerPage(); }
 
 @Component
 struct CustomerPage {
+  private pageSearch: PageSearch = new PageSearch();
+  private submitSearch(): void { this.pageSearch.cancel(); this.reload(); }
+  private visibleFilter: string = '';
+
+  private setTier(item: MerchantCustomer, tier: string): void {
+    if (this.processingId) return;
+    IBestDialogUtil.open({ title: I18n.text(this.language, '确认价格层级', 'Confirm price tier'),
+      message: I18n.text(this.language, `将 ${item.nickname || '该客户'} 的价格层级改为 ${tier}？`, `Change the price tier for ${item.nickname || 'this customer'} to ${tier}?`),
+      showCancelButton: true, cancelButtonText: I18n.text(this.language, '取消', 'Cancel'), confirmButtonText: I18n.text(this.language, '确认修改', 'Confirm'),
+      onConfirm: () => this.applyTier(item, tier) });
+  }
+  private toggleAuth(item: MerchantCustomer): void {
+    if (this.processingId) return;
+    IBestDialogUtil.open({ title: I18n.text(this.language, '确认价格授权', 'Confirm price access'),
+      message: item.priceAuthorized ? I18n.text(this.language, '确认取消该客户的价格查看权限？', 'Revoke this customer’s access to prices?') : I18n.text(this.language, '确认允许该客户查看授权价格？', 'Allow this customer to view authorised prices?'),
+      showCancelButton: true, cancelButtonText: I18n.text(this.language, '取消', 'Cancel'), confirmButtonText: I18n.text(this.language, '确认', 'Confirm'),
+      onConfirm: () => this.applyAuth(item) });
+  }
+
+  aboutToDisappear(): void { this.pageSearch.cancel(); this.pageScope.dispose(); }
+
+  private pageScope: PageReadScope = new PageReadScope();
+  @State private pageError: string = '';
+
   @StorageProp('safeBottom') private safeBottom: number = 16;
   @StorageLink('language') private language: string = 'zh-CN';
   @StorageLink('darkMode') private darkMode: boolean = false;
@@ -30,42 +57,62 @@ struct CustomerPage {
     { value: '' }, { value: 'normal' }, { value: 'promoter' }, { value: 'member' }, { value: 'blacklist' }
   ];
 
-  aboutToAppear(): void { this.reload(); }
-  private async reload(): Promise<void> { this.page = 1; this.customers = []; await this.load(false); }
+  aboutToAppear(): void { this.pageScope.open(); this.reload(); }
+  private async reload(): Promise<void> { await this.load(false); }
   private async load(append: boolean): Promise<void> {
+    if (!this.pageScope.isCurrent()) return;
     if (append && this.loading) return;
+    if (!append && this.loading && this.visibleFilter === `${this.kind}:${this.keyword.trim()}`) return;
+    const pageTicket = this.pageScope.begin('load');
+    this.pageError = '';
+    const filterKey = `${this.kind}:${this.keyword.trim()}`;
+    if (filterKey !== this.visibleFilter) { this.customers = []; this.total = 0; this.page = 1; this.visibleFilter = filterKey; }
     const epoch = ++this.requestEpoch;
     const requestedKind = this.kind;
     const requestedKeyword = this.keyword.trim();
-    const requestedPage = this.page;
+    const requestedPage = append ? this.page : 1;
     this.loading = true;
     try {
-      const result = await BusinessRepository.customers(requestedKind, requestedKeyword, requestedPage);
+      const result = await this.pageScope.read(BusinessRepository.customers(requestedKind, requestedKeyword, requestedPage), pageTicket);
       if (epoch !== this.requestEpoch) return;
       this.customers = append ? this.customers.concat(result.list) : result.list;
-      this.total = result.total;
+      this.total = result.total; this.page = requestedPage;
     } catch (error) {
+      if (!this.pageScope.accepts(pageTicket)) return;
+      this.pageError = SecondaryPolicy.errorKind(error);
       if (epoch === this.requestEpoch) {
         if (append && this.page === requestedPage) this.page = Math.max(1, requestedPage - 1);
-        IBestToast.show(error instanceof ApiError ? error.message : I18n.text(this.language, '客户加载失败', 'Failed to load customers'));
+        IBestToast.show(error instanceof ApiError ? SecondaryPolicy.message(error, this.language) : I18n.text(this.language, '客户加载失败', 'Failed to load customers'));
       }
     } finally {
+      if (this.pageScope.accepts(pageTicket)) {
       if (epoch === this.requestEpoch) this.loading = false;
     }
+    }
   }
-  private async setTier(item: MerchantCustomer, tier: string): Promise<void> {
+  private async applyTier(item: MerchantCustomer, tier: string): Promise<void> {
+    if (!this.pageScope.isCurrent()) return;
     if (this.processingId) return;
     this.processingId = item.id;
-    try { await BusinessRepository.setCustomerTier(item.id, tier); IBestToast.show(I18n.text(this.language, '价格层级已更新', 'Price tier updated')); await this.reload(); }
-    catch (error) { IBestToast.show(error instanceof ApiError ? error.message : I18n.text(this.language, '更新失败', 'Update failed')); }
-    finally { this.processingId = ''; }
+    const mutationTicket = this.pageScope.begin('applyTier');
+    try { await this.pageScope.read(BusinessRepository.setCustomerTier(item.id, tier), mutationTicket); IBestToast.show(I18n.text(this.language, '价格层级已更新', 'Price tier updated')); await this.pageScope.read(this.reload(), mutationTicket); }
+    catch (error) {
+      if (!this.pageScope.accepts(mutationTicket)) return; IBestToast.show(error instanceof ApiError ? SecondaryPolicy.message(error, this.language) : I18n.text(this.language, '更新失败', 'Update failed')); }
+    finally {
+      if (this.pageScope.accepts(mutationTicket)) { this.processingId = ''; }
+    }
   }
-  private async toggleAuth(item: MerchantCustomer): Promise<void> {
+  private async applyAuth(item: MerchantCustomer): Promise<void> {
+    if (!this.pageScope.isCurrent()) return;
     if (this.processingId) return;
     this.processingId = item.id;
-    try { await BusinessRepository.authorizeCustomer(item.id, !item.priceAuthorized); IBestToast.show(item.priceAuthorized ? I18n.text(this.language, '已取消价格授权', 'Price access revoked') : I18n.text(this.language, '已授权价格', 'Price access granted')); await this.reload(); }
-    catch (error) { IBestToast.show(error instanceof ApiError ? error.message : I18n.text(this.language, '操作失败', 'Operation failed')); }
-    finally { this.processingId = ''; }
+    const mutationTicket = this.pageScope.begin('applyAuth');
+    try { await this.pageScope.read(BusinessRepository.authorizeCustomer(item.id, !item.priceAuthorized), mutationTicket); IBestToast.show(item.priceAuthorized ? I18n.text(this.language, '已取消价格授权', 'Price access revoked') : I18n.text(this.language, '已授权价格', 'Price access granted')); await this.pageScope.read(this.reload(), mutationTicket); }
+    catch (error) {
+      if (!this.pageScope.accepts(mutationTicket)) return; IBestToast.show(error instanceof ApiError ? SecondaryPolicy.message(error, this.language) : I18n.text(this.language, '操作失败', 'Operation failed')); }
+    finally {
+      if (this.pageScope.accepts(mutationTicket)) { this.processingId = ''; }
+    }
   }
   private confirmBlacklist(item: MerchantCustomer): void {
     const next = !item.blocked;
@@ -82,21 +129,26 @@ struct CustomerPage {
     });
   }
   private async toggleBlacklist(item: MerchantCustomer, next: boolean): Promise<void> {
+    if (!this.pageScope.isCurrent()) return;
     if (this.processingId) return;
     this.processingId = item.id;
+    const mutationTicket = this.pageScope.begin('toggleBlacklist');
     try {
-      await BusinessRepository.setCustomerBlacklist(item.id, next);
+      await this.pageScope.read(BusinessRepository.setCustomerBlacklist(item.id, next), mutationTicket);
       IBestToast.show(next ? I18n.text(this.language, '已加入本店黑名单', 'Added to this store\'s blacklist') : I18n.text(this.language, '已移出本店黑名单', 'Removed from this store\'s blacklist'));
-      await this.reload();
+      await this.pageScope.read(this.reload(), mutationTicket);
     } catch (error) {
-      IBestToast.show(error instanceof ApiError ? error.message : I18n.text(this.language, '黑名单操作失败', 'Blacklist operation failed'));
+      if (!this.pageScope.accepts(mutationTicket)) return;
+      IBestToast.show(error instanceof ApiError ? SecondaryPolicy.message(error, this.language) : I18n.text(this.language, '黑名单操作失败', 'Blacklist operation failed'));
     } finally {
+      if (this.pageScope.accepts(mutationTicket)) {
       this.processingId = '';
     }
+    }
   }
   private async call(item: MerchantCustomer): Promise<void> {
     try { await PhoneService.dial(this.getUIContext().getHostContext() as common.UIAbilityContext, item.phone); }
-    catch (error) { IBestToast.show(error instanceof Error ? error.message : I18n.text(this.language, '无法打开拨号界面', 'Unable to open the dialer')); }
+    catch (error) { IBestToast.show(error instanceof Error ? SecondaryPolicy.message(error, this.language) : I18n.text(this.language, '无法打开拨号界面', 'Unable to open the dialer')); }
   }
 
   private kindLabel(value: string): string {
@@ -114,7 +166,7 @@ struct CustomerPage {
   @Builder private card(item: MerchantCustomer) {
     Column({ space: 10 }) {
       Row({ space: 10 }) {
-        if (item.avatar) Image(item.avatar).width(48).height(48).borderRadius(24).objectFit(ImageFit.Cover); else Column() { Text(item.nickname.slice(0, 1)).fontColor(DesignTokens.onBrand(this.darkMode)); }.width(48).height(48).borderRadius(24).backgroundColor(DesignTokens.brand(this.darkMode)).justifyContent(FlexAlign.Center);
+        if (item.avatar) ResilientRemoteImage({ originalUrl: item.avatar, widthValue: 48, heightValue: 48, radius: 24 }); else Column() { Text(item.nickname.slice(0, 1)).fontColor(DesignTokens.onBrand(this.darkMode)); }.width(48).height(48).borderRadius(24).backgroundColor(DesignTokens.brand(this.darkMode)).justifyContent(FlexAlign.Center);
         Column({ space: 4 }) { Row({ space: 6 }) { Text(item.nickname || I18n.text(this.language, '未命名客户', 'Unnamed customer')).fontSize(15).fontWeight(FontWeight.Medium); if (item.groupTag) IBestTag({ text: item.groupTag, plain: true, color: DesignTokens.brand(this.darkMode) }); if (item.blocked) IBestTag({ text: I18n.text(this.language, '黑名单', 'Blacklist'), plain: true, color: DesignTokens.danger(this.darkMode) }); }.width('100%'); Text(item.phone ? this.maskedPhone(item.phone) : I18n.text(this.language, '未绑定手机', 'No phone number')).fontSize(12).fontColor(DesignTokens.muted(this.darkMode)); }.layoutWeight(1).alignItems(HorizontalAlign.Start);
         Text(`¥${item.totalSpent.toFixed(2)}`).fontSize(15).fontWeight(FontWeight.Bold).fontColor(DesignTokens.brand(this.darkMode));
       }.width('100%');
@@ -124,12 +176,12 @@ struct CustomerPage {
         Text(I18n.text(this.language, '批发', 'Wholesale')).fontSize(12).padding({ left: 10, right: 10, top: 6, bottom: 6 }).borderRadius(14).backgroundColor(item.priceTier === 'wholesale' ? DesignTokens.brand(this.darkMode) : DesignTokens.subtle(this.darkMode)).fontColor(item.priceTier === 'wholesale' ? DesignTokens.onBrand(this.darkMode) : DesignTokens.muted(this.darkMode)).onClick(() => this.setTier(item, 'wholesale'));
         Text(I18n.text(this.language, '会员', 'Member')).fontSize(12).padding({ left: 10, right: 10, top: 6, bottom: 6 }).borderRadius(14).backgroundColor(item.priceTier === 'member' ? DesignTokens.brand(this.darkMode) : DesignTokens.subtle(this.darkMode)).fontColor(item.priceTier === 'member' ? DesignTokens.onBrand(this.darkMode) : DesignTokens.muted(this.darkMode)).onClick(() => this.setTier(item, 'member'));
         Blank();
-        IBestButton({ text: item.priceAuthorized ? I18n.text(this.language, '取消授权', 'Revoke') : I18n.text(this.language, '授权价格', 'Grant access'), plain: true, color: item.priceAuthorized ? DesignTokens.brand(this.darkMode) : DesignTokens.brand(this.darkMode), onBtnClick: () => this.toggleAuth(item) });
+        IBestButton({ btnHeight: 44, text: item.priceAuthorized ? I18n.text(this.language, '取消授权', 'Revoke') : I18n.text(this.language, '授权价格', 'Grant access'), plain: true, color: item.priceAuthorized ? DesignTokens.brand(this.darkMode) : DesignTokens.brand(this.darkMode), onBtnClick: () => this.toggleAuth(item) });
       }.width('100%');
       Row() {
         Text(item.blocked ? I18n.text(this.language, '该客户已被限制在本店下单', 'This customer cannot order from this store') : I18n.text(this.language, '黑名单仅对当前店铺生效', 'The blacklist only applies to this store'))
-          .fontSize(11).fontColor(item.blocked ? DesignTokens.danger(this.darkMode) : DesignTokens.muted(this.darkMode)).layoutWeight(1);
-        IBestButton({
+          .fontSize(12).fontColor(item.blocked ? DesignTokens.danger(this.darkMode) : DesignTokens.muted(this.darkMode)).layoutWeight(1);
+        IBestButton({ btnHeight: 44,
           text: item.blocked ? I18n.text(this.language, '恢复购买', 'Restore access') : I18n.text(this.language, '加入黑名单', 'Blacklist'),
           plain: true,
           type: item.blocked ? 'primary' : 'danger',
@@ -138,15 +190,16 @@ struct CustomerPage {
         });
       }.width('100%');
       if (item.phone) {
-        IBestButton({ text: I18n.text(this.language, '联系客户', 'Contact customer'), plain: true, color: DesignTokens.brand(this.darkMode), btnWidth: '100%', onBtnClick: () => this.call(item) });
+        IBestButton({ btnHeight: 44, text: I18n.text(this.language, '联系客户', 'Contact customer'), plain: true, color: DesignTokens.brand(this.darkMode), btnWidth: '100%', onBtnClick: () => this.call(item) });
       }
-    }.width('100%').padding(14).backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(DesignTokens.RADIUS_LARGE);
+    }.width('100%').padding(14).backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(16);
   }
   build() {
     NavDestination() {
       Column() {
-        IBestNavBar({ title: I18n.text(this.language, '客户管理', 'Customer management'), isShowStatusBar: true, navBarBgColor: DesignTokens.surface(this.darkMode), onLeftClick: () => AppRouter.back() });
-        IBestSearch({ value: $keyword, placeholder: I18n.text(this.language, '搜索姓名或手机号', 'Search name or phone'), isRound: true, outBgColor: DesignTokens.page(this.darkMode), innerBgColor: DesignTokens.card(this.darkMode), showRightButton: true, rightButtonText: I18n.text(this.language, '搜索', 'Search'), onSearch: () => this.reload(), onRightButtonClick: () => this.reload() });
+        SecondaryHeader({ title: I18n.text(this.language, '客户管理', 'Customer management'), isShowStatusBar: true, navBarBgColor: DesignTokens.surface(this.darkMode), onLeftClick: () => AppRouter.back() });
+          SecondaryFeedback({ errorKind: this.pageError, updating: this.loading, onRetry: () => this.load(false) });
+        IBestSearch({ value: $keyword, placeholder: I18n.text(this.language, '搜索姓名或手机号', 'Search name or phone'), isRound: true, outBgColor: DesignTokens.page(this.darkMode), innerBgColor: DesignTokens.card(this.darkMode), showRightButton: true, rightButtonText: I18n.text(this.language, '搜索', 'Search'), onInput: () => this.pageSearch.schedule(() => this.reload()), onSearch: () => this.submitSearch(), onRightButtonClick: () => this.submitSearch() });
         Scroll() { Row({ space: 8 }) { ForEach(this.kinds, (item: CustomerKind) => { Text(this.kindLabel(item.value)).fontSize(13).padding({ left: 14, right: 14, top: 8, bottom: 8 }).backgroundColor(this.kind === item.value ? DesignTokens.brand(this.darkMode) : DesignTokens.card(this.darkMode)).fontColor(this.kind === item.value ? DesignTokens.onBrand(this.darkMode) : DesignTokens.muted(this.darkMode)).borderRadius(18).onClick(() => { this.kind = item.value; this.reload(); }); }, (item: CustomerKind) => item.value); }.padding({ left: 16, right: 16, bottom: 10 }); }.scrollable(ScrollDirection.Horizontal).scrollBar(BarState.Off);
         if (!this.loading && this.customers.length === 0) Column() { IBestEmpty({ description: I18n.text(this.language, '暂无客户', 'No customers') }); }.layoutWeight(1).justifyContent(FlexAlign.Center);
         else List({ space: 10 }) { ForEach(this.customers, (item: MerchantCustomer) => { ListItem() { this.card(item); } }, (item: MerchantCustomer) => item.id); if (this.loading) ListItem() { Row() { LoadingProgress().width(24).height(24).color(DesignTokens.brand(this.darkMode)); }.height(52).justifyContent(FlexAlign.Center); } }.layoutWeight(1).padding({ left: 16, right: 16, bottom: this.safeBottom + 16 }).onReachEnd(() => { if (!this.loading && this.customers.length < this.total) { this.page += 1; this.load(true); } });
diff --git a/native/harmony-merchant/entry/src/main/ets/features/business/DecoratePage.ets b/native/harmony-merchant/entry/src/main/ets/features/business/DecoratePage.ets
index ea33d70b..cd9d6a24 100644
--- a/native/harmony-merchant/entry/src/main/ets/features/business/DecoratePage.ets
+++ b/native/harmony-merchant/entry/src/main/ets/features/business/DecoratePage.ets
@@ -1,5 +1,8 @@
+import { LeaveGuard } from '../../core/ui/LeaveGuard';
+import { PageReadScope, SecondaryPolicy } from '../../core/ui/SecondaryPolicy';
+import { SecondaryFeedback, SecondaryHeader, SecondarySaveBar } from '../../core/ui/SecondaryPage';
 import { common } from '@kit.AbilityKit';
-import { IBestActionSheet, IBestButton, IBestNavBar, IBestToast } from '@ibestservices/ibest-ui';
+import { IBestActionSheet, IBestButton, IBestToast } from '@ibestservices/ibest-ui';
 import { FileUploadService } from '../../core/file/FileUploadService';
 import { AppRouter } from '../../core/navigation/AppRouter';
 import { ApiError } from '../../core/network/ApiClient';
@@ -18,6 +21,24 @@ export function DecorateBuilder(_: string) { DecoratePage(); }
 
 @Component
 struct DecoratePage {
+  @StorageProp('windowWidth') private windowWidth: number = 390;
+  @State private verifying: boolean = false;
+  private handleBack(): void {
+
+
+    this.leaveGuard.request(this.language, this.saving || this.uploading, this.editorReady && this.formStamp() !== this.loadedStamp, () => AppRouter.back());
+  }
+
+  private leaveGuard: LeaveGuard = new LeaveGuard();
+  @State private editorReady: boolean = false;
+  private loadedStamp: string = '';
+  private formStamp(): string { return JSON.stringify([this.themeColor, this.fontStyle, this.layout, this.banners]); }
+
+  aboutToDisappear(): void { this.pageScope.dispose(); }
+
+  private pageScope: PageReadScope = new PageReadScope();
+  @State private pageError: string = '';
+
   @StorageProp('safeBottom') private safeBottom: number = 16;
   @StorageLink('language') private language: string = 'zh-CN';
   @StorageLink('darkMode') private darkMode: boolean = false;
@@ -35,27 +56,43 @@ struct DecoratePage {
     { value: '#3B82F6' }, { value: '#10B981' }, { value: '#F59E0B' }, { value: '#374151' }
   ];
 
-  aboutToAppear(): void { this.load(); }
+  aboutToAppear(): void { this.pageScope.open(); this.load(); }
 
   private async load(): Promise<void> {
+    if (!this.pageScope.isCurrent()) return;
+    const pageTicket = this.pageScope.begin('load');
+    this.loading = true;
+    this.pageError = '';
     try {
-      const data = await BusinessRepository.decorate();
+      const data = await this.pageScope.read(BusinessRepository.decorate(), pageTicket);
+      if (!Array.isArray(data.banners)) throw new Error('invalid-design');
+      const keepDraft = this.editorReady && this.formStamp() !== this.loadedStamp;
+      this.loadedStamp = JSON.stringify([data.themeColor, data.fontStyle, data.productLayout, data.banners]);
       this.merchantId = data.merchantId;
+      if (!keepDraft) {
       this.themeColor = data.themeColor;
       this.fontStyle = data.fontStyle;
       this.layout = data.productLayout;
       this.banners = data.banners;
+      }
+      this.editorReady = true; this.verifying = false;
     } catch (error) {
-      IBestToast.show(error instanceof ApiError ? error.message
+      if (!this.pageScope.accepts(pageTicket)) return;
+      this.pageError = SecondaryPolicy.errorKind(error);
+      IBestToast.show(error instanceof ApiError ? SecondaryPolicy.message(error, this.language)
         : I18n.text(this.language, '装修配置加载失败', 'Failed to load store design'));
     }
     try {
-      this.previewProducts = (await ProductRepository.list('active', '', 1, 4)).list;
+      this.previewProducts = (await this.pageScope.read(ProductRepository.list('active', '', 1, 4), pageTicket)).list;
     } catch (_) {
+      if (!this.pageScope.accepts(pageTicket)) return;
+      this.pageError = SecondaryPolicy.errorKind(_);
       this.previewProducts = [];
     } finally {
+      if (this.pageScope.accepts(pageTicket)) {
       this.loading = false;
     }
+    }
   }
 
   private addBanner(): void {
@@ -86,7 +123,7 @@ struct DecoratePage {
       urls.forEach((url: string) => next.push({ image: url }));
       this.banners = next;
     } catch (error) {
-      IBestToast.show(error instanceof Error ? error.message : I18n.text(this.language, '上传失败', 'Upload failed'));
+      IBestToast.show(error instanceof Error ? SecondaryPolicy.message(error, this.language) : I18n.text(this.language, '上传失败', 'Upload failed'));
     } finally {
       this.uploading = false;
     }
@@ -100,7 +137,7 @@ struct DecoratePage {
       const url = await FileUploadService.captureAndUploadImage(context, 'product');
       if (url) this.banners = this.banners.concat([{ image: url }]).slice(0, 5);
     } catch (error) {
-      IBestToast.show(error instanceof Error ? error.message
+      IBestToast.show(error instanceof Error ? SecondaryPolicy.message(error, this.language)
         : I18n.text(this.language, '拍照上传失败', 'Failed to capture and upload photo'));
     } finally {
       this.uploading = false;
@@ -113,6 +150,9 @@ struct DecoratePage {
 
   private async save(): Promise<void> {
     if (this.saving) return;
+    if (!this.editorReady || this.loading || this.verifying) return;
+    if (!this.pageScope.isCurrent()) return;
+    const writeTicket = this.pageScope.begin('save');
     if (this.uploading) {
       IBestToast.show(I18n.text(this.language, 'Banner 仍在上传，请稍候', 'A banner is still uploading. Please wait.'));
       return;
@@ -128,14 +168,20 @@ struct DecoratePage {
       banners: this.banners,
       productLayout: this.layout
     };
+    const submittedStamp = this.formStamp();
     this.saving = true;
     try {
       await BusinessRepository.saveDecorate(body);
+      if (!this.pageScope.accepts(writeTicket)) return;
+      this.loadedStamp = submittedStamp;
       IBestToast.show(I18n.text(this.language, '店铺装修已保存', 'Store design saved'));
     } catch (error) {
-      IBestToast.show(error instanceof ApiError ? error.message : I18n.text(this.language, '保存失败', 'Failed to save'));
+      if (!this.pageScope.accepts(writeTicket)) return;
+      this.verifying = true;
+      await this.load();
+      IBestToast.show(error instanceof ApiError ? SecondaryPolicy.message(error, this.language) : I18n.text(this.language, '保存失败', 'Failed to save'));
     } finally {
-      this.saving = false;
+      if (this.pageScope.accepts(writeTicket)) this.saving = false;
     }
   }
 
@@ -150,7 +196,7 @@ struct DecoratePage {
   @Builder
   private miniProduct(item: ProductSummary) {
     Column({ space: 4 }) {
-      if (item.images.length > 0) {
+      if ((item.images || []).length > 0) {
         Image(item.images[0]).width('100%').height(this.layout === 'singleLarge' ? 88 : 62)
           .objectFit(ImageFit.Cover).borderRadius(6);
       } else {
@@ -175,7 +221,7 @@ struct DecoratePage {
       if (this.banners.length > 0) {
         Image(this.banners[0].image).width('100%').height(86).objectFit(ImageFit.Cover);
       } else {
-        Column() { Text(I18n.text(this.language, '尚未添加 Banner', 'No banner added')).fontSize(10).fontColor(DesignTokens.muted(this.darkMode)); }
+        Column() { Text(I18n.text(this.language, '尚未添加 Banner', 'No banner added')).fontSize(12).fontColor(DesignTokens.muted(this.darkMode)); }
           .width('100%').height(86).justifyContent(FlexAlign.Center).backgroundColor(DesignTokens.subtle(this.darkMode));
       }
       Flex({ wrap: FlexWrap.Wrap, justifyContent: FlexAlign.SpaceBetween }) {
@@ -191,70 +237,74 @@ struct DecoratePage {
   build() {
     NavDestination() {
       Column() {
-        IBestNavBar({ title: I18n.text(this.language, '店铺装修', 'Store design'), isShowStatusBar: true,
-          navBarBgColor: DesignTokens.surface(this.darkMode), onLeftClick: () => AppRouter.back() });
-        if (this.loading) {
+        SecondaryHeader({ title: I18n.text(this.language, '店铺装修', 'Store design'), isShowStatusBar: true,
+          navBarBgColor: DesignTokens.surface(this.darkMode), onLeftClick: () => this.handleBack() });
+          SecondaryFeedback({ errorKind: this.pageError, updating: this.loading, onRetry: () => this.load() });
+        if (this.loading && !this.editorReady) {
           Column() { LoadingProgress().width(34).height(34).color(DesignTokens.brand(this.darkMode)); }
             .layoutWeight(1).justifyContent(FlexAlign.Center);
         } else {
           Scroll() {
-            Column({ space: 14 }) {
-              this.phonePreview();
-              Column({ space: 12 }) {
-                Text(I18n.text(this.language, `主题色 · ${this.themeColor}`, `Theme colour · ${this.themeColor}`))
-                  .fontSize(18).fontWeight(FontWeight.Bold).width('100%');
-                Flex({ wrap: FlexWrap.Wrap }) {
-                  ForEach(this.colors, (item: DecorateColorOption) => {
-                    Column() {
-                      if (this.themeColor === item.value) MerchantIcon({ name: 'success', color: Color.White, iconSize: 15 });
-                    }.width(38).height(38).margin({ right: 10, bottom: 10 }).backgroundColor(item.value)
-                      .borderRadius(19).justifyContent(FlexAlign.Center).onClick(() => this.themeColor = item.value);
-                  }, (item: DecorateColorOption) => item.value);
-                }.width('100%');
-                Text(I18n.text(this.language, '字体风格', 'Typography')).fontSize(15).fontWeight(FontWeight.Medium).width('100%');
-                Row({ space: 8 }) {
-                  this.option('modern', I18n.text(this.language, '现代', 'Modern'), this.fontStyle, () => this.fontStyle = 'modern');
-                  this.option('elegant', I18n.text(this.language, '雅致', 'Elegant'), this.fontStyle, () => this.fontStyle = 'elegant');
-                  this.option('playful', I18n.text(this.language, '俏皮', 'Playful'), this.fontStyle, () => this.fontStyle = 'playful');
-                  this.option('minimal', I18n.text(this.language, '极简', 'Minimal'), this.fontStyle, () => this.fontStyle = 'minimal');
-                }.width('100%');
-              }.width('100%').padding(16).backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(DesignTokens.RADIUS_LARGE);
-
-              Column({ space: 12 }) {
-                Text(I18n.text(this.language, `首页轮播图 · ${this.banners.length} / 5`, `Home banners · ${this.banners.length} / 5`))
-                  .fontSize(18).fontWeight(FontWeight.Bold).width('100%');
-                ForEach(this.banners, (item: BannerItem, index: number) => {
-                  Stack({ alignContent: Alignment.TopEnd }) {
-                    Image(item.image).width('100%').height(150).objectFit(ImageFit.Cover).borderRadius(12);
-                    Text(index === 0 ? I18n.text(this.language, '首张 · 删除', 'First · Delete') : I18n.text(this.language, '删除', 'Delete'))
-                      .fontSize(12).fontColor(Color.White).padding(8).backgroundColor('#99000000')
-                      .borderRadius({ topRight: 12, bottomLeft: 12 }).onClick(() => this.remove(index));
+            GridRow({ columns: this.windowWidth >= 720 ? 2 : 1, gutter: 12 }) {
+              GridCol() {
+                this.phonePreview();
+              }
+              GridCol() {
+                Column({ space: 12 }) {
+                  Text(I18n.text(this.language, `主题色 · ${this.themeColor}`, `Theme colour · ${this.themeColor}`))
+                    .fontSize(18).fontWeight(FontWeight.Bold).width('100%');
+                  Flex({ wrap: FlexWrap.Wrap }) {
+                    ForEach(this.colors, (item: DecorateColorOption) => {
+                      Column() {
+                        if (this.themeColor === item.value) MerchantIcon({ name: 'success', color: Color.White, iconSize: 15 });
+                      }.width(38).height(38).margin({ right: 10, bottom: 10 }).backgroundColor(item.value)
+                        .borderRadius(19).justifyContent(FlexAlign.Center).onClick(() => this.themeColor = item.value);
+                    }, (item: DecorateColorOption) => item.value);
                   }.width('100%');
-                }, (item: BannerItem) => item.image);
-                IBestButton({
-                  text: this.uploading ? I18n.text(this.language, '上传中…', 'Uploading…') : I18n.text(this.language, '添加轮播图', 'Add banner'),
-                  plain: true, color: this.themeColor, btnWidth: '100%', loading: this.uploading,
-                  disabled: this.uploading || this.banners.length >= 5, onBtnClick: () => this.addBanner()
-                });
-              }.width('100%').padding(16).backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(DesignTokens.RADIUS_LARGE);
-
-              Column({ space: 12 }) {
-                Text(I18n.text(this.language, '商品布局', 'Product layout')).fontSize(18).fontWeight(FontWeight.Bold).width('100%');
-                Row({ space: 8 }) {
-                  this.option('waterfall', I18n.text(this.language, '瀑布流', 'Waterfall'), this.layout, () => this.layout = 'waterfall');
-                  this.option('twoColumn', I18n.text(this.language, '双列', 'Two columns'), this.layout, () => this.layout = 'twoColumn');
-                  this.option('singleLarge', I18n.text(this.language, '单列大图', 'Large single column'), this.layout, () => this.layout = 'singleLarge');
-                }.width('100%');
-              }.width('100%').padding(16).backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(DesignTokens.RADIUS_LARGE);
-
-              IBestButton({ text: I18n.text(this.language, '保存并发布', 'Save and publish'), type: 'primary',
-                color: DesignTokens.brand(this.darkMode), fontColor: DesignTokens.onBrand(this.darkMode),
-                btnWidth: '100%', loading: this.saving,
-                disabled: this.saving || this.uploading, onBtnClick: () => this.save() });
-            }.width('100%').padding({ left: 16, right: 16, top: 12, bottom: this.safeBottom + 20 });
-          }.layoutWeight(1).align(Alignment.Top).scrollBar(BarState.Off);
+                  Text(I18n.text(this.language, '字体风格', 'Typography')).fontSize(15).fontWeight(FontWeight.Medium).width('100%');
+                  Row({ space: 8 }) {
+                    this.option('modern', I18n.text(this.language, '现代', 'Modern'), this.fontStyle, () => this.fontStyle = 'modern');
+                    this.option('elegant', I18n.text(this.language, '雅致', 'Elegant'), this.fontStyle, () => this.fontStyle = 'elegant');
+                    this.option('playful', I18n.text(this.language, '俏皮', 'Playful'), this.fontStyle, () => this.fontStyle = 'playful');
+                    this.option('minimal', I18n.text(this.language, '极简', 'Minimal'), this.fontStyle, () => this.fontStyle = 'minimal');
+                  }.width('100%');
+                }.width('100%').padding(16).backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(16);
+              }
+              GridCol() {
+                Column({ space: 12 }) {
+                  Text(I18n.text(this.language, `首页轮播图 · ${this.banners.length} / 5`, `Home banners · ${this.banners.length} / 5`))
+                    .fontSize(18).fontWeight(FontWeight.Bold).width('100%');
+                  ForEach(this.banners, (item: BannerItem, index: number) => {
+                    Stack({ alignContent: Alignment.TopEnd }) {
+                      Image(item.image).width('100%').height(150).objectFit(ImageFit.Cover).borderRadius(12);
+                      Text(index === 0 ? I18n.text(this.language, '首张 · 删除', 'First · Delete') : I18n.text(this.language, '删除', 'Delete'))
+                        .fontSize(12).fontColor(Color.White).padding(8).backgroundColor('#99000000')
+                        .borderRadius({ topRight: 12, bottomLeft: 12 }).onClick(() => this.remove(index));
+                    }.width('100%');
+                  }, (item: BannerItem) => item.image);
+                  IBestButton({ btnHeight: 44,
+                    text: this.uploading ? I18n.text(this.language, '上传中…', 'Uploading…') : I18n.text(this.language, '添加轮播图', 'Add banner'),
+                    plain: true, color: this.themeColor, btnWidth: '100%', loading: this.uploading,
+                    disabled: this.uploading || this.banners.length >= 5, onBtnClick: () => this.addBanner()
+                  });
+                }.width('100%').padding(16).backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(16);
+              }
+              GridCol() {
+                Column({ space: 12 }) {
+                  Text(I18n.text(this.language, '商品布局', 'Product layout')).fontSize(18).fontWeight(FontWeight.Bold).width('100%');
+                  Row({ space: 8 }) {
+                    this.option('waterfall', I18n.text(this.language, '瀑布流', 'Waterfall'), this.layout, () => this.layout = 'waterfall');
+                    this.option('twoColumn', I18n.text(this.language, '双列', 'Two columns'), this.layout, () => this.layout = 'twoColumn');
+                    this.option('singleLarge', I18n.text(this.language, '单列大图', 'Large single column'), this.layout, () => this.layout = 'singleLarge');
+                  }.width('100%');
+                }.width('100%').padding(16).backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(16);
+              }
+            }.width('100%').padding({ left: 16, right: 16, top: 12, bottom: 12 });
+          }.enabled(this.editorReady && !this.saving && !this.loading).layoutWeight(1).align(Alignment.Top).scrollBar(BarState.Off);
         }
+        SecondarySaveBar({ saving: this.saving, changed: this.editorReady && this.formStamp() !== this.loadedStamp,
+          canSave: this.editorReady && !this.loading && !this.verifying && !this.uploading && this.formStamp() !== this.loadedStamp, onSave: () => this.save() });
       }.width('100%').height('100%').backgroundColor(DesignTokens.page(this.darkMode));
-    }.hideTitleBar(true);
+    }.hideTitleBar(true).onBackPressed((): boolean => { this.handleBack(); return true; });
   }
 }
diff --git a/native/harmony-merchant/entry/src/main/ets/features/business/MarketingPage.ets b/native/harmony-merchant/entry/src/main/ets/features/business/MarketingPage.ets
index 85a6e0b6..4ff54154 100644
--- a/native/harmony-merchant/entry/src/main/ets/features/business/MarketingPage.ets
+++ b/native/harmony-merchant/entry/src/main/ets/features/business/MarketingPage.ets
@@ -1,4 +1,7 @@
-import { IBestButton, IBestDialogUtil, IBestEmpty, IBestField, IBestNavBar, IBestTag, IBestToast } from '@ibestservices/ibest-ui';
+import { LeaveGuard } from '../../core/ui/LeaveGuard';
+import { PageReadScope, SecondaryPolicy } from '../../core/ui/SecondaryPolicy';
+import { SecondaryFeedback, SecondaryHeader } from '../../core/ui/SecondaryPage';
+import { IBestButton, IBestDialogUtil, IBestEmpty, IBestField, IBestTag, IBestToast } from '@ibestservices/ibest-ui';
 import { AppRouter } from '../../core/navigation/AppRouter';
 import { ApiError } from '../../core/network/ApiClient';
 import { DesignTokens } from '../../core/theme/DesignTokens';
@@ -12,6 +15,26 @@ export function MarketingBuilder(_: string) { MarketingPage(); }
 
 @Component
 struct MarketingPage {
+  private visibleFilter: string = '';
+
+  private handleBack(): void {
+    if (this.editorVisible) { this.closeLocalEditor(); return; }
+
+    this.leaveGuard.request(this.language, false, false, () => AppRouter.back());
+  }
+
+  private editStamp: string = '';
+  private localStamp(): string { return JSON.stringify([this.editingId, this.name, this.type, this.amount, this.discountPercent, this.threshold, this.stock, this.perUserLimit, this.validFrom, this.validTo]); }
+  private closeLocalEditor(): void {
+    this.leaveGuard.request(this.language, this.formSaving || Boolean(this.actionId), this.localStamp() !== this.editStamp, () => { this.editorVisible = false; });
+  }
+
+  private leaveGuard: LeaveGuard = new LeaveGuard();
+  aboutToDisappear(): void { this.pageScope.dispose(); }
+
+  private pageScope: PageReadScope = new PageReadScope();
+  @State private pageError: string = '';
+
   @StorageProp('safeBottom') private safeBottom: number = 16;
   @StorageLink('language') private language: string = 'zh-CN';
   @StorageLink('darkMode') private darkMode: boolean = false;
@@ -36,39 +59,50 @@ struct MarketingPage {
   @State private actionId: string = '';
   private requestEpoch: number = 0;
 
-  aboutToAppear(): void { this.load(); }
+  aboutToAppear(): void { this.pageScope.open(); this.load(); }
   private async load(): Promise<void> {
+    if (!this.pageScope.isCurrent()) return;
+    const pageTicket = this.pageScope.begin('load');
+    this.pageError = '';
+    const filterKey = `${this.activityKind}:${this.couponStatus}`;
+    if (filterKey !== this.visibleFilter) { this.coupons = []; this.activities = []; this.visibleFilter = filterKey; }
     const epoch = ++this.requestEpoch;
     const requestedKind = this.activityKind;
     const requestedStatus = this.couponStatus;
     this.loading = true;
     try {
-      const result = await BusinessRepository.marketingOverview();
+      const result = await this.pageScope.read(BusinessRepository.marketingOverview(), pageTicket);
       if (epoch === this.requestEpoch) this.overview = result;
-    } catch (_) {}
+    } catch (_) {
+      if (!this.pageScope.accepts(pageTicket)) return;
+      this.pageError = SecondaryPolicy.errorKind(_);}
     try {
       if (requestedKind === 'coupon') {
-        const result = await BusinessRepository.coupons(requestedStatus === 'all' ? '' : requestedStatus, 1);
+        const result = await this.pageScope.read(BusinessRepository.coupons(requestedStatus === 'all' ? '' : requestedStatus, 1), pageTicket);
         if (epoch === this.requestEpoch) this.coupons = result.list;
       } else {
-        const result = await BusinessRepository.marketingActivities(requestedKind);
+        const result = await this.pageScope.read(BusinessRepository.marketingActivities(requestedKind), pageTicket);
         if (epoch === this.requestEpoch) this.activities = result.list;
       }
     }
     catch (error) {
+      if (!this.pageScope.accepts(pageTicket)) return;
+      this.pageError = SecondaryPolicy.errorKind(error);
       if (epoch === this.requestEpoch) {
-        IBestToast.show(error instanceof ApiError ? error.message : I18n.text(this.language, '营销数据加载失败', 'Failed to load marketing data'));
+        IBestToast.show(error instanceof ApiError ? SecondaryPolicy.message(error, this.language) : I18n.text(this.language, '营销数据加载失败', 'Failed to load marketing data'));
       }
     }
-    finally { if (epoch === this.requestEpoch) this.loading = false; }
+    finally {
+      if (this.pageScope.accepts(pageTicket)) { if (epoch === this.requestEpoch) this.loading = false; }
+    }
   }
   private open(item?: MarketingCoupon): void {
     this.editingId = item?.id || ''; this.name = item?.name || ''; this.type = item?.type || 'fullReduce';
     this.amount = item?.amount ? `${item.amount}` : ''; this.threshold = item?.threshold ? `${item.threshold}` : '';
     this.discountPercent = item?.discountPercent ? `${item.discountPercent}` : '90';
     this.stock = item ? `${item.stock}` : '100'; this.perUserLimit = item ? `${item.perUserLimit}` : '1';
-    this.validFrom = item?.validFrom.slice(0, 10) || ''; this.validTo = item?.validTo.slice(0, 10) || '';
-    this.editorVisible = true;
+    this.validFrom = item?.validFrom?.slice(0, 10) || ''; this.validTo = item?.validTo?.slice(0, 10) || '';
+    this.editStamp = this.localStamp(); this.editorVisible = true;
   }
   private duplicate(item: MarketingCoupon): void {
     this.open(item);
@@ -76,11 +110,12 @@ struct MarketingPage {
     this.name = I18n.text(this.language, `${item.name} 副本`, `${item.name} copy`);
   }
   private async save(): Promise<void> {
+    if (!this.pageScope.isCurrent()) return;
     if (this.formSaving) return;
     const amount = Number(this.amount), discountPercent = Number(this.discountPercent);
     const stock = Number(this.stock), threshold = Number(this.threshold || '0'), perUserLimit = Number(this.perUserLimit);
     if (!this.name.trim() || !Number.isFinite(stock) || stock < 1 || !Number.isFinite(perUserLimit) || perUserLimit < 1 ||
-      !this.validFrom || !this.validTo || this.validTo < this.validFrom) {
+      !SecondaryPolicy.validBusinessDate(this.validFrom) || !SecondaryPolicy.validBusinessDate(this.validTo) || this.validTo < this.validFrom) {
       IBestToast.show(I18n.text(this.language, '请完整填写优惠券信息', 'Complete all coupon details')); return;
     }
     if ((this.type === 'fullReduce' && (!Number.isFinite(amount) || amount <= 0 || threshold <= amount)) ||
@@ -97,33 +132,47 @@ struct MarketingPage {
       perUserLimit: Math.floor(perUserLimit), scope: 'all'
     };
     this.formSaving = true;
+    const mutationTicket = this.pageScope.begin('save');
     try {
-      if (this.editingId) await BusinessRepository.updateCoupon(this.editingId, body);
-      else await BusinessRepository.createCoupon(body);
+      if (this.editingId) await this.pageScope.read(BusinessRepository.updateCoupon(this.editingId, body), mutationTicket);
+      else await this.pageScope.read(BusinessRepository.createCoupon(body), mutationTicket);
       this.editorVisible = false;
       IBestToast.show(I18n.text(this.language, '优惠券已保存', 'Coupon saved'));
-      await this.load();
+      await this.pageScope.read(this.load(), mutationTicket);
     } catch (error) {
-      IBestToast.show(error instanceof ApiError ? error.message : I18n.text(this.language, '保存失败', 'Failed to save'));
+      if (!this.pageScope.accepts(mutationTicket)) return;
+      IBestToast.show(error instanceof ApiError ? SecondaryPolicy.message(error, this.language) : I18n.text(this.language, '保存失败', 'Failed to save'));
     } finally {
+      if (this.pageScope.accepts(mutationTicket)) {
       this.formSaving = false;
     }
+    }
   }
   private async toggle(item: MarketingCoupon): Promise<void> {
+    if (!this.pageScope.isCurrent()) return;
     if (this.actionId || this.formSaving) return;
     this.actionId = item.id;
-    try { await BusinessRepository.toggleCoupon(item.id, item.status !== 'active'); await this.load(); }
-    catch (error) { IBestToast.show(error instanceof ApiError ? error.message : I18n.text(this.language, '操作失败', 'Operation failed')); }
-    finally { this.actionId = ''; }
+    const mutationTicket = this.pageScope.begin('toggle');
+    try { await this.pageScope.read(BusinessRepository.toggleCoupon(item.id, item.status !== 'active'), mutationTicket); await this.pageScope.read(this.load(), mutationTicket); }
+    catch (error) {
+      if (!this.pageScope.accepts(mutationTicket)) return; IBestToast.show(error instanceof ApiError ? SecondaryPolicy.message(error, this.language) : I18n.text(this.language, '操作失败', 'Operation failed')); }
+    finally {
+      if (this.pageScope.accepts(mutationTicket)) { this.actionId = ''; }
+    }
   }
   private remove(item: MarketingCoupon): void { IBestDialogUtil.open({ title: I18n.text(this.language, '删除优惠券', 'Delete coupon'), message: I18n.text(this.language, `确定删除“${item.name}”吗？`, `Delete “${item.name}”?`), showCancelButton: true,
       cancelButtonText: I18n.text(this.language, '取消', 'Cancel'), confirmButtonText: I18n.text(this.language, '确认', 'Confirm'), confirmButtonColor: DesignTokens.danger(this.darkMode), onConfirm: () => { this.deleteCoupon(item.id); } }); }
   private async deleteCoupon(id: string): Promise<void> {
+    if (!this.pageScope.isCurrent()) return;
     if (this.actionId || this.formSaving) return;
     this.actionId = id;
-    try { await BusinessRepository.deleteCoupon(id); await this.load(); }
-    catch (error) { IBestToast.show(error instanceof ApiError ? error.message : I18n.text(this.language, '删除失败', 'Failed to delete')); }
-    finally { this.actionId = ''; }
+    const mutationTicket = this.pageScope.begin('deleteCoupon');
+    try { await this.pageScope.read(BusinessRepository.deleteCoupon(id), mutationTicket); await this.pageScope.read(this.load(), mutationTicket); }
+    catch (error) {
+      if (!this.pageScope.accepts(mutationTicket)) return; IBestToast.show(error instanceof ApiError ? SecondaryPolicy.message(error, this.language) : I18n.text(this.language, '删除失败', 'Failed to delete')); }
+    finally {
+      if (this.pageScope.accepts(mutationTicket)) { this.actionId = ''; }
+    }
   }
 
   private selectKind(kind: string): void {
@@ -132,7 +181,7 @@ struct MarketingPage {
     this.load();
   }
 
-  @Builder private metric(value: number, label: string) { Column({ space: 5 }) { Text(`${value}`).fontSize(21).fontWeight(FontWeight.Bold); Text(label).fontSize(11).fontColor(DesignTokens.muted(this.darkMode)); }.layoutWeight(1); }
+  @Builder private metric(value: number, label: string) { Column({ space: 5 }) { Text(`${value}`).fontSize(21).fontWeight(FontWeight.Bold); Text(label).fontSize(12).fontColor(DesignTokens.muted(this.darkMode)); }.layoutWeight(1); }
   @Builder
   private couponCard(item: MarketingCoupon) {
     Row({ space: 12 }) {
@@ -144,7 +193,7 @@ struct MarketingPage {
         Text(item.threshold
           ? I18n.text(this.language, `满¥${item.threshold}可用`, `Spend ¥${item.threshold}`)
           : I18n.text(this.language, '无门槛', 'No minimum'))
-          .fontSize(10).fontColor(DesignTokens.muted(this.darkMode));
+          .fontSize(12).fontColor(DesignTokens.muted(this.darkMode));
       }.width(76);
       Column({ space: 5 }) {
         Row({ space: 6 }) {
@@ -160,21 +209,21 @@ struct MarketingPage {
         Text(I18n.text(this.language,
           `库存 ${item.stock}  领取 ${item.received}  使用 ${item.used}  每人限 ${item.perUserLimit}`,
           `Stock ${item.stock}  Claimed ${item.received}  Used ${item.used}  Limit ${item.perUserLimit}`))
-          .fontSize(11).fontColor(DesignTokens.muted(this.darkMode));
+          .fontSize(12).fontColor(DesignTokens.muted(this.darkMode));
         Text(I18n.text(this.language, `${item.validFrom.slice(0, 10)} 至 ${item.validTo.slice(0, 10)}`,
           `${item.validFrom.slice(0, 10)} to ${item.validTo.slice(0, 10)}`))
-          .fontSize(11).fontColor(DesignTokens.muted(this.darkMode));
+          .fontSize(12).fontColor(DesignTokens.muted(this.darkMode));
         Row({ space: 6 }) {
           Blank();
           if (item.status !== 'ended') {
-            IBestButton({ text: item.status === 'active' ? I18n.text(this.language, '暂停', 'Pause') : I18n.text(this.language, '启用', 'Enable'), plain: true, color: DesignTokens.brand(this.darkMode), onBtnClick: () => this.toggle(item) });
+            IBestButton({ btnHeight: 44, text: item.status === 'active' ? I18n.text(this.language, '暂停', 'Pause') : I18n.text(this.language, '启用', 'Enable'), plain: true, color: DesignTokens.brand(this.darkMode), onBtnClick: () => this.toggle(item) });
           }
-          IBestButton({ text: I18n.text(this.language, '复制', 'Copy'), plain: true, onBtnClick: () => this.duplicate(item) });
-          IBestButton({ text: I18n.text(this.language, '编辑', 'Edit'), plain: true, onBtnClick: () => this.open(item) });
-          IBestButton({ text: I18n.text(this.language, '删除', 'Delete'), plain: true, type: 'danger', onBtnClick: () => this.remove(item) });
+          IBestButton({ btnHeight: 44, text: I18n.text(this.language, '复制', 'Copy'), plain: true, onBtnClick: () => this.duplicate(item) });
+          IBestButton({ btnHeight: 44, text: I18n.text(this.language, '编辑', 'Edit'), plain: true, onBtnClick: () => this.open(item) });
+          IBestButton({ btnHeight: 44, text: I18n.text(this.language, '删除', 'Delete'), plain: true, type: 'danger', onBtnClick: () => this.remove(item) });
         }.width('100%');
       }.layoutWeight(1).alignItems(HorizontalAlign.Start);
-    }.width('100%').padding(14).backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(DesignTokens.RADIUS_LARGE);
+    }.width('100%').padding(14).backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(16);
   }
 
   @Builder
@@ -196,27 +245,28 @@ struct MarketingPage {
           Text(I18n.text(this.language, `活动价 ¥${Number(item.price).toFixed(2)}`, `Offer price ¥${Number(item.price).toFixed(2)}`)).fontSize(13).fontColor(DesignTokens.brand(this.darkMode)).width('100%');
         }
         if (item.kind === 'flashSale') {
-          Text(I18n.text(this.language, `库存 ${item.stock || 0} · 已售 ${item.sold || 0}`, `Stock ${item.stock || 0} · Sold ${item.sold || 0}`)).fontSize(11).fontColor(DesignTokens.muted(this.darkMode)).width('100%');
+          Text(I18n.text(this.language, `库存 ${item.stock || 0} · 已售 ${item.sold || 0}`, `Stock ${item.stock || 0} · Sold ${item.sold || 0}`)).fontSize(12).fontColor(DesignTokens.muted(this.darkMode)).width('100%');
         } else {
-          Text(I18n.text(this.language, `${item.groupSize || 0} 人成团 · ${item.validHours || 0} 小时有效`, `${item.groupSize || 0} people · Valid for ${item.validHours || 0} hours`)).fontSize(11).fontColor(DesignTokens.muted(this.darkMode)).width('100%');
+          Text(I18n.text(this.language, `${item.groupSize || 0} 人成团 · ${item.validHours || 0} 小时有效`, `${item.groupSize || 0} people · Valid for ${item.validHours || 0} hours`)).fontSize(12).fontColor(DesignTokens.muted(this.darkMode)).width('100%');
         }
         if (item.validFrom && item.validTo) {
           Text(I18n.text(this.language, `${item.validFrom.slice(0, 16).replace('T', ' ')} 至 ${item.validTo.slice(0, 16).replace('T', ' ')}`,
             `${item.validFrom.slice(0, 16).replace('T', ' ')} to ${item.validTo.slice(0, 16).replace('T', ' ')}`))
-            .fontSize(10).fontColor(DesignTokens.muted(this.darkMode)).width('100%');
+            .fontSize(12).fontColor(DesignTokens.muted(this.darkMode)).width('100%');
         }
       }.layoutWeight(1).alignItems(HorizontalAlign.Start);
-    }.width('100%').padding(14).backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(DesignTokens.RADIUS_LARGE);
+    }.width('100%').padding(14).backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(16);
   }
 
   build() {
     NavDestination() {
       Stack({ alignContent: Alignment.Bottom }) {
         Column() {
-          IBestNavBar({ title: I18n.text(this.language, '营销中心', 'Marketing'), isShowStatusBar: true, navBarBgColor: DesignTokens.surface(this.darkMode), onLeftClick: () => AppRouter.back() });
+          SecondaryHeader({ title: I18n.text(this.language, '营销中心', 'Marketing'), isShowStatusBar: true, navBarBgColor: DesignTokens.surface(this.darkMode), onLeftClick: () => this.handleBack() });
+          SecondaryFeedback({ errorKind: this.pageError, updating: this.loading, onRetry: () => this.load() });
           if (this.loading) Column() { LoadingProgress().width(34).height(34).color(DesignTokens.brand(this.darkMode)); }.layoutWeight(1).justifyContent(FlexAlign.Center);
           else Scroll() { Column({ space: 14 }) {
-            if (this.overview) Row() { this.metric(this.overview.coupons.active, I18n.text(this.language, '生效优惠券', 'Active coupons')); this.metric(this.overview.coupons.totalReceived || 0, I18n.text(this.language, '累计领取', 'Total claimed')); this.metric(this.overview.flashSales.active, I18n.text(this.language, '限时活动', 'Flash sales')); this.metric(this.overview.groupBuys.active, I18n.text(this.language, '团购活动', 'Group buys')); }.width('100%').padding(16).backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(DesignTokens.RADIUS_LARGE);
+            if (this.overview) Row() { this.metric(this.overview.coupons.active, I18n.text(this.language, '生效优惠券', 'Active coupons')); this.metric(this.overview.coupons.totalReceived || 0, I18n.text(this.language, '累计领取', 'Total claimed')); this.metric(this.overview.flashSales.active, I18n.text(this.language, '限时活动', 'Flash sales')); this.metric(this.overview.groupBuys.active, I18n.text(this.language, '团购活动', 'Group buys')); }.width('100%').padding(16).backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(16);
             Row({ space: 8 }) {
               ForEach(['coupon', 'flashSale', 'groupBuy'], (kind: string) => {
                 Text(kind === 'coupon' ? I18n.text(this.language, '优惠券', 'Coupons') : kind === 'flashSale' ? I18n.text(this.language, '限时活动', 'Flash sales') : I18n.text(this.language, '团购活动', 'Group buys'))
@@ -227,7 +277,7 @@ struct MarketingPage {
               }, (kind: string) => kind);
             }.width('100%');
             if (this.activityKind === 'coupon') {
-              Row() { Text(I18n.text(this.language, '我的优惠券', 'My coupons')).fontSize(18).fontWeight(FontWeight.Bold); Blank(); IBestButton({ text: I18n.text(this.language, '新增优惠券', 'Add coupon'), type: 'primary', color: DesignTokens.brand(this.darkMode), fontColor: DesignTokens.onBrand(this.darkMode), onBtnClick: () => this.open() }); }.width('100%');
+              Row() { Text(I18n.text(this.language, '我的优惠券', 'My coupons')).fontSize(18).fontWeight(FontWeight.Bold); Blank(); IBestButton({ btnHeight: 44, text: I18n.text(this.language, '新增优惠券', 'Add coupon'), type: 'primary', color: DesignTokens.brand(this.darkMode), fontColor: DesignTokens.onBrand(this.darkMode), onBtnClick: () => this.open() }); }.width('100%');
               Row({ space: 8 }) {
                 ForEach(['all', 'active', 'pending', 'ended'], (status: string) => {
                   Text(status === 'all' ? I18n.text(this.language, '全部', 'All') : status === 'active' ? I18n.text(this.language, '进行中', 'Active') : status === 'pending' ? I18n.text(this.language, '未开始', 'Upcoming') : I18n.text(this.language, '已结束', 'Ended'))
@@ -276,13 +326,13 @@ struct MarketingPage {
             IBestField({ value: $validFrom, label: I18n.text(this.language, '开始日期', 'Start date'), placeholder: 'YYYY-MM-DD' });
             IBestField({ value: $validTo, label: I18n.text(this.language, '结束日期', 'End date'), placeholder: 'YYYY-MM-DD' });
             Row({ space: 10 }) {
-              IBestButton({ text: I18n.text(this.language, '取消', 'Cancel'), plain: true, btnWidth: '48%', disabled: this.formSaving, onBtnClick: () => this.editorVisible = false });
-              IBestButton({ text: I18n.text(this.language, '保存', 'Save'), type: 'primary', color: DesignTokens.brand(this.darkMode), fontColor: DesignTokens.onBrand(this.darkMode), btnWidth: '48%', loading: this.formSaving, disabled: this.formSaving, onBtnClick: () => this.save() });
+              IBestButton({ btnHeight: 44, text: I18n.text(this.language, '取消', 'Cancel'), plain: true, btnWidth: '48%', disabled: this.formSaving, onBtnClick: () => this.closeLocalEditor() });
+              IBestButton({ btnHeight: 44, text: I18n.text(this.language, '保存', 'Save'), type: 'primary', color: DesignTokens.brand(this.darkMode), fontColor: DesignTokens.onBrand(this.darkMode), btnWidth: '48%', loading: this.formSaving, disabled: this.formSaving, onBtnClick: () => this.save() });
             }.width('100%');
-          }.width('100%').padding({ left: 20, right: 20, top: 22, bottom: this.safeBottom + 22 })
+          }.width('100%').padding({ left: 16, right: 16, top: 22, bottom: this.safeBottom + 22 })
             .backgroundColor(DesignTokens.card(this.darkMode)).borderRadius({ topLeft: 24, topRight: 24 });
         }
       }.width('100%').height('100%');
-    }.hideTitleBar(true);
+    }.hideTitleBar(true).onBackPressed((): boolean => { this.handleBack(); return true; });
   }
 }
diff --git a/native/harmony-merchant/entry/src/main/ets/features/business/PriceRulePage.ets b/native/harmony-merchant/entry/src/main/ets/features/business/PriceRulePage.ets
index a2850ae7..f69c164a 100644
--- a/native/harmony-merchant/entry/src/main/ets/features/business/PriceRulePage.ets
+++ b/native/harmony-merchant/entry/src/main/ets/features/business/PriceRulePage.ets
@@ -1,18 +1,31 @@
-import { common } from '@kit.AbilityKit';
-import { IBestButton, IBestDialogUtil, IBestNavBar, IBestSwitch, IBestToast } from '@ibestservices/ibest-ui';
+import { LeaveGuard } from '../../core/ui/LeaveGuard';
+import { PageReadScope, SecondaryPolicy } from '../../core/ui/SecondaryPolicy';
+import { SecondaryFeedback, SecondaryHeader, SecondarySaveBar } from '../../core/ui/SecondaryPage';
+import { IBestButton, IBestDialogUtil, IBestSwitch, IBestToast } from '@ibestservices/ibest-ui';
 import { AppRouter } from '../../core/navigation/AppRouter';
 import { ApiError } from '../../core/network/ApiClient';
 import { DesignTokens } from '../../core/theme/DesignTokens';
 import { I18n } from '../../core/i18n/I18n';
 import { ShopPriceRule } from './BusinessModels';
 import { BusinessRepository } from './BusinessRepository';
-import { CachedPriceRule, PriceRuleStore } from './PriceRuleStore';
 
 @Builder
 export function PriceRuleBuilder(_: string) { PriceRulePage(); }
 
 @Component
 struct PriceRulePage {
+  @StorageProp('windowWidth') private windowWidth: number = 390;
+  @State private verifying: boolean = false;
+  private handleBack(): void {
+
+
+    this.leaveGuard.request(this.language, this.saving, this.dirty, () => AppRouter.back());
+  }
+
+  private leaveGuard: LeaveGuard = new LeaveGuard();
+  private pageScope: PageReadScope = new PageReadScope();
+  @State private pageError: string = '';
+
   @StorageProp('safeBottom') private safeBottom: number = 16;
   @StorageLink('language') private language: string = 'zh-CN';
   @StorageLink('darkMode') private darkMode: boolean = false;
@@ -22,22 +35,15 @@ struct PriceRulePage {
   @State private member: string = 'member';
   @State private loading: boolean = true;
   @State private saving: boolean = false;
-  private dirty: boolean = false;
-  private saveTimer: number = -1;
+  @State private dirty: boolean = false;
+  @State private loaded: boolean = false;
+  private savedRule: string = '';
 
-  aboutToAppear(): void { this.load(); }
+  aboutToAppear(): void { this.pageScope.open(); this.load(); }
+
+  aboutToDisappear(): void { this.pageScope.dispose();}
 
-  aboutToDisappear(): void {
-    if (this.saveTimer >= 0) {
-      clearTimeout(this.saveTimer);
-      this.saveTimer = -1;
-    }
-    if (this.dirty) this.flushSave();
-  }
 
-  private context(): common.UIAbilityContext {
-    return this.getUIContext().getHostContext() as common.UIAbilityContext;
-  }
 
   private applyRule(data: ShopPriceRule): void {
     this.guest = data.guestAllow;
@@ -56,75 +62,49 @@ struct PriceRulePage {
   }
 
   private async load(): Promise<void> {
-    let cached: CachedPriceRule | null = null;
+    if (!this.pageScope.isCurrent()) return;
+    const pageTicket = this.pageScope.begin('load');
+    this.pageError = '';
+    this.loading = true;
     try {
-      cached = await PriceRuleStore.read(this.context());
-      if (cached) {
-        this.applyRule(cached.rule);
-        this.loading = false;
-      }
-      if (cached?.dirty) {
-        const synced = await BusinessRepository.savePriceRule(cached.rule);
-        this.applyRule(synced);
-        await PriceRuleStore.write(this.context(), synced, false);
-      } else {
-        const remote = await BusinessRepository.priceRule();
-        this.applyRule(remote);
-        await PriceRuleStore.write(this.context(), remote, false);
-      }
+      const remote = await this.pageScope.read(BusinessRepository.priceRule(), pageTicket);
+      if (!this.dirty) this.applyRule(remote);
+      this.savedRule = JSON.stringify(remote); this.loaded = true; this.verifying = false;
+      this.dirty = JSON.stringify(this.currentRule()) !== this.savedRule;
+      // Legacy unowned dirty cache is preserved, never silently uploaded into the current account.
     } catch (error) {
-      if (!cached) IBestToast.show(error instanceof ApiError ? error.message
-        : I18n.text(this.language, '价格规则加载失败', 'Failed to load pricing rules'));
+      if (!this.pageScope.accepts(pageTicket)) return;
+      this.pageError = SecondaryPolicy.errorKind(error);
+      IBestToast.show(I18n.text(this.language, '价格规则加载失败，请重试后再编辑', 'Load pricing rules before editing. Please retry.'));
+
     } finally {
-      this.loading = false;
+      if (this.pageScope.accepts(pageTicket)) { this.loading = false; }
     }
   }
 
-  private queueSave(tier: string): void {
-    this.dirty = true;
-    PriceRuleStore.write(this.context(), this.currentRule(), true);
-    if (this.saveTimer >= 0) clearTimeout(this.saveTimer);
-    this.saveTimer = setTimeout((): void => {
-      this.saveTimer = -1;
-      this.flushSave();
-    }, 250);
-    IBestToast.show(I18n.text(this.language, `${tier} · 已保存`, `${tier} · Saved`));
+  private queueSave(_tier: string): void {
+    this.dirty = this.loaded && JSON.stringify(this.currentRule()) !== this.savedRule;
   }
 
   private async flushSave(): Promise<void> {
     if (this.saving || !this.dirty) return;
+    if (!this.pageScope.isCurrent()) return;
+    const writeTicket = this.pageScope.begin('save');
+    if (!this.loaded || this.verifying || this.loading) return;
     const body = this.currentRule();
-    this.dirty = false;
     this.saving = true;
-    let failed = false;
-    await PriceRuleStore.write(this.context(), body, true);
     try {
       const synced = await BusinessRepository.savePriceRule(body);
-      if (!this.dirty) {
-        this.applyRule(synced);
-        await PriceRuleStore.write(this.context(), synced, false);
-      } else {
-        await PriceRuleStore.write(this.context(), this.currentRule(), true);
-      }
+      if (!this.pageScope.accepts(writeTicket)) return;
+      this.savedRule = JSON.stringify(synced);
+      this.dirty = JSON.stringify(this.currentRule()) !== this.savedRule;
+      IBestToast.show(I18n.text(this.language, '价格规则已保存', 'Pricing rules saved'));
     } catch (error) {
-      this.dirty = true;
-      failed = true;
-      IBestToast.show(error instanceof ApiError
-        ? I18n.text(this.language, `${error.message}，修改已保存在本机`, `${error.message}. Changes are saved on this device.`)
-        : I18n.text(this.language, '网络不可用，修改已保存在本机', 'Network unavailable. Changes are saved on this device.'));
-    } finally {
-      this.saving = false;
-      // A new edit made while this request was in flight is flushed next.
-      // A network failure stays dirty for the next edit/page visit instead of
-      // hammering the server every 250 ms while the device is offline.
-      if (this.dirty && !failed) {
-        if (this.saveTimer >= 0) clearTimeout(this.saveTimer);
-        this.saveTimer = setTimeout((): void => {
-          this.saveTimer = -1;
-          this.flushSave();
-        }, 250);
-      }
-    }
+      if (!this.pageScope.accepts(writeTicket)) return;
+      this.verifying = true;
+      await this.load();
+      IBestToast.show(I18n.text(this.language, '保存未确认，请保留草稿并重新读取规则核对结果。', 'Save was not confirmed. Keep the draft and reload to verify the result.'));
+    } finally { if (this.pageScope.accepts(writeTicket)) this.saving = false; }
   }
 
   private reset(): void {
@@ -158,87 +138,96 @@ struct PriceRulePage {
     Text(label).fontSize(13).textAlign(TextAlign.Center).padding({ top: 9, bottom: 9 }).layoutWeight(1)
       .backgroundColor(current === value ? DesignTokens.brand(this.darkMode) : DesignTokens.subtle(this.darkMode))
       .fontColor(current === value ? DesignTokens.onBrand(this.darkMode) : DesignTokens.text(this.darkMode))
-      .borderRadius(17).onClick(onTap);
+      .constraintSize({ minHeight: 44 }).borderRadius(10).onClick(onTap);
   }
 
   build() {
     NavDestination() {
       Column() {
-        IBestNavBar({
+        SecondaryHeader({
           title: I18n.text(this.language, '价格显示规则', 'Price display rules'),
-          isShowStatusBar: true, navBarBgColor: DesignTokens.surface(this.darkMode), onLeftClick: () => AppRouter.back()
+          isShowStatusBar: true, navBarBgColor: DesignTokens.surface(this.darkMode), onLeftClick: () => this.handleBack()
         });
-        if (this.loading) {
+          SecondaryFeedback({ errorKind: this.pageError, updating: this.loading, onRetry: () => this.load() });
+        if (this.loading && !this.loaded) {
           Column() { LoadingProgress().width(34).height(34).color(DesignTokens.brand(this.darkMode)); }
             .layoutWeight(1).justifyContent(FlexAlign.Center);
         } else {
           Scroll() {
-            Column({ space: 14 }) {
-              Column({ space: 8 }) {
-                Text(I18n.text(this.language, '价格显示规则', 'Price display rules')).fontSize(20).fontWeight(FontWeight.Bold).width('100%');
-                Text(I18n.text(this.language, '按访问者身份展示对应价格，修改后全店商品立即生效。', 'Show prices by visitor role. Changes apply to all products immediately.'))
-                  .fontSize(12).fontColor(DesignTokens.muted(this.darkMode)).width('100%');
-                Text(this.summary()).fontSize(11).fontColor(DesignTokens.brand(this.darkMode)).width('100%');
-              }.width('100%').padding(18).backgroundColor(DesignTokens.brandTint(this.darkMode)).borderRadius(DesignTokens.RADIUS_LARGE);
-
-              Column() {
-                Row() {
-                  Column({ space: 4 }) {
-                    Text(I18n.text(this.language, '未登录访客', 'Signed-out visitors')).fontSize(16).fontWeight(FontWeight.Medium);
-                    Text(this.guest
-                      ? I18n.text(this.language, '允许浏览，价格按普通客户规则显示', 'Can browse using customer pricing')
-                      : I18n.text(this.language, '禁止进入，必须登录后看店', 'Must sign in before browsing'))
-                      .fontSize(12).fontColor(DesignTokens.muted(this.darkMode));
-                  }.layoutWeight(1).alignItems(HorizontalAlign.Start);
-                  IBestSwitch({ value: $guest, activeColor: DesignTokens.brand(this.darkMode),
-                    onChange: () => this.queueSave(I18n.text(this.language, '未登录访客', 'Signed-out visitors')) });
-                }.width('100%');
-              }.width('100%').padding(16).backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(DesignTokens.RADIUS_LARGE);
-
-              Column({ space: 10 }) {
-                Text(I18n.text(this.language, '普通客户', 'Customers')).fontSize(16).fontWeight(FontWeight.Medium).width('100%');
-                Text(I18n.text(this.language, '已登录但未授权门店', 'Signed in but not an authorised store')).fontSize(11).fontColor(DesignTokens.muted(this.darkMode)).width('100%');
-                Row({ space: 8 }) {
-                  this.choice('retail', I18n.text(this.language, '显示零售价', 'Show retail'), this.customer, () => {
-                    this.customer = 'retail'; this.queueSave(I18n.text(this.language, '普通客户', 'Customers'));
-                  });
-                  this.choice('hidden', I18n.text(this.language, '隐藏价格', 'Hide prices'), this.customer, () => {
-                    this.customer = 'hidden'; this.queueSave(I18n.text(this.language, '普通客户', 'Customers'));
-                  });
-                }.width('100%');
-              }.width('100%').padding(16).backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(DesignTokens.RADIUS_LARGE);
-
-              Column({ space: 10 }) {
-                Text(I18n.text(this.language, '授权门店', 'Authorised stores')).fontSize(16).fontWeight(FontWeight.Medium).width('100%');
-                Text(I18n.text(this.language, '已申请代理或加盟门店', 'Agency or franchise stores')).fontSize(11).fontColor(DesignTokens.muted(this.darkMode)).width('100%');
-                Row({ space: 8 }) {
-                  this.choice('wholesale', I18n.text(this.language, '批发价', 'Wholesale'), this.agency, () => {
-                    this.agency = 'wholesale'; this.queueSave(I18n.text(this.language, '授权门店', 'Authorised stores'));
-                  });
-                  this.choice('retail', I18n.text(this.language, '零售价', 'Retail'), this.agency, () => {
-                    this.agency = 'retail'; this.queueSave(I18n.text(this.language, '授权门店', 'Authorised stores'));
-                  });
-                }.width('100%');
-              }.width('100%').padding(16).backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(DesignTokens.RADIUS_LARGE);
-
-              Column({ space: 10 }) {
-                Text(I18n.text(this.language, '会员客户', 'Member customers')).fontSize(16).fontWeight(FontWeight.Medium).width('100%');
-                Row({ space: 8 }) {
-                  this.choice('member', I18n.text(this.language, '会员价', 'Member price'), this.member, () => {
-                    this.member = 'member'; this.queueSave(I18n.text(this.language, '会员客户', 'Member customers'));
-                  });
-                  this.choice('retail', I18n.text(this.language, '零售价', 'Retail'), this.member, () => {
-                    this.member = 'retail'; this.queueSave(I18n.text(this.language, '会员客户', 'Member customers'));
-                  });
-                }.width('100%');
-              }.width('100%').padding(16).backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(DesignTokens.RADIUS_LARGE);
-
-              IBestButton({ text: I18n.text(this.language, '恢复默认规则', 'Restore defaults'),
-                plain: true, type: 'danger', btnWidth: '100%', loading: this.saving, onBtnClick: () => this.reset() });
-            }.width('100%').padding({ left: 16, right: 16, top: 12, bottom: this.safeBottom + 20 });
-          }.layoutWeight(1).align(Alignment.Top);
+            GridRow({ columns: this.windowWidth >= 720 ? 2 : 1, gutter: 12 }) {
+              GridCol() {
+                Column({ space: 8 }) {
+                  Text(I18n.text(this.language, '价格显示规则', 'Price display rules')).fontSize(20).fontWeight(FontWeight.Bold).width('100%');
+                  Text(I18n.text(this.language, '按访问者身份展示对应价格，保存后全店商品生效。', 'Show prices by visitor role. Save to apply changes to all products.'))
+                    .fontSize(12).fontColor(DesignTokens.muted(this.darkMode)).width('100%');
+                  Text(this.summary()).fontSize(12).fontColor(DesignTokens.brand(this.darkMode)).width('100%');
+                }.width('100%').padding(16).backgroundColor(DesignTokens.brandTint(this.darkMode)).borderRadius(16);
+              }
+              GridCol() {
+                Column() {
+                  Row() {
+                    Column({ space: 4 }) {
+                      Text(I18n.text(this.language, '未登录访客', 'Signed-out visitors')).fontSize(16).fontWeight(FontWeight.Medium);
+                      Text(this.guest
+                        ? I18n.text(this.language, '允许浏览，价格按普通客户规则显示', 'Can browse using customer pricing')
+                        : I18n.text(this.language, '禁止进入，必须登录后看店', 'Must sign in before browsing'))
+                        .fontSize(12).fontColor(DesignTokens.muted(this.darkMode));
+                    }.layoutWeight(1).alignItems(HorizontalAlign.Start);
+                    IBestSwitch({ value: $guest, activeColor: DesignTokens.brand(this.darkMode),
+                      onChange: () => this.queueSave(I18n.text(this.language, '未登录访客', 'Signed-out visitors')) });
+                  }.width('100%');
+                }.width('100%').padding(16).backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(16);
+              }
+              GridCol() {
+                Column({ space: 10 }) {
+                  Text(I18n.text(this.language, '普通客户', 'Customers')).fontSize(16).fontWeight(FontWeight.Medium).width('100%');
+                  Text(I18n.text(this.language, '已登录但未授权门店', 'Signed in but not an authorised store')).fontSize(12).fontColor(DesignTokens.muted(this.darkMode)).width('100%');
+                  Row({ space: 8 }) {
+                    this.choice('retail', I18n.text(this.language, '显示零售价', 'Show retail'), this.customer, () => {
+                      this.customer = 'retail'; this.queueSave(I18n.text(this.language, '普通客户', 'Customers'));
+                    });
+                    this.choice('hidden', I18n.text(this.language, '隐藏价格', 'Hide prices'), this.customer, () => {
+                      this.customer = 'hidden'; this.queueSave(I18n.text(this.language, '普通客户', 'Customers'));
+                    });
+                  }.width('100%');
+                }.width('100%').padding(16).backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(16);
+              }
+              GridCol() {
+                Column({ space: 10 }) {
+                  Text(I18n.text(this.language, '授权门店', 'Authorised stores')).fontSize(16).fontWeight(FontWeight.Medium).width('100%');
+                  Text(I18n.text(this.language, '已申请代理或加盟门店', 'Agency or franchise stores')).fontSize(12).fontColor(DesignTokens.muted(this.darkMode)).width('100%');
+                  Row({ space: 8 }) {
+                    this.choice('wholesale', I18n.text(this.language, '批发价', 'Wholesale'), this.agency, () => {
+                      this.agency = 'wholesale'; this.queueSave(I18n.text(this.language, '授权门店', 'Authorised stores'));
+                    });
+                    this.choice('retail', I18n.text(this.language, '零售价', 'Retail'), this.agency, () => {
+                      this.agency = 'retail'; this.queueSave(I18n.text(this.language, '授权门店', 'Authorised stores'));
+                    });
+                  }.width('100%');
+                }.width('100%').padding(16).backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(16);
+              }
+              GridCol() {
+                Column({ space: 10 }) {
+                  Text(I18n.text(this.language, '会员客户', 'Member customers')).fontSize(16).fontWeight(FontWeight.Medium).width('100%');
+                  Row({ space: 8 }) {
+                    this.choice('member', I18n.text(this.language, '会员价', 'Member price'), this.member, () => {
+                      this.member = 'member'; this.queueSave(I18n.text(this.language, '会员客户', 'Member customers'));
+                    });
+                    this.choice('retail', I18n.text(this.language, '零售价', 'Retail'), this.member, () => {
+                      this.member = 'retail'; this.queueSave(I18n.text(this.language, '会员客户', 'Member customers'));
+                    });
+                  }.width('100%');
+                }.width('100%').padding(16).backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(16);
+              }
+              GridCol() {
+                IBestButton({ btnHeight: 44, text: I18n.text(this.language, '恢复默认规则', 'Restore defaults'),
+                  plain: true, type: 'danger', btnWidth: '100%', loading: this.saving, onBtnClick: () => this.reset() });
+              }
+            }.width('100%').padding({ left: 16, right: 16, top: 12, bottom: 12 });
+          }.enabled(this.loaded && !this.saving && !this.loading).layoutWeight(1).align(Alignment.Top);
+          SecondarySaveBar({ saving: this.saving, canSave: this.loaded && this.dirty && !this.verifying && !this.loading, changed: this.dirty, onSave: () => this.flushSave() });
         }
       }.width('100%').height('100%').backgroundColor(DesignTokens.page(this.darkMode));
-    }.hideTitleBar(true);
+    }.hideTitleBar(true).onBackPressed((): boolean => { this.handleBack(); return true; });
   }
 }
diff --git a/native/harmony-merchant/entry/src/main/ets/features/business/StaffPage.ets b/native/harmony-merchant/entry/src/main/ets/features/business/StaffPage.ets
index 97207c70..203d1204 100644
--- a/native/harmony-merchant/entry/src/main/ets/features/business/StaffPage.ets
+++ b/native/harmony-merchant/entry/src/main/ets/features/business/StaffPage.ets
@@ -1,3 +1,6 @@
+import { LeaveGuard } from '../../core/ui/LeaveGuard';
+import { PageReadScope, SecondaryPolicy } from '../../core/ui/SecondaryPolicy';
+import { SecondaryFeedback, SecondaryHeader } from '../../core/ui/SecondaryPage';
 import { common } from '@kit.AbilityKit';
 import {
   IBestActionSheet,
@@ -5,7 +8,6 @@ import {
   IBestDialogUtil,
   IBestEmpty,
   IBestField,
-  IBestNavBar,
   IBestSkeleton,
   IBestTag,
   IBestToast
@@ -25,6 +27,24 @@ export function StaffsBuilder(_: string) { StaffPage(); }
 
 @Component
 struct StaffPage {
+  private handleBack(): void {
+    if (this.editorVisible) { this.closeLocalEditor(); return; }
+
+    this.leaveGuard.request(this.language, false, false, () => AppRouter.back());
+  }
+
+  private editStamp: string = '';
+  private localStamp(): string { return JSON.stringify([this.editingId, this.name, this.phone, this.role, this.editingStatus, this.editingPermissions]); }
+  private closeLocalEditor(): void {
+    this.leaveGuard.request(this.language, this.saving || Boolean(this.actionId), this.localStamp() !== this.editStamp, () => { this.editorVisible = false; });
+  }
+
+  private leaveGuard: LeaveGuard = new LeaveGuard();
+  aboutToDisappear(): void { this.pageScope.dispose(); }
+
+  private pageScope: PageReadScope = new PageReadScope();
+  @State private pageError: string = '';
+
   @StorageProp('safeBottom') private safeBottom: number = 16;
   @StorageLink('language') private language: string = 'zh-CN';
   @StorageLink('darkMode') private darkMode: boolean = false;
@@ -45,23 +65,30 @@ struct StaffPage {
   @State private actionId: string = '';
   private readonly defaultPermissions: string[] = ['product:read', 'product:write', 'order:read'];
 
-  aboutToAppear(): void { this.load(); }
+  aboutToAppear(): void { this.pageScope.open(); this.load(); }
 
   private async load(): Promise<void> {
+    if (!this.pageScope.isCurrent()) return;
     if (this.loading) return;
+    const pageTicket = this.pageScope.begin('load');
+    this.pageError = '';
     this.loading = true;
     try {
-      const result = await BusinessRepository.staffs('', 1, 100);
+      const result = await this.pageScope.read(BusinessRepository.staffs('', 1, 100), pageTicket);
       this.staffs = result.list;
       this.loadError = '';
     } catch (error) {
-      this.loadError = error instanceof ApiError ? error.message
+      if (!this.pageScope.accepts(pageTicket)) return;
+      this.pageError = SecondaryPolicy.errorKind(error);
+      this.loadError = error instanceof ApiError ? SecondaryPolicy.message(error, this.language)
         : I18n.text(this.language, '员工加载失败，请稍后重试', 'Failed to load staff. Try again later.');
       if (this.staffs.length > 0) IBestToast.show(this.loadError);
     } finally {
+      if (this.pageScope.accepts(pageTicket)) {
       this.loading = false;
       this.refreshing = false;
     }
+    }
   }
 
   private async refresh(): Promise<void> {
@@ -79,15 +106,14 @@ struct StaffPage {
     this.phone = item?.phone || '';
     this.role = item?.role || 'sales';
     this.editingStatus = item?.status || 'active';
-    this.editingPermissions = item?.permissions || this.defaultPermissions;
-    this.editorVisible = true;
+    this.editingPermissions = (item?.permissions || this.defaultPermissions).slice();
+    this.editStamp = this.localStamp(); this.editorVisible = true;
   }
 
-  private closeEditor(): void {
-    if (!this.saving) this.editorVisible = false;
-  }
+  private closeEditor(): void { this.closeLocalEditor(); }
 
   private async save(): Promise<void> {
+    if (!this.pageScope.isCurrent()) return;
     if (this.saving || this.actionId) return;
     if (!this.name.trim() || !InputValidators.isMainlandPhone(this.phone)) {
       IBestToast.show(I18n.text(this.language, '请填写姓名和正确手机号', 'Enter a name and valid phone number'));
@@ -98,17 +124,21 @@ struct StaffPage {
       permissions: this.editingPermissions.length > 0 ? this.editingPermissions : this.defaultPermissions
     };
     this.saving = true;
+    const mutationTicket = this.pageScope.begin('save');
     try {
-      if (this.editingId) await BusinessRepository.updateStaff(this.editingId, body);
-      else await BusinessRepository.createStaff(body);
+      if (this.editingId) await this.pageScope.read(BusinessRepository.updateStaff(this.editingId, body), mutationTicket);
+      else await this.pageScope.read(BusinessRepository.createStaff(body), mutationTicket);
       this.editorVisible = false;
       IBestToast.show(I18n.text(this.language, '员工已保存', 'Staff member saved'));
-      await this.load();
+      await this.pageScope.read(this.load(), mutationTicket);
     } catch (error) {
-      IBestToast.show(error instanceof ApiError ? error.message : I18n.text(this.language, '保存失败', 'Failed to save'));
+      if (!this.pageScope.accepts(mutationTicket)) return;
+      IBestToast.show(error instanceof ApiError ? SecondaryPolicy.message(error, this.language) : I18n.text(this.language, '保存失败', 'Failed to save'));
     } finally {
+      if (this.pageScope.accepts(mutationTicket)) {
       this.saving = false;
     }
+    }
   }
 
   private showStaffActions(item: StaffItem): void {
@@ -135,21 +165,26 @@ struct StaffPage {
   }
 
   private async updateEmployment(item: StaffItem): Promise<void> {
+    if (!this.pageScope.isCurrent()) return;
     if (this.actionId || this.saving) return;
     this.actionId = item.id;
     const body: StaffMutation = {
       name: item.name, phone: item.phone, role: item.role, status: 'left', permissions: item.permissions
     };
+    const mutationTicket = this.pageScope.begin('updateEmployment');
     try {
-      await BusinessRepository.updateStaff(item.id, body);
+      await this.pageScope.read(BusinessRepository.updateStaff(item.id, body), mutationTicket);
       IBestToast.show(I18n.text(this.language, '已办理离职', 'Marked as left'));
-      await this.load();
+      await this.pageScope.read(this.load(), mutationTicket);
     } catch (error) {
-      IBestToast.show(error instanceof ApiError ? error.message
+      if (!this.pageScope.accepts(mutationTicket)) return;
+      IBestToast.show(error instanceof ApiError ? SecondaryPolicy.message(error, this.language)
         : I18n.text(this.language, '状态更新失败', 'Failed to update status'));
     } finally {
+      if (this.pageScope.accepts(mutationTicket)) {
       this.actionId = '';
     }
+    }
   }
 
   private roleLabel(value: string): string {
@@ -216,7 +251,7 @@ struct StaffPage {
     try {
       await PhoneService.dial(this.getUIContext().getHostContext() as common.UIAbilityContext, item.phone);
     } catch (error) {
-      IBestToast.show(error instanceof Error ? error.message
+      IBestToast.show(error instanceof Error ? SecondaryPolicy.message(error, this.language)
         : I18n.text(this.language, '无法打开拨号界面', 'Unable to open the dialer'));
     }
   }
@@ -227,7 +262,7 @@ struct StaffPage {
       Text(value).fontSize(emphasized ? 20 : 18).fontWeight(FontWeight.Bold)
         .fontColor(emphasized ? DesignTokens.brand(this.darkMode) : DesignTokens.text(this.darkMode))
         .maxLines(1).textOverflow({ overflow: TextOverflow.Ellipsis });
-      Text(label).fontSize(11).fontColor(DesignTokens.muted(this.darkMode)).maxLines(1);
+      Text(label).fontSize(12).fontColor(DesignTokens.muted(this.darkMode)).maxLines(1);
     }.layoutWeight(1).alignItems(HorizontalAlign.Start);
   }
 
@@ -241,7 +276,7 @@ struct StaffPage {
         Row({ space: 5 }) {
           Circle().width(6).height(6).fill(DesignTokens.success(this.darkMode));
           Text(I18n.text(this.language, '数据实时更新', 'Live data'))
-            .fontSize(10).fontColor(DesignTokens.muted(this.darkMode));
+            .fontSize(12).fontColor(DesignTokens.muted(this.darkMode));
         }
       }.width('100%');
       Row({ space: 12 }) {
@@ -253,7 +288,7 @@ struct StaffPage {
         this.overviewMetric(`${this.countLeftStaff()}`, I18n.text(this.language, '已离职', 'Former staff'));
       }.width('100%');
     }.width('100%').padding({ left: 16, right: 16, top: 14, bottom: 14 })
-      .backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(DesignTokens.RADIUS_LARGE)
+      .backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(16)
       .border({ width: 0.5, color: DesignTokens.border(this.darkMode) });
   }
 
@@ -262,7 +297,7 @@ struct StaffPage {
     Column({ space: 2 }) {
       Text(label).fontSize(12).fontWeight(this.roleFilter === value ? FontWeight.Medium : FontWeight.Regular)
         .fontColor(this.roleFilter === value ? DesignTokens.brand(this.darkMode) : DesignTokens.muted(this.darkMode));
-      Text(`${this.roleCount(value)}`).fontSize(10)
+      Text(`${this.roleCount(value)}`).fontSize(12)
         .fontColor(this.roleFilter === value ? DesignTokens.brand(this.darkMode) : DesignTokens.muted(this.darkMode));
     }.layoutWeight(1).height(44).justifyContent(FlexAlign.Center)
       .backgroundColor(this.roleFilter === value ? DesignTokens.selectedSurface(this.darkMode) : Color.Transparent)
@@ -319,12 +354,12 @@ struct StaffPage {
         Column({ space: 3 }) {
           Text(`¥${(item.monthlyPerformance || 0).toFixed(2)}`).fontSize(15).fontWeight(FontWeight.Bold)
             .fontColor(DesignTokens.brand(this.darkMode)).maxLines(1);
-          Text(I18n.text(this.language, '本月业绩', 'This month')).fontSize(10)
+          Text(I18n.text(this.language, '本月业绩', 'This month')).fontSize(12)
             .fontColor(DesignTokens.muted(this.darkMode));
         }.alignItems(HorizontalAlign.End);
       }.width('100%');
       if (item.permissions.length > 0) {
-        Text(this.permissionSummary(item)).fontSize(11).fontColor(DesignTokens.muted(this.darkMode))
+        Text(this.permissionSummary(item)).fontSize(12).fontColor(DesignTokens.muted(this.darkMode))
           .width('100%').maxLines(2).textOverflow({ overflow: TextOverflow.Ellipsis });
       }
       if (item.status === 'active') {
@@ -343,7 +378,7 @@ struct StaffPage {
         }
       }.width('100%');
     }.width('100%').padding(14).backgroundColor(DesignTokens.card(this.darkMode))
-      .borderRadius(DesignTokens.RADIUS_LARGE).border({ width: 0.5, color: DesignTokens.border(this.darkMode) });
+      .borderRadius(16).border({ width: 0.5, color: DesignTokens.border(this.darkMode) });
   }
 
   @Builder
@@ -352,7 +387,7 @@ struct StaffPage {
       ForEach([0, 1, 2], (_: number) => {
         Column() { IBestSkeleton({ rows: 3, animated: true, radius: 8 }); }
           .width('100%').height(132).padding(14)
-          .backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(DesignTokens.RADIUS_LARGE);
+          .backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(16);
       });
     }.width('100%').padding({ left: 16, right: 16, top: 10 });
   }
@@ -366,7 +401,7 @@ struct StaffPage {
             : I18n.text(this.language, '邀请员工', 'Invite staff member'))
             .fontSize(20).fontWeight(FontWeight.Bold).fontColor(DesignTokens.text(this.darkMode));
           Text(I18n.text(this.language, '员工资料和原有权限规则将保持不变', 'Existing access rules remain unchanged'))
-            .fontSize(11).fontColor(DesignTokens.muted(this.darkMode));
+            .fontSize(12).fontColor(DesignTokens.muted(this.darkMode));
         }.alignItems(HorizontalAlign.Start).layoutWeight(1);
         Column() { MerchantIcon({ name: 'cross', color: DesignTokens.muted(this.darkMode), iconSize: 19 }); }
           .width(38).height(38).justifyContent(FlexAlign.Center)
@@ -384,14 +419,14 @@ struct StaffPage {
         this.roleSelector('manager', I18n.text(this.language, '店长', 'Manager'));
       }.width('100%');
       Row({ space: 10 }) {
-        IBestButton({ text: I18n.text(this.language, '取消', 'Cancel'), plain: true,
+        IBestButton({ btnHeight: 44, text: I18n.text(this.language, '取消', 'Cancel'), plain: true,
           color: DesignTokens.muted(this.darkMode), btnWidth: '48%', disabled: this.saving,
           onBtnClick: () => this.closeEditor() });
-        IBestButton({ text: I18n.text(this.language, '保存', 'Save'), type: 'primary',
+        IBestButton({ btnHeight: 44, text: I18n.text(this.language, '保存', 'Save'), type: 'primary',
           color: DesignTokens.brand(this.darkMode), fontColor: DesignTokens.onBrand(this.darkMode), btnWidth: '48%',
           loading: this.saving, disabled: this.saving || this.actionId.length > 0, onBtnClick: () => this.save() });
       }.width('100%');
-    }.width('100%').padding({ left: 20, right: 20, top: 20, bottom: this.safeBottom + 20 })
+    }.width('100%').padding({ left: 16, right: 16, top: 20, bottom: this.safeBottom + 20 })
       .backgroundColor(DesignTokens.elevatedSurface(this.darkMode))
       .borderRadius({ topLeft: 26, topRight: 26 }).border({ width: 0.5, color: DesignTokens.border(this.darkMode) });
   }
@@ -400,14 +435,15 @@ struct StaffPage {
     NavDestination() {
       Stack({ alignContent: Alignment.Bottom }) {
         Column() {
-          IBestNavBar({
+          SecondaryHeader({
             title: I18n.text(this.language, '员工管理', 'Staff management'), isShowRight: true,
             rightLabel: I18n.text(this.language, '邀请', 'Invite'), rightLabelColor: DesignTokens.brand(this.darkMode),
             rightIcon: 'add-o', rightIconColor: DesignTokens.brand(this.darkMode), rightIconSize: 19,
             isShowStatusBar: true, navBarBgColor: DesignTokens.surface(this.darkMode),
             titleColor: DesignTokens.text(this.darkMode), leftIconColor: DesignTokens.text(this.darkMode),
-            onLeftClick: () => AppRouter.back(), onRightClick: () => this.open()
+            onLeftClick: () => this.handleBack(), onRightClick: () => this.open()
           });
+          SecondaryFeedback({ errorKind: this.pageError, updating: this.loading, onRetry: () => this.load() });
           Column({ space: 10 }) {
             this.overviewCard();
             Row({ space: 4 }) {
@@ -425,7 +461,7 @@ struct StaffPage {
           } else if (this.loadError && this.staffs.length === 0) {
             Column({ space: 12 }) {
               IBestEmpty({ description: this.loadError });
-              IBestButton({ text: I18n.text(this.language, '重新加载', 'Reload'), type: 'primary',
+              IBestButton({ btnHeight: 44, text: I18n.text(this.language, '重新加载', 'Reload'), type: 'primary',
                 color: DesignTokens.brand(this.darkMode), fontColor: DesignTokens.onBrand(this.darkMode),
                 onBtnClick: () => this.load() });
             }.layoutWeight(1).justifyContent(FlexAlign.Center);
@@ -439,9 +475,9 @@ struct StaffPage {
                 if (this.loadError) {
                   ListItem() {
                     Row() {
-                      Text(this.loadError).fontSize(11).fontColor(DesignTokens.muted(this.darkMode));
+                      Text(this.loadError).fontSize(12).fontColor(DesignTokens.muted(this.darkMode));
                       Blank();
-                      Text(I18n.text(this.language, '重试', 'Retry')).fontSize(11)
+                      Text(I18n.text(this.language, '重试', 'Retry')).fontSize(12)
                         .fontColor(DesignTokens.brand(this.darkMode)).onClick(() => this.load());
                     }.width('100%').padding(10).backgroundColor(DesignTokens.subtle(this.darkMode)).borderRadius(10);
                   }
@@ -461,6 +497,6 @@ struct StaffPage {
           this.editorSheet();
         }
       }.width('100%').height('100%');
-    }.hideTitleBar(true);
+    }.hideTitleBar(true).onBackPressed((): boolean => { this.handleBack(); return true; });
   }
 }
diff --git a/native/harmony-merchant/entry/src/main/ets/features/business/StoreAuthPage.ets b/native/harmony-merchant/entry/src/main/ets/features/business/StoreAuthPage.ets
index 89189534..926fad45 100644
--- a/native/harmony-merchant/entry/src/main/ets/features/business/StoreAuthPage.ets
+++ b/native/harmony-merchant/entry/src/main/ets/features/business/StoreAuthPage.ets
@@ -1,5 +1,8 @@
+import { LeaveGuard } from '../../core/ui/LeaveGuard';
+import { PageReadScope, SecondaryPolicy } from '../../core/ui/SecondaryPolicy';
+import { SecondaryFeedback, SecondaryHeader, SecondarySaveBar } from '../../core/ui/SecondaryPage';
 import {
-  IBestButton, IBestCheckbox, IBestCheckboxGroup, IBestField, IBestNavBar, IBestToast
+  IBestButton, IBestCheckbox, IBestCheckboxGroup, IBestField, IBestToast
 } from '@ibestservices/ibest-ui';
 import { AppRouter } from '../../core/navigation/AppRouter';
 import { IdRouteParams } from '../../core/navigation/RouteParams';
@@ -19,6 +22,24 @@ export function StoreAuthBuilder(_: string, raw: Object) {
 
 @Component
 struct StoreAuthPage {
+  @StorageProp('windowWidth') private windowWidth: number = 390;
+  @State private verifying: boolean = false;
+  private handleBack(): void {
+
+
+    this.leaveGuard.request(this.language, this.saving, this.editorReady && this.formStamp() !== this.loadedStamp, () => AppRouter.back());
+  }
+
+  private leaveGuard: LeaveGuard = new LeaveGuard();
+  @State private editorReady: boolean = false;
+  private loadedStamp: string = '';
+  private formStamp(): string { return JSON.stringify([this.level, this.tiers, this.policies, this.validFrom, this.validTo]); }
+
+  aboutToDisappear(): void { this.pageScope.dispose(); }
+
+  private pageScope: PageReadScope = new PageReadScope();
+  @State private pageError: string = '';
+
   params: IdRouteParams = new IdRouteParams();
   @StorageProp('safeBottom') private safeBottom: number = 16;
   @StorageLink('language') private language: string = 'zh-CN';
@@ -32,32 +53,40 @@ struct StoreAuthPage {
   @State private saving: boolean = false;
   private readonly tierGroup: string = 'store-price-tiers';
 
-  aboutToAppear(): void { this.load(); }
+  aboutToAppear(): void { this.pageScope.open(); this.load(); }
 
   private async load(): Promise<void> {
+    if (!this.pageScope.isCurrent()) return;
+    const pageTicket = this.pageScope.begin('load');
+    this.loading = true;
+    this.pageError = '';
     try {
-      const data = await BusinessRepository.storeAuth(this.params.id);
-      const categories = await ProductRepository.platformCategories();
+      const data = await this.pageScope.read(BusinessRepository.storeAuth(this.params.id), pageTicket);
+      const categories = await this.pageScope.read(ProductRepository.platformCategories(), pageTicket);
+      if (!Array.isArray(categories) || !Array.isArray(data.visiblePriceTiers)) throw new Error('invalid-authorisation');
+      const keepDraft = this.editorReady && this.formStamp() !== this.loadedStamp;
+      const nextPolicies = categories.filter((item: CategoryItem): boolean => !item.parentId).map((item: CategoryItem): ProductPolicy => {
+        const current = (data.productPolicies || []).find((policy: ProductPolicy): boolean => policy.categoryId === item.id);
+        return { categoryId: item.id, categoryName: item.name, enabled: current?.enabled || false, markupPercent: current?.markupPercent ?? 15 };
+      });
+      this.loadedStamp = JSON.stringify([data.level, data.visiblePriceTiers, nextPolicies, data.authValidFrom?.slice(0, 10) || '', data.authValidTo?.slice(0, 10) || '']);
+      if (!keepDraft) {
       this.level = data.level;
-      this.tiers = data.visiblePriceTiers || [];
+      this.tiers = data.visiblePriceTiers.slice();
       this.validFrom = data.authValidFrom ? data.authValidFrom.slice(0, 10) : '';
       this.validTo = data.authValidTo ? data.authValidTo.slice(0, 10) : '';
-      const existing = data.productPolicies || [];
-      this.policies = categories.filter((item: CategoryItem): boolean => !item.parentId)
-        .map((item: CategoryItem): ProductPolicy => {
-          const current = existing.find((policy: ProductPolicy): boolean => policy.categoryId === item.id);
-          return {
-            categoryId: item.id,
-            categoryName: item.name,
-            enabled: current?.enabled || false,
-            markupPercent: current?.markupPercent ?? 15
-          };
-        });
+      this.policies = nextPolicies;
+      }
+      this.editorReady = true; this.verifying = false;
     } catch (error) {
-      IBestToast.show(error instanceof ApiError ? error.message : I18n.text(this.language, '授权信息加载失败', 'Failed to load authorisation'));
+      if (!this.pageScope.accepts(pageTicket)) return;
+      this.pageError = SecondaryPolicy.errorKind(error);
+      IBestToast.show(error instanceof ApiError ? SecondaryPolicy.message(error, this.language) : I18n.text(this.language, '授权信息加载失败', 'Failed to load authorisation'));
     } finally {
+      if (this.pageScope.accepts(pageTicket)) {
       this.loading = false;
     }
+    }
   }
 
   private selectLevel(level: string): void {
@@ -66,27 +95,19 @@ struct StoreAuthPage {
   }
 
   private togglePolicy(categoryId: string, enabled: boolean): void {
-    this.policies = this.policies.map((item: ProductPolicy): ProductPolicy => {
-      if (item.categoryId === categoryId) item.enabled = enabled;
-      return item;
-    });
+    this.policies = this.policies.map((item: ProductPolicy): ProductPolicy => ({ categoryId: item.categoryId, categoryName: item.categoryName,
+      enabled: item.categoryId === categoryId ? enabled : item.enabled, markupPercent: item.markupPercent }));
   }
 
   private adjustPolicy(categoryId: string, delta: number): void {
-    this.policies = this.policies.map((item: ProductPolicy): ProductPolicy => {
-      if (item.categoryId === categoryId) {
-        item.markupPercent = Math.max(0, Math.min(100, item.markupPercent + delta));
-      }
-      return item;
-    });
+    this.policies = this.policies.map((item: ProductPolicy): ProductPolicy => ({ categoryId: item.categoryId, categoryName: item.categoryName,
+      enabled: item.enabled, markupPercent: item.categoryId === categoryId ? Math.max(0, Math.min(100, item.markupPercent + delta)) : item.markupPercent }));
   }
 
   private selectAll(): void {
     const target = this.policies.some((item: ProductPolicy): boolean => !item.enabled);
-    this.policies = this.policies.map((item: ProductPolicy): ProductPolicy => {
-      item.enabled = target;
-      return item;
-    });
+    this.policies = this.policies.map((item: ProductPolicy): ProductPolicy => ({ categoryId: item.categoryId, categoryName: item.categoryName,
+      enabled: target, markupPercent: item.markupPercent }));
   }
 
   private enabledCount(): number {
@@ -95,11 +116,14 @@ struct StoreAuthPage {
 
   private async save(): Promise<void> {
     if (this.saving) return;
+    if (!this.editorReady || this.loading || this.verifying) return;
+    if (!this.pageScope.isCurrent()) return;
+    const writeTicket = this.pageScope.begin('save');
     if (this.tiers.length === 0) {
       IBestToast.show(I18n.text(this.language, '请至少选择一种可见价格', 'Select at least one visible price tier'));
       return;
     }
-    if (!this.validFrom || !this.validTo || this.validTo < this.validFrom) {
+    if (!SecondaryPolicy.validBusinessDate(this.validFrom) || !SecondaryPolicy.validBusinessDate(this.validTo) || this.validTo < this.validFrom) {
       IBestToast.show(I18n.text(this.language, '请填写正确的授权有效期', 'Enter a valid authorisation period'));
       return;
     }
@@ -111,15 +135,21 @@ struct StoreAuthPage {
       authValidFrom: this.validFrom,
       authValidTo: this.validTo
     };
+    const submittedStamp = this.formStamp();
     this.saving = true;
     try {
       await BusinessRepository.saveStoreAuth(this.params.id, body);
+      if (!this.pageScope.accepts(writeTicket)) return;
+      this.loadedStamp = submittedStamp;
       IBestToast.show(I18n.text(this.language, '门店授权已保存', 'Store authorisation saved'));
       AppRouter.back();
     } catch (error) {
-      IBestToast.show(error instanceof ApiError ? error.message : I18n.text(this.language, '保存失败', 'Failed to save'));
+      if (!this.pageScope.accepts(writeTicket)) return;
+      this.verifying = true;
+      await this.load();
+      IBestToast.show(error instanceof ApiError ? SecondaryPolicy.message(error, this.language) : I18n.text(this.language, '保存失败', 'Failed to save'));
     } finally {
-      this.saving = false;
+      if (this.pageScope.accepts(writeTicket)) this.saving = false;
     }
   }
 
@@ -131,7 +161,7 @@ struct StoreAuthPage {
         .borderRadius(12);
       Column({ space: 4 }) {
         Text(title).fontSize(15).fontWeight(FontWeight.Medium);
-        Text(description).fontSize(11).fontColor(DesignTokens.muted(this.darkMode));
+        Text(description).fontSize(12).fontColor(DesignTokens.muted(this.darkMode));
       }.layoutWeight(1).alignItems(HorizontalAlign.Start);
       if (this.level === value) MerchantIcon({ name: 'success', color: DesignTokens.brand(this.darkMode), iconSize: 18 });
     }.width('100%').padding(12).backgroundColor(this.level === value ? DesignTokens.selectedSurface(this.darkMode) : DesignTokens.subtle(this.darkMode))
@@ -152,9 +182,9 @@ struct StoreAuthPage {
       }.width('100%');
       if (item.enabled) {
         Row({ space: 10 }) {
-          IBestButton({ text: '−5%', plain: true, color: DesignTokens.brand(this.darkMode), onBtnClick: () => this.adjustPolicy(item.categoryId, -5) });
+          IBestButton({ btnHeight: 44, text: '−5%', plain: true, color: DesignTokens.brand(this.darkMode), onBtnClick: () => this.adjustPolicy(item.categoryId, -5) });
           Text(`${item.markupPercent}%`).fontSize(16).fontWeight(FontWeight.Bold).textAlign(TextAlign.Center).layoutWeight(1);
-          IBestButton({ text: '+5%', plain: true, color: DesignTokens.brand(this.darkMode), onBtnClick: () => this.adjustPolicy(item.categoryId, 5) });
+          IBestButton({ btnHeight: 44, text: '+5%', plain: true, color: DesignTokens.brand(this.darkMode), onBtnClick: () => this.adjustPolicy(item.categoryId, 5) });
         }.width('100%');
       }
     }.width('100%').padding({ top: 12, bottom: 12 }).border({ width: { bottom: 1 }, color: DesignTokens.border(this.darkMode) });
@@ -163,55 +193,62 @@ struct StoreAuthPage {
   build() {
     NavDestination() {
       Column() {
-        IBestNavBar({
-          title: I18n.text(this.language, '门店授权', 'Store authorisation'), isShowRight: true, rightLabel: I18n.text(this.language, '保存', 'Save'), isShowStatusBar: true,
-          navBarBgColor: DesignTokens.surface(this.darkMode), onLeftClick: () => AppRouter.back(), onRightClick: () => this.save()
+        SecondaryHeader({
+          title: I18n.text(this.language, '门店授权', 'Store authorisation'), isShowStatusBar: true,
+          navBarBgColor: DesignTokens.surface(this.darkMode), onLeftClick: () => this.handleBack()
         });
-        if (this.loading) {
+          SecondaryFeedback({ errorKind: this.pageError, updating: this.loading, onRetry: () => this.load() });
+        if (this.loading && !this.editorReady) {
           Column() { LoadingProgress().width(34).height(34).color(DesignTokens.brand(this.darkMode)); }
             .layoutWeight(1).justifyContent(FlexAlign.Center);
         } else {
           Scroll() {
-            Column({ space: 14 }) {
-              Column({ space: 10 }) {
-                Text(I18n.text(this.language, '门店等级', 'Store level')).fontSize(17).fontWeight(FontWeight.Bold).width('100%');
-                this.levelCard('A', I18n.text(this.language, 'A 级 · 旗舰', 'Level A · Flagship'), I18n.text(this.language, '所有商品可上架，批发价与零售价', 'All products with wholesale and retail prices'));
-                this.levelCard('B', I18n.text(this.language, 'B 级 · 普通', 'Level B · Standard'), I18n.text(this.language, '按授权分类上架，默认零售价', 'Authorised categories with retail pricing'));
-                this.levelCard('C', I18n.text(this.language, 'C 级 · 体验', 'Level C · Trial'), I18n.text(this.language, '少量样品，默认零售价', 'Limited samples with retail pricing'));
-              }.width('100%').padding(16).backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(DesignTokens.RADIUS_LARGE);
-              Column({ space: 12 }) {
-                Text(I18n.text(this.language, '可见价格', 'Visible prices')).fontSize(17).fontWeight(FontWeight.Bold).width('100%');
-                IBestCheckboxGroup({ activeList: $tiers, group: this.tierGroup, placeDirection: Axis.Vertical }) {
-                  IBestCheckbox({ group: this.tierGroup, name: 'retail', label: I18n.text(this.language, '零售价', 'Retail') });
-                  IBestCheckbox({ group: this.tierGroup, name: 'wholesale', label: I18n.text(this.language, '批发价', 'Wholesale') });
-                  IBestCheckbox({ group: this.tierGroup, name: 'member', label: I18n.text(this.language, '会员价', 'Member') });
-                }
-              }.width('100%').padding(16).backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(DesignTokens.RADIUS_LARGE);
-              Column() {
-                Row() {
-                  Text(I18n.text(this.language, `可上架商品 · ${this.enabledCount()} / ${this.policies.length}`,
-                    `Available products · ${this.enabledCount()} / ${this.policies.length}`))
-                    .fontSize(17).fontWeight(FontWeight.Bold).layoutWeight(1);
-                  Text(this.enabledCount() === this.policies.length ? I18n.text(this.language, '取消全选', 'Clear all') : I18n.text(this.language, '全选', 'Select all'))
-                    .fontSize(13).fontColor(DesignTokens.brand(this.darkMode)).onClick(() => this.selectAll());
-                }.width('100%').padding({ bottom: 6 });
-                ForEach(this.policies, (item: ProductPolicy) => { this.policyRow(item); }, (item: ProductPolicy) => item.categoryId);
-              }.width('100%').padding(16).backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(DesignTokens.RADIUS_LARGE);
-              Column({ space: 12 }) {
-                Text(I18n.text(this.language, '授权有效期', 'Authorisation period')).fontSize(17).fontWeight(FontWeight.Bold).width('100%');
-                IBestField({ value: $validFrom, label: I18n.text(this.language, '开始日期', 'Start date'), placeholder: 'YYYY-MM-DD' });
-                IBestField({ value: $validTo, label: I18n.text(this.language, '结束日期', 'End date'), placeholder: 'YYYY-MM-DD' });
-                Text(I18n.text(this.language, '授权到期后门店自动失去商品和价格查看权限。', 'Product and price access ends automatically when authorisation expires.')).fontSize(11)
-                  .fontColor(DesignTokens.muted(this.darkMode)).width('100%');
-              }.width('100%').padding(16).backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(DesignTokens.RADIUS_LARGE);
-              IBestButton({
-                text: I18n.text(this.language, '保存授权', 'Save authorisation'), type: 'primary', color: DesignTokens.brand(this.darkMode), fontColor: DesignTokens.onBrand(this.darkMode), btnWidth: '100%',
-                loading: this.saving, disabled: this.saving, onBtnClick: () => this.save()
-              });
-            }.width('100%').padding({ left: 16, right: 16, top: 12, bottom: this.safeBottom + 20 });
-          }.layoutWeight(1).align(Alignment.Top).scrollBar(BarState.Off);
+            GridRow({ columns: this.windowWidth >= 720 ? 2 : 1, gutter: 12 }) {
+              GridCol() {
+                Column({ space: 10 }) {
+                  Text(I18n.text(this.language, '门店等级', 'Store level')).fontSize(17).fontWeight(FontWeight.Bold).width('100%');
+                  this.levelCard('A', I18n.text(this.language, 'A 级 · 旗舰', 'Level A · Flagship'), I18n.text(this.language, '所有商品可上架，批发价与零售价', 'All products with wholesale and retail prices'));
+                  this.levelCard('B', I18n.text(this.language, 'B 级 · 普通', 'Level B · Standard'), I18n.text(this.language, '按授权分类上架，默认零售价', 'Authorised categories with retail pricing'));
+                  this.levelCard('C', I18n.text(this.language, 'C 级 · 体验', 'Level C · Trial'), I18n.text(this.language, '少量样品，默认零售价', 'Limited samples with retail pricing'));
+                }.width('100%').padding(16).backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(16);
+              }
+              GridCol() {
+                Column({ space: 12 }) {
+                  Text(I18n.text(this.language, '可见价格', 'Visible prices')).fontSize(17).fontWeight(FontWeight.Bold).width('100%');
+                  IBestCheckboxGroup({ activeList: $tiers, group: this.tierGroup, placeDirection: Axis.Vertical }) {
+                    IBestCheckbox({ group: this.tierGroup, name: 'retail', label: I18n.text(this.language, '零售价', 'Retail') });
+                    IBestCheckbox({ group: this.tierGroup, name: 'wholesale', label: I18n.text(this.language, '批发价', 'Wholesale') });
+                    IBestCheckbox({ group: this.tierGroup, name: 'member', label: I18n.text(this.language, '会员价', 'Member') });
+                  }
+                }.width('100%').padding(16).backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(16);
+              }
+              GridCol() {
+                Column() {
+                  Row() {
+                    Text(I18n.text(this.language, `可上架商品 · ${this.enabledCount()} / ${this.policies.length}`,
+                      `Available products · ${this.enabledCount()} / ${this.policies.length}`))
+                      .fontSize(17).fontWeight(FontWeight.Bold).layoutWeight(1);
+                    Text(this.enabledCount() === this.policies.length ? I18n.text(this.language, '取消全选', 'Clear all') : I18n.text(this.language, '全选', 'Select all'))
+                      .fontSize(13).fontColor(DesignTokens.brand(this.darkMode)).onClick(() => this.selectAll());
+                  }.width('100%').padding({ bottom: 6 });
+                  ForEach(this.policies, (item: ProductPolicy) => { this.policyRow(item); }, (item: ProductPolicy) => item.categoryId);
+                }.width('100%').padding(16).backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(16);
+              }
+              GridCol() {
+                Column({ space: 12 }) {
+                  Text(I18n.text(this.language, '授权有效期', 'Authorisation period')).fontSize(17).fontWeight(FontWeight.Bold).width('100%');
+                  IBestField({ value: $validFrom, label: I18n.text(this.language, '开始日期', 'Start date'), placeholder: 'YYYY-MM-DD' });
+                  IBestField({ value: $validTo, label: I18n.text(this.language, '结束日期', 'End date'), placeholder: 'YYYY-MM-DD' });
+                  Text(I18n.text(this.language, '授权到期后门店自动失去商品和价格查看权限。', 'Product and price access ends automatically when authorisation expires.')).fontSize(12)
+                    .fontColor(DesignTokens.muted(this.darkMode)).width('100%');
+                }.width('100%').padding(16).backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(16);
+              }
+            }.width('100%').padding({ left: 16, right: 16, top: 12, bottom: 12 });
+          }.enabled(this.editorReady && !this.saving && !this.loading).layoutWeight(1).align(Alignment.Top).scrollBar(BarState.Off);
         }
+        SecondarySaveBar({ saving: this.saving, changed: this.editorReady && this.formStamp() !== this.loadedStamp,
+          canSave: this.editorReady && !this.loading && !this.verifying && this.formStamp() !== this.loadedStamp, onSave: () => this.save() });
       }.width('100%').height('100%').backgroundColor(DesignTokens.page(this.darkMode));
-    }.hideTitleBar(true);
+    }.hideTitleBar(true).onBackPressed((): boolean => { this.handleBack(); return true; });
   }
 }
diff --git a/native/harmony-merchant/entry/src/main/ets/features/business/StorePage.ets b/native/harmony-merchant/entry/src/main/ets/features/business/StorePage.ets
index 7f8c97f7..61d3a234 100644
--- a/native/harmony-merchant/entry/src/main/ets/features/business/StorePage.ets
+++ b/native/harmony-merchant/entry/src/main/ets/features/business/StorePage.ets
@@ -1,6 +1,8 @@
+import { PageReadScope, PageSearch, SecondaryPolicy } from '../../core/ui/SecondaryPolicy';
+import { SecondaryFeedback, SecondaryHeader } from '../../core/ui/SecondaryPage';
 import { common } from '@kit.AbilityKit';
 import {
-  IBestButton, IBestDialogUtil, IBestEmpty, IBestNavBar, IBestSearch, IBestTag, IBestToast
+  IBestButton, IBestDialogUtil, IBestEmpty, IBestSearch, IBestTag, IBestToast
 } from '@ibestservices/ibest-ui';
 import { PhoneService } from '../../core/device/PhoneService';
 import { AppRouter } from '../../core/navigation/AppRouter';
@@ -22,6 +24,15 @@ export function StoresBuilder(_: string, raw: Object) {
 
 @Component
 struct StorePage {
+  private pageSearch: PageSearch = new PageSearch();
+  private submitSearch(): void { this.pageSearch.cancel(); this.reload(false); }
+  private visibleFilter: string = '';
+
+  aboutToDisappear(): void { this.pageSearch.cancel(); this.pageScope.dispose(); }
+
+  private pageScope: PageReadScope = new PageReadScope();
+  @State private pageError: string = '';
+
   params: FilterRouteParams = new FilterRouteParams();
   @StorageProp('safeBottom') private safeBottom: number = 16;
   @StorageLink('language') private language: string = 'zh-CN';
@@ -39,7 +50,7 @@ struct StorePage {
     { value: '' }, { value: 'active' }, { value: 'pending' }, { value: 'cancelled' }
   ];
 
-  aboutToAppear(): void {
+  aboutToAppear(): void { this.pageScope.open();
     if (this.filters.some((item: StoreFilter): boolean => item.value === this.params.status)) {
       this.status = this.params.status;
     }
@@ -56,32 +67,40 @@ struct StorePage {
   }
 
   private async reload(fromRefresh: boolean): Promise<void> {
-    this.page = 1;
     if (fromRefresh) this.refreshing = true;
-    this.stores = [];
     await this.load(false);
     this.refreshing = false;
   }
 
   private async load(append: boolean): Promise<void> {
+    if (!this.pageScope.isCurrent()) return;
     if (append && this.loading) return;
+    if (!append && this.loading && this.visibleFilter === this.keyword.trim()) return;
+    const pageTicket = this.pageScope.begin('load');
+    this.pageError = '';
+    const filterKey = this.keyword.trim();
+    if (filterKey !== this.visibleFilter) { this.stores = []; this.total = 0; this.page = 1; this.visibleFilter = filterKey; }
     const epoch = ++this.requestEpoch;
     const requestedKeyword = this.keyword.trim();
-    const requestedPage = this.page;
+    const requestedPage = append ? this.page : 1;
     this.loading = true;
     try {
-      const result = await BusinessRepository.stores(requestedKeyword, requestedPage, 100);
+      const result = await this.pageScope.read(BusinessRepository.stores(requestedKeyword, requestedPage, 100), pageTicket);
       if (epoch !== this.requestEpoch) return;
       this.stores = append ? this.stores.concat(result.list) : result.list;
-      this.total = result.total;
+      this.total = result.total; this.page = requestedPage;
     } catch (error) {
+      if (!this.pageScope.accepts(pageTicket)) return;
+      this.pageError = SecondaryPolicy.errorKind(error);
       if (epoch === this.requestEpoch) {
         if (append && this.page === requestedPage) this.page = Math.max(1, requestedPage - 1);
-        IBestToast.show(error instanceof ApiError ? error.message : I18n.text(this.language, '门店加载失败', 'Failed to load stores'));
+        IBestToast.show(error instanceof ApiError ? SecondaryPolicy.message(error, this.language) : I18n.text(this.language, '门店加载失败', 'Failed to load stores'));
       }
     } finally {
+      if (this.pageScope.accepts(pageTicket)) {
       if (epoch === this.requestEpoch) this.loading = false;
     }
+    }
   }
 
   private remove(item: StoreItem): void {
@@ -97,17 +116,22 @@ struct StorePage {
   }
 
   private async deleteStore(id: string): Promise<void> {
+    if (!this.pageScope.isCurrent()) return;
     if (this.processingId) return;
     this.processingId = id;
+    const mutationTicket = this.pageScope.begin('deleteStore');
     try {
-      await BusinessRepository.deleteStore(id);
+      await this.pageScope.read(BusinessRepository.deleteStore(id), mutationTicket);
       IBestToast.show(I18n.text(this.language, '门店申请已驳回', 'Store request rejected'));
-      await this.reload(false);
+      await this.pageScope.read(this.reload(false), mutationTicket);
     } catch (error) {
-      IBestToast.show(error instanceof ApiError ? error.message : I18n.text(this.language, '删除失败', 'Failed to delete'));
+      if (!this.pageScope.accepts(mutationTicket)) return;
+      IBestToast.show(error instanceof ApiError ? SecondaryPolicy.message(error, this.language) : I18n.text(this.language, '删除失败', 'Failed to delete'));
     } finally {
+      if (this.pageScope.accepts(mutationTicket)) {
       this.processingId = '';
     }
+    }
   }
 
   private formatDate(date: Date): string {
@@ -118,47 +142,21 @@ struct StorePage {
   }
 
   private approve(item: StoreItem): void {
-    IBestDialogUtil.open({
-      title: I18n.text(this.language, '通过门店申请', 'Approve store request'),
-      message: I18n.text(this.language, `通过“${item.name}”入驻申请，并颁发 ${item.level || 'A'} 级一年授权？`, `Approve “${item.name}” with one-year level ${item.level || 'A'} authorisation?`),
-      showCancelButton: true,
-      cancelButtonText: I18n.text(this.language, '取消', 'Cancel'),
-      confirmButtonText: I18n.text(this.language, '确认', 'Confirm'),
-      confirmButtonColor: DesignTokens.brand(this.darkMode),
-      onConfirm: () => { this.approveStore(item); }
-    });
+    this.approveStore(item);
   }
 
+
   private async approveStore(item: StoreItem): Promise<void> {
     if (this.processingId) return;
-    this.processingId = item.id;
-    const start = new Date();
-    const end = new Date(start.getTime());
-    end.setFullYear(end.getFullYear() + 1);
-    const body: StoreAuthConfig = {
-      storeId: item.id,
-      level: item.level || 'A',
-      visiblePriceTiers: ['retail', 'wholesale'],
-      productPolicies: [],
-      authValidFrom: this.formatDate(start),
-      authValidTo: this.formatDate(end)
-    };
-    try {
-      await BusinessRepository.saveStoreAuth(item.id, body);
-      IBestToast.show(I18n.text(this.language, '门店申请已通过', 'Store request approved'));
-      await this.reload(false);
-    } catch (error) {
-      IBestToast.show(error instanceof ApiError ? error.message : I18n.text(this.language, '审核失败', 'Review failed'));
-    } finally {
-      this.processingId = '';
-    }
+    // Review level, dates and scope before explicitly saving the authorisation.
+    AppRouter.push('StoreAuth', new IdRouteParams(item.id));
   }
 
   private async call(item: StoreItem): Promise<void> {
     try {
       await PhoneService.dial(this.getUIContext().getHostContext() as common.UIAbilityContext, item.phone);
     } catch (error) {
-      IBestToast.show(error instanceof Error ? error.message : I18n.text(this.language, '无法打开拨号界面', 'Unable to open the dialer'));
+      IBestToast.show(error instanceof Error ? SecondaryPolicy.message(error, this.language) : I18n.text(this.language, '无法打开拨号界面', 'Unable to open the dialer'));
     }
   }
 
@@ -186,59 +184,60 @@ struct StorePage {
       Row({ space: 8 }) {
         Text(I18n.text(this.language, `等级 ${item.level || 'C'}`, `Level ${item.level || 'C'}`)).fontSize(12).fontColor(DesignTokens.brand(this.darkMode));
         if (item.authValidTo) {
-          Text(I18n.text(this.language, `有效至 ${item.authValidTo.slice(0, 10)}`, `Valid until ${item.authValidTo.slice(0, 10)}`)).fontSize(11).fontColor(DesignTokens.muted(this.darkMode));
+          Text(I18n.text(this.language, `有效至 ${item.authValidTo.slice(0, 10)}`, `Valid until ${item.authValidTo.slice(0, 10)}`)).fontSize(12).fontColor(DesignTokens.muted(this.darkMode));
         }
         Blank();
         if (item.status === 'pending') {
-          IBestButton({
+          IBestButton({ btnHeight: 44,
             text: I18n.text(this.language, '驳回', 'Reject'), plain: true, type: 'danger', loading: this.processingId === item.id,
             onBtnClick: () => this.remove(item)
           });
-          IBestButton({
+          IBestButton({ btnHeight: 44,
             text: I18n.text(this.language, '通过', 'Approve'), type: 'primary', color: DesignTokens.brand(this.darkMode), fontColor: DesignTokens.onBrand(this.darkMode), loading: this.processingId === item.id,
             onBtnClick: () => this.approve(item)
           });
         } else if (item.status === 'active') {
-          IBestButton({ text: I18n.text(this.language, '联系', 'Contact'), plain: true, color: DesignTokens.brand(this.darkMode), onBtnClick: () => this.call(item) });
-          IBestButton({
+          IBestButton({ btnHeight: 44, text: I18n.text(this.language, '联系', 'Contact'), plain: true, color: DesignTokens.brand(this.darkMode), onBtnClick: () => this.call(item) });
+          IBestButton({ btnHeight: 44,
             text: I18n.text(this.language, '授权', 'Authorise'), plain: true, color: DesignTokens.brand(this.darkMode),
             onBtnClick: () => AppRouter.push('StoreAuth', new IdRouteParams(item.id))
           });
         }
       }.width('100%');
     }.width('100%').alignItems(HorizontalAlign.Start).padding(14)
-      .backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(DesignTokens.RADIUS_LARGE);
+      .backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(16);
   }
 
   build() {
     NavDestination() {
       Column() {
-        IBestNavBar({
+        SecondaryHeader({
           title: I18n.text(this.language, '门店管理', 'Store management'), isShowStatusBar: true,
           navBarBgColor: DesignTokens.surface(this.darkMode), onLeftClick: () => AppRouter.back()
         });
+          SecondaryFeedback({ errorKind: this.pageError, updating: this.loading, onRetry: () => this.load(false) });
         Row() {
           Column({ space: 3 }) {
             Text(`${this.count('')}`).fontSize(23).fontWeight(FontWeight.Bold).fontColor(DesignTokens.text(this.darkMode));
-            Text(I18n.text(this.language, '合作门店', 'Partner stores')).fontSize(11).fontColor(DesignTokens.muted(this.darkMode));
+            Text(I18n.text(this.language, '合作门店', 'Partner stores')).fontSize(12).fontColor(DesignTokens.muted(this.darkMode));
           }.layoutWeight(1);
           Divider().vertical(true).height(36).color(DesignTokens.border(this.darkMode));
           Column({ space: 3 }) {
             Text(`${this.count('active')}`).fontSize(23).fontWeight(FontWeight.Bold).fontColor(DesignTokens.brand(this.darkMode));
-            Text(I18n.text(this.language, '已授权', 'Authorised')).fontSize(11).fontColor(DesignTokens.muted(this.darkMode));
+            Text(I18n.text(this.language, '已授权', 'Authorised')).fontSize(12).fontColor(DesignTokens.muted(this.darkMode));
           }.layoutWeight(1);
           Divider().vertical(true).height(36).color(DesignTokens.border(this.darkMode));
           Column({ space: 3 }) {
             Text(`${this.count('pending')}`).fontSize(23).fontWeight(FontWeight.Bold).fontColor(DesignTokens.text(this.darkMode));
-            Text(I18n.text(this.language, '待审核', 'Pending')).fontSize(11).fontColor(DesignTokens.muted(this.darkMode));
+            Text(I18n.text(this.language, '待审核', 'Pending')).fontSize(12).fontColor(DesignTokens.muted(this.darkMode));
           }.layoutWeight(1);
-        }.width('94%').padding(18).margin({ top: 8, bottom: 10 }).backgroundColor(DesignTokens.card(this.darkMode))
+        }.width('94%').padding(16).margin({ top: 8, bottom: 10 }).backgroundColor(DesignTokens.card(this.darkMode))
           .borderRadius(16).border({ width: 0.5, color: DesignTokens.border(this.darkMode) });
         IBestSearch({
           value: $keyword, placeholder: I18n.text(this.language, '搜索门店或联系人', 'Search store or contact'), isRound: true,
           outBgColor: DesignTokens.page(this.darkMode), innerBgColor: DesignTokens.card(this.darkMode),
           showRightButton: true, rightButtonText: I18n.text(this.language, '搜索', 'Search'),
-          onSearch: () => this.reload(false), onRightButtonClick: () => this.reload(false)
+          onInput: () => this.pageSearch.schedule(() => this.reload(false)), onSearch: () => this.submitSearch(), onRightButtonClick: () => this.submitSearch()
         });
         Row({ space: 8 }) {
           ForEach(this.filters, (filter: StoreFilter) => {
diff --git a/native/harmony-merchant/entry/src/main/ets/features/chat/ChatDetailPage.ets b/native/harmony-merchant/entry/src/main/ets/features/chat/ChatDetailPage.ets
index cdbcf804..17fa5392 100644
--- a/native/harmony-merchant/entry/src/main/ets/features/chat/ChatDetailPage.ets
+++ b/native/harmony-merchant/entry/src/main/ets/features/chat/ChatDetailPage.ets
@@ -1,6 +1,8 @@
+import { PageReadScope, SecondaryPolicy } from '../../core/ui/SecondaryPolicy';
+import { SecondaryFeedback, SecondaryHeader } from '../../core/ui/SecondaryPage';
 import { util } from '@kit.ArkTS';
 import { common } from '@kit.AbilityKit';
-import { IBestButton, IBestImagePreview, IBestNavBar, IBestToast } from '@ibestservices/ibest-ui';
+import { IBestButton, IBestImagePreview, IBestToast } from '@ibestservices/ibest-ui';
 import { FileUploadService } from '../../core/file/FileUploadService';
 import { AppRouter } from '../../core/navigation/AppRouter';
 import { IdRouteParams } from '../../core/navigation/RouteParams';
@@ -28,6 +30,9 @@ export function ChatDetailBuilder(_: string, raw: Object) {
 
 @Component
 struct ChatDetailPage {
+  private pageScope: PageReadScope = new PageReadScope();
+  @State private pageError: string = '';
+
   params: IdRouteParams = new IdRouteParams();
   @StorageProp('safeBottom') private safeBottom: number = 16;
   @StorageLink('language') private language: string = 'zh-CN';
@@ -49,7 +54,7 @@ struct ChatDetailPage {
   private unsubscribe?: () => void;
   private typingTimer: number = -1;
 
-  aboutToAppear(): void {
+  aboutToAppear(): void { this.pageScope.open();
     AppStorage.setOrCreate<string>('activeChatSessionId', this.params.id);
     this.unsubscribe = HarmonyRealtime.shared().subscribe(
       (event: RealtimeEnvelope): void => this.onRealtime(event)
@@ -58,7 +63,7 @@ struct ChatDetailPage {
     this.load();
   }
 
-  aboutToDisappear(): void {
+  aboutToDisappear(): void { this.pageScope.dispose();
     if ((AppStorage.get<string>('activeChatSessionId') || '') === this.params.id) {
       AppStorage.setOrCreate<string>('activeChatSessionId', '');
     }
@@ -91,30 +96,41 @@ struct ChatDetailPage {
   }
 
   private async load(): Promise<void> {
+    if (!this.pageScope.isCurrent()) return;
+    const pageTicket = this.pageScope.begin('load');
+    this.pageError = '';
     this.errorMessage = '';
     let criticalFailure: string = '';
     try {
-      this.session = await BusinessRepository.chatSession(this.params.id);
+      this.session = await this.pageScope.read(BusinessRepository.chatSession(this.params.id), pageTicket);
     } catch (error) {
-      criticalFailure = error instanceof ApiError ? error.message : 'i18n:conversation-detail-load';
+      if (!this.pageScope.accepts(pageTicket)) return;
+      this.pageError = SecondaryPolicy.errorKind(error);
+      criticalFailure = error instanceof ApiError ? SecondaryPolicy.message(error, this.language) : 'i18n:conversation-detail-load';
     }
     try {
-      this.messages = await BusinessRepository.chatMessages(this.params.id);
+      this.messages = await this.pageScope.read(BusinessRepository.chatMessages(this.params.id), pageTicket);
       this.hasEarlier = this.messages.length >= 30;
     } catch (error) {
+      if (!this.pageScope.accepts(pageTicket)) return;
+      this.pageError = SecondaryPolicy.errorKind(error);
       if (!criticalFailure) {
-        criticalFailure = error instanceof ApiError ? error.message : 'i18n:message-history-load';
+        criticalFailure = error instanceof ApiError ? SecondaryPolicy.message(error, this.language) : 'i18n:message-history-load';
       }
     }
     try {
-      this.replies = await BusinessRepository.quickReplies();
+      this.replies = await this.pageScope.read(BusinessRepository.quickReplies(), pageTicket);
     } catch (_) {
+      if (!this.pageScope.accepts(pageTicket)) return;
+      this.pageError = SecondaryPolicy.errorKind(_);
       this.replies = [];
     }
     if (!criticalFailure) {
       try {
-        await BusinessRepository.markRead(this.params.id);
-      } catch (_) {}
+        await this.pageScope.read(BusinessRepository.markRead(this.params.id), pageTicket);
+      } catch (_) {
+      if (!this.pageScope.accepts(pageTicket)) return;
+      this.pageError = SecondaryPolicy.errorKind(_);}
       HarmonyRealtime.shared().markChatRead(this.params.id);
       this.scrollBottom(120);
     } else {
@@ -138,6 +154,7 @@ struct ChatDetailPage {
   }
 
   private async send(type: string, content: string, retryId: string = ''): Promise<void> {
+    if (!this.pageScope.isCurrent()) return;
     const normalized = content.trim();
     if (!normalized || this.sending) return;
     if (this.session?.status === 'closed') {
@@ -161,17 +178,21 @@ struct ChatDetailPage {
     }
     this.scrollBottom();
     const body: SendMessageRequest = { type, content: normalized };
+    const mutationTicket = this.pageScope.begin('send');
     try {
-      const serverMessage = await BusinessRepository.sendMessage(this.params.id, body);
+      const serverMessage = await this.pageScope.read(BusinessRepository.sendMessage(this.params.id, body), mutationTicket);
       this.messages = ChatMessagePolicy.confirmHttp(this.messages, optimistic.id, serverMessage);
       this.scrollBottom();
     } catch (error) {
+      if (!this.pageScope.accepts(mutationTicket)) return;
       this.messages = ChatMessagePolicy.markFailed(this.messages, optimistic.id);
-      IBestToast.show(error instanceof ApiError ? error.message
+      IBestToast.show(error instanceof ApiError ? SecondaryPolicy.message(error, this.language)
         : I18n.text(this.language, '发送失败，点击红色提示重试', 'Send failed. Tap the red status to retry.'));
     } finally {
+      if (this.pageScope.accepts(mutationTicket)) {
       this.sending = false;
     }
+    }
   }
 
   private async sendImage(): Promise<void> {
@@ -181,7 +202,7 @@ struct ChatDetailPage {
       const urls = await FileUploadService.chooseAndUploadImages(1, 'chat');
       if (urls.length > 0) await this.send('image', urls[0]);
     } catch (error) {
-      IBestToast.show(error instanceof Error ? error.message : I18n.text(this.language, '图片发送失败', 'Failed to send image'));
+      IBestToast.show(error instanceof Error ? SecondaryPolicy.message(error, this.language) : I18n.text(this.language, '图片发送失败', 'Failed to send image'));
     } finally {
       this.uploading = false;
     }
@@ -195,7 +216,7 @@ struct ChatDetailPage {
       const url = await FileUploadService.captureAndUploadImage(context, 'chat');
       if (url) await this.send('image', url);
     } catch (error) {
-      IBestToast.show(error instanceof Error ? error.message
+      IBestToast.show(error instanceof Error ? SecondaryPolicy.message(error, this.language)
         : I18n.text(this.language, '拍照发送失败', 'Failed to capture and send photo'));
     } finally {
       this.uploading = false;
@@ -203,12 +224,15 @@ struct ChatDetailPage {
   }
 
   private async loadEarlier(): Promise<void> {
+    if (!this.pageScope.isCurrent()) return;
     if (this.messages.length === 0 || this.loadingEarlier || !this.hasEarlier) return;
+    const pageTicket = this.pageScope.begin('loadEarlier');
+    this.pageError = '';
     const firstServer = this.messages.find((item: ChatMessage): boolean => !item.id.startsWith('local-'));
     if (!firstServer) return;
     this.loadingEarlier = true;
     try {
-      const older = await BusinessRepository.chatMessages(this.params.id, firstServer.id);
+      const older = await this.pageScope.read(BusinessRepository.chatMessages(this.params.id, firstServer.id), pageTicket);
       if (older.length > 0) {
         this.messages = ChatMessagePolicy.prependEarlier(this.messages, older);
         if (older.length < 30) this.hasEarlier = false;
@@ -217,11 +241,15 @@ struct ChatDetailPage {
         IBestToast.show(I18n.text(this.language, '没有更早消息了', 'No earlier messages'));
       }
     } catch (error) {
-      IBestToast.show(error instanceof ApiError ? error.message
+      if (!this.pageScope.accepts(pageTicket)) return;
+      this.pageError = SecondaryPolicy.errorKind(error);
+      IBestToast.show(error instanceof ApiError ? SecondaryPolicy.message(error, this.language)
         : I18n.text(this.language, '历史消息加载失败', 'Failed to load message history'));
     } finally {
+      if (this.pageScope.accepts(pageTicket)) {
       this.loadingEarlier = false;
     }
+    }
   }
 
   private onTextChanged(value: string): void {
@@ -257,7 +285,7 @@ struct ChatDetailPage {
   @Builder
   private bubble(item: ChatMessage) {
     if (item.sender === 'system') {
-      Text(item.content).fontSize(11).fontColor(DesignTokens.muted(this.darkMode))
+      Text(item.content).fontSize(12).fontColor(DesignTokens.muted(this.darkMode))
         .padding({ left: 10, right: 10, top: 5, bottom: 5 })
         .backgroundColor(DesignTokens.subtle(this.darkMode)).borderRadius(10).margin({ top: 6, bottom: 6 });
     } else {
@@ -281,7 +309,7 @@ struct ChatDetailPage {
               : item.sendState === 'failed'
                 ? I18n.text(this.language, '发送失败，点击重试', 'Send failed. Tap to retry.')
                 : item.read ? I18n.text(this.language, '已读', 'Read') : I18n.text(this.language, '未读', 'Unread'))
-              .fontSize(10).fontColor(item.sendState === 'failed' ? DesignTokens.danger(this.darkMode) : DesignTokens.muted(this.darkMode))
+              .fontSize(12).fontColor(item.sendState === 'failed' ? DesignTokens.danger(this.darkMode) : DesignTokens.muted(this.darkMode))
               .textAlign(TextAlign.End).width('100%')
               .onClick(() => { if (item.sendState === 'failed') this.send(item.type, item.content, item.id); });
           }
@@ -299,20 +327,21 @@ struct ChatDetailPage {
   build() {
     NavDestination() {
       Column() {
-        IBestNavBar({
+        SecondaryHeader({
           title: this.session?.userName || I18n.text(this.language, '在线客服', 'Customer service'), isShowRight: true,
           rightLabel: this.userTyping
             ? I18n.text(this.language, '正在输入…', 'Typing…')
             : this.session?.online ? I18n.text(this.language, '在线', 'Online') : I18n.text(this.language, '离线', 'Offline'),
           isShowStatusBar: true, navBarBgColor: DesignTokens.surface(this.darkMode), onLeftClick: () => AppRouter.back()
         });
+          SecondaryFeedback({ errorKind: this.pageError, updating: this.loading, onRetry: () => this.load() });
         if (this.loading) {
           Column() { LoadingProgress().width(34).height(34).color(DesignTokens.brand(this.darkMode)); }
             .layoutWeight(1).justifyContent(FlexAlign.Center);
         } else if (this.errorMessage) {
           Column({ space: 12 }) {
             Text(I18n.state(this.language, this.errorMessage)).fontSize(13).fontColor(DesignTokens.muted(this.darkMode));
-            IBestButton({ text: I18n.text(this.language, '重新加载', 'Reload'), type: 'primary', color: DesignTokens.brand(this.darkMode), fontColor: DesignTokens.onBrand(this.darkMode), onBtnClick: () => {
+            IBestButton({ btnHeight: 44, text: I18n.text(this.language, '重新加载', 'Reload'), type: 'primary', color: DesignTokens.brand(this.darkMode), fontColor: DesignTokens.onBrand(this.darkMode), onBtnClick: () => {
               this.loading = true;
               this.load();
             } });
@@ -349,14 +378,14 @@ struct ChatDetailPage {
                   .width(54).height(54).justifyContent(FlexAlign.Center).backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(12);
                 Text(this.uploading
                   ? I18n.text(this.language, '上传中…', 'Uploading…')
-                  : I18n.text(this.language, '图片', 'Image')).fontSize(11).fontColor(DesignTokens.muted(this.darkMode));
+                  : I18n.text(this.language, '图片', 'Image')).fontSize(12).fontColor(DesignTokens.muted(this.darkMode));
               }.width(84).opacity(this.uploading || this.sending ? 0.5 : 1).onClick(() => this.sendImage());
               Column({ space: 7 }) {
                 Column() { MerchantIcon({ name: 'photograph', color: DesignTokens.brand(this.darkMode), iconSize: 24 }); }
                   .width(54).height(54).justifyContent(FlexAlign.Center).backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(12);
                 Text(this.uploading
                   ? I18n.text(this.language, '上传中…', 'Uploading…')
-                  : I18n.text(this.language, '拍照', 'Camera')).fontSize(11).fontColor(DesignTokens.muted(this.darkMode));
+                  : I18n.text(this.language, '拍照', 'Camera')).fontSize(12).fontColor(DesignTokens.muted(this.darkMode));
               }.width(84).opacity(this.uploading || this.sending ? 0.5 : 1).onClick(() => this.takePhoto());
             }.width('100%').height(150).padding(16).backgroundColor(DesignTokens.subtle(this.darkMode));
           }
@@ -374,7 +403,7 @@ struct ChatDetailPage {
                 .padding({ left: 12, right: 12, top: 8, bottom: 8 }).backgroundColor(DesignTokens.card(this.darkMode))
                 .borderRadius(12).maxLength(1000).onChange((value: string) => this.onTextChanged(value));
               if (this.text.trim()) {
-                IBestButton({
+                IBestButton({ btnHeight: 44,
                   text: I18n.text(this.language, '发送', 'Send'), type: 'primary', color: DesignTokens.brand(this.darkMode), fontColor: DesignTokens.onBrand(this.darkMode), loading: this.sending,
                   disabled: this.sending || this.uploading,
                   onBtnClick: () => this.send(this.draftType, this.text)
diff --git a/native/harmony-merchant/entry/src/main/ets/features/chat/ChatSessionsPage.ets b/native/harmony-merchant/entry/src/main/ets/features/chat/ChatSessionsPage.ets
index 24a6f363..6ff144b5 100644
--- a/native/harmony-merchant/entry/src/main/ets/features/chat/ChatSessionsPage.ets
+++ b/native/harmony-merchant/entry/src/main/ets/features/chat/ChatSessionsPage.ets
@@ -1,4 +1,6 @@
-import { IBestButton, IBestEmpty, IBestNavBar, IBestSearch, IBestTag, IBestToast } from '@ibestservices/ibest-ui';
+import { PageReadScope, SecondaryPolicy } from '../../core/ui/SecondaryPolicy';
+import { SecondaryFeedback, SecondaryHeader } from '../../core/ui/SecondaryPage';
+import { IBestButton, IBestEmpty, IBestSearch, IBestTag, IBestToast } from '@ibestservices/ibest-ui';
 import { AppRouter } from '../../core/navigation/AppRouter';
 import { FilterRouteParams, IdRouteParams } from '../../core/navigation/RouteParams';
 import { ApiError } from '../../core/network/ApiClient';
@@ -20,6 +22,9 @@ export function ChatSessionsBuilder(_: string, raw: Object) {
 
 @Component
 struct ChatSessionsPage {
+  private pageScope: PageReadScope = new PageReadScope();
+  @State private pageError: string = '';
+
   params: FilterRouteParams = new FilterRouteParams();
   @StorageProp('safeBottom') private safeBottom: number = 16;
   @StorageLink('language') private language: string = 'zh-CN';
@@ -32,7 +37,7 @@ struct ChatSessionsPage {
   @State private errorMessage: string = '';
   private unsubscribe?: () => void;
 
-  aboutToAppear(): void {
+  aboutToAppear(): void { this.pageScope.open();
     this.unreadOnly = this.params.status === 'unread';
     this.unsubscribe = HarmonyRealtime.shared().subscribe(
       (event: RealtimeEnvelope): void => this.onRealtime(event)
@@ -40,7 +45,7 @@ struct ChatSessionsPage {
     this.load(false);
   }
 
-  aboutToDisappear(): void {
+  aboutToDisappear(): void { this.pageScope.dispose();
     this.unsubscribe?.();
     this.unsubscribe = undefined;
   }
@@ -77,18 +82,25 @@ struct ChatSessionsPage {
   }
 
   private async load(fromRefresh: boolean): Promise<void> {
+    if (!this.pageScope.isCurrent()) return;
+    const pageTicket = this.pageScope.begin('load');
+    this.pageError = '';
     if (fromRefresh) this.refreshing = true;
     else if (this.sessions.length === 0) this.loading = true;
     this.errorMessage = '';
     try {
-      this.sessions = this.sortSessions(await BusinessRepository.chatSessions());
+      this.sessions = this.sortSessions(await this.pageScope.read(BusinessRepository.chatSessions(), pageTicket));
     } catch (error) {
-      this.errorMessage = error instanceof ApiError ? error.message : 'i18n:conversations-load';
+      if (!this.pageScope.accepts(pageTicket)) return;
+      this.pageError = SecondaryPolicy.errorKind(error);
+      this.errorMessage = error instanceof ApiError ? SecondaryPolicy.message(error, this.language) : 'i18n:conversations-load';
       if (fromRefresh) IBestToast.show(I18n.state(this.language, this.errorMessage));
     } finally {
+      if (this.pageScope.accepts(pageTicket)) {
       this.loading = false;
       this.refreshing = false;
     }
+    }
   }
 
   private visible(): ChatSession[] {
@@ -148,30 +160,31 @@ struct ChatSessionsPage {
       Column({ space: 6 }) {
         Row() {
           Text(item.userName).fontSize(15).fontWeight(FontWeight.Medium).layoutWeight(1).maxLines(1);
-          Text(this.timeLabel(item.lastMessageAt)).fontSize(11).fontColor(DesignTokens.muted(this.darkMode));
+          Text(this.timeLabel(item.lastMessageAt)).fontSize(12).fontColor(DesignTokens.muted(this.darkMode));
         }.width('100%');
         Row({ space: 7 }) {
           Text(this.preview(item)).fontSize(12).fontColor(DesignTokens.muted(this.darkMode))
             .layoutWeight(1).maxLines(1);
           if (item.status === 'closed') IBestTag({ text: I18n.text(this.language, '已关闭', 'Closed'), plain: true });
           if (item.unreadCount > 0) {
-            Text(item.unreadCount > 99 ? '99+' : `${item.unreadCount}`).fontSize(10).fontColor(Color.White)
+            Text(item.unreadCount > 99 ? '99+' : `${item.unreadCount}`).fontSize(12).fontColor(Color.White)
               .textAlign(TextAlign.Center).padding({ left: 6, right: 6, top: 2, bottom: 2 })
               .backgroundColor(DesignTokens.danger(this.darkMode)).borderRadius(10);
           }
         }.width('100%');
       }.layoutWeight(1).alignItems(HorizontalAlign.Start);
-    }.width('100%').padding(14).backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(DesignTokens.RADIUS_LARGE)
+    }.width('100%').padding(14).backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(16)
       .onClick(() => this.open(item));
   }
 
   build() {
     NavDestination() {
       Column() {
-        IBestNavBar({
+        SecondaryHeader({
           title: I18n.text(this.language, '客户消息', 'Customer messages'), isShowStatusBar: true, navBarBgColor: DesignTokens.surface(this.darkMode),
           onLeftClick: () => AppRouter.back()
         });
+          SecondaryFeedback({ errorKind: this.pageError, updating: this.loading, onRetry: () => this.load(false) });
         IBestSearch({
           value: $keyword, placeholder: I18n.text(this.language, '搜索客户或消息', 'Search customers or messages'), isRound: true,
           outBgColor: DesignTokens.page(this.darkMode), innerBgColor: DesignTokens.card(this.darkMode)
@@ -192,7 +205,7 @@ struct ChatSessionsPage {
         } else if (this.errorMessage && this.sessions.length === 0) {
           Column({ space: 12 }) {
             Text(I18n.state(this.language, this.errorMessage)).fontSize(13).fontColor(DesignTokens.muted(this.darkMode));
-            IBestButton({ text: I18n.text(this.language, '重新加载', 'Reload'), type: 'primary', color: DesignTokens.brand(this.darkMode), fontColor: DesignTokens.onBrand(this.darkMode), onBtnClick: () => this.load(false) });
+            IBestButton({ btnHeight: 44, text: I18n.text(this.language, '重新加载', 'Reload'), type: 'primary', color: DesignTokens.brand(this.darkMode), fontColor: DesignTokens.onBrand(this.darkMode), onBtnClick: () => this.load(false) });
           }.layoutWeight(1).justifyContent(FlexAlign.Center);
         } else {
           Refresh({ refreshing: $$this.refreshing }) {
diff --git a/native/harmony-merchant/entry/src/main/ets/features/legal/AgreementsPage.ets b/native/harmony-merchant/entry/src/main/ets/features/legal/AgreementsPage.ets
index c879b7aa..ae14f4b4 100644
--- a/native/harmony-merchant/entry/src/main/ets/features/legal/AgreementsPage.ets
+++ b/native/harmony-merchant/entry/src/main/ets/features/legal/AgreementsPage.ets
@@ -1,3 +1,5 @@
+import { PageReadScope, SecondaryPolicy } from '../../core/ui/SecondaryPolicy';
+import { SecondaryFeedback, SecondaryHeader } from '../../core/ui/SecondaryPage';
 import { IBestButton, IBestNavBar } from '@ibestservices/ibest-ui';
 import { AppRouter } from '../../core/navigation/AppRouter';
 import { ApiError } from '../../core/network/ApiClient';
@@ -14,6 +16,11 @@ export function AgreementsBuilder(_: string, raw: Object) {
 
 @Component
 struct AgreementsPage {
+  aboutToDisappear(): void { this.pageScope.dispose(); }
+
+  private pageScope: PageReadScope = new PageReadScope();
+  @State private pageError: string = '';
+
   params: AgreementRouteParams = new AgreementRouteParams('user');
   @StorageProp('safeBottom') private safeBottom: number = 16;
   @StorageLink('language') @Watch('onLanguageChanged') private language: string = 'zh-CN';
@@ -25,7 +32,7 @@ struct AgreementsPage {
   @State private loading: boolean = true;
   private requestEpoch: number = 0;
 
-  aboutToAppear(): void {
+  aboutToAppear(): void { this.pageScope.open();
     this.load();
   }
 
@@ -34,11 +41,13 @@ struct AgreementsPage {
   }
 
   private async load(): Promise<void> {
+    const pageTicket = this.pageScope.begin('load');
+    this.pageError = '';
     const epoch = ++this.requestEpoch;
     this.loading = true;
     this.errorMessage = '';
     try {
-      const data: LegalAgreementSet = await LegalRepository.agreements();
+      const data: LegalAgreementSet = await this.pageScope.read(LegalRepository.agreements(), pageTicket);
       if (epoch !== this.requestEpoch) return;
       const section = this.params.kind === 'privacy' ? data.privacy :
         this.params.kind === 'collect' ? data.collect : data.user;
@@ -46,21 +55,26 @@ struct AgreementsPage {
       this.updatedAt = section.updatedAt;
       this.body = section.body;
     } catch (error) {
+      if (!this.pageScope.accepts(pageTicket)) return;
+      this.pageError = SecondaryPolicy.errorKind(error);
       if (epoch === this.requestEpoch) {
-        this.errorMessage = error instanceof ApiError ? error.message : 'i18n:agreement-load';
+        this.errorMessage = error instanceof ApiError ? SecondaryPolicy.message(error, this.language) : 'i18n:agreement-load';
       }
     } finally {
+      if (this.pageScope.accepts(pageTicket)) {
       if (epoch === this.requestEpoch) this.loading = false;
     }
+    }
   }
 
   build() {
     NavDestination() {
       Column() {
-        IBestNavBar({
+        SecondaryHeader({
           title: this.title || I18n.text(this.language, '法律协议', 'Legal agreements'),
           isShowStatusBar: true, navBarBgColor: DesignTokens.surface(this.darkMode), onLeftClick: () => AppRouter.back()
         });
+          SecondaryFeedback({ errorKind: this.pageError, updating: this.loading, onRetry: () => this.load() });
         if (this.loading) {
           Column({ space: 10 }) {
             Column({ space: 10 }) {
@@ -68,14 +82,14 @@ struct AgreementsPage {
               Column().width('100%').height(12).backgroundColor(DesignTokens.subtle(this.darkMode)).borderRadius(6);
               Column().width('92%').height(12).backgroundColor(DesignTokens.subtle(this.darkMode)).borderRadius(6);
               Column().width('96%').height(12).backgroundColor(DesignTokens.subtle(this.darkMode)).borderRadius(6);
-            }.width('86%').padding(20).backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(20);
+            }.width('86%').padding(16).backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(20);
             Text(I18n.text(this.language, '正在加载协议', 'Loading agreement')).fontSize(13).fontColor(DesignTokens.muted(this.darkMode));
           }.layoutWeight(1).justifyContent(FlexAlign.Center);
         } else if (this.errorMessage) {
           Column({ space: 12 }) {
             MerchantIcon({ name: 'replay', color: DesignTokens.brand(this.darkMode), iconSize: 30 });
             Text(I18n.state(this.language, this.errorMessage)).fontSize(14).fontColor(DesignTokens.muted(this.darkMode));
-            IBestButton({ text: I18n.text(this.language, '重新加载', 'Reload'), type: 'primary', color: DesignTokens.brand(this.darkMode), fontColor: DesignTokens.onBrand(this.darkMode), onBtnClick: () => this.load() });
+            IBestButton({ btnHeight: 44, text: I18n.text(this.language, '重新加载', 'Reload'), type: 'primary', color: DesignTokens.brand(this.darkMode), fontColor: DesignTokens.onBrand(this.darkMode), onBtnClick: () => this.load() });
           }.layoutWeight(1).justifyContent(FlexAlign.Center);
         } else {
           Scroll() {
@@ -90,7 +104,7 @@ struct AgreementsPage {
                     .wordBreak(WordBreak.BREAK_ALL);
                   if (this.updatedAt) {
                     Text(I18n.text(this.language, `更新日期：${this.updatedAt}`, `Updated: ${this.updatedAt}`))
-                      .fontSize(11).fontColor(DesignTokens.muted(this.darkMode)).width('100%');
+                      .fontSize(12).fontColor(DesignTokens.muted(this.darkMode)).width('100%');
                   }
                 }.layoutWeight(1).alignItems(HorizontalAlign.Start);
               }.width('100%');
@@ -103,8 +117,8 @@ struct AgreementsPage {
             }
             .width('calc(100% - 28vp)')
             .constraintSize({ maxWidth: 760 })
-            .padding({ left: 20, right: 20, top: 18, bottom: this.safeBottom + 24 })
-            .backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(DesignTokens.RADIUS_LARGE)
+            .padding({ left: 16, right: 16, top: 18, bottom: this.safeBottom + 24 })
+            .backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(16)
             .margin({ left: 14, right: 14, top: 14, bottom: this.safeBottom + 18 });
           }
           .layoutWeight(1)
diff --git a/native/harmony-merchant/entry/src/main/ets/features/member/MembershipPage.ets b/native/harmony-merchant/entry/src/main/ets/features/member/MembershipPage.ets
index 4ffab8d0..d2da9532 100644
--- a/native/harmony-merchant/entry/src/main/ets/features/member/MembershipPage.ets
+++ b/native/harmony-merchant/entry/src/main/ets/features/member/MembershipPage.ets
@@ -1,5 +1,7 @@
+import { PageReadScope, SecondaryPolicy } from '../../core/ui/SecondaryPolicy';
+import { SecondaryFeedback, SecondaryHeader } from '../../core/ui/SecondaryPage';
 import { common } from '@kit.AbilityKit';
-import { IBestButton, IBestNavBar, IBestTag, IBestToast } from '@ibestservices/ibest-ui';
+import { IBestButton, IBestTag, IBestToast } from '@ibestservices/ibest-ui';
 import { HuaweiIapService } from '../../core/iap/HuaweiIapService';
 import { IapTransactionPolicy } from '../../core/iap/IapTransactionPolicy';
 import { AppRouter } from '../../core/navigation/AppRouter';
@@ -17,6 +19,11 @@ export function MembershipBuilder(_: string) { MembershipPage(); }
 
 @Component
 struct MembershipPage {
+  aboutToDisappear(): void { this.pageScope.dispose(); }
+
+  private pageScope: PageReadScope = new PageReadScope();
+  @State private pageError: string = '';
+
   @StorageProp('safeBottom') private safeBottom: number = 16;
   @StorageLink('language') @Watch('onLanguageChanged') private language: string = 'zh-CN';
   @StorageLink('darkMode') private darkMode: boolean = false;
@@ -30,7 +37,7 @@ struct MembershipPage {
   @State private restoring: boolean = false;
   private requestEpoch: number = 0;
 
-  aboutToAppear(): void { this.load(); }
+  aboutToAppear(): void { this.pageScope.open(); this.load(); }
 
   private onLanguageChanged(): void { this.load(); }
 
@@ -39,41 +46,55 @@ struct MembershipPage {
   }
 
   private async load(): Promise<void> {
+    const pageTicket = this.pageScope.begin('load');
+    this.pageError = '';
     const epoch = ++this.requestEpoch;
     this.loading = true;
     let failedSections: number = 0;
     try {
-      const plans = await BusinessRepository.memberPlans();
+      const plans = await this.pageScope.read(BusinessRepository.memberPlans(), pageTicket);
       if (epoch === this.requestEpoch) this.plans = plans;
     } catch (_) {
+      if (!this.pageScope.accepts(pageTicket)) return;
+      this.pageError = SecondaryPolicy.errorKind(_);
       failedSections += 1;
     }
     try {
-      const membership = await BusinessRepository.membership();
+      const membership = await this.pageScope.read(BusinessRepository.membership(), pageTicket);
       if (epoch === this.requestEpoch) this.membership = membership;
     } catch (_) {
+      if (!this.pageScope.accepts(pageTicket)) return;
+      this.pageError = SecondaryPolicy.errorKind(_);
       failedSections += 1;
     }
     try {
-      const quota = await BusinessRepository.quota();
+      const quota = await this.pageScope.read(BusinessRepository.quota(), pageTicket);
       if (epoch === this.requestEpoch) this.quota = quota;
     } catch (_) {
+      if (!this.pageScope.accepts(pageTicket)) return;
+      this.pageError = SecondaryPolicy.errorKind(_);
       failedSections += 1;
     }
     try {
-      const notices = await BusinessRepository.membershipNotices();
+      const notices = await this.pageScope.read(BusinessRepository.membershipNotices(), pageTicket);
       if (epoch === this.requestEpoch) this.notices = notices;
     } catch (_) {
+      if (!this.pageScope.accepts(pageTicket)) return;
+      this.pageError = SecondaryPolicy.errorKind(_);
       failedSections += 1;
     }
     try {
-      const payments = await BusinessRepository.payments();
+      const payments = await this.pageScope.read(BusinessRepository.payments(), pageTicket);
       if (epoch === this.requestEpoch) this.payments = payments;
     } catch (_) {
+      if (!this.pageScope.accepts(pageTicket)) return;
+      this.pageError = SecondaryPolicy.errorKind(_);
       failedSections += 1;
     } finally {
+      if (this.pageScope.accepts(pageTicket)) {
       if (epoch === this.requestEpoch) this.loading = false;
     }
+    }
     if (epoch === this.requestEpoch && failedSections > 0) {
       IBestToast.show(I18n.text(this.language,
         `部分会员数据暂时加载失败（${failedSections}/5），其他内容仍可使用`,
@@ -109,7 +130,7 @@ struct MembershipPage {
         : I18n.text(this.language, '权益已激活，请稍后点击恢复购买完成确认', 'Benefits are active. Use Restore Purchases later to finish confirmation.'));
       await this.load();
     } catch (error) {
-      IBestToast.show(error instanceof ApiError ? error.message : error instanceof Error ? error.message
+      IBestToast.show(error instanceof ApiError ? SecondaryPolicy.message(error, this.language) : error instanceof Error ? SecondaryPolicy.message(error, this.language)
         : I18n.text(this.language, '购买未完成', 'Purchase was not completed'));
     } finally {
       this.purchasing = '';
@@ -143,7 +164,7 @@ struct MembershipPage {
           `恢复完成：成功 ${restored} 条，待处理 ${failed} 条`,
           `Restore completed: ${restored} succeeded, ${failed} pending`));
     } catch (error) {
-      IBestToast.show(error instanceof Error ? error.message : I18n.text(this.language, '恢复购买失败', 'Failed to restore purchases'));
+      IBestToast.show(error instanceof Error ? SecondaryPolicy.message(error, this.language) : I18n.text(this.language, '恢复购买失败', 'Failed to restore purchases'));
     } finally {
       this.restoring = false;
     }
@@ -156,7 +177,7 @@ struct MembershipPage {
       await HuaweiIapService.manageSubscriptions(this.context());
       await this.load();
     } catch (error) {
-      IBestToast.show(error instanceof Error ? error.message
+      IBestToast.show(error instanceof Error ? SecondaryPolicy.message(error, this.language)
         : I18n.text(this.language, '无法打开华为订阅管理', 'Unable to open Huawei subscription management'));
     } finally {
       this.purchasing = '';
@@ -224,7 +245,7 @@ struct MembershipPage {
       Text(`¥${item.price.toFixed(2)} / ${this.periodLabel(item)}`)
         .fontSize(23).fontWeight(FontWeight.Bold).fontColor(DesignTokens.brand(this.darkMode)).width('100%');
       if (item.originalPrice && item.originalPrice > item.price) {
-        Text(I18n.text(this.language, `原价 ¥${item.originalPrice.toFixed(2)}`, `Regular ¥${item.originalPrice.toFixed(2)}`)).fontSize(11)
+        Text(I18n.text(this.language, `原价 ¥${item.originalPrice.toFixed(2)}`, `Regular ¥${item.originalPrice.toFixed(2)}`)).fontSize(12)
           .fontColor(DesignTokens.muted(this.darkMode)).decoration({ type: TextDecorationType.LineThrough });
       }
       ForEach(this.planRights(item), (feature: string) => {
@@ -233,7 +254,7 @@ struct MembershipPage {
           Text(feature).fontSize(13).fontColor(DesignTokens.muted(this.darkMode)).layoutWeight(1);
         }.width('100%');
       }, (feature: string) => feature);
-      IBestButton({
+      IBestButton({ btnHeight: 44,
         text: this.membership?.planId === item.id
           ? I18n.text(this.language, '续费', 'Renew')
           : I18n.text(this.language, '立即开通', 'Subscribe'), type: 'primary',
@@ -242,13 +263,13 @@ struct MembershipPage {
         onBtnClick: () => this.purchase(item)
       });
     }.width('100%').alignItems(HorizontalAlign.Start).padding(16)
-      .backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(DesignTokens.RADIUS_LARGE);
+      .backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(16);
   }
 
   build() {
     NavDestination() {
       Column() {
-        IBestNavBar({
+        SecondaryHeader({
           title: I18n.text(this.language, '会员中心', 'Membership'), isShowRight: true,
           rightLabel: this.restoring
             ? I18n.text(this.language, '恢复中…', 'Restoring…')
@@ -256,6 +277,7 @@ struct MembershipPage {
           isShowStatusBar: true, navBarBgColor: DesignTokens.surface(this.darkMode), onLeftClick: () => AppRouter.back(),
           onRightClick: () => this.restorePurchases()
         });
+          SecondaryFeedback({ errorKind: this.pageError, updating: this.loading, onRetry: () => this.load() });
         if (this.loading) {
           Column() { LoadingProgress().width(34).height(34).color(DesignTokens.brand(this.darkMode)); }
             .layoutWeight(1).justifyContent(FlexAlign.Center);
@@ -298,7 +320,7 @@ struct MembershipPage {
                       .fontSize(12).fontColor(DesignTokens.brand(this.darkMode)).width('100%');
                   }
                   if (this.membership.autoRenew) {
-                    IBestButton({
+                    IBestButton({ btnHeight: 44,
                       text: I18n.text(this.language, '管理自动续费', 'Manage auto-renewal'),
                       plain: true, color: DesignTokens.brand(this.darkMode), btnWidth: '100%',
                       loading: this.purchasing === '__manage__',
@@ -306,8 +328,8 @@ struct MembershipPage {
                       onBtnClick: () => this.manageSubscription()
                     });
                   }
-                }.width('100%').padding(18).backgroundColor(DesignTokens.brandTint(this.darkMode))
-                  .borderRadius(DesignTokens.RADIUS_LARGE);
+                }.width('100%').padding(16).backgroundColor(DesignTokens.brandTint(this.darkMode))
+                  .borderRadius(16);
               }
               if (this.quota) {
                 Column({ space: 8 }) {
@@ -326,7 +348,7 @@ struct MembershipPage {
                   Progress({ value: this.quota.impressionUsed, total: Math.max(1, this.quota.impressionLimit) })
                     .color(DesignTokens.brand(this.darkMode)).width('100%');
                 }.width('100%').padding(16).backgroundColor(DesignTokens.card(this.darkMode))
-                  .borderRadius(DesignTokens.RADIUS_LARGE);
+                  .borderRadius(16);
               }
               if (this.plansOf('basic').length > 0) {
                 Text(I18n.text(this.language, '基础会员', 'Core membership')).fontSize(18).fontWeight(FontWeight.Bold).width('100%');
@@ -349,7 +371,7 @@ struct MembershipPage {
                 Row() {
                   Column({ space: 4 }) {
                     Text(item.planName).fontSize(14).fontWeight(FontWeight.Medium);
-                    Text(item.createdAt.slice(0, 10)).fontSize(11).fontColor(DesignTokens.muted(this.darkMode));
+                    Text(item.createdAt.slice(0, 10)).fontSize(12).fontColor(DesignTokens.muted(this.darkMode));
                   }.layoutWeight(1).alignItems(HorizontalAlign.Start);
                   Text(`¥${item.amount.toFixed(2)}`).fontSize(14);
                   IBestTag({ text: this.statusLabel(item.status), plain: true, color: DesignTokens.brand(this.darkMode) });
diff --git a/native/harmony-merchant/entry/src/main/ets/features/order/AfterSaleDetailPage.ets b/native/harmony-merchant/entry/src/main/ets/features/order/AfterSaleDetailPage.ets
index dd033535..435111e5 100644
--- a/native/harmony-merchant/entry/src/main/ets/features/order/AfterSaleDetailPage.ets
+++ b/native/harmony-merchant/entry/src/main/ets/features/order/AfterSaleDetailPage.ets
@@ -1,5 +1,8 @@
+import { LeaveGuard } from '../../core/ui/LeaveGuard';
+import { PageReadScope, SecondaryPolicy } from '../../core/ui/SecondaryPolicy';
+import { SecondaryFeedback, SecondaryHeader } from '../../core/ui/SecondaryPage';
 import {
-  IBestButton, IBestField, IBestImagePreview, IBestNavBar, IBestTag, IBestToast
+  IBestButton, IBestField, IBestImagePreview, IBestTag, IBestToast
 } from '@ibestservices/ibest-ui';
 import { AppRouter } from '../../core/navigation/AppRouter';
 import { IdRouteParams } from '../../core/navigation/RouteParams';
@@ -19,6 +22,39 @@ export function AfterSaleDetailBuilder(_: string, raw: Object) {
 
 @Component
 struct AfterSaleDetailPage {
+  private async verifyReceipt(): Promise<void> {
+    if (!this.item || this.submitting || !this.pageScope.isCurrent()) return;
+    const ticket = this.pageScope.begin('verifyReceipt');
+    this.submitting = true;
+    try {
+      const current = await this.pageScope.read(OrderRepository.refundDetail(this.item.id), ticket);
+      this.item = current; this.receiptPending = false;
+      if (current.status !== 'pending') { this.editorVisible = false; await this.load(); }
+      else IBestToast.show(I18n.text(this.language, '当前仍为待处理，请核对后再确认，未重复提交。', 'Still pending. Review before confirming again; no action was resubmitted.'));
+    } catch (error) {
+      if (this.pageScope.accepts(ticket)) IBestToast.show(I18n.text(this.language, '暂时无法核对结果，请稍后继续核对，不要重复提交。', 'Unable to verify the result. Verify later; do not resubmit.'));
+    } finally { if (this.pageScope.accepts(ticket)) this.submitting = false; }
+  }
+
+  @State private receiptPending: boolean = false;
+  private handleBack(): void {
+    if (this.editorVisible) { this.closeLocalEditor(); return; }
+
+    this.leaveGuard.request(this.language, false, false, () => AppRouter.back());
+  }
+
+  private editStamp: string = '';
+  private localStamp(): string { return JSON.stringify([this.action, this.refundAmount, this.rejectReason]); }
+  private closeLocalEditor(): void {
+    this.leaveGuard.request(this.language, this.submitting, this.localStamp() !== this.editStamp, () => { this.editorVisible = false; });
+  }
+
+  private leaveGuard: LeaveGuard = new LeaveGuard();
+  aboutToDisappear(): void { this.pageScope.dispose(); }
+
+  private pageScope: PageReadScope = new PageReadScope();
+  @State private pageError: string = '';
+
   params: IdRouteParams = new IdRouteParams();
   @StorageProp('safeBottom') private safeBottom: number = 16;
   @StorageLink('language') private language: string = 'zh-CN';
@@ -31,18 +67,25 @@ struct AfterSaleDetailPage {
   @State private rejectReason: string = '';
   @State private submitting: boolean = false;
 
-  aboutToAppear(): void { this.load(); }
+  aboutToAppear(): void { this.pageScope.open(); this.load(); }
 
   private async load(): Promise<void> {
+    if (!this.pageScope.isCurrent()) return;
+    const pageTicket = this.pageScope.begin('load');
+    this.pageError = '';
     this.loading = true;
     try {
-      this.item = await OrderRepository.refundDetail(this.params.id);
+      this.item = await this.pageScope.read(OrderRepository.refundDetail(this.params.id), pageTicket);
     } catch (error) {
-      IBestToast.show(error instanceof ApiError ? error.message
+      if (!this.pageScope.accepts(pageTicket)) return;
+      this.pageError = SecondaryPolicy.errorKind(error);
+      IBestToast.show(error instanceof ApiError ? SecondaryPolicy.message(error, this.language)
         : I18n.text(this.language, '售后详情加载失败', 'Failed to load after-sales details'));
     } finally {
+      if (this.pageScope.accepts(pageTicket)) {
       this.loading = false;
     }
+    }
   }
 
   private statusLabel(status: string): string {
@@ -59,11 +102,14 @@ struct AfterSaleDetailPage {
     this.action = action;
     this.refundAmount = `${this.item.applyAmount}`;
     this.rejectReason = '';
-    this.editorVisible = true;
+    this.editStamp = this.localStamp(); this.editorVisible = true;
   }
 
   private async submit(): Promise<void> {
     if (!this.item || this.submitting) return;
+    if (!this.pageScope.isCurrent()) return;
+    if (this.receiptPending) { await this.verifyReceipt(); return; }
+    const ticket = this.pageScope.begin('submit');
     let approvedAmount: number | undefined = undefined;
     let rejectReason: string | undefined = undefined;
     if (this.action === 'agree') {
@@ -82,18 +128,20 @@ struct AfterSaleDetailPage {
     this.submitting = true;
     try {
       if (this.action === 'agree') {
-        await OrderRepository.agreeRefund(this.item.id, approvedAmount);
+        await this.pageScope.read(OrderRepository.agreeRefund(this.item.id, approvedAmount), ticket);
       } else {
-        await OrderRepository.rejectRefund(this.item.id, rejectReason || '');
+        await this.pageScope.read(OrderRepository.rejectRefund(this.item.id, rejectReason || ''), ticket);
       }
       this.editorVisible = false;
       IBestToast.show(I18n.text(this.language, '售后已处理', 'After-sales request handled'));
       await this.load();
     } catch (error) {
-      IBestToast.show(error instanceof ApiError ? error.message
+      if (!this.pageScope.accepts(ticket)) return;
+      this.receiptPending = true;
+      IBestToast.show(error instanceof ApiError ? SecondaryPolicy.message(error, this.language)
         : I18n.text(this.language, '售后处理失败', 'Failed to handle after-sales request'));
     } finally {
-      this.submitting = false;
+      if (this.pageScope.accepts(ticket)) this.submitting = false;
     }
   }
 
@@ -117,7 +165,7 @@ struct AfterSaleDetailPage {
               .fontSize(13).fontColor(DesignTokens.muted(this.darkMode)).width('100%');
           }
         }.width('100%').alignItems(HorizontalAlign.Start).padding(16).backgroundColor(DesignTokens.card(this.darkMode))
-          .borderRadius(DesignTokens.RADIUS_LARGE);
+          .borderRadius(16);
 
         Column({ space: 10 }) {
           Text(I18n.text(this.language, '申请说明', 'Request details')).fontSize(16).fontWeight(FontWeight.Medium).width('100%');
@@ -129,7 +177,7 @@ struct AfterSaleDetailPage {
             `Submitted: ${item.createdAt.replace('T', ' ').slice(0, 19)}`))
             .fontSize(12).fontColor(DesignTokens.muted(this.darkMode)).width('100%');
         }.width('100%').alignItems(HorizontalAlign.Start).padding(16).backgroundColor(DesignTokens.card(this.darkMode))
-          .borderRadius(DesignTokens.RADIUS_LARGE);
+          .borderRadius(16);
 
         if (item.evidence.length > 0) {
           Column({ space: 10 }) {
@@ -143,7 +191,7 @@ struct AfterSaleDetailPage {
                   }));
               }, (url: string) => url);
             }.width('100%');
-          }.width('100%').padding(16).backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(DesignTokens.RADIUS_LARGE);
+          }.width('100%').padding(16).backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(16);
         }
 
         if (item.order) {
@@ -154,12 +202,12 @@ struct AfterSaleDetailPage {
             }
             Text(I18n.text(this.language, `订单号 ${item.order.no}`, `Order ${item.order.no}`)).fontSize(13).fontColor(DesignTokens.muted(this.darkMode)).width('100%');
             Text(I18n.text(this.language, `实付 ¥${item.order.payAmount.toFixed(2)}`, `Paid ¥${item.order.payAmount.toFixed(2)}`)).fontSize(14).width('100%');
-            IBestButton({
+            IBestButton({ btnHeight: 44,
               text: I18n.text(this.language, '查看订单详情', 'View order'), plain: true, color: DesignTokens.brand(this.darkMode), btnWidth: '100%',
               onBtnClick: () => AppRouter.push('OrderDetail', new IdRouteParams(item.orderId))
             });
           }.width('100%').alignItems(HorizontalAlign.Start).padding(16).backgroundColor(DesignTokens.card(this.darkMode))
-            .borderRadius(DesignTokens.RADIUS_LARGE);
+            .borderRadius(16);
         }
         if (item.merchantReply || item.refundAmount !== undefined) {
           Column({ space: 8 }) {
@@ -173,7 +221,7 @@ struct AfterSaleDetailPage {
               Text(I18n.text(this.language, `退款金额 ¥${item.refundAmount.toFixed(2)}`, `Refund amount ¥${item.refundAmount.toFixed(2)}`))
                 .width('100%').wordBreak(WordBreak.BREAK_ALL).fontSize(14).fontColor(DesignTokens.text(this.darkMode));
             }
-          }.width('100%').padding(16).backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(DesignTokens.RADIUS_LARGE);
+          }.width('100%').padding(16).backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(16);
         }
       }.width('100%').constraintSize({ maxWidth: 960 }).padding({ left: 16, right: 16, top: 12, bottom: this.safeBottom + (item.status === 'pending' ? 104 : 24) });
     }.layoutWeight(1).align(Alignment.Top).scrollBar(BarState.Off);
@@ -183,10 +231,11 @@ struct AfterSaleDetailPage {
     NavDestination() {
       Stack({ alignContent: Alignment.Bottom }) {
         Column() {
-          IBestNavBar({
+          SecondaryHeader({
             title: I18n.text(this.language, '售后详情', 'After-sales details'), isShowStatusBar: true, navBarBgColor: DesignTokens.surface(this.darkMode),
-            onLeftClick: () => AppRouter.back()
+            onLeftClick: () => this.handleBack()
           });
+          SecondaryFeedback({ errorKind: this.pageError, updating: this.loading, onRetry: () => this.load() });
           if (this.loading && !this.item) {
             Column() { LoadingProgress().width(34).height(34).color(DesignTokens.brand(this.darkMode)); }
               .layoutWeight(1).justifyContent(FlexAlign.Center);
@@ -195,8 +244,8 @@ struct AfterSaleDetailPage {
           } else {
             Column({ space: 12 }) {
               Text(I18n.text(this.language, '售后记录不存在或无权访问', 'This request does not exist or is not accessible')).fontSize(14).fontColor(DesignTokens.muted(this.darkMode));
-              IBestButton({ text: I18n.text(this.language, '返回', 'Back'), plain: true, onBtnClick: () => AppRouter.back() });
-              IBestButton({ text: I18n.text(this.language, '重试', 'Retry'), plain: true,
+              IBestButton({ btnHeight: 44, text: I18n.text(this.language, '返回', 'Back'), plain: true, onBtnClick: () => AppRouter.back() });
+              IBestButton({ btnHeight: 44, text: I18n.text(this.language, '重试', 'Retry'), plain: true,
                 color: DesignTokens.brand(this.darkMode), onBtnClick: () => this.load() });
             }.layoutWeight(1).justifyContent(FlexAlign.Center);
           }
@@ -204,15 +253,15 @@ struct AfterSaleDetailPage {
 
         if (this.item?.status === 'pending' && !this.editorVisible) {
           Row({ space: 10 }) {
-            IBestButton({ text: I18n.text(this.language, '驳回', 'Reject'), plain: true, type: 'danger', btnWidth: '48%', onBtnClick: () => this.open('reject') });
-            IBestButton({ text: I18n.text(this.language, '同意退款', 'Approve refund'), type: 'primary', color: DesignTokens.brand(this.darkMode), fontColor: DesignTokens.onBrand(this.darkMode), btnWidth: '48%', onBtnClick: () => this.open('agree') });
+            IBestButton({ btnHeight: 44, text: I18n.text(this.language, '驳回', 'Reject'), plain: true, type: 'danger', btnWidth: '48%', onBtnClick: () => this.open('reject') });
+            IBestButton({ btnHeight: 44, text: I18n.text(this.language, '同意退款', 'Approve refund'), type: 'primary', color: DesignTokens.brand(this.darkMode), fontColor: DesignTokens.onBrand(this.darkMode), btnWidth: '48%', onBtnClick: () => this.open('agree') });
           }.width('100%').padding({ left: 16, right: 16, top: 12, bottom: this.safeBottom + 12 })
             .backgroundColor(DesignTokens.card(this.darkMode)).border({ width: { top: 1 }, color: DesignTokens.border(this.darkMode) });
         }
 
         if (this.editorVisible) {
           Column().width('100%').height('100%').backgroundColor('#99000000')
-            .onClick(() => { if (!this.submitting) this.editorVisible = false; });
+            .onClick(() => this.closeLocalEditor());
           Scroll() {
           Column({ space: 14 }) {
             Text(this.action === 'agree'
@@ -224,23 +273,19 @@ struct AfterSaleDetailPage {
               IBestField({ value: $rejectReason, label: I18n.text(this.language, '驳回原因', 'Rejection reason'), placeholder: I18n.text(this.language, '请输入具体原因', 'Enter a specific reason') });
             }
             Row({ space: 10 }) {
-              IBestButton({ text: I18n.text(this.language, '取消', 'Cancel'), plain: true, btnWidth: '48%', disabled: this.submitting, onBtnClick: () => this.editorVisible = false });
-              IBestButton({
-                text: I18n.text(this.language, '确定', 'Confirm'), type: this.action === 'agree' ? 'primary' : 'danger',
+              IBestButton({ btnHeight: 44, text: I18n.text(this.language, '取消', 'Cancel'), plain: true, btnWidth: '48%', disabled: this.submitting, onBtnClick: () => this.closeLocalEditor() });
+              IBestButton({ btnHeight: 44,
+                text: this.receiptPending ? I18n.text(this.language, '核对处理结果', 'Verify result') : I18n.text(this.language, '确定', 'Confirm'), type: this.action === 'agree' ? 'primary' : 'danger',
                 color: this.action === 'agree' ? DesignTokens.brand(this.darkMode) : DesignTokens.danger(this.darkMode), btnWidth: '48%',
                 loading: this.submitting, disabled: this.submitting, onBtnClick: () => this.submit()
               });
             }.width('100%');
-          }.width('100%').padding({ left: 20, right: 20, top: 22, bottom: this.safeBottom + 22 })
+          }.width('100%').padding({ left: 16, right: 16, top: 22, bottom: this.safeBottom + 22 })
             .backgroundColor(DesignTokens.card(this.darkMode));
           }.width('100%').constraintSize({ maxHeight: '80%', maxWidth: 720 }).align(Alignment.Top)
             .borderRadius({ topLeft: DesignTokens.RADIUS_XL, topRight: DesignTokens.RADIUS_XL }).clip(true);
         }
       }.width('100%').height('100%');
-    }.hideTitleBar(true).onBackPressed((): boolean => {
-      if (!this.editorVisible) return false;
-      if (!this.submitting) this.editorVisible = false;
-      return true;
-    });
+    }.hideTitleBar(true).onBackPressed((): boolean => { this.handleBack(); return true; });
   }
 }
diff --git a/native/harmony-merchant/entry/src/main/ets/features/order/AfterSalePage.ets b/native/harmony-merchant/entry/src/main/ets/features/order/AfterSalePage.ets
index 31cac13c..3b513b2b 100644
--- a/native/harmony-merchant/entry/src/main/ets/features/order/AfterSalePage.ets
+++ b/native/harmony-merchant/entry/src/main/ets/features/order/AfterSalePage.ets
@@ -1,5 +1,8 @@
+import { LeaveGuard } from '../../core/ui/LeaveGuard';
+import { PageReadScope, SecondaryPolicy } from '../../core/ui/SecondaryPolicy';
+import { SecondaryFeedback, SecondaryHeader } from '../../core/ui/SecondaryPage';
 import { OrderProductSummary } from './OrderProductSummary';
-import { IBestButton, IBestEmpty, IBestField, IBestNavBar, IBestToast } from '@ibestservices/ibest-ui';
+import { IBestButton, IBestEmpty, IBestField, IBestToast } from '@ibestservices/ibest-ui';
 import { AppRouter } from '../../core/navigation/AppRouter';
 import { FilterRouteParams, IdRouteParams } from '../../core/navigation/RouteParams';
 import { ApiError } from '../../core/network/ApiClient';
@@ -16,6 +19,41 @@ export function AfterSaleBuilder(_: string, raw: Object) { AfterSalePage({ param
 
 @Component
 struct AfterSalePage {
+  private async verifyReceipt(): Promise<void> {
+    if (!this.selected || this.submitting || !this.pageScope.isCurrent()) return;
+    const ticket = this.pageScope.begin('verifyReceipt');
+    this.submitting = true;
+    try {
+      const current = await this.pageScope.read(OrderRepository.refundDetail(this.selected.id), ticket);
+      this.selected = current; this.receiptPending = false;
+      if (current.status !== 'pending') { this.editorVisible = false; await this.reload(); }
+      else IBestToast.show(I18n.text(this.language, '当前仍为待处理，请核对后再确认，未重复提交。', 'Still pending. Review before confirming again; no action was resubmitted.'));
+    } catch (error) {
+      if (this.pageScope.accepts(ticket)) IBestToast.show(I18n.text(this.language, '暂时无法核对结果，请稍后继续核对，不要重复提交。', 'Unable to verify the result. Verify later; do not resubmit.'));
+    } finally { if (this.pageScope.accepts(ticket)) this.submitting = false; }
+  }
+
+  @State private receiptPending: boolean = false;
+  private visibleFilter: string = '';
+
+  private handleBack(): void {
+    if (this.editorVisible) { this.closeLocalEditor(); return; }
+
+    this.leaveGuard.request(this.language, false, false, () => AppRouter.back());
+  }
+
+  private editStamp: string = '';
+  private localStamp(): string { return JSON.stringify([this.action, this.refundAmount, this.rejectReason]); }
+  private closeLocalEditor(): void {
+    this.leaveGuard.request(this.language, this.submitting, this.localStamp() !== this.editStamp, () => { this.editorVisible = false; });
+  }
+
+  private leaveGuard: LeaveGuard = new LeaveGuard();
+  aboutToDisappear(): void { this.pageScope.dispose(); }
+
+  private pageScope: PageReadScope = new PageReadScope();
+  @State private pageError: string = '';
+
   params: FilterRouteParams = new FilterRouteParams();
   @StorageProp('windowWidth') private windowWidth: number = 390;
   @State private refreshing: boolean = false;
@@ -42,7 +80,7 @@ struct AfterSalePage {
     { value: 'in_progress', label: 'in_progress' }, { value: 'completed', label: 'completed' }
   ];
 
-  aboutToAppear(): void {
+  aboutToAppear(): void { this.pageScope.open();
     if (this.filters.some((item: RefundFilter) => item.value === this.params.status)) this.status = this.params.status;
     this.reload();
   }
@@ -52,38 +90,50 @@ struct AfterSalePage {
   }
 
   private async reload(): Promise<void> {
-    this.page = 1; await this.load(false);
+    await this.load(false);
   }
 
   private async load(append: boolean): Promise<void> {
+    if (!this.pageScope.isCurrent()) return;
     if (append && this.loading) return;
+    const pageTicket = this.pageScope.begin('load');
+    this.pageError = '';
+    const filterKey = this.status;
+    if (filterKey !== this.visibleFilter) { this.refunds = []; this.total = 0; this.page = 1; this.visibleFilter = filterKey; }
     const epoch = ++this.requestEpoch;
     const requestedStatus = this.status;
-    const requestedPage = this.page;
+    const requestedPage = append ? this.page : 1;
     this.loading = true;
     this.errorMessage = '';
     try {
-      const result = await OrderRepository.refunds(requestedStatus, requestedPage);
+      const result = await this.pageScope.read(OrderRepository.refunds(requestedStatus, requestedPage), pageTicket);
       if (epoch !== this.requestEpoch) return;
       this.refunds = append ? this.refunds.concat(result.list) : result.list;
-      this.total = result.total;
+      this.total = result.total; this.page = requestedPage;
     } catch (error) {
+      if (!this.pageScope.accepts(pageTicket)) return;
+      this.pageError = SecondaryPolicy.errorKind(error);
       if (epoch === this.requestEpoch) {
         if (append && this.page === requestedPage) this.page = Math.max(1, requestedPage - 1);
-        this.errorMessage = error instanceof ApiError ? error.message : 'i18n:refunds-load';
+        this.errorMessage = error instanceof ApiError ? SecondaryPolicy.message(error, this.language) : 'i18n:refunds-load';
       }
     } finally {
+      if (this.pageScope.accepts(pageTicket)) {
       if (epoch === this.requestEpoch) { this.loading = false; this.refreshing = false; }
     }
+    }
   }
 
   private open(item: RefundSummary, action: string): void {
     this.selected = item; this.action = action;
-    this.refundAmount = `${item.applyAmount}`; this.rejectReason = ''; this.editorVisible = true;
+    this.refundAmount = `${item.applyAmount}`; this.rejectReason = ''; this.editStamp = this.localStamp(); this.editorVisible = true;
   }
 
   private async submit(): Promise<void> {
     if (!this.selected || this.submitting) return;
+    if (!this.pageScope.isCurrent()) return;
+    if (this.receiptPending) { await this.verifyReceipt(); return; }
+    const ticket = this.pageScope.begin('submit');
     this.submitting = true;
     try {
       if (this.action === 'agree') {
@@ -91,15 +141,17 @@ struct AfterSalePage {
         if (amount === undefined) {
           IBestToast.show(I18n.text(this.language, '退款金额必须大于0且不超过申请金额', 'Refund amount must be greater than 0 and no more than the requested amount')); return;
         }
-        await OrderRepository.agreeRefund(this.selected.id, amount);
+        await this.pageScope.read(OrderRepository.agreeRefund(this.selected.id, amount), ticket);
       } else {
         const reason = AfterSalePolicy.rejectReason(this.rejectReason);
         if (reason === undefined) { IBestToast.show(I18n.text(this.language, '请填写驳回原因', 'Enter a rejection reason')); return; }
-        await OrderRepository.rejectRefund(this.selected.id, reason);
+        await this.pageScope.read(OrderRepository.rejectRefund(this.selected.id, reason), ticket);
       }
       this.editorVisible = false; IBestToast.show(I18n.text(this.language, '已处理', 'Request handled')); await this.reload();
-    } catch (error) { IBestToast.show(error instanceof ApiError ? error.message : I18n.text(this.language, '售后处理失败', 'Failed to handle after-sales request')); }
-    finally { this.submitting = false; }
+    } catch (error) {
+      if (!this.pageScope.accepts(ticket)) return;
+      this.receiptPending = true; IBestToast.show(error instanceof ApiError ? SecondaryPolicy.message(error, this.language) : I18n.text(this.language, '售后处理失败', 'Failed to handle after-sales request')); }
+    finally { if (this.pageScope.accepts(ticket)) this.submitting = false; }
   }
 
   private statusLabel(value: string): string {
@@ -139,25 +191,26 @@ struct AfterSalePage {
       Text(I18n.text(this.language, `申请金额 ¥${item.applyAmount.toFixed(2)}`, `Requested ¥${item.applyAmount.toFixed(2)}`))
         .width('100%').wordBreak(WordBreak.BREAK_ALL).fontSize(18).fontWeight(FontWeight.Bold)
         .fontColor(DesignTokens.brand(this.darkMode));
-      Text(item.createdAt.replace('T', ' ').slice(0, 16)).width('100%').fontSize(11).fontColor(DesignTokens.muted(this.darkMode));
-      IBestButton({ text: I18n.text(this.language, '查看详情', 'View details'), plain: true, btnWidth: '100%',
+      Text(item.createdAt.replace('T', ' ').slice(0, 16)).width('100%').fontSize(12).fontColor(DesignTokens.muted(this.darkMode));
+      IBestButton({ btnHeight: 44, text: I18n.text(this.language, '查看详情', 'View details'), plain: true, btnWidth: '100%',
         color: DesignTokens.muted(this.darkMode), onBtnClick: () => AppRouter.push('AfterSaleDetail', new IdRouteParams(item.id)) });
       if (item.status === 'pending') {
         Row({ space: 8 }) {
-          IBestButton({ text: I18n.text(this.language, '驳回', 'Reject'), plain: true, btnWidth: '48%', type: 'danger', onBtnClick: () => this.open(item, 'reject') });
-          IBestButton({ text: I18n.text(this.language, '同意退款', 'Approve refund'), btnWidth: '48%', type: 'primary',
+          IBestButton({ btnHeight: 44, text: I18n.text(this.language, '驳回', 'Reject'), plain: true, btnWidth: '48%', type: 'danger', onBtnClick: () => this.open(item, 'reject') });
+          IBestButton({ btnHeight: 44, text: I18n.text(this.language, '同意退款', 'Approve refund'), btnWidth: '48%', type: 'primary',
             color: DesignTokens.brand(this.darkMode), fontColor: DesignTokens.onBrand(this.darkMode), onBtnClick: () => this.open(item, 'agree') });
         }.width('100%');
       }
     }.width('100%').alignItems(HorizontalAlign.Start).padding(16)
-      .backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(DesignTokens.RADIUS_LARGE);
+      .backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(16);
   }
 
   build() {
     NavDestination() {
       Stack({ alignContent: Alignment.Bottom }) {
         Column() {
-          IBestNavBar({ title: I18n.text(this.language, '售后处理', 'After-sales'), isShowStatusBar: true, navBarBgColor: DesignTokens.surface(this.darkMode), onLeftClick: () => AppRouter.back() });
+          SecondaryHeader({ title: I18n.text(this.language, '售后处理', 'After-sales'), isShowStatusBar: true, navBarBgColor: DesignTokens.surface(this.darkMode), onLeftClick: () => this.handleBack() });
+          SecondaryFeedback({ errorKind: this.pageError, updating: this.loading, onRetry: () => this.load(false) });
           Scroll() { Row({ space: 8 }) { ForEach(this.filters, (item: RefundFilter) => { Text(this.statusLabel(item.value)).fontSize(13).fontColor(this.status === item.value ? DesignTokens.brand(this.darkMode) : DesignTokens.muted(this.darkMode)).padding({ left: 14, right: 14, top: 8, bottom: 8 }).border({ width: { bottom: 2 }, color: this.status === item.value ? DesignTokens.brand(this.darkMode) : Color.Transparent }).onClick(() => { this.status = item.value; this.reload(); }); }, (item: RefundFilter) => item.value); }.padding(12); }.scrollable(ScrollDirection.Horizontal).scrollBar(BarState.Off);
           if (this.errorMessage) {
             Text(I18n.state(this.language, this.errorMessage)).width('100%').fontSize(12).padding(12).fontColor(DesignTokens.muted(this.darkMode))
@@ -173,21 +226,17 @@ struct AfterSalePage {
         }.width('100%').height('100%').backgroundColor(DesignTokens.page(this.darkMode));
         if (this.editorVisible) {
           Column().width('100%').height('100%').backgroundColor('#99000000')
-            .onClick(() => { if (!this.submitting) this.editorVisible = false; });
+            .onClick(() => this.closeLocalEditor());
           Scroll() {
           Column({ space: 14 }) {
             Text(this.action === 'agree' ? I18n.text(this.language, '确认退款', 'Confirm refund') : I18n.text(this.language, '驳回售后', 'Reject request')).fontSize(20).fontWeight(FontWeight.Bold);
             if (this.action === 'agree') IBestField({ value: $refundAmount, label: I18n.text(this.language, '退款金额', 'Refund amount'), type: 'decimal', placeholder: I18n.text(this.language, '请输入退款金额', 'Enter the refund amount') });
             else IBestField({ value: $rejectReason, label: I18n.text(this.language, '驳回原因', 'Rejection reason'), placeholder: I18n.text(this.language, '请输入具体原因', 'Enter a specific reason') });
-            Row({ space: 10 }) { IBestButton({ text: I18n.text(this.language, '取消', 'Cancel'), plain: true, btnWidth: '48%', disabled: this.submitting, onBtnClick: () => { this.editorVisible = false; } }); IBestButton({ text: I18n.text(this.language, '确定', 'Confirm'), type: this.action === 'agree' ? 'primary' : 'danger', color: this.action === 'agree' ? DesignTokens.brand(this.darkMode) : DesignTokens.danger(this.darkMode), btnWidth: '48%', loading: this.submitting, disabled: this.submitting, onBtnClick: () => this.submit() }); }.width('100%');
-          }.width('100%').padding({ left: 20, right: 20, top: 22, bottom: this.safeBottom + 22 }).backgroundColor(DesignTokens.card(this.darkMode)).borderRadius({ topLeft: DesignTokens.RADIUS_XL, topRight: DesignTokens.RADIUS_XL });
+            Row({ space: 10 }) { IBestButton({ btnHeight: 44, text: I18n.text(this.language, '取消', 'Cancel'), plain: true, btnWidth: '48%', disabled: this.submitting, onBtnClick: () => { this.closeLocalEditor(); } }); IBestButton({ btnHeight: 44, text: this.receiptPending ? I18n.text(this.language, '核对处理结果', 'Verify result') : I18n.text(this.language, '确定', 'Confirm'), type: this.action === 'agree' ? 'primary' : 'danger', color: this.action === 'agree' ? DesignTokens.brand(this.darkMode) : DesignTokens.danger(this.darkMode), btnWidth: '48%', loading: this.submitting, disabled: this.submitting, onBtnClick: () => this.submit() }); }.width('100%');
+          }.width('100%').padding({ left: 16, right: 16, top: 22, bottom: this.safeBottom + 22 }).backgroundColor(DesignTokens.card(this.darkMode)).borderRadius({ topLeft: DesignTokens.RADIUS_XL, topRight: DesignTokens.RADIUS_XL });
           }.width('100%').constraintSize({ maxWidth: 720, maxHeight: '80%' }).align(Alignment.Top).clip(true);
         }
       }.width('100%').height('100%');
-    }.hideTitleBar(true).onBackPressed((): boolean => {
-      if (!this.editorVisible) return false;
-      if (!this.submitting) this.editorVisible = false;
-      return true;
-    });
+    }.hideTitleBar(true).onBackPressed((): boolean => { this.handleBack(); return true; });
   }
 }
diff --git a/native/harmony-merchant/entry/src/main/ets/features/order/OrderDetailPage.ets b/native/harmony-merchant/entry/src/main/ets/features/order/OrderDetailPage.ets
index 901d95a2..73416ad2 100644
--- a/native/harmony-merchant/entry/src/main/ets/features/order/OrderDetailPage.ets
+++ b/native/harmony-merchant/entry/src/main/ets/features/order/OrderDetailPage.ets
@@ -1,8 +1,9 @@
+import { SecondaryHeader } from '../../core/ui/SecondaryPage';
 import { common } from '@kit.AbilityKit';
 import { KeyboardAvoidMode } from '@kit.ArkUI';
 import { pasteboard } from '@kit.BasicServicesKit';
 import { systemShare } from '@kit.ShareKit';
-import { IBestActionSheet, IBestDialogUtil, IBestField, IBestNavBar, IBestToast } from '@ibestservices/ibest-ui';
+import { IBestActionSheet, IBestDialogUtil, IBestField, IBestToast } from '@ibestservices/ibest-ui';
 import { AppRouter } from '../../core/navigation/AppRouter';
 import { PhoneService } from '../../core/device/PhoneService';
 import { IdRouteParams } from '../../core/navigation/RouteParams';
@@ -457,7 +458,7 @@ struct OrderDetailPage {
           if (this.actionMode === 'ship') this.shippingForm();
           else if (this.actionMode === 'parse') this.parseForm();
           else this.sharingForm();
-        }.width('100%').padding(18).enabled(!this.submitting);
+        }.width('100%').padding(16).enabled(!this.submitting);
       }.layoutWeight(1).width('100%').align(Alignment.Top).scrollBar(BarState.Off);
       if (this.actionError) Text(this.errorText()).fontSize(12).fontColor(DesignTokens.danger(this.darkMode)).width('100%').padding({ left: 18, right: 18, top: 8 });
       Row({ space: 12 }) {
@@ -484,7 +485,7 @@ struct OrderDetailPage {
     NavDestination() {
       Stack({ alignContent: Alignment.Bottom }) {
         Column() {
-          IBestNavBar({ title: I18n.text(this.language, '订单详情', 'Order details'), isShowStatusBar: true,
+          SecondaryHeader({ title: I18n.text(this.language, '订单详情', 'Order details'), isShowStatusBar: true,
             navBarBgColor: DesignTokens.surface(this.darkMode), onLeftClick: () => this.leave() });
           if (this.loading && !this.order) {
             Scroll() { this.skeleton(); }.layoutWeight(1).width('100%').align(Alignment.Top);
@@ -509,7 +510,7 @@ struct OrderDetailPage {
             }.layoutWeight(1).align(Alignment.Top).scrollBar(BarState.Off);
             this.actionBar();
           } else {
-            Column({ space: 18 }) {
+            Column({ space: 12 }) {
               MerchantIcon({ name: 'orders-o', iconSize: 54, color: DesignTokens.muted(this.darkMode) });
               Text(I18n.text(this.language, '暂时无法加载订单', 'Unable to load this order')).fontSize(16).fontWeight(FontWeight.Medium).fontColor(DesignTokens.text(this.darkMode));
               Text(I18n.text(this.language, '请检查网络后重试，订单信息不会丢失。', 'Check your connection and try again. Your order is unchanged.')).fontSize(13).textAlign(TextAlign.Center).fontColor(DesignTokens.muted(this.darkMode));
diff --git a/native/harmony-merchant/entry/src/main/ets/features/order/OrderListPage.ets b/native/harmony-merchant/entry/src/main/ets/features/order/OrderListPage.ets
index 73f0dba5..c91d3aa5 100644
--- a/native/harmony-merchant/entry/src/main/ets/features/order/OrderListPage.ets
+++ b/native/harmony-merchant/entry/src/main/ets/features/order/OrderListPage.ets
@@ -1,3 +1,4 @@
+import { PageReadScope, PageSearch, SecondaryPolicy } from '../../core/ui/SecondaryPolicy';
 import { PrimaryNavigationMetrics } from '../shell/PrimaryNavigationMetrics';
 import { IBestButton, IBestEmpty, IBestSearch } from '@ibestservices/ibest-ui';
 import { AppRouter } from '../../core/navigation/AppRouter';
@@ -14,6 +15,12 @@ interface OrderFilter { value: string; label: string; }
 
 @Component
 export struct OrderListPage {
+  private pageScope: PageReadScope = new PageReadScope();
+  private pageSearch: PageSearch = new PageSearch();
+  private visibleFilter: string = '';
+  @StorageProp('sessionEpoch') @Watch('onSessionChanged') private sessionEpoch: number = 0;
+  private onSessionChanged(): void { this.pageScope.dispose(); this.requestEpoch++; this.orders = []; this.total = 0; this.loading = false; this.pageSearch.cancel(); }
+  aboutToDisappear(): void { this.pageScope.dispose(); this.requestEpoch++; this.pageSearch.cancel(); }
   @Prop @Watch('onInitialStatus') initialStatus: string = '';
   @Prop @Watch('onActivation') activationEpoch: number = 0;
   showBack: boolean = false;
@@ -38,7 +45,7 @@ export struct OrderListPage {
     { value: 'cancelled', label: 'cancelled' }
   ];
 
-  aboutToAppear(): void { this.status = this.initialStatus; this.reload(); }
+  aboutToAppear(): void { this.pageScope.open(); this.status = this.initialStatus; this.reload(); }
 
   private onInitialStatus(): void {
     if (this.status === this.initialStatus) return;
@@ -49,26 +56,32 @@ export struct OrderListPage {
   private onActivation(): void { this.reload(); }
 
   private async reload(): Promise<void> {
-    this.page = 1; await this.load(false);
+    this.pageSearch.cancel(); await this.load(false);
   }
 
   private async load(append: boolean): Promise<void> {
+    if (!this.pageScope.isCurrent()) return;
     if (append && this.loading) return;
+    const condition = `${this.status}:${this.keyword.trim()}`;
+    if (this.loading && condition === this.visibleFilter) return;
+    if (condition !== this.visibleFilter) { this.orders = []; this.total = 0; this.page = 1; }
+    this.visibleFilter = condition;
+    const ticket = this.pageScope.begin('list');
     const epoch = ++this.requestEpoch;
     const requestedStatus = this.status;
     const requestedKeyword = this.keyword.trim();
-    const requestedPage = this.page;
+    const requestedPage = append ? this.page : 1;
     this.loading = true;
     this.errorMessage = '';
     try {
-      const result = await OrderRepository.list(requestedStatus, requestedKeyword, requestedPage);
+      const result = await this.pageScope.read(OrderRepository.list(requestedStatus, requestedKeyword, requestedPage), ticket);
       if (epoch !== this.requestEpoch) return;
       this.orders = append ? this.orders.concat(result.list) : result.list;
-      this.total = result.total;
+      this.total = result.total; this.page = requestedPage;
     } catch (error) {
       if (epoch === this.requestEpoch) {
         if (append && this.page === requestedPage) this.page = Math.max(1, requestedPage - 1);
-        this.errorMessage = error instanceof ApiError ? error.message : 'i18n:orders-load';
+        this.errorMessage = error instanceof ApiError ? SecondaryPolicy.message(error, this.language) : 'i18n:orders-load';
       }
     } finally {
       if (epoch === this.requestEpoch) {
@@ -142,19 +155,19 @@ export struct OrderListPage {
       Row() {
         if (this.showBack) {
           Column() { MerchantIcon({ name: 'arrow-left', color: DesignTokens.text(this.darkMode), iconSize: 25 }); }
-            .width(34).height(38).justifyContent(FlexAlign.Center).onClick(() => AppRouter.back());
+            .width(44).height(44).justifyContent(FlexAlign.Center).onClick(() => AppRouter.back());
         }
         Column({ space: 2 }) {
-          Text(I18n.text(this.language, '订单', 'Orders')).fontSize(26).fontWeight(FontWeight.Bold).fontColor(DesignTokens.text(this.darkMode));
+          Text(I18n.text(this.language, '订单', 'Orders')).fontSize(this.showBack ? 20 : 26).fontWeight(FontWeight.Bold).fontColor(DesignTokens.text(this.darkMode));
           Text(I18n.text(this.language, `共 ${this.total} 单`, `${this.total} total`)).fontSize(12).fontColor(DesignTokens.muted(this.darkMode));
         }.alignItems(HorizontalAlign.Start);
         Blank();
         IBestButton({ text: I18n.text(this.language, '售后', 'After-sales'), plain: true, color: DesignTokens.brand(this.darkMode), onBtnClick: () => AppRouter.push('AfterSale') });
-      }.width('100%').padding({ top: this.safeTop + 12, left: 16, right: 16, bottom: 10 });
+      }.width('100%').padding({ top: this.safeTop + (this.showBack ? 6 : 12), left: this.showBack ? 8 : 16, right: 16, bottom: 10 });
       IBestSearch({
         value: $keyword, placeholder: I18n.text(this.language, '搜索订单号或客户', 'Search order number or customer'), isRound: true,
         outBgColor: DesignTokens.page(this.darkMode), innerBgColor: DesignTokens.card(this.darkMode), showRightButton: true,
-        rightButtonText: I18n.text(this.language, '搜索', 'Search'), onSearch: () => this.reload(), onRightButtonClick: () => this.reload(), onClear: () => this.reload()
+        rightButtonText: I18n.text(this.language, '搜索', 'Search'), onInput: () => this.pageSearch.schedule(() => this.reload()), onSearch: () => this.reload(), onRightButtonClick: () => this.reload(), onClear: () => this.reload()
       });
       Scroll() {
         Row({ space: 8 }) {
diff --git a/native/harmony-merchant/entry/src/main/ets/features/plaza/AgencyPage.ets b/native/harmony-merchant/entry/src/main/ets/features/plaza/AgencyPage.ets
index ca912f02..c8fc920b 100644
--- a/native/harmony-merchant/entry/src/main/ets/features/plaza/AgencyPage.ets
+++ b/native/harmony-merchant/entry/src/main/ets/features/plaza/AgencyPage.ets
@@ -1,5 +1,9 @@
+import { ResilientRemoteImage } from '../../core/ui/ResilientRemoteImage';
+import { LeaveGuard } from '../../core/ui/LeaveGuard';
+import { PageReadScope, SecondaryPolicy } from '../../core/ui/SecondaryPolicy';
+import { SecondaryFeedback, SecondaryHeader } from '../../core/ui/SecondaryPage';
 import {
-  IBestButton, IBestDialogUtil, IBestEmpty, IBestField, IBestNavBar, IBestTag, IBestToast
+  IBestButton, IBestDialogUtil, IBestEmpty, IBestField, IBestTag, IBestToast
 } from '@ibestservices/ibest-ui';
 import { AppRouter } from '../../core/navigation/AppRouter';
 import { IdRouteParams } from '../../core/navigation/RouteParams';
@@ -17,6 +21,24 @@ export function AgencyProductsBuilder(_: string, raw: Object) {
 
 @Component
 struct AgencyPage {
+  private handleBack(): void {
+    if (this.editorVisible) { this.closeLocalEditor(); return; }
+
+    this.leaveGuard.request(this.language, false, false, () => AppRouter.back());
+  }
+
+  private editStamp: string = '';
+  private localStamp(): string { return JSON.stringify([this.editingId, this.retailPrice, this.markupRatio]); }
+  private closeLocalEditor(): void {
+    this.leaveGuard.request(this.language, this.saving || Boolean(this.actionId), this.localStamp() !== this.editStamp, () => { this.editorVisible = false; });
+  }
+
+  private leaveGuard: LeaveGuard = new LeaveGuard();
+  aboutToDisappear(): void { this.pageScope.dispose(); }
+
+  private pageScope: PageReadScope = new PageReadScope();
+  @State private pageError: string = '';
+
   params: IdRouteParams = new IdRouteParams();
   @StorageProp('safeBottom') private safeBottom: number = 16;
   @StorageLink('language') private language: string = 'zh-CN';
@@ -35,24 +57,31 @@ struct AgencyPage {
   @State private showSuccessHint: boolean = false;
   private requestEpoch: number = 0;
 
-  aboutToAppear(): void {
+  aboutToAppear(): void { this.pageScope.open();
     this.showSuccessHint = this.params.action === 'applied';
     this.load();
   }
 
   private async load(): Promise<void> {
+    if (!this.pageScope.isCurrent()) return;
+    const pageTicket = this.pageScope.begin('load');
+    this.pageError = '';
     const epoch = ++this.requestEpoch;
     this.loading = true;
     try {
-      const result = await BusinessRepository.agencyApplications('');
+      const result = await this.pageScope.read(BusinessRepository.agencyApplications(''), pageTicket);
       if (epoch === this.requestEpoch) this.items = result;
     } catch (error) {
+      if (!this.pageScope.accepts(pageTicket)) return;
+      this.pageError = SecondaryPolicy.errorKind(error);
       if (epoch === this.requestEpoch) {
-        IBestToast.show(error instanceof ApiError ? error.message : I18n.text(this.language, '代理商品加载失败', 'Failed to load agency products'));
+        IBestToast.show(error instanceof ApiError ? SecondaryPolicy.message(error, this.language) : I18n.text(this.language, '代理商品加载失败', 'Failed to load agency products'));
       }
     } finally {
+      if (this.pageScope.accepts(pageTicket)) {
       if (epoch === this.requestEpoch) this.loading = false;
     }
+    }
   }
 
   private visibleItems(): AgencyApplication[] {
@@ -85,7 +114,7 @@ struct AgencyPage {
     this.factoryPrice = item.factoryPrice;
     this.retailPrice = item.myRetailPrice.toFixed(2);
     this.markupRatio = item.markupRatio.toFixed(2);
-    this.editorVisible = true;
+    this.editStamp = this.localStamp(); this.editorVisible = true;
   }
 
   private syncFromPrice(): void {
@@ -103,6 +132,7 @@ struct AgencyPage {
   }
 
   private async save(): Promise<void> {
+    if (!this.pageScope.isCurrent()) return;
     if (this.saving) return;
     const price = Number(this.retailPrice);
     const ratio = Number(this.markupRatio);
@@ -112,16 +142,20 @@ struct AgencyPage {
     }
     const body: AgencyMutation = { myRetailPrice: price, markupRatio: ratio };
     this.saving = true;
+    const mutationTicket = this.pageScope.begin('save');
     try {
-      await BusinessRepository.updateAgencyApplication(this.editingId, body);
+      await this.pageScope.read(BusinessRepository.updateAgencyApplication(this.editingId, body), mutationTicket);
       this.editorVisible = false;
       IBestToast.show(I18n.text(this.language, '代理售价已更新', 'Agency price updated'));
-      await this.load();
+      await this.pageScope.read(this.load(), mutationTicket);
     } catch (error) {
-      IBestToast.show(error instanceof ApiError ? error.message : I18n.text(this.language, '代理售价保存失败', 'Failed to save agency price'));
+      if (!this.pageScope.accepts(mutationTicket)) return;
+      IBestToast.show(error instanceof ApiError ? SecondaryPolicy.message(error, this.language) : I18n.text(this.language, '代理售价保存失败', 'Failed to save agency price'));
     } finally {
+      if (this.pageScope.accepts(mutationTicket)) {
       this.saving = false;
     }
+    }
   }
 
   private confirmCancel(item: AgencyApplication): void {
@@ -139,17 +173,22 @@ struct AgencyPage {
   }
 
   private async cancel(id: string): Promise<void> {
+    if (!this.pageScope.isCurrent()) return;
     if (this.actionId || this.saving) return;
     this.actionId = id;
+    const mutationTicket = this.pageScope.begin('cancel');
     try {
-      await BusinessRepository.cancelAgencyApplication(id);
+      await this.pageScope.read(BusinessRepository.cancelAgencyApplication(id), mutationTicket);
       IBestToast.show(I18n.text(this.language, '代理关系已撤销', 'Agency relationship removed'));
-      await this.load();
+      await this.pageScope.read(this.load(), mutationTicket);
     } catch (error) {
-      IBestToast.show(error instanceof ApiError ? error.message : I18n.text(this.language, '撤销失败', 'Failed to withdraw'));
+      if (!this.pageScope.accepts(mutationTicket)) return;
+      IBestToast.show(error instanceof ApiError ? SecondaryPolicy.message(error, this.language) : I18n.text(this.language, '撤销失败', 'Failed to withdraw'));
     } finally {
+      if (this.pageScope.accepts(mutationTicket)) {
       this.actionId = '';
     }
+    }
   }
 
   private confirmOffline(item: AgencyApplication): void {
@@ -162,24 +201,29 @@ struct AgencyPage {
   }
 
   private async changeStatus(id: string, status: string): Promise<void> {
+    if (!this.pageScope.isCurrent()) return;
     if (this.actionId || this.saving) return;
     this.actionId = id;
+    const mutationTicket = this.pageScope.begin('changeStatus');
     try {
-      await BusinessRepository.updateAgencyApplication(id, { status });
+      await this.pageScope.read(BusinessRepository.updateAgencyApplication(id, { status }), mutationTicket);
       IBestToast.show(status === 'approved' ? I18n.text(this.language, '已重新上架', 'Published again') : I18n.text(this.language, '已下架', 'Unpublished'));
-      await this.load();
+      await this.pageScope.read(this.load(), mutationTicket);
     } catch (error) {
-      IBestToast.show(error instanceof ApiError ? error.message : I18n.text(this.language, '操作失败', 'Operation failed'));
+      if (!this.pageScope.accepts(mutationTicket)) return;
+      IBestToast.show(error instanceof ApiError ? SecondaryPolicy.message(error, this.language) : I18n.text(this.language, '操作失败', 'Operation failed'));
     } finally {
+      if (this.pageScope.accepts(mutationTicket)) {
       this.actionId = '';
     }
+    }
   }
 
   @Builder
   private card(item: AgencyApplication) {
     Column({ space: 10 }) {
       Row({ space: 11 }) {
-        Image(item.productImage).width(74).height(74).borderRadius(10).objectFit(ImageFit.Cover);
+        ResilientRemoteImage({ originalUrl: item.productImage, widthValue: 74, heightValue: 74, radius: 10 });
         Column({ space: 5 }) {
           Row() {
             Text(item.productName).fontSize(15).fontWeight(FontWeight.Medium).layoutWeight(1).maxLines(1);
@@ -191,43 +235,44 @@ struct AgencyPage {
             .fontSize(12).fontColor(DesignTokens.muted(this.darkMode));
           Text(I18n.text(this.language, `加价 ${item.markupRatio.toFixed(2)}% · 同步 ${item.syncStatus || '--'}`,
             `Markup ${item.markupRatio.toFixed(2)}% · Sync ${item.syncStatus || '--'}`))
-            .fontSize(11).fontColor(DesignTokens.muted(this.darkMode));
+            .fontSize(12).fontColor(DesignTokens.muted(this.darkMode));
         }.layoutWeight(1).alignItems(HorizontalAlign.Start);
       }.width('100%');
       Row() {
         Text(I18n.text(this.language, `申请于 ${item.appliedAt ? item.appliedAt.slice(0, 10) : '--'}`,
           `Applied ${item.appliedAt ? item.appliedAt.slice(0, 10) : '--'}`))
-          .fontSize(11).fontColor(DesignTokens.muted(this.darkMode)).layoutWeight(1);
+          .fontSize(12).fontColor(DesignTokens.muted(this.darkMode)).layoutWeight(1);
         if (item.status === 'pending' || item.status === 'approved') {
-          IBestButton({ text: I18n.text(this.language, '调整售价', 'Edit price'), plain: true, color: DesignTokens.brand(this.darkMode), onBtnClick: () => this.openEditor(item) });
+          IBestButton({ btnHeight: 44, text: I18n.text(this.language, '调整售价', 'Edit price'), plain: true, color: DesignTokens.brand(this.darkMode), onBtnClick: () => this.openEditor(item) });
         }
-        if (item.status === 'pending') IBestButton({ text: I18n.text(this.language, '取消申请', 'Withdraw'), plain: true, type: 'danger', onBtnClick: () => this.confirmCancel(item) });
+        if (item.status === 'pending') IBestButton({ btnHeight: 44, text: I18n.text(this.language, '取消申请', 'Withdraw'), plain: true, type: 'danger', onBtnClick: () => this.confirmCancel(item) });
         else if (item.status === 'approved') {
-          IBestButton({ text: I18n.text(this.language, '下架', 'Unpublish'), plain: true, type: 'danger', onBtnClick: () => this.confirmOffline(item) });
-          IBestButton({ text: I18n.text(this.language, '代理设置', 'Agency settings'), type: 'primary', color: DesignTokens.brand(this.darkMode), fontColor: DesignTokens.onBrand(this.darkMode), onBtnClick: () => this.openEditor(item) });
+          IBestButton({ btnHeight: 44, text: I18n.text(this.language, '下架', 'Unpublish'), plain: true, type: 'danger', onBtnClick: () => this.confirmOffline(item) });
+          IBestButton({ btnHeight: 44, text: I18n.text(this.language, '代理设置', 'Agency settings'), type: 'primary', color: DesignTokens.brand(this.darkMode), fontColor: DesignTokens.onBrand(this.darkMode), onBtnClick: () => this.openEditor(item) });
         } else if (item.status === 'offline') {
-          IBestButton({ text: I18n.text(this.language, '删除', 'Delete'), plain: true, type: 'danger', onBtnClick: () => this.confirmCancel(item) });
-          IBestButton({ text: I18n.text(this.language, '重新上架', 'Publish again'), type: 'primary', color: DesignTokens.brand(this.darkMode), fontColor: DesignTokens.onBrand(this.darkMode), onBtnClick: () => this.changeStatus(item.id, 'approved') });
+          IBestButton({ btnHeight: 44, text: I18n.text(this.language, '删除', 'Delete'), plain: true, type: 'danger', onBtnClick: () => this.confirmCancel(item) });
+          IBestButton({ btnHeight: 44, text: I18n.text(this.language, '重新上架', 'Publish again'), type: 'primary', color: DesignTokens.brand(this.darkMode), fontColor: DesignTokens.onBrand(this.darkMode), onBtnClick: () => this.changeStatus(item.id, 'approved') });
         } else if (item.status === 'rejected') {
-          IBestButton({ text: I18n.text(this.language, '删除', 'Delete'), plain: true, type: 'danger', onBtnClick: () => this.confirmCancel(item) });
-          IBestButton({ text: I18n.text(this.language, '查看厂家', 'View factory'), type: 'primary', color: DesignTokens.brand(this.darkMode), fontColor: DesignTokens.onBrand(this.darkMode), onBtnClick: () => AppRouter.push('FactoryDetail', new IdRouteParams(item.factoryId)) });
+          IBestButton({ btnHeight: 44, text: I18n.text(this.language, '删除', 'Delete'), plain: true, type: 'danger', onBtnClick: () => this.confirmCancel(item) });
+          IBestButton({ btnHeight: 44, text: I18n.text(this.language, '查看厂家', 'View factory'), type: 'primary', color: DesignTokens.brand(this.darkMode), fontColor: DesignTokens.onBrand(this.darkMode), onBtnClick: () => AppRouter.push('FactoryDetail', new IdRouteParams(item.factoryId)) });
         }
       }.width('100%');
-    }.width('100%').padding(14).backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(DesignTokens.RADIUS_LARGE);
+    }.width('100%').padding(14).backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(16);
   }
 
   build() {
     NavDestination() {
       Stack({ alignContent: Alignment.Bottom }) {
         Column() {
-          IBestNavBar({ title: I18n.text(this.language, '代理商品', 'Agency products'), isShowStatusBar: true, navBarBgColor: DesignTokens.surface(this.darkMode), onLeftClick: () => AppRouter.back() });
+          SecondaryHeader({ title: I18n.text(this.language, '代理商品', 'Agency products'), isShowStatusBar: true, navBarBgColor: DesignTokens.surface(this.darkMode), onLeftClick: () => this.handleBack() });
+          SecondaryFeedback({ errorKind: this.pageError, updating: this.loading, onRetry: () => this.load() });
           if (this.showSuccessHint) {
             Row({ space: 10 }) {
               MerchantIcon({ name: 'success', color: Color.White, iconSize: 18 });
               Column({ space: 2 }) {
                 Text(I18n.text(this.language, '代理申请已提交', 'Agency request submitted')).fontSize(14).fontWeight(FontWeight.Bold).fontColor(Color.White);
                 Text(I18n.text(this.language, '厂家审核通过后即可配置并上架商品', 'Configure and publish products after factory approval'))
-                  .fontSize(11).fontColor('#F4FFF0');
+                  .fontSize(12).fontColor('#F4FFF0');
               }.layoutWeight(1).alignItems(HorizontalAlign.Start);
               Column() { MerchantIcon({ name: 'cross', color: Color.White, iconSize: 17 }); }
                 .width(32).height(32).justifyContent(FlexAlign.Center).onClick(() => this.showSuccessHint = false);
@@ -239,7 +284,7 @@ struct AgencyPage {
             this.statCell(`${this.countStatus('approved')}`, I18n.text(this.language, '已通过', 'Approved'));
             this.statCell(`${this.factoryCount()}`, I18n.text(this.language, '合作厂家', 'Factories'));
           }.margin({ left: 16, right: 16, top: 10 }).padding({ top: 12, bottom: 12 })
-            .backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(DesignTokens.RADIUS_LARGE).width('auto');
+            .backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(16).width('auto');
           Scroll() {
             Row({ space: 8 }) {
               ForEach(['', 'pending', 'approved', 'rejected', 'offline'], (value: string) => {
@@ -259,7 +304,7 @@ struct AgencyPage {
               IBestEmpty({ description: this.status
                 ? I18n.text(this.language, '当前状态暂无代理商品', 'No agency products in this status')
                 : I18n.text(this.language, '暂无代理商品', 'No agency products') });
-              IBestButton({ text: I18n.text(this.language, '去选品广场', 'Browse sourcing plaza'), type: 'primary', color: DesignTokens.brand(this.darkMode), fontColor: DesignTokens.onBrand(this.darkMode), onBtnClick: () => AppRouter.push('Plaza') });
+              IBestButton({ btnHeight: 44, text: I18n.text(this.language, '去选品广场', 'Browse sourcing plaza'), type: 'primary', color: DesignTokens.brand(this.darkMode), fontColor: DesignTokens.onBrand(this.darkMode), onBtnClick: () => AppRouter.push('Plaza') });
             }.layoutWeight(1).justifyContent(FlexAlign.Center);
           } else {
             List({ space: 10 }) {
@@ -283,24 +328,24 @@ struct AgencyPage {
               onChange: () => this.syncFromRatio()
             });
             Row({ space: 10 }) {
-              IBestButton({ text: I18n.text(this.language, '取消', 'Cancel'), plain: true, btnWidth: '48%', disabled: this.saving, onBtnClick: () => { this.editorVisible = false; } });
-              IBestButton({
+              IBestButton({ btnHeight: 44, text: I18n.text(this.language, '取消', 'Cancel'), plain: true, btnWidth: '48%', disabled: this.saving, onBtnClick: () => { this.closeLocalEditor(); } });
+              IBestButton({ btnHeight: 44,
                 text: I18n.text(this.language, '保存', 'Save'), type: 'primary', color: DesignTokens.brand(this.darkMode), fontColor: DesignTokens.onBrand(this.darkMode), btnWidth: '48%', loading: this.saving, disabled: this.saving,
                 onBtnClick: () => this.save()
               });
             }.width('100%');
-          }.width('100%').padding({ left: 20, right: 20, top: 22, bottom: this.safeBottom + 22 })
+          }.width('100%').padding({ left: 16, right: 16, top: 22, bottom: this.safeBottom + 22 })
             .backgroundColor(DesignTokens.card(this.darkMode)).borderRadius({ topLeft: 24, topRight: 24 });
         }
       }.width('100%').height('100%');
-    }.hideTitleBar(true);
+    }.hideTitleBar(true).onBackPressed((): boolean => { this.handleBack(); return true; });
   }
 
   @Builder
   private statCell(value: string, label: string) {
     Column({ space: 3 }) {
       Text(value).fontSize(19).fontWeight(FontWeight.Bold).fontColor(DesignTokens.text(this.darkMode));
-      Text(label).fontSize(10).fontColor(DesignTokens.muted(this.darkMode));
+      Text(label).fontSize(12).fontColor(DesignTokens.muted(this.darkMode));
     }.layoutWeight(1);
   }
 }
diff --git a/native/harmony-merchant/entry/src/main/ets/features/plaza/FactoryPage.ets b/native/harmony-merchant/entry/src/main/ets/features/plaza/FactoryPage.ets
index 18217646..db60fa44 100644
--- a/native/harmony-merchant/entry/src/main/ets/features/plaza/FactoryPage.ets
+++ b/native/harmony-merchant/entry/src/main/ets/features/plaza/FactoryPage.ets
@@ -1,6 +1,9 @@
+import { LeaveGuard } from '../../core/ui/LeaveGuard';
+import { PageReadScope, SecondaryPolicy } from '../../core/ui/SecondaryPolicy';
+import { SecondaryFeedback, SecondaryHeader } from '../../core/ui/SecondaryPage';
 import { common } from '@kit.AbilityKit';
 import {
-  IBestButton, IBestField, IBestImagePreview, IBestNavBar, IBestSwitch, IBestTag, IBestToast
+  IBestButton, IBestField, IBestImagePreview, IBestSwitch, IBestTag, IBestToast
 } from '@ibestservices/ibest-ui';
 import { PhoneService } from '../../core/device/PhoneService';
 import { AppRouter } from '../../core/navigation/AppRouter';
@@ -19,6 +22,22 @@ export function FactoryDetailBuilder(_: string, raw: Object) {
 
 @Component
 struct FactoryPage {
+  private handleBack(): void {
+    if (this.showAgency) { this.closeLocalEditor(); return; }
+
+    this.leaveGuard.request(this.language, false, false, () => AppRouter.back());
+  }
+
+  private editStamp: string = '';
+  private localStamp(): string { return JSON.stringify([this.markupText, this.message, this.autoSync]); }
+  private closeLocalEditor(): void {
+    this.leaveGuard.request(this.language, this.applying, this.localStamp() !== this.editStamp, () => { this.showAgency = false; });
+  }
+
+  private leaveGuard: LeaveGuard = new LeaveGuard();
+  private pageScope: PageReadScope = new PageReadScope();
+  @State private pageError: string = '';
+
   params: IdRouteParams = new IdRouteParams();
   @StorageProp('safeBottom') private safeBottom: number = 16;
   @StorageLink('language') private language: string = 'zh-CN';
@@ -36,25 +55,32 @@ struct FactoryPage {
   @State private ratingSubmitting: boolean = false;
   private routeTimer: number = -1;
 
-  aboutToAppear(): void { this.load(); }
+  aboutToAppear(): void { this.pageScope.open(); this.load(); }
 
-  aboutToDisappear(): void {
+  aboutToDisappear(): void { this.pageScope.dispose();
     if (this.routeTimer >= 0) clearTimeout(this.routeTimer);
     this.routeTimer = -1;
   }
 
   private async load(): Promise<void> {
+    if (!this.pageScope.isCurrent()) return;
+    const pageTicket = this.pageScope.begin('load');
+    this.pageError = '';
     this.loading = true;
     try {
-      const detail = await BusinessRepository.plazaFactory(this.params.id);
-      const productPage = await BusinessRepository.plazaProducts('', 1, this.params.id);
+      const detail = await this.pageScope.read(BusinessRepository.plazaFactory(this.params.id), pageTicket);
+      const productPage = await this.pageScope.read(BusinessRepository.plazaProducts('', 1, this.params.id), pageTicket);
       this.data = detail;
       this.products = productPage.list;
     } catch (error) {
-      IBestToast.show(error instanceof ApiError ? error.message : I18n.text(this.language, '厂家加载失败', 'Failed to load factory'));
+      if (!this.pageScope.accepts(pageTicket)) return;
+      this.pageError = SecondaryPolicy.errorKind(error);
+      IBestToast.show(error instanceof ApiError ? SecondaryPolicy.message(error, this.language) : I18n.text(this.language, '厂家加载失败', 'Failed to load factory'));
     } finally {
+      if (this.pageScope.accepts(pageTicket)) {
       this.loading = false;
     }
+    }
   }
 
   private contactName(): string {
@@ -74,36 +100,46 @@ struct FactoryPage {
   }
 
   private async follow(): Promise<void> {
+    if (!this.pageScope.isCurrent()) return;
     if (!this.data || this.following) return;
     const next = !this.data.followed;
     this.following = true;
+    const mutationTicket = this.pageScope.begin('follow');
     try {
-      await BusinessRepository.followFactory(this.data.id, next);
+      await this.pageScope.read(BusinessRepository.followFactory(this.data.id, next), mutationTicket);
       this.data.followed = next;
       this.data = this.data;
       IBestToast.show(next ? I18n.text(this.language, '已关注厂家', 'Factory followed') : I18n.text(this.language, '已取消关注', 'Factory unfollowed'));
     } catch (error) {
-      IBestToast.show(error instanceof ApiError ? error.message : I18n.text(this.language, '操作失败', 'Operation failed'));
+      if (!this.pageScope.accepts(mutationTicket)) return;
+      IBestToast.show(error instanceof ApiError ? SecondaryPolicy.message(error, this.language) : I18n.text(this.language, '操作失败', 'Operation failed'));
     } finally {
+      if (this.pageScope.accepts(mutationTicket)) {
       this.following = false;
     }
+    }
   }
 
   private async rate(score: number): Promise<void> {
+    if (!this.pageScope.isCurrent()) return;
     if (!this.data || this.rating > 0 || this.ratingSubmitting) return;
     this.ratingSubmitting = true;
+    const mutationTicket = this.pageScope.begin('rate');
     try {
-      const result = await BusinessRepository.rateFactory(this.data.id, score);
+      const result = await this.pageScope.read(BusinessRepository.rateFactory(this.data.id, score), mutationTicket);
       this.data.rating = result.rating;
       this.data.ratingCount = result.ratingCount;
       this.data = this.data;
       this.rating = score;
       IBestToast.show(I18n.text(this.language, '评价已提交', 'Rating submitted'));
     } catch (error) {
-      IBestToast.show(error instanceof ApiError ? error.message : I18n.text(this.language, '评价失败', 'Failed to submit rating'));
+      if (!this.pageScope.accepts(mutationTicket)) return;
+      IBestToast.show(error instanceof ApiError ? SecondaryPolicy.message(error, this.language) : I18n.text(this.language, '评价失败', 'Failed to submit rating'));
     } finally {
+      if (this.pageScope.accepts(mutationTicket)) {
       this.ratingSubmitting = false;
     }
+    }
   }
 
   private async callFactory(): Promise<void> {
@@ -115,7 +151,7 @@ struct FactoryPage {
     try {
       await PhoneService.dial(this.getUIContext().getHostContext() as common.UIAbilityContext, phone);
     } catch (error) {
-      IBestToast.show(error instanceof Error ? error.message : I18n.text(this.language, '无法打开拨号界面', 'Unable to open the dialer'));
+      IBestToast.show(error instanceof Error ? SecondaryPolicy.message(error, this.language) : I18n.text(this.language, '无法打开拨号界面', 'Unable to open the dialer'));
     }
   }
 
@@ -128,10 +164,11 @@ struct FactoryPage {
       IBestToast.show(I18n.text(this.language, '该厂家暂无可代理商品', 'This factory has no products available for agency'));
       return;
     }
-    this.showAgency = true;
+    this.editStamp = this.localStamp(); this.showAgency = true;
   }
 
   private async applyAgency(): Promise<void> {
+    if (!this.pageScope.isCurrent()) return;
     if (!this.data || this.applying) return;
     const markup = Number(this.markupText);
     if (!Number.isFinite(markup) || markup < 0 || markup > 100) {
@@ -147,8 +184,9 @@ struct FactoryPage {
       message: this.message.trim()
     };
     this.applying = true;
+    const mutationTicket = this.pageScope.begin('applyAgency');
     try {
-      await BusinessRepository.applyAgency(body);
+      await this.pageScope.read(BusinessRepository.applyAgency(body), mutationTicket);
       this.showAgency = false;
       IBestToast.show(I18n.text(this.language, `已提交 ${productIds.length} 件商品的代理申请`, `Agency request submitted for ${productIds.length} products`));
       this.routeTimer = setTimeout((): void => {
@@ -156,10 +194,13 @@ struct FactoryPage {
         AppRouter.replace('AgencyProducts', new IdRouteParams('', 'applied'));
       }, 800);
     } catch (error) {
-      IBestToast.show(error instanceof ApiError ? error.message : I18n.text(this.language, '代理申请失败', 'Agency request failed'));
+      if (!this.pageScope.accepts(mutationTicket)) return;
+      IBestToast.show(error instanceof ApiError ? SecondaryPolicy.message(error, this.language) : I18n.text(this.language, '代理申请失败', 'Agency request failed'));
     } finally {
+      if (this.pageScope.accepts(mutationTicket)) {
       this.applying = false;
     }
+    }
   }
 
   @Builder
@@ -172,7 +213,7 @@ struct FactoryPage {
           Text(I18n.text(this.language, `¥${item.startPrice.toFixed(2)} 起`, `From ¥${item.startPrice.toFixed(2)}`)).fontSize(15).fontWeight(FontWeight.Bold)
             .fontColor(DesignTokens.brand(this.darkMode));
           Blank();
-          Text(I18n.text(this.language, `${item.agencyCount} 家代理`, `${item.agencyCount} agents`)).fontSize(11).fontColor(DesignTokens.muted(this.darkMode));
+          Text(I18n.text(this.language, `${item.agencyCount} 家代理`, `${item.agencyCount} agents`)).fontSize(12).fontColor(DesignTokens.muted(this.darkMode));
         }.width('100%');
       }.layoutWeight(1).alignItems(HorizontalAlign.Start);
     }.width('100%').padding(11).backgroundColor(DesignTokens.subtle(this.darkMode)).borderRadius(12);
@@ -183,7 +224,7 @@ struct FactoryPage {
     Column({ space: 14 }) {
       Row() {
         Text(I18n.text(this.language, '申请代理', 'Apply for agency')).fontSize(20).fontWeight(FontWeight.Bold).layoutWeight(1);
-        Text(I18n.text(this.language, '关闭', 'Close')).fontSize(14).fontColor(DesignTokens.muted(this.darkMode)).onClick(() => this.showAgency = false);
+        Text(I18n.text(this.language, '关闭', 'Close')).fontSize(14).fontColor(DesignTokens.muted(this.darkMode)).onClick(() => this.closeLocalEditor());
       }.width('100%');
       Text(I18n.text(this.language, `将申请前 ${Math.min(5, this.products.length)} 件可代理商品，可在“我的代理”继续管理。`,
         `The first ${Math.min(5, this.products.length)} products will be requested. Manage them under My agency.`))
@@ -192,7 +233,7 @@ struct FactoryPage {
       Row() {
         Column({ space: 3 }) {
           Text(I18n.text(this.language, '自动同步厂家价格', 'Sync factory prices')).fontSize(14).fontWeight(FontWeight.Medium);
-          Text(I18n.text(this.language, '厂家调价时按加价比例更新', 'Apply the markup when factory prices change')).fontSize(11).fontColor(DesignTokens.muted(this.darkMode));
+          Text(I18n.text(this.language, '厂家调价时按加价比例更新', 'Apply the markup when factory prices change')).fontSize(12).fontColor(DesignTokens.muted(this.darkMode));
         }.layoutWeight(1).alignItems(HorizontalAlign.Start);
         IBestSwitch({ value: $autoSync, activeColor: DesignTokens.brand(this.darkMode) });
       }.width('100%');
@@ -202,20 +243,21 @@ struct FactoryPage {
           .height(88).width('100%').padding(10).backgroundColor(DesignTokens.subtle(this.darkMode)).borderRadius(10)
           .maxLength(300);
       }.width('100%').alignItems(HorizontalAlign.Start);
-      IBestButton({
+      IBestButton({ btnHeight: 44,
         text: I18n.text(this.language, '确认提交', 'Submit'), type: 'primary', color: DesignTokens.brand(this.darkMode), fontColor: DesignTokens.onBrand(this.darkMode), btnWidth: '100%',
         loading: this.applying, onBtnClick: () => this.applyAgency()
       });
-    }.width('100%').padding({ left: 20, right: 20, top: 18, bottom: this.safeBottom + 18 });
+    }.width('100%').padding({ left: 16, right: 16, top: 18, bottom: this.safeBottom + 18 });
   }
 
   build() {
     NavDestination() {
       Column() {
-        IBestNavBar({
+        SecondaryHeader({
           title: I18n.text(this.language, '厂家详情', 'Factory details'), isShowStatusBar: true, navBarBgColor: DesignTokens.surface(this.darkMode),
-          onLeftClick: () => AppRouter.back()
+          onLeftClick: () => this.handleBack()
         });
+          SecondaryFeedback({ errorKind: this.pageError, updating: this.loading, onRetry: () => this.load() });
         if (this.loading) {
           Column() { LoadingProgress().width(34).height(34).color(DesignTokens.brand(this.darkMode)); }
             .layoutWeight(1).justifyContent(FlexAlign.Center);
@@ -241,17 +283,17 @@ struct FactoryPage {
                     `${this.data.region} · ${this.data.years || 1} years in business`)).fontSize(12)
                     .fontColor(DesignTokens.muted(this.darkMode));
                 }.layoutWeight(1).alignItems(HorizontalAlign.Start);
-                IBestButton({
+                IBestButton({ btnHeight: 44,
                   text: this.data.followed ? I18n.text(this.language, '已关注', 'Following') : I18n.text(this.language, '关注', 'Follow'), plain: true, color: DesignTokens.brand(this.darkMode),
                   loading: this.following, disabled: this.following, onBtnClick: () => this.follow()
                 });
-              }.width('100%').padding(16).backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(DesignTokens.RADIUS_LARGE);
+              }.width('100%').padding(16).backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(16);
 
               Row() {
-                Column({ space: 3 }) { Text(`${this.data.productCount || this.products.length}`).fontSize(20).fontWeight(FontWeight.Bold); Text(I18n.text(this.language, '在售商品', 'Products')).fontSize(11).fontColor(DesignTokens.muted(this.darkMode)); }.layoutWeight(1);
-                Column({ space: 3 }) { Text(`${this.data.agencyCount || 0}`).fontSize(20).fontWeight(FontWeight.Bold); Text(I18n.text(this.language, '合作代理', 'Agents')).fontSize(11).fontColor(DesignTokens.muted(this.darkMode)); }.layoutWeight(1);
-                Column({ space: 3 }) { Text(`¥${(this.data.monthGmv || this.data.gmv || 0).toFixed(0)}`).fontSize(20).fontWeight(FontWeight.Bold); Text(I18n.text(this.language, '经营规模', 'Business volume')).fontSize(11).fontColor(DesignTokens.muted(this.darkMode)); }.layoutWeight(1);
-              }.width('100%').padding({ top: 16, bottom: 16 }).backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(DesignTokens.RADIUS_LARGE);
+                Column({ space: 3 }) { Text(`${this.data.productCount || this.products.length}`).fontSize(20).fontWeight(FontWeight.Bold); Text(I18n.text(this.language, '在售商品', 'Products')).fontSize(12).fontColor(DesignTokens.muted(this.darkMode)); }.layoutWeight(1);
+                Column({ space: 3 }) { Text(`${this.data.agencyCount || 0}`).fontSize(20).fontWeight(FontWeight.Bold); Text(I18n.text(this.language, '合作代理', 'Agents')).fontSize(12).fontColor(DesignTokens.muted(this.darkMode)); }.layoutWeight(1);
+                Column({ space: 3 }) { Text(`¥${(this.data.monthGmv || this.data.gmv || 0).toFixed(0)}`).fontSize(20).fontWeight(FontWeight.Bold); Text(I18n.text(this.language, '经营规模', 'Business volume')).fontSize(12).fontColor(DesignTokens.muted(this.darkMode)); }.layoutWeight(1);
+              }.width('100%').padding({ top: 16, bottom: 16 }).backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(16);
 
               Column({ space: 9 }) {
                 Text(I18n.text(this.language, '厂家介绍', 'About the factory')).fontSize(18).fontWeight(FontWeight.Bold).width('100%');
@@ -259,7 +301,7 @@ struct FactoryPage {
                   .lineHeight(22).width('100%');
                 Text(I18n.text(this.language, `主营：${this.data.categories.join('、') || '--'}`, `Categories: ${this.data.categories.join(', ') || '--'}`)).fontSize(13).width('100%');
                 Text(I18n.text(this.language, `地址：${this.data.address || this.data.contact?.address || '--'}`, `Address: ${this.data.address || this.data.contact?.address || '--'}`)).fontSize(13).width('100%');
-              }.width('100%').padding(16).backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(DesignTokens.RADIUS_LARGE);
+              }.width('100%').padding(16).backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(16);
 
               if ((this.data.qualifications || []).length > 0) {
                 Column({ space: 10 }) {
@@ -269,12 +311,12 @@ struct FactoryPage {
                       ForEach(this.data.qualifications || [], (item: PlazaQualification) => {
                         Column({ space: 5 }) {
                           Image(item.image).width(116).height(82).objectFit(ImageFit.Cover).borderRadius(10);
-                          Text(item.name).fontSize(11).maxLines(1).width(116).textAlign(TextAlign.Center);
+                          Text(item.name).fontSize(12).maxLines(1).width(116).textAlign(TextAlign.Center);
                         }.onClick(() => this.previewQualification(item));
                       }, (item: PlazaQualification) => item.id);
                     }
                   }.scrollable(ScrollDirection.Horizontal).scrollBar(BarState.Off).width('100%');
-                }.width('100%').padding(16).backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(DesignTokens.RADIUS_LARGE);
+                }.width('100%').padding(16).backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(16);
               }
 
               Column({ space: 10 }) {
@@ -283,13 +325,13 @@ struct FactoryPage {
                 if (this.data.contact?.workTime) {
                   Text(I18n.text(this.language, `工作时间：${this.data.contact.workTime}`, `Hours: ${this.data.contact.workTime}`)).fontSize(12).fontColor(DesignTokens.muted(this.darkMode)).width('100%');
                 }
-                IBestButton({ text: I18n.text(this.language, '联系厂家', 'Contact factory'), plain: true, color: DesignTokens.brand(this.darkMode), btnWidth: '100%', onBtnClick: () => this.callFactory() });
-              }.width('100%').padding(16).backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(DesignTokens.RADIUS_LARGE);
+                IBestButton({ btnHeight: 44, text: I18n.text(this.language, '联系厂家', 'Contact factory'), plain: true, color: DesignTokens.brand(this.darkMode), btnWidth: '100%', onBtnClick: () => this.callFactory() });
+              }.width('100%').padding(16).backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(16);
 
               Column({ space: 10 }) {
                 Row() { Text(I18n.text(this.language, '可代理商品', 'Agency products')).fontSize(18).fontWeight(FontWeight.Bold).layoutWeight(1); Text(I18n.text(this.language, `${this.products.length} 件`, `${this.products.length} products`)).fontSize(12).fontColor(DesignTokens.muted(this.darkMode)); }.width('100%');
                 ForEach(this.products.slice(0, 5), (item: PlazaProduct) => { this.productCard(item); }, (item: PlazaProduct) => item.productId);
-              }.width('100%').padding(16).backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(DesignTokens.RADIUS_LARGE);
+              }.width('100%').padding(16).backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(16);
 
               Column({ space: 10 }) {
                 Text(I18n.text(this.language, '为厂家评分', 'Rate this factory')).fontSize(18).fontWeight(FontWeight.Bold).width('100%');
@@ -304,9 +346,9 @@ struct FactoryPage {
                       .onClick(() => this.rate(score));
                   });
                 }.width('100%');
-              }.width('100%').padding(16).backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(DesignTokens.RADIUS_LARGE);
+              }.width('100%').padding(16).backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(16);
 
-              IBestButton({
+              IBestButton({ btnHeight: 44,
                 text: I18n.text(this.language, '申请代理该厂家商品', 'Apply to sell this factory\'s products'), type: 'primary', color: DesignTokens.brand(this.darkMode), fontColor: DesignTokens.onBrand(this.darkMode),
                 btnWidth: '100%', onBtnClick: () => this.openAgency()
               });
@@ -316,8 +358,8 @@ struct FactoryPage {
       }.width('100%').height('100%').backgroundColor(DesignTokens.page(this.darkMode))
         .bindSheet(this.showAgency, this.agencySheet(), {
           height: SheetSize.MEDIUM,
-          onWillDismiss: () => { this.showAgency = false; }
+          onWillDismiss: () => { this.closeLocalEditor(); }
         });
-    }.hideTitleBar(true);
+    }.hideTitleBar(true).onBackPressed((): boolean => { this.handleBack(); return true; });
   }
 }
diff --git a/native/harmony-merchant/entry/src/main/ets/features/plaza/PlazaPage.ets b/native/harmony-merchant/entry/src/main/ets/features/plaza/PlazaPage.ets
index 214df0a2..1f782ae2 100644
--- a/native/harmony-merchant/entry/src/main/ets/features/plaza/PlazaPage.ets
+++ b/native/harmony-merchant/entry/src/main/ets/features/plaza/PlazaPage.ets
@@ -1,5 +1,7 @@
+import { PageReadScope, PageSearch, SecondaryPolicy } from '../../core/ui/SecondaryPolicy';
+import { SecondaryFeedback, SecondaryHeader } from '../../core/ui/SecondaryPage';
 import {
-  IBestButton, IBestDialogUtil, IBestEmpty, IBestNavBar, IBestSearch, IBestTag, IBestToast
+  IBestButton, IBestDialogUtil, IBestEmpty, IBestSearch, IBestTag, IBestToast
 } from '@ibestservices/ibest-ui';
 import { LengthMetrics } from '@kit.ArkUI';
 import { AppRouter } from '../../core/navigation/AppRouter';
@@ -19,6 +21,21 @@ export function PlazaBuilder(_: string) { PlazaPage(); }
 
 @Component
 struct PlazaPage {
+  private pageSearch: PageSearch = new PageSearch();
+  private submitSearch(): void { this.pageSearch.cancel(); this.load(); }
+  @State private draftScope: string = 'stores';
+  @State private visibilityReady: boolean = false;
+  private visibleFilter: string = '';
+  private handleBack(): void {
+    if (this.savingVisibility || this.applyingProductId || this.ratingFactoryId) return;
+    if (this.showFilters || this.showVisibility) { this.showFilters = false; this.showVisibility = false; return; }
+    AppRouter.back();
+  }
+  aboutToDisappear(): void { this.pageSearch.cancel(); this.pageScope.dispose(); }
+
+  private pageScope: PageReadScope = new PageReadScope();
+  @State private pageError: string = '';
+
   @StorageProp('safeBottom') private safeBottom: number = 16;
   @StorageLink('language') private language: string = 'zh-CN';
   @StorageLink('darkMode') private darkMode: boolean = false;
@@ -49,16 +66,21 @@ struct PlazaPage {
   @State private savingVisibility: boolean = false;
   private requestEpoch: number = 0;
 
-  aboutToAppear(): void {
+  aboutToAppear(): void { this.pageScope.open();
     this.load();
     this.loadVisibility();
     this.loadFilterOptions();
   }
 
   private async loadFilterOptions(): Promise<void> {
+    if (!this.pageScope.isCurrent()) return;
+    const pageTicket = this.pageScope.begin('loadFilterOptions');
+    this.pageError = '';
     try {
-      this.filterOptions = await BusinessRepository.plazaFilterOptions();
+      this.filterOptions = await this.pageScope.read(BusinessRepository.plazaFilterOptions(), pageTicket);
     } catch (_) {
+      if (!this.pageScope.accepts(pageTicket)) return;
+      this.pageError = SecondaryPolicy.errorKind(_);
       this.filterOptions = {
         productTags: [
           { value: I18n.contract('🔥本周热推'), label: I18n.contract('🔥本周热推') },
@@ -85,76 +107,103 @@ struct PlazaPage {
   }
 
   private async loadVisibility(): Promise<void> {
-    try { this.plazaScope = (await BusinessRepository.plazaVisibility()).scope; } catch (_) { }
+    if (!this.pageScope.isCurrent()) return;
+    const pageTicket = this.pageScope.begin('loadVisibility');
+    this.pageError = '';
+    try { this.plazaScope = (await this.pageScope.read(BusinessRepository.plazaVisibility(), pageTicket)).scope; this.visibilityReady = true; } catch (_) {
+      if (!this.pageScope.accepts(pageTicket)) return;
+      this.pageError = SecondaryPolicy.errorKind(_); }
   }
 
   private async saveVisibility(scope: string): Promise<void> {
+    if (!this.pageScope.isCurrent()) return;
     if (scope === this.plazaScope || this.savingVisibility) return;
+    if (!this.visibilityReady) return;
     this.savingVisibility = true;
+    const mutationTicket = this.pageScope.begin('saveVisibility');
     try {
-      const result = await BusinessRepository.savePlazaVisibility(scope);
+      const result = await this.pageScope.read(BusinessRepository.savePlazaVisibility(scope), mutationTicket);
       this.plazaScope = result.scope;
       this.showVisibility = false;
       IBestToast.show(scope === 'public'
         ? I18n.text(this.language, '广场商品已对所有客户可见', 'Products are visible to all customers')
         : I18n.text(this.language, '广场商品仅授权门店可见', 'Products are visible only to authorised stores'));
     } catch (error) {
-      IBestToast.show(error instanceof ApiError ? error.message
+      if (!this.pageScope.accepts(mutationTicket)) return;
+      IBestToast.show(error instanceof ApiError ? SecondaryPolicy.message(error, this.language)
         : I18n.text(this.language, '可见范围保存失败', 'Failed to save visibility'));
     } finally {
+      if (this.pageScope.accepts(mutationTicket)) {
       this.savingVisibility = false;
     }
+    }
   }
 
   private async load(): Promise<void> {
+    if (!this.pageScope.isCurrent()) return;
+    const pageTicket = this.pageScope.begin('load');
+    this.pageError = '';
     const epoch = ++this.requestEpoch;
     const requestedMode = this.mode;
+    const condition = JSON.stringify([this.mode, this.keyword.trim(), this.region, this.category, this.minRating, this.activeTag]);
+    if (condition !== this.visibleFilter) { this.products = []; this.factories = []; this.total = 0; this.page = 1; }
+    this.visibleFilter = condition;
     this.loading = true;
-    this.page = 1;
     try {
       if (requestedMode === 'products') {
-        const result = await BusinessRepository.plazaProducts(this.keyword.trim(), 1, '', this.activeTag);
+        const result = await this.pageScope.read(BusinessRepository.plazaProducts(this.keyword.trim(), 1, '', this.activeTag), pageTicket);
         if (epoch !== this.requestEpoch) return;
-        this.products = result.list;
+        this.products = result.list; this.page = 1;
         this.total = result.total;
       } else {
-        const result = await BusinessRepository.plazaFactories(
+        const result = await this.pageScope.read(BusinessRepository.plazaFactories(
           this.keyword.trim(), this.region.trim(), this.category.trim(), this.minRating
-        );
+        ), pageTicket);
         if (epoch !== this.requestEpoch) return;
         this.factories = result;
       }
     } catch (error) {
+      if (!this.pageScope.accepts(pageTicket)) return;
+      this.pageError = SecondaryPolicy.errorKind(error);
       if (epoch === this.requestEpoch) {
-        IBestToast.show(error instanceof ApiError ? error.message
+        IBestToast.show(error instanceof ApiError ? SecondaryPolicy.message(error, this.language)
           : I18n.text(this.language, '选品数据加载失败', 'Failed to load sourcing data'));
       }
     } finally {
+      if (this.pageScope.accepts(pageTicket)) {
       if (epoch === this.requestEpoch) this.loading = false;
     }
+    }
   }
 
   private async loadMore(): Promise<void> {
+    if (!this.pageScope.isCurrent()) return;
     if (this.mode !== 'products' || this.loadingMore || this.products.length >= this.total) return;
+    const pageTicket = this.pageScope.begin('loadMore');
+    this.pageError = '';
     const epoch = this.requestEpoch;
     this.loadingMore = true;
     try {
       const next = this.page + 1;
-      const result: PageResult<PlazaProduct> = await BusinessRepository.plazaProducts(
+      const result: PageResult<PlazaProduct> = await this.pageScope.read(BusinessRepository.plazaProducts(
         this.keyword.trim(), next, '', this.activeTag
-      );
+      ), pageTicket);
       if (epoch !== this.requestEpoch) return;
       this.products = this.products.concat(result.list);
       this.page = next;
       this.total = result.total;
     } catch (error) {
+      if (!this.pageScope.accepts(pageTicket)) return;
+      this.pageError = SecondaryPolicy.errorKind(error);
       if (epoch === this.requestEpoch) {
-        IBestToast.show(error instanceof ApiError ? error.message
+        IBestToast.show(error instanceof ApiError ? SecondaryPolicy.message(error, this.language)
           : I18n.text(this.language, '加载更多失败', 'Failed to load more'));
       }
     } finally {
+      if (this.pageScope.accepts(pageTicket)) {
       this.loadingMore = false;
     }
+    }
   }
 
   private switchMode(next: string): void {
@@ -184,6 +233,7 @@ struct PlazaPage {
   }
 
   private applyFilters(): void {
+    if (this.activeTag === this.draftTag && this.region === this.draftRegion && this.category === this.draftCategory && this.minRating === this.draftMinRating) { this.showFilters = false; return; }
     this.activeTag = this.draftTag;
     this.region = this.draftRegion;
     this.category = this.draftCategory;
@@ -212,6 +262,7 @@ struct PlazaPage {
   }
 
   private async apply(item: PlazaProduct): Promise<void> {
+    if (!this.pageScope.isCurrent()) return;
     if (this.applyingProductId) return;
     const body: AgencyRequest = {
       factoryId: item.factoryId,
@@ -221,32 +272,41 @@ struct PlazaPage {
       message: ''
     };
     this.applyingProductId = item.productId;
+    const mutationTicket = this.pageScope.begin('apply');
     try {
-      await BusinessRepository.applyAgency(body);
+      await this.pageScope.read(BusinessRepository.applyAgency(body), mutationTicket);
       IBestToast.show(I18n.text(this.language, '已提交，待厂家审核', 'Submitted for factory review'));
     } catch (error) {
-      IBestToast.show(error instanceof ApiError ? error.message
+      if (!this.pageScope.accepts(mutationTicket)) return;
+      IBestToast.show(error instanceof ApiError ? SecondaryPolicy.message(error, this.language)
         : I18n.text(this.language, '申请失败', 'Request failed'));
     } finally {
+      if (this.pageScope.accepts(mutationTicket)) {
       this.applyingProductId = '';
     }
+    }
   }
 
   private async rateFactory(item: PlazaFactory, score: number): Promise<void> {
+    if (!this.pageScope.isCurrent()) return;
     if (this.ratingFactoryId) return;
     this.ratingFactoryId = item.id;
+    const mutationTicket = this.pageScope.begin('rateFactory');
     try {
-      const result = await BusinessRepository.rateFactory(item.id, score);
+      const result = await this.pageScope.read(BusinessRepository.rateFactory(item.id, score), mutationTicket);
       item.rating = result.rating;
       item.ratingCount = result.ratingCount;
       this.factories = this.factories.slice();
       IBestToast.show(I18n.text(this.language, `已评价 ${score} 星`, `Rated ${score} stars`));
     } catch (error) {
-      IBestToast.show(error instanceof ApiError ? error.message
+      if (!this.pageScope.accepts(mutationTicket)) return;
+      IBestToast.show(error instanceof ApiError ? SecondaryPolicy.message(error, this.language)
         : I18n.text(this.language, '评价失败', 'Failed to submit rating'));
     } finally {
+      if (this.pageScope.accepts(mutationTicket)) {
       this.ratingFactoryId = '';
     }
+    }
   }
 
   @Builder
@@ -266,17 +326,17 @@ struct PlazaPage {
         }.width('100%');
         Text(item.factoryName).fontSize(12).fontColor(DesignTokens.muted(this.darkMode)).width('100%').maxLines(1);
         if ((item.tags || []).length > 0) {
-          Text((item.tags || []).slice(0, 2).join(' · ')).fontSize(10).fontColor(DesignTokens.brand(this.darkMode)).maxLines(1).width('100%');
+          Text((item.tags || []).slice(0, 2).join(' · ')).fontSize(12).fontColor(DesignTokens.brand(this.darkMode)).maxLines(1).width('100%');
         }
         Row() {
           Column({ space: 2 }) {
             Text(I18n.text(this.language, `¥${item.startPrice.toFixed(2)} 起`, `From ¥${item.startPrice.toFixed(2)}`))
               .fontSize(16).fontWeight(FontWeight.Bold).fontColor(DesignTokens.brand(this.darkMode));
             Text(I18n.text(this.language, `${item.agencyCount} 位商家代理`, `${item.agencyCount} agents`))
-              .fontSize(10).fontColor(DesignTokens.muted(this.darkMode));
+              .fontSize(12).fontColor(DesignTokens.muted(this.darkMode));
           }.alignItems(HorizontalAlign.Start);
           Blank();
-          IBestButton({
+          IBestButton({ btnHeight: 44,
             text: this.applyingProductId === item.productId
               ? I18n.text(this.language, '提交中', 'Submitting') : I18n.text(this.language, '申请代理', 'Apply'),
             type: 'primary', color: DesignTokens.brand(this.darkMode), fontColor: DesignTokens.onBrand(this.darkMode), buttonSize: 'small',
@@ -286,7 +346,7 @@ struct PlazaPage {
         }.width('100%');
       }.layoutWeight(1).alignItems(HorizontalAlign.Start);
     }.width('100%').padding(13).backgroundColor(DesignTokens.card(this.darkMode))
-      .borderRadius(DesignTokens.RADIUS_LARGE).border({ width: 0.5, color: DesignTokens.border(this.darkMode) });
+      .borderRadius(16).border({ width: 0.5, color: DesignTokens.border(this.darkMode) });
   }
 
   @Builder
@@ -308,7 +368,7 @@ struct PlazaPage {
         Row() {
           Text(I18n.text(this.language, `${item.productCount || 0} 商品 · ${item.agencyCount || 0} 代理`,
             `${item.productCount || 0} products · ${item.agencyCount || 0} agents`))
-            .fontSize(11).fontColor(DesignTokens.muted(this.darkMode));
+            .fontSize(12).fontColor(DesignTokens.muted(this.darkMode));
           Blank();
           Row({ space: 3 }) {
             ForEach([1, 2, 3, 4, 5], (score: number) => {
@@ -321,7 +381,7 @@ struct PlazaPage {
       }.layoutWeight(1).alignItems(HorizontalAlign.Start)
         .onClick(() => AppRouter.push('FactoryDetail', new IdRouteParams(item.id)));
     }.width('100%').padding(13).backgroundColor(DesignTokens.card(this.darkMode))
-      .borderRadius(DesignTokens.RADIUS_LARGE).border({ width: 0.5, color: DesignTokens.border(this.darkMode) });
+      .borderRadius(16).border({ width: 0.5, color: DesignTokens.border(this.darkMode) });
   }
 
   @Builder
@@ -329,7 +389,7 @@ struct PlazaPage {
     Text(this.optionLabel(option)).fontSize(12).fontColor(selected ? DesignTokens.onBrand(this.darkMode) : DesignTokens.text(this.darkMode))
       .padding({ left: 13, right: 13, top: 8, bottom: 8 })
       .backgroundColor(selected ? DesignTokens.brand(this.darkMode) : DesignTokens.subtle(this.darkMode))
-      .borderRadius(17).onClick(onSelect);
+      .constraintSize({ minHeight: 44 }).borderRadius(10).onClick(onSelect);
   }
 
   @Builder
@@ -369,7 +429,7 @@ struct PlazaPage {
         this.optionGroup(I18n.text(this.language, '最低评分', 'Minimum rating'), this.filterOptions.ratingOptions,
           `${this.draftMinRating}`, (value: string): void => { this.draftMinRating = Number(value || '0'); });
       }
-      IBestButton({
+      IBestButton({ btnHeight: 44,
         text: I18n.text(this.language, '查看结果', 'View results'), type: 'primary', color: DesignTokens.brand(this.darkMode), fontColor: DesignTokens.onBrand(this.darkMode),
         btnWidth: '100%', onBtnClick: () => this.applyFilters()
       });
@@ -390,19 +450,23 @@ struct PlazaPage {
       Text(I18n.text(this.language, '该设置独立于货源筛选，仅影响其他客户查看你的商品。',
         'This only controls who can see your products.'))
         .fontSize(12).fontColor(DesignTokens.muted(this.darkMode)).width('100%');
-      IBestButton({
+      IBestButton({ btnHeight: 44,
         text: I18n.text(this.language, '仅授权门店', 'Authorised stores'),
-        type: this.plazaScope === 'stores' ? 'primary' : 'default', color: DesignTokens.brand(this.darkMode), fontColor: DesignTokens.onBrand(this.darkMode),
-        plain: this.plazaScope !== 'stores', btnWidth: '100%', loading: this.savingVisibility,
-        onBtnClick: () => this.saveVisibility('stores')
+        type: this.draftScope === 'stores' ? 'primary' : 'default', color: DesignTokens.brand(this.darkMode), fontColor: DesignTokens.onBrand(this.darkMode),
+        plain: this.draftScope !== 'stores', btnWidth: '100%', loading: this.savingVisibility,
+        onBtnClick: () => { this.draftScope = 'stores'; }
       });
-      IBestButton({
+      IBestButton({ btnHeight: 44,
         text: I18n.text(this.language, '所有客户', 'All customers'),
-        type: this.plazaScope === 'public' ? 'primary' : 'default', color: DesignTokens.brand(this.darkMode), fontColor: DesignTokens.onBrand(this.darkMode),
-        plain: this.plazaScope !== 'public', btnWidth: '100%', loading: this.savingVisibility,
-        onBtnClick: () => this.saveVisibility('public')
+        type: this.draftScope === 'public' ? 'primary' : 'default', color: DesignTokens.brand(this.darkMode), fontColor: DesignTokens.onBrand(this.darkMode),
+        plain: this.draftScope !== 'public', btnWidth: '100%', loading: this.savingVisibility,
+        onBtnClick: () => { this.draftScope = 'public'; }
       });
-    }.width('100%').padding(18).backgroundColor(DesignTokens.surface(this.darkMode))
+      IBestButton({ btnHeight: 48, text: I18n.text(this.language, '保存可见范围', 'Save visibility'),
+        type: 'primary', color: DesignTokens.brand(this.darkMode), fontColor: DesignTokens.onBrand(this.darkMode),
+        btnWidth: '100%', loading: this.savingVisibility, disabled: !this.visibilityReady || this.savingVisibility,
+        onBtnClick: () => { if (this.draftScope === this.plazaScope) this.showVisibility = false; else this.saveVisibility(this.draftScope); } });
+    }.width('100%').padding(16).backgroundColor(DesignTokens.surface(this.darkMode))
       .borderRadius({ bottomLeft: 22, bottomRight: 22 })
       .shadow({ color: this.darkMode ? '#76000000' : '#24000000', radius: 20, offsetY: 8 });
   }
@@ -419,7 +483,7 @@ struct PlazaPage {
               Column().width('55%').height(12).borderRadius(6).backgroundColor(DesignTokens.subtle(this.darkMode));
               Column().width('70%').height(18).borderRadius(8).backgroundColor(DesignTokens.subtle(this.darkMode));
             }.layoutWeight(1).alignItems(HorizontalAlign.Start);
-          }.width('100%').padding(13).backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(DesignTokens.RADIUS_LARGE);
+          }.width('100%').padding(13).backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(16);
         });
       }.width('100%').padding({ left: 16, right: 16, top: 8 });
     } else if ((this.mode === 'products' && this.products.length === 0) ||
@@ -468,16 +532,16 @@ struct PlazaPage {
           .onClick(() => this.showFilters ? this.showFilters = false : this.openFilters());
       }.width('100%');
       Row({ space: 10 }) {
-        IBestButton({
+        IBestButton({ btnHeight: 44,
           text: I18n.text(this.language, '发布我的商品', 'Publish product'), plain: true,
           color: DesignTokens.brand(this.darkMode), btnWidth: '48%',
           onBtnClick: () => AppRouter.push('ProductEdit', new IdRouteParams())
         });
-        IBestButton({
+        IBestButton({ btnHeight: 44,
           text: I18n.text(this.language, '可见范围', 'Visibility'), plain: true,
           color: DesignTokens.brand(this.darkMode), btnWidth: '48%', onBtnClick: () => {
             this.showFilters = false;
-            this.showVisibility = true;
+            this.draftScope = this.plazaScope; this.showVisibility = true;
           }
         });
       }.width('100%');
@@ -487,17 +551,18 @@ struct PlazaPage {
   build() {
     NavDestination() {
       Column() {
-        IBestNavBar({
+        SecondaryHeader({
           title: I18n.text(this.language, '厂家选品', 'Factory sourcing'), isShowRight: true,
           rightLabel: I18n.text(this.language, '我的代理', 'My agency'), isShowStatusBar: true,
-          navBarBgColor: DesignTokens.surface(this.darkMode), onLeftClick: () => AppRouter.back(),
+          navBarBgColor: DesignTokens.surface(this.darkMode), onLeftClick: () => this.handleBack(),
           onRightClick: () => AppRouter.push('AgencyProducts')
         });
+          SecondaryFeedback({ errorKind: this.pageError, updating: this.loading, onRetry: () => this.load() });
         IBestSearch({
           value: $keyword, placeholder: I18n.text(this.language, '搜索商品或厂家', 'Search products or factories'),
           isRound: true, outBgColor: DesignTokens.page(this.darkMode), innerBgColor: DesignTokens.card(this.darkMode),
           showRightButton: true, rightButtonText: I18n.text(this.language, '搜索', 'Search'),
-          onSearch: () => this.load(), onRightButtonClick: () => this.load()
+          onInput: () => this.pageSearch.schedule(() => this.load()), onSearch: () => this.submitSearch(), onRightButtonClick: () => this.submitSearch()
         });
         this.controlBar();
         Stack({ alignContent: Alignment.Top }) {
@@ -510,14 +575,6 @@ struct PlazaPage {
           }
         }.width('100%').layoutWeight(1);
       }.width('100%').height('100%').backgroundColor(DesignTokens.page(this.darkMode));
-    }.hideTitleBar(true).onBackPressed((): boolean => {
-      if (this.showFilters || this.showVisibility) {
-        this.showFilters = false;
-        this.showVisibility = false;
-        return true;
-      }
-      AppRouter.back();
-      return true;
-    });
+    }.hideTitleBar(true).onBackPressed((): boolean => { this.handleBack(); return true; });
   }
 }
diff --git a/native/harmony-merchant/entry/src/main/ets/features/product/CategoryPage.ets b/native/harmony-merchant/entry/src/main/ets/features/product/CategoryPage.ets
index 7363c1cc..6940d4d6 100644
--- a/native/harmony-merchant/entry/src/main/ets/features/product/CategoryPage.ets
+++ b/native/harmony-merchant/entry/src/main/ets/features/product/CategoryPage.ets
@@ -1,5 +1,8 @@
+import { LeaveGuard } from '../../core/ui/LeaveGuard';
+import { PageReadScope, SecondaryPolicy } from '../../core/ui/SecondaryPolicy';
+import { SecondaryFeedback, SecondaryHeader } from '../../core/ui/SecondaryPage';
 import {
-  IBestButton, IBestDialogUtil, IBestEmpty, IBestField, IBestNavBar, IBestTag, IBestToast
+  IBestButton, IBestDialogUtil, IBestEmpty, IBestField, IBestTag, IBestToast
 } from '@ibestservices/ibest-ui';
 import { AppRouter } from '../../core/navigation/AppRouter';
 import { ApiError } from '../../core/network/ApiClient';
@@ -13,6 +16,24 @@ export function CategoryManagerBuilder(_: string) { CategoryPage(); }
 
 @Component
 struct CategoryPage {
+  private handleBack(): void {
+    if (this.editorVisible) { this.closeLocalEditor(); return; }
+
+    this.leaveGuard.request(this.language, false, false, () => AppRouter.back());
+  }
+
+  private editStamp: string = '';
+  private localStamp(): string { return JSON.stringify([this.editingId, this.editingName, this.editingParentId]); }
+  private closeLocalEditor(): void {
+    this.leaveGuard.request(this.language, this.saving || Boolean(this.workingId), this.localStamp() !== this.editStamp, () => { this.editorVisible = false; });
+  }
+
+  private leaveGuard: LeaveGuard = new LeaveGuard();
+  aboutToDisappear(): void { this.pageScope.dispose(); }
+
+  private pageScope: PageReadScope = new PageReadScope();
+  @State private pageError: string = '';
+
   @StorageProp('safeBottom') private safeBottom: number = 16;
   @StorageLink('language') private language: string = 'zh-CN';
   @StorageLink('darkMode') private darkMode: boolean = false;
@@ -28,21 +49,27 @@ struct CategoryPage {
   @State private saving: boolean = false;
   @State private workingId: string = '';
 
-  aboutToAppear(): void { this.load(); }
+  aboutToAppear(): void { this.pageScope.open(); this.load(); }
 
   private async load(): Promise<void> {
+    const pageTicket = this.pageScope.begin('load');
+    this.pageError = '';
     this.loading = true;
     this.errorMessage = '';
     let merchantLoaded: boolean = false;
     try {
-      this.categories = await ProductRepository.merchantCategories();
+      this.categories = await this.pageScope.read(ProductRepository.merchantCategories(), pageTicket);
       merchantLoaded = true;
     } catch (error) {
-      this.errorMessage = error instanceof ApiError ? error.message : 'i18n:category-load';
+      if (!this.pageScope.accepts(pageTicket)) return;
+      this.pageError = SecondaryPolicy.errorKind(error);
+      this.errorMessage = error instanceof ApiError ? SecondaryPolicy.message(error, this.language) : 'i18n:category-load';
     }
     try {
-      this.platformCategories = await ProductRepository.platformCategories();
+      this.platformCategories = await this.pageScope.read(ProductRepository.platformCategories(), pageTicket);
     } catch (_) {
+      if (!this.pageScope.accepts(pageTicket)) return;
+      this.pageError = SecondaryPolicy.errorKind(_);
       if (merchantLoaded) {
         IBestToast.show(I18n.text(this.language,
           '平台标准分类暂时无法加载，自定义分类仍可管理',
@@ -70,7 +97,7 @@ struct CategoryPage {
     this.editingId = item?.id || '';
     this.editingName = item?.name || '';
     this.editingParentId = item?.parentId || parentId;
-    this.editorVisible = true;
+    this.editStamp = this.localStamp(); this.editorVisible = true;
   }
 
   private async save(): Promise<void> {
@@ -95,7 +122,7 @@ struct CategoryPage {
       IBestToast.show(I18n.text(this.language, '分类已保存', 'Category saved'));
       await this.load();
     } catch (error) {
-      IBestToast.show(error instanceof ApiError ? error.message
+      IBestToast.show(error instanceof ApiError ? SecondaryPolicy.message(error, this.language)
         : I18n.text(this.language, '保存失败', 'Failed to save'));
     } finally {
       this.saving = false;
@@ -124,7 +151,7 @@ struct CategoryPage {
       IBestToast.show(I18n.text(this.language, '已删除', 'Deleted'));
       await this.load();
     } catch (error) {
-      IBestToast.show(error instanceof ApiError ? error.message
+      IBestToast.show(error instanceof ApiError ? SecondaryPolicy.message(error, this.language)
         : I18n.text(this.language, '删除失败', 'Failed to delete'));
     } finally {
       this.workingId = '';
@@ -145,7 +172,7 @@ struct CategoryPage {
       await ProductRepository.sortCategories(siblings.map((current: CategoryItem): string => current.id));
       await this.load();
     } catch (error) {
-      IBestToast.show(error instanceof ApiError ? error.message
+      IBestToast.show(error instanceof ApiError ? SecondaryPolicy.message(error, this.language)
         : I18n.text(this.language, '排序保存失败', 'Failed to save order'));
     } finally {
       this.workingId = '';
@@ -164,10 +191,10 @@ struct CategoryPage {
   @Builder
   private controls(item: CategoryItem) {
     Row({ space: 5 }) {
-      IBestButton({ text: '↑', plain: true, disabled: this.workingId.length > 0 || this.saving || this.siblingIndex(item) <= 0, onBtnClick: () => this.move(item, -1) });
-      IBestButton({ text: '↓', plain: true, disabled: this.workingId.length > 0 || this.saving || this.siblingIndex(item) >= this.siblingCount(item) - 1, onBtnClick: () => this.move(item, 1) });
-      IBestButton({ text: I18n.text(this.language, '编辑', 'Edit'), plain: true, color: DesignTokens.brand(this.darkMode), disabled: this.workingId.length > 0 || this.saving, onBtnClick: () => this.openEditor(item) });
-      IBestButton({ text: I18n.text(this.language, '删除', 'Delete'), plain: true, type: 'danger', loading: this.workingId === item.id, disabled: this.workingId.length > 0 || this.saving, onBtnClick: () => this.confirmDelete(item) });
+      IBestButton({ btnHeight: 44, text: '↑', plain: true, disabled: this.workingId.length > 0 || this.saving || this.siblingIndex(item) <= 0, onBtnClick: () => this.move(item, -1) });
+      IBestButton({ btnHeight: 44, text: '↓', plain: true, disabled: this.workingId.length > 0 || this.saving || this.siblingIndex(item) >= this.siblingCount(item) - 1, onBtnClick: () => this.move(item, 1) });
+      IBestButton({ btnHeight: 44, text: I18n.text(this.language, '编辑', 'Edit'), plain: true, color: DesignTokens.brand(this.darkMode), disabled: this.workingId.length > 0 || this.saving, onBtnClick: () => this.openEditor(item) });
+      IBestButton({ btnHeight: 44, text: I18n.text(this.language, '删除', 'Delete'), plain: true, type: 'danger', loading: this.workingId === item.id, disabled: this.workingId.length > 0 || this.saving, onBtnClick: () => this.confirmDelete(item) });
     }
   }
 
@@ -181,7 +208,7 @@ struct CategoryPage {
             IBestTag({ text: I18n.text(this.language, '一级', 'Primary'), plain: true, color: DesignTokens.brand(this.darkMode) });
           }
           Text(I18n.text(this.language, `${this.children(this.categories, item.id).length} 个子分类`,
-            `${this.children(this.categories, item.id).length} subcategories`)).fontSize(11)
+            `${this.children(this.categories, item.id).length} subcategories`)).fontSize(12)
             .fontColor(DesignTokens.muted(this.darkMode));
         }.layoutWeight(1).alignItems(HorizontalAlign.Start);
         this.controls(item);
@@ -194,11 +221,11 @@ struct CategoryPage {
         }.width('100%').padding({ top: 8, bottom: 8, left: 8 })
           .border({ width: { top: 1 }, color: DesignTokens.border(this.darkMode) });
       }, (child: CategoryItem) => child.id);
-      IBestButton({
+      IBestButton({ btnHeight: 44,
         text: I18n.text(this.language, '添加子分类', 'Add subcategory'), plain: true, color: DesignTokens.brand(this.darkMode),
         btnWidth: '100%', onBtnClick: () => this.openEditor(undefined, item.id)
       });
-    }.width('100%').padding(14).backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(DesignTokens.RADIUS_LARGE);
+    }.width('100%').padding(14).backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(16);
   }
 
   @Builder
@@ -208,11 +235,11 @@ struct CategoryPage {
         Text(item.name).fontSize(15).fontWeight(FontWeight.Medium).layoutWeight(1);
         Text(I18n.text(this.language, `${this.children(this.platformCategories, item.id).length} 个二级分类`,
           `${this.children(this.platformCategories, item.id).length} secondary categories`))
-          .fontSize(11).fontColor(DesignTokens.muted(this.darkMode));
+          .fontSize(12).fontColor(DesignTokens.muted(this.darkMode));
       }.width('100%');
       Flex({ wrap: FlexWrap.Wrap }) {
         ForEach(this.children(this.platformCategories, item.id), (child: CategoryItem) => {
-          Text(child.name).fontSize(11).padding({ left: 9, right: 9, top: 6, bottom: 6 })
+          Text(child.name).fontSize(12).padding({ left: 9, right: 9, top: 6, bottom: 6 })
             .margin({ right: 6, bottom: 6 }).backgroundColor(DesignTokens.subtle(this.darkMode)).borderRadius(14);
         }, (child: CategoryItem) => child.id);
       }.width('100%');
@@ -227,7 +254,7 @@ struct CategoryPage {
           : this.editingParentId ? I18n.text(this.language, '新增子分类', 'Add subcategory')
             : I18n.text(this.language, '新增一级分类', 'Add primary category'))
           .fontSize(20).fontWeight(FontWeight.Bold).layoutWeight(1);
-        Text(I18n.text(this.language, '关闭', 'Close')).fontSize(14).fontColor(DesignTokens.muted(this.darkMode)).onClick(() => this.editorVisible = false);
+        Text(I18n.text(this.language, '关闭', 'Close')).fontSize(14).fontColor(DesignTokens.muted(this.darkMode)).onClick(() => this.closeLocalEditor());
       }.width('100%');
       if (this.editingParentId) {
         Row() {
@@ -236,24 +263,25 @@ struct CategoryPage {
         }.width('100%');
       }
       IBestField({ value: $editingName, label: I18n.text(this.language, '分类名称', 'Category name'), placeholder: I18n.text(this.language, '不超过10个字', 'Up to 10 characters'), maxlength: 10, hasBorder: false });
-      IBestButton({ text: I18n.text(this.language, '保存', 'Save'), type: 'primary', color: DesignTokens.brand(this.darkMode), fontColor: DesignTokens.onBrand(this.darkMode), btnWidth: '100%', loading: this.saving, disabled: this.saving || this.workingId.length > 0, onBtnClick: () => this.save() });
-    }.width('100%').padding({ left: 20, right: 20, top: 22, bottom: this.safeBottom + 22 });
+      IBestButton({ btnHeight: 44, text: I18n.text(this.language, '保存', 'Save'), type: 'primary', color: DesignTokens.brand(this.darkMode), fontColor: DesignTokens.onBrand(this.darkMode), btnWidth: '100%', loading: this.saving, disabled: this.saving || this.workingId.length > 0, onBtnClick: () => this.save() });
+    }.width('100%').padding({ left: 16, right: 16, top: 22, bottom: this.safeBottom + 22 });
   }
 
   build() {
     NavDestination() {
       Column() {
-        IBestNavBar({
+        SecondaryHeader({
           title: I18n.text(this.language, '商品分类', 'Product categories'), isShowStatusBar: true, navBarBgColor: DesignTokens.surface(this.darkMode),
-          onLeftClick: () => AppRouter.back()
+          onLeftClick: () => this.handleBack()
         });
+          SecondaryFeedback({ errorKind: this.pageError, updating: this.loading, onRetry: () => this.load() });
         if (this.loading) {
           Column() { LoadingProgress().width(32).height(32).color(DesignTokens.brand(this.darkMode)); }
             .layoutWeight(1).justifyContent(FlexAlign.Center);
         } else if (this.errorMessage) {
           Column({ space: 12 }) {
             IBestEmpty({ description: I18n.state(this.language, this.errorMessage), emptyImage: 'network' });
-            IBestButton({ text: I18n.text(this.language, '重试', 'Retry'), color: DesignTokens.brand(this.darkMode), fontColor: DesignTokens.onBrand(this.darkMode), onBtnClick: () => this.load() });
+            IBestButton({ btnHeight: 44, text: I18n.text(this.language, '重试', 'Retry'), color: DesignTokens.brand(this.darkMode), fontColor: DesignTokens.onBrand(this.darkMode), onBtnClick: () => this.load() });
           }.layoutWeight(1).justifyContent(FlexAlign.Center);
         } else {
           Scroll() {
@@ -261,16 +289,16 @@ struct CategoryPage {
               Row() {
                 Column({ space: 3 }) {
                   Text(I18n.text(this.language, '自定义分类', 'Custom categories')).fontSize(18).fontWeight(FontWeight.Bold);
-                  Text(I18n.text(this.language, '支持两级分类与同级排序', 'Two levels with custom ordering')).fontSize(11).fontColor(DesignTokens.muted(this.darkMode));
+                  Text(I18n.text(this.language, '支持两级分类与同级排序', 'Two levels with custom ordering')).fontSize(12).fontColor(DesignTokens.muted(this.darkMode));
                 }.layoutWeight(1).alignItems(HorizontalAlign.Start);
-                IBestButton({ text: I18n.text(this.language, '新增一级', 'Add primary'), type: 'primary', color: DesignTokens.brand(this.darkMode), fontColor: DesignTokens.onBrand(this.darkMode), onBtnClick: () => this.openEditor() });
+                IBestButton({ btnHeight: 44, text: I18n.text(this.language, '新增一级', 'Add primary'), type: 'primary', color: DesignTokens.brand(this.darkMode), fontColor: DesignTokens.onBrand(this.darkMode), onBtnClick: () => this.openEditor() });
               }.width('100%');
               if (this.roots(this.categories).length === 0) IBestEmpty({ description: I18n.text(this.language, '暂无自定义分类', 'No custom categories') });
               ForEach(this.roots(this.categories), (item: CategoryItem) => { this.customRoot(item); }, (item: CategoryItem) => item.id);
               Row() {
                 Column({ space: 3 }) {
                   Text(I18n.text(this.language, '平台标准分类', 'Platform categories')).fontSize(18).fontWeight(FontWeight.Bold);
-                  Text(I18n.text(this.language, '只读，用于商品审核和商城统一归类', 'Read-only categories used for review and storefront consistency')).fontSize(11).fontColor(DesignTokens.muted(this.darkMode));
+                  Text(I18n.text(this.language, '只读，用于商品审核和商城统一归类', 'Read-only categories used for review and storefront consistency')).fontSize(12).fontColor(DesignTokens.muted(this.darkMode));
                 }.layoutWeight(1).alignItems(HorizontalAlign.Start);
                 Text(this.showPlatform ? I18n.text(this.language, '收起', 'Collapse') : I18n.text(this.language, '查看全部', 'View all')).fontSize(13).fontColor(DesignTokens.brand(this.darkMode))
                   .onClick(() => this.showPlatform = !this.showPlatform);
@@ -284,8 +312,8 @@ struct CategoryPage {
       }.width('100%').height('100%').backgroundColor(DesignTokens.page(this.darkMode))
         .bindSheet(this.editorVisible, this.editor(), {
           height: SheetSize.MEDIUM,
-          onWillDismiss: () => { this.editorVisible = false; }
+          onWillDismiss: () => { this.closeLocalEditor(); }
         });
-    }.hideTitleBar(true);
+    }.hideTitleBar(true).onBackPressed((): boolean => { this.handleBack(); return true; });
   }
 }
diff --git a/native/harmony-merchant/entry/src/main/ets/features/product/ProductEditPage.ets b/native/harmony-merchant/entry/src/main/ets/features/product/ProductEditPage.ets
index 7901d8f9..7e650d02 100644
--- a/native/harmony-merchant/entry/src/main/ets/features/product/ProductEditPage.ets
+++ b/native/harmony-merchant/entry/src/main/ets/features/product/ProductEditPage.ets
@@ -1,3 +1,5 @@
+import { SecondaryPolicy } from '../../core/ui/SecondaryPolicy';
+import { SecondaryHeader } from '../../core/ui/SecondaryPage';
 import { KeyboardAvoidMode } from '@kit.ArkUI';
 import {
   IBestButton,
@@ -5,7 +7,6 @@ import {
   IBestDialogUtil,
   IBestField,
   IBestImagePreview,
-  IBestNavBar,
   IBestToast
 } from '@ibestservices/ibest-ui';
 import { common } from '@kit.AbilityKit';
@@ -341,7 +342,7 @@ struct ProductEditPage {
     try {
       const urls = await FileUploadService.chooseAndUploadImages(1, 'product');
       if (this.current() && epoch === this.skuEpoch && urls.length > 0) this.skuImage = urls[0];
-    } catch (error) { if (this.current() && epoch === this.skuEpoch) IBestToast.show(error instanceof Error ? error.message
+    } catch (error) { if (this.current() && epoch === this.skuEpoch) IBestToast.show(error instanceof Error ? SecondaryPolicy.message(error, this.language)
       : I18n.text(this.language, '规格图片上传失败', 'Option image upload failed')); }
     finally { if (this.current() && epoch === this.skuEpoch) this.skuUploading = false; }
   }
@@ -355,7 +356,7 @@ struct ProductEditPage {
       const url = await FileUploadService.captureAndUploadImage(context, 'product');
       if (this.current() && epoch === this.skuEpoch && url) this.skuImage = url;
     } catch (error) {
-      if (this.current() && epoch === this.skuEpoch) IBestToast.show(error instanceof Error ? error.message
+      if (this.current() && epoch === this.skuEpoch) IBestToast.show(error instanceof Error ? SecondaryPolicy.message(error, this.language)
         : I18n.text(this.language, '规格图片拍摄上传失败', 'Failed to capture and upload option image'));
     } finally {
       if (this.current() && epoch === this.skuEpoch) this.skuUploading = false;
@@ -535,7 +536,7 @@ struct ProductEditPage {
       IBestToast.show(nextStatus === 'draft' ? I18n.text(this.language, '草稿已保存', 'Draft saved') : I18n.text(this.language, '已提交审核', 'Submitted for review'));
       AppRouter.back();
     } catch (error) {
-      if (this.current()) IBestToast.show(error instanceof ApiError ? error.message : I18n.text(this.language, '保存失败，内容已保留，请重试', 'Save failed. Your changes are kept. Try again.'));
+      if (this.current()) IBestToast.show(error instanceof ApiError ? SecondaryPolicy.message(error, this.language) : I18n.text(this.language, '保存失败，内容已保留，请重试', 'Save failed. Your changes are kept. Try again.'));
     } finally { this.session.finishSave(); if (this.current()) this.saving = false; }
   }
 
@@ -727,7 +728,7 @@ struct ProductEditPage {
             .fontSize(13).fontColor(DesignTokens.muted(this.darkMode)).width('100%');
           ProductMediaEditor({ items: $detailDraft, detail: true, busy: this.uploading, onAdd: () => this.addImageMenu(true),
             onPreview: (i: number) => this.previewImage(i, true), onMenu: (i: number) => this.imageMenu(i, true), onRetry: (i: number) => this.failedImageMenu(i, true) });
-        }.width('100%').padding(18);
+        }.width('100%').padding(16);
       }.layoutWeight(1).width('100%').align(Alignment.Top);
       this.panelFooter('完成', 'Done', () => this.closeDetails(true), () => this.closeDetails(false));
     }.height('92%').width('100%').backgroundColor(DesignTokens.card(this.darkMode)).borderRadius({ topLeft: 24, topRight: 24 }).clip(true);
@@ -770,7 +771,7 @@ struct ProductEditPage {
               ResilientRemoteImage({ originalUrl: sku.image || '', widthValue: 56, heightValue: 56 });
               Column({ space: 6 }) {
                 Text(sku.specsLabel).fontSize(14).fontWeight(FontWeight.Medium).fontColor(DesignTokens.text(this.darkMode));
-                if (!sku.active) Text(I18n.text(this.language, '已停用', 'Disabled')).fontSize(11).fontColor(DesignTokens.muted(this.darkMode));
+                if (!sku.active) Text(I18n.text(this.language, '已停用', 'Disabled')).fontSize(12).fontColor(DesignTokens.muted(this.darkMode));
                 Text(I18n.text(this.language, `零售 ¥${Number(sku.priceRetail).toFixed(2)} · 库存 ${sku.stock}`, `Retail ¥${Number(sku.priceRetail).toFixed(2)} · Stock ${sku.stock}`))
                   .fontSize(12).fontColor(DesignTokens.muted(this.darkMode));
               }.layoutWeight(1).alignItems(HorizontalAlign.Start);
@@ -823,7 +824,7 @@ struct ProductEditPage {
               : I18n.text(this.language, '将所有 SKU 的库存设为此整数，不是增加库存。', 'Replace every SKU stock with this whole number; this does not add stock.'))
               .fontSize(13).fontColor(DesignTokens.muted(this.darkMode)).width('100%');
           }
-        }.width('100%').padding(18);
+        }.width('100%').padding(16);
       }.layoutWeight(1).width('100%').align(Alignment.Top).scrollBar(BarState.Off);
       this.panelFooter(this.directSku ? '应用到商品' : '保存修改', this.directSku ? 'Apply to product' : 'Save changes', () => { if (this.skuEditorVisible) this.saveSku(); else if (this.specEditorVisible) this.saveSpecGroup(); else this.applyBatch(); }, () => this.closeInner());
     }.height('92%').width('100%').backgroundColor(DesignTokens.card(this.darkMode)).borderRadius({ topLeft: 24, topRight: 24 }).clip(true);
@@ -833,7 +834,7 @@ struct ProductEditPage {
     NavDestination() {
       Stack({ alignContent: Alignment.Bottom }) {
         Column() {
-          IBestNavBar({ title: this.params.id ? I18n.text(this.language, '编辑商品', 'Edit product') : I18n.text(this.language, '新增商品', 'Add product'),
+          SecondaryHeader({ title: this.params.id ? I18n.text(this.language, '编辑商品', 'Edit product') : I18n.text(this.language, '新增商品', 'Add product'),
             isShowStatusBar: true, navBarBgColor: DesignTokens.surface(this.darkMode), onLeftClick: () => this.requestBack() });
           if (this.loading) {
             Column({ space: 16 }) {
diff --git a/native/harmony-merchant/entry/src/main/ets/features/product/ProductListPage.ets b/native/harmony-merchant/entry/src/main/ets/features/product/ProductListPage.ets
index 458eda31..3baa6352 100644
--- a/native/harmony-merchant/entry/src/main/ets/features/product/ProductListPage.ets
+++ b/native/harmony-merchant/entry/src/main/ets/features/product/ProductListPage.ets
@@ -1,3 +1,4 @@
+import { SecondaryBackHandler, SecondaryPolicy } from '../../core/ui/SecondaryPolicy';
 import { PrimaryNavigationMetrics } from '../shell/PrimaryNavigationMetrics';
 import { IBestButton, IBestDialogUtil, IBestEmpty, IBestToast } from '@ibestservices/ibest-ui';
 import { AppRouter } from '../../core/navigation/AppRouter';
@@ -33,6 +34,8 @@ struct ProductStatusOption {
 
 @Component
 export struct ProductListPage {
+  backHandler: SecondaryBackHandler = new SecondaryBackHandler();
+  private back(): void { if (!this.backHandler.closeOverlay()) AppRouter.back(); }
   @Prop @Watch('onInitialStatus') initialStatus: string = '';
   @Prop @Watch('onActivation') activationEpoch: number = 0;
   showBack: boolean = false;
@@ -65,11 +68,18 @@ export struct ProductListPage {
   ];
 
   aboutToAppear(): void {
+    this.backHandler.closeOverlay = (): boolean => {
+      if (this.processingBatch) return true;
+      if (this.panel) { this.panel = ''; return true; }
+      if (this.selectionMode) { this.selectionMode = false; this.selectedIds = []; return true; }
+      return false;
+    };
     this.status = this.initialStatus;
     this.reload();
   }
 
   aboutToDisappear(): void {
+    this.backHandler.closeOverlay = () => false;
     this.requestEpoch += 1;
     this.loading = false;
     if (this.searchTimer >= 0) clearTimeout(this.searchTimer);
@@ -117,7 +127,8 @@ export struct ProductListPage {
     try {
       const result = await ProductRepository.list(requestedStatus, requestedKeyword, requestedPage);
       if (epoch !== this.requestEpoch || session !== this.sessionEpoch) return;
-      const incoming = Array.isArray(result.list) ? result.list : [];
+      if (!Array.isArray(result.list)) throw new Error('invalid-products');
+      const incoming = result.list;
       this.products = append ? this.products.concat(incoming.filter((item: ProductSummary): boolean =>
         !this.products.some((current: ProductSummary): boolean => current.id === item.id))) : incoming;
       this.dataSource.update(this.products);
@@ -125,7 +136,7 @@ export struct ProductListPage {
       this.total = result.total;
     } catch (error) {
       if (epoch === this.requestEpoch) {
-        this.errorMessage = error instanceof ApiError ? error.message : 'i18n:product-load';
+        this.errorMessage = error instanceof ApiError ? SecondaryPolicy.message(error, this.language) : 'i18n:product-load';
       }
     } finally {
       if (epoch === this.requestEpoch) {
@@ -229,7 +240,7 @@ export struct ProductListPage {
       this.selectedIds = [];
       await this.reload();
     } catch (error) {
-      IBestToast.show(error instanceof ApiError ? error.message
+      IBestToast.show(error instanceof ApiError ? SecondaryPolicy.message(error, this.language)
         : I18n.text(this.language, '操作失败', 'Operation failed'));
     } finally {
       this.processingBatch = false;
@@ -337,7 +348,7 @@ export struct ProductListPage {
         if (this.showBack) {
           Button({ type: ButtonType.Normal }) {
             MerchantIcon({ name: 'arrow-left', color: DesignTokens.text(this.darkMode), iconSize: 24 });
-          }.width(38).height(42).backgroundColor(Color.Transparent).onClick((): void => AppRouter.back());
+          }.width(44).height(44).backgroundColor(Color.Transparent).onClick((): void => this.back());
         }
         Column({ space: 4 }) {
           Text(I18n.text(this.language, '商品', 'Products')).fontSize(27).fontWeight(FontWeight.Bold).fontColor(DesignTokens.text(this.darkMode));
@@ -350,7 +361,7 @@ export struct ProductListPage {
           }.padding({ left: 14, right: 14 });
         }.height(42).borderRadius(13).backgroundColor(DesignTokens.selectedSurface(this.darkMode))
           .onClick((): void => AppRouter.push('ProductEdit', new IdRouteParams()));
-      }.width('100%').padding({ top: this.safeTop + 12, left: 18, right: 18, bottom: 16 });
+      }.width('100%').padding({ top: this.safeTop + (this.showBack ? 6 : 12), left: this.showBack ? 8 : 18, right: 16, bottom: this.showBack ? 8 : 16 });
 
       Row({ space: 9 }) {
         MerchantIcon({ name: 'search', color: DesignTokens.muted(this.darkMode), iconSize: 20 });
diff --git a/native/harmony-merchant/entry/src/main/ets/features/profile/AboutPage.ets b/native/harmony-merchant/entry/src/main/ets/features/profile/AboutPage.ets
index 60573d9c..e270b211 100644
--- a/native/harmony-merchant/entry/src/main/ets/features/profile/AboutPage.ets
+++ b/native/harmony-merchant/entry/src/main/ets/features/profile/AboutPage.ets
@@ -1,5 +1,7 @@
+import { PageReadScope, SecondaryPolicy } from '../../core/ui/SecondaryPolicy';
+import { SecondaryFeedback, SecondaryHeader } from '../../core/ui/SecondaryPage';
 import { common } from '@kit.AbilityKit';
-import { IBestDialogUtil, IBestNavBar, IBestToast } from '@ibestservices/ibest-ui';
+import { IBestDialogUtil, IBestToast } from '@ibestservices/ibest-ui';
 import { AppConfig } from '../../core/config/AppConfig';
 import { PhoneService } from '../../core/device/PhoneService';
 import { I18n } from '../../core/i18n/I18n';
@@ -15,15 +17,24 @@ export function AboutAppBuilder(_: string) { AboutPage(); }
 
 @Component
 struct AboutPage {
+  aboutToDisappear(): void { this.pageScope.dispose(); }
+
+  private pageScope: PageReadScope = new PageReadScope();
+  @State private pageError: string = '';
+
   @StorageProp('safeBottom') private safeBottom: number = 16;
   @StorageLink('language') private language: string = 'zh-CN';
   @StorageLink('darkMode') private darkMode: boolean = false;
   @State private publicSettings: PublicSystemSettings | null = null;
 
-  aboutToAppear(): void { this.load(); }
+  aboutToAppear(): void { this.pageScope.open(); this.load(); }
 
   private async load(): Promise<void> {
-    try { this.publicSettings = await PublicSettingsRepository.load(); } catch (_) {}
+    const pageTicket = this.pageScope.begin('load');
+    this.pageError = '';
+    try { this.publicSettings = await this.pageScope.read(PublicSettingsRepository.load(), pageTicket); } catch (_) {
+      if (!this.pageScope.accepts(pageTicket)) return;
+      this.pageError = SecondaryPolicy.errorKind(_);}
   }
 
   private context(): common.UIAbilityContext {
@@ -60,7 +71,7 @@ struct AboutPage {
     Row({ space: 12 }) {
       MerchantIcon({ name: icon, color: DesignTokens.brand(this.darkMode), iconSize: 21 });
       Text(title).fontSize(14).fontWeight(FontWeight.Medium).fontColor(DesignTokens.text(this.darkMode)).layoutWeight(1);
-      if (detail) Text(detail).fontSize(11).fontColor(DesignTokens.muted(this.darkMode));
+      if (detail) Text(detail).fontSize(12).fontColor(DesignTokens.muted(this.darkMode));
       MerchantIcon({ name: 'arrow', color: DesignTokens.muted(this.darkMode), iconSize: 16 });
     }.width('100%').height(58).padding({ left: 16, right: 16 })
       .border({ width: { bottom: border ? 1 : 0 }, color: DesignTokens.border(this.darkMode) }).onClick(action);
@@ -69,10 +80,11 @@ struct AboutPage {
   build() {
     NavDestination() {
       Column() {
-        IBestNavBar({
+        SecondaryHeader({
           title: I18n.text(this.language, '关于经纬商家端', 'About Jingwei Merchant'), isShowStatusBar: true,
           navBarBgColor: DesignTokens.surface(this.darkMode), onLeftClick: () => AppRouter.back()
         });
+          SecondaryFeedback({ errorKind: this.pageError, onRetry: () => this.load() });
         Scroll() {
           Column({ space: 16 }) {
             Column({ space: 12 }) {
@@ -112,7 +124,7 @@ struct AboutPage {
             Text(I18n.text(this.language,
               'Copyright © 经纬科技。保留所有权利。',
               'Copyright © Jingwei Technology. All rights reserved.'))
-              .fontSize(11).fontColor(DesignTokens.muted(this.darkMode)).textAlign(TextAlign.Center).width('100%');
+              .fontSize(12).fontColor(DesignTokens.muted(this.darkMode)).textAlign(TextAlign.Center).width('100%');
           }.width('100%').constraintSize({ maxWidth: 680 })
             .padding({ left: 16, right: 16, top: 16, bottom: this.safeBottom + 24 });
         }.layoutWeight(1).align(Alignment.Top).scrollBar(BarState.Off);
diff --git a/native/harmony-merchant/entry/src/main/ets/features/profile/AccountSecurityPage.ets b/native/harmony-merchant/entry/src/main/ets/features/profile/AccountSecurityPage.ets
index 0fb3dd60..60c3061d 100644
--- a/native/harmony-merchant/entry/src/main/ets/features/profile/AccountSecurityPage.ets
+++ b/native/harmony-merchant/entry/src/main/ets/features/profile/AccountSecurityPage.ets
@@ -1,3 +1,4 @@
+import { SecondaryHeader } from '../../core/ui/SecondaryPage';
 import { IBestNavBar } from '@ibestservices/ibest-ui';
 import { I18n } from '../../core/i18n/I18n';
 import { AppRouter } from '../../core/navigation/AppRouter';
@@ -29,9 +30,9 @@ struct AccountSecurityPage {
         .backgroundColor(DesignTokens.iconSurface(this.darkMode)).borderRadius(14);
       Column({ space: 4 }) {
         Text(title).fontSize(15).fontWeight(FontWeight.Medium).fontColor(DesignTokens.text(this.darkMode)).width('100%');
-        Text(description).fontSize(11).fontColor(DesignTokens.muted(this.darkMode)).width('100%').maxLines(2);
+        Text(description).fontSize(12).fontColor(DesignTokens.muted(this.darkMode)).width('100%').maxLines(2);
       }.layoutWeight(1).alignItems(HorizontalAlign.Start);
-      if (value) Text(value).fontSize(11).fontColor(DesignTokens.muted(this.darkMode));
+      if (value) Text(value).fontSize(12).fontColor(DesignTokens.muted(this.darkMode));
       MerchantIcon({ name: 'arrow', color: DesignTokens.muted(this.darkMode), iconSize: 16 });
     }.width('100%').height(72).padding({ left: 15, right: 15 })
       .border({ width: { bottom: border ? 1 : 0 }, color: DesignTokens.border(this.darkMode) }).onClick(action);
@@ -40,7 +41,7 @@ struct AccountSecurityPage {
   build() {
     NavDestination() {
       Column() {
-        IBestNavBar({
+        SecondaryHeader({
           title: I18n.text(this.language, '账号与安全', 'Account and security'), isShowStatusBar: true,
           navBarBgColor: DesignTokens.surface(this.darkMode), onLeftClick: () => AppRouter.back()
         });
@@ -55,7 +56,7 @@ struct AccountSecurityPage {
               Text(I18n.text(this.language,
                 '敏感操作需要密码或短信验证码确认', 'Sensitive changes require a password or SMS verification code'))
                 .fontSize(12).fontColor(DesignTokens.muted(this.darkMode)).textAlign(TextAlign.Center);
-            }.width('100%').padding(20).backgroundColor(DesignTokens.card(this.darkMode))
+            }.width('100%').padding(16).backgroundColor(DesignTokens.card(this.darkMode))
               .borderRadius(DesignTokens.RADIUS_XL).border({ width: 1, color: DesignTokens.border(this.darkMode) });
             Column() {
               this.securityRow('phone-o', I18n.text(this.language, '登录手机号', 'Sign-in phone'),
@@ -70,11 +71,11 @@ struct AccountSecurityPage {
                 I18n.text(this.language, '管理联系人、经营品类与店铺地址', 'Manage contacts, categories and store address'),
                 '', () => AppRouter.push('Profile'), false);
             }.width('100%').backgroundColor(DesignTokens.card(this.darkMode))
-              .borderRadius(DesignTokens.RADIUS_LARGE).border({ width: 1, color: DesignTokens.border(this.darkMode) }).clip(true);
+              .borderRadius(16).border({ width: 1, color: DesignTokens.border(this.darkMode) }).clip(true);
             Text(I18n.text(this.language,
               '经纬不会在任何页面展示、保存或发送你的明文密码。',
               'Jingwei never displays, stores or sends your password in plain text.'))
-              .fontSize(11).fontColor(DesignTokens.muted(this.darkMode)).lineHeight(18).width('100%').padding({ left: 6, right: 6 });
+              .fontSize(12).fontColor(DesignTokens.muted(this.darkMode)).lineHeight(18).width('100%').padding({ left: 6, right: 6 });
           }.width('100%').constraintSize({ maxWidth: 680 })
             .padding({ left: 16, right: 16, top: 16, bottom: this.safeBottom + 24 });
         }.layoutWeight(1).align(Alignment.Top).scrollBar(BarState.Off);
diff --git a/native/harmony-merchant/entry/src/main/ets/features/profile/AppearanceSettingsPage.ets b/native/harmony-merchant/entry/src/main/ets/features/profile/AppearanceSettingsPage.ets
index ab47f377..f0411bc1 100644
--- a/native/harmony-merchant/entry/src/main/ets/features/profile/AppearanceSettingsPage.ets
+++ b/native/harmony-merchant/entry/src/main/ets/features/profile/AppearanceSettingsPage.ets
@@ -1,3 +1,4 @@
+import { SecondaryHeader } from '../../core/ui/SecondaryPage';
 import { IBestNavBar } from '@ibestservices/ibest-ui';
 import { I18n } from '../../core/i18n/I18n';
 import { AppRouter } from '../../core/navigation/AppRouter';
@@ -92,10 +93,10 @@ struct AppearanceSettingsPage {
   build() {
     NavDestination() {
       Column() {
-        IBestNavBar({ title: I18n.text(this.language, '主题与语言', 'Theme and language'), isShowStatusBar: true,
+        SecondaryHeader({ title: I18n.text(this.language, '主题与语言', 'Theme and language'), isShowStatusBar: true,
           navBarBgColor: DesignTokens.surface(this.darkMode), onLeftClick: () => AppRouter.back() });
         Scroll() {
-          Column({ space: 24 }) {
+          Column({ space: 12 }) {
             Row({ space: 14 }) {
               Image($r('app.media.commerce_appearance_lite')).width(48).height(48).objectFit(ImageFit.Contain);
               Column({ space: 5 }) {
@@ -133,9 +134,9 @@ struct AppearanceSettingsPage {
                 .layoutWeight(1).lineHeight(19);
             }.width('100%').padding(14).backgroundColor(DesignTokens.subtle(this.darkMode)).borderRadius(14)
               .onClick((): void => { if (this.saveState === 'failed') AppearanceService.retryPersistence(); });
-          }.width('100%').constraintSize({ maxWidth: 680 }).padding({ left: 20, right: 20, top: 24, bottom: this.safeBottom + 24 });
+          }.width('100%').constraintSize({ maxWidth: 680 }).padding({ left: 16, right: 16, top: 12, bottom: this.safeBottom + 24 });
         }.layoutWeight(1).align(Alignment.Top).scrollBar(BarState.Off);
       }.width('100%').height('100%').backgroundColor(DesignTokens.page(this.darkMode));
     }.hideTitleBar(true);
   }
-}
\ No newline at end of file
+}
diff --git a/native/harmony-merchant/entry/src/main/ets/features/profile/LocationPickerPage.ets b/native/harmony-merchant/entry/src/main/ets/features/profile/LocationPickerPage.ets
index 0c091e51..85d512ba 100644
--- a/native/harmony-merchant/entry/src/main/ets/features/profile/LocationPickerPage.ets
+++ b/native/harmony-merchant/entry/src/main/ets/features/profile/LocationPickerPage.ets
@@ -1,8 +1,11 @@
+import { SecondaryPolicy } from '../../core/ui/SecondaryPolicy';
+import { SessionStore } from '../../core/storage/SessionStore';
+import { SecondaryHeader } from '../../core/ui/SecondaryPage';
 import { abilityAccessCtrl, common, Permissions } from '@kit.AbilityKit';
 import { BusinessError } from '@kit.BasicServicesKit';
 import { geoLocationManager } from '@kit.LocationKit';
 import { MapComponent, map, mapCommon, site } from '@kit.MapKit';
-import { IBestButton, IBestNavBar, IBestSearch, IBestToast } from '@ibestservices/ibest-ui';
+import { IBestButton, IBestSearch, IBestToast } from '@ibestservices/ibest-ui';
 import { AppRouter } from '../../core/navigation/AppRouter';
 import { DesignTokens } from '../../core/theme/DesignTokens';
 import { MerchantIcon } from '../../core/theme/MerchantIcon';
@@ -23,6 +26,9 @@ export function LocationPickerBuilder(_: string) { LocationPickerPage(); }
 
 @Component
 struct LocationPickerPage {
+  private selectionToken: string = '';
+  private selectionOwner: string = '';
+
   @StorageProp('safeBottom') private safeBottom: number = 16;
   @StorageLink('language') private language: string = 'zh-CN';
   @StorageLink('darkMode') private darkMode: boolean = false;
@@ -69,6 +75,8 @@ struct LocationPickerPage {
   };
 
   aboutToAppear(): void {
+    this.selectionToken = AppStorage.get<string>('locationRequestToken') || '';
+    this.selectionOwner = SessionStore.shared().user?.id || '';
     const savedLatitude = AppStorage.get<number>('pickedLatitude') || 0;
     const savedLongitude = AppStorage.get<number>('pickedLongitude') || 0;
     const hasSavedLocation = AppStorage.get<boolean>('pickedLocationSet') || false;
@@ -130,7 +138,7 @@ struct LocationPickerPage {
       }
       await this.reverseGeocode();
     } catch (error) {
-      IBestToast.show(error instanceof Error ? error.message : I18n.text(this.language, '当前位置获取失败', 'Failed to get current location'));
+      IBestToast.show(error instanceof Error ? SecondaryPolicy.message(error, this.language) : I18n.text(this.language, '当前位置获取失败', 'Failed to get current location'));
     } finally { this.locating = false; }
   }
 
@@ -210,7 +218,7 @@ struct LocationPickerPage {
     } catch (error) {
       if (epoch !== this.searchEpoch) return;
       this.searchResults = [];
-      IBestToast.show(error instanceof Error ? error.message : I18n.text(this.language, '地点搜索失败', 'Place search failed'));
+      IBestToast.show(error instanceof Error ? SecondaryPolicy.message(error, this.language) : I18n.text(this.language, '地点搜索失败', 'Place search failed'));
     } finally {
       if (epoch === this.searchEpoch) this.searching = false;
     }
@@ -241,6 +249,7 @@ struct LocationPickerPage {
   }
 
   private confirm(): void {
+    if (!this.selectionToken || this.selectionToken !== AppStorage.get<string>('locationRequestToken') || this.selectionOwner !== (SessionStore.shared().user?.id || '')) { AppRouter.back(); return; }
     if (this.resolvingAddress) {
       IBestToast.show(I18n.text(this.language, '正在确认地址，请稍候', 'Confirming the address. Please wait.'));
       return;
@@ -254,6 +263,7 @@ struct LocationPickerPage {
     AppStorage.setOrCreate<number>('pickedLatitude', this.latitude);
     AppStorage.setOrCreate<number>('pickedLongitude', this.longitude);
     AppStorage.setOrCreate<boolean>('pickedLocationSet', true);
+    AppStorage.setOrCreate('pickedLocationToken', this.selectionToken);
     AppStorage.setOrCreate<number>('locationEpoch', (AppStorage.get<number>('locationEpoch') || 0) + 1);
     AppRouter.back();
   }
@@ -261,7 +271,7 @@ struct LocationPickerPage {
   build() {
     NavDestination() {
       Column() {
-        IBestNavBar({
+        SecondaryHeader({
           title: I18n.text(this.language, '地图选址', 'Choose location'), isShowStatusBar: true,
           navBarBgColor: DesignTokens.surface(this.darkMode), titleColor: DesignTokens.text(this.darkMode),
           leftIconColor: DesignTokens.text(this.darkMode), onLeftClick: () => AppRouter.back()
@@ -299,7 +309,7 @@ struct LocationPickerPage {
                       Column({ space: 3 }) {
                         Text(item.name).fontSize(14).fontWeight(FontWeight.Medium)
                           .fontColor(DesignTokens.text(this.darkMode)).width('100%').maxLines(1);
-                        Text(item.address).fontSize(11).fontColor(DesignTokens.muted(this.darkMode))
+                        Text(item.address).fontSize(12).fontColor(DesignTokens.muted(this.darkMode))
                           .width('100%').maxLines(2);
                       }.layoutWeight(1).alignItems(HorizontalAlign.Start);
                     }.width('100%').padding({ left: 12, right: 12, top: 9, bottom: 9 })
@@ -326,8 +336,8 @@ struct LocationPickerPage {
             Column({ space: 8 }) {
               Text(I18n.text(this.language, '地图服务尚未配置', 'Map service is not configured')).fontSize(16).fontWeight(FontWeight.Medium);
               Text(I18n.text(this.language, '仍可使用系统定位与地址解析', 'System location and address lookup remain available')).fontSize(12).fontColor(DesignTokens.muted(this.darkMode));
-            }.padding(18).backgroundColor(DesignTokens.elevatedSurface(this.darkMode))
-              .borderRadius(DesignTokens.RADIUS_LARGE).zIndex(3);
+            }.padding(16).backgroundColor(DesignTokens.elevatedSurface(this.darkMode))
+              .borderRadius(16).zIndex(3);
           }
 
           Column({ space: 10 }) {
diff --git a/native/harmony-merchant/entry/src/main/ets/features/profile/NotificationSettingsPage.ets b/native/harmony-merchant/entry/src/main/ets/features/profile/NotificationSettingsPage.ets
index e203aaa2..18ea60f0 100644
--- a/native/harmony-merchant/entry/src/main/ets/features/profile/NotificationSettingsPage.ets
+++ b/native/harmony-merchant/entry/src/main/ets/features/profile/NotificationSettingsPage.ets
@@ -1,5 +1,7 @@
+import { PageReadScope, SecondaryPolicy } from '../../core/ui/SecondaryPolicy';
+import { SecondaryFeedback, SecondaryHeader } from '../../core/ui/SecondaryPage';
 import { common } from '@kit.AbilityKit';
-import { IBestNavBar, IBestToast } from '@ibestservices/ibest-ui';
+import { IBestToast } from '@ibestservices/ibest-ui';
 import { I18n } from '../../core/i18n/I18n';
 import { AppRouter } from '../../core/navigation/AppRouter';
 import { HarmonyPushClient, PushPreferences } from '../../core/push/HarmonyPushClient';
@@ -12,6 +14,11 @@ export function NotificationSettingsBuilder(_: string) { NotificationSettingsPag
 
 @Component
 struct NotificationSettingsPage {
+  aboutToDisappear(): void { this.pageScope.dispose(); }
+
+  private pageScope: PageReadScope = new PageReadScope();
+  @State private pageError: string = '';
+
   @StorageProp('safeBottom') private safeBottom: number = 16;
   @StorageLink('language') private language: string = 'zh-CN';
   @StorageLink('darkMode') private darkMode: boolean = false;
@@ -22,7 +29,7 @@ struct NotificationSettingsPage {
   @State private saving: boolean = false;
   private lastSaved: PushPreferences = { orders: true, refunds: true, chat: true };
 
-  aboutToAppear(): void { this.load(); }
+  aboutToAppear(): void { this.pageScope.open(); this.load(); }
 
   private context(): common.UIAbilityContext {
     return this.getUIContext().getHostContext() as common.UIAbilityContext;
@@ -35,22 +42,30 @@ struct NotificationSettingsPage {
   }
 
   private async load(): Promise<void> {
+    const pageTicket = this.pageScope.begin('load');
+    this.pageError = '';
     this.loading = true;
     try {
-      const cached = await PushPreferenceStore.read(this.context());
+      const cached = await this.pageScope.read(PushPreferenceStore.read(this.context()), pageTicket);
       this.apply(cached);
       this.lastSaved = cached;
-    } catch (_) {}
+    } catch (_) {
+      if (!this.pageScope.accepts(pageTicket)) return;
+      this.pageError = SecondaryPolicy.errorKind(_);}
     try {
-      const remote = await HarmonyPushClient.getPreferences();
+      const remote = await this.pageScope.read(HarmonyPushClient.getPreferences(), pageTicket);
       this.apply(remote);
       this.lastSaved = remote;
-      await PushPreferenceStore.write(this.context(), remote);
+      await this.pageScope.read(PushPreferenceStore.write(this.context(), remote), pageTicket);
     } catch (_) {
+      if (!this.pageScope.accepts(pageTicket)) return;
+      this.pageError = SecondaryPolicy.errorKind(_);
       IBestToast.show(I18n.text(this.language,
         '当前显示设备上最后一次成功保存的通知设置',
         'Showing the last notification settings saved on this device'));
-    } finally { this.loading = false; }
+    } finally {
+      if (this.pageScope.accepts(pageTicket)) { this.loading = false; }
+    }
   }
 
   private async save(next: PushPreferences): Promise<void> {
@@ -79,7 +94,7 @@ struct NotificationSettingsPage {
     Row({ space: 12 }) {
       Column({ space: 4 }) {
         Text(title).fontSize(15).fontWeight(FontWeight.Medium).fontColor(DesignTokens.text(this.darkMode)).width('100%');
-        Text(description).fontSize(11).fontColor(DesignTokens.muted(this.darkMode)).width('100%').maxLines(2);
+        Text(description).fontSize(12).fontColor(DesignTokens.muted(this.darkMode)).width('100%').maxLines(2);
       }.layoutWeight(1).alignItems(HorizontalAlign.Start);
       Toggle({ type: ToggleType.Switch, isOn: value })
         .selectedColor(DesignTokens.brand(this.darkMode)).enabled(!this.loading && !this.saving)
@@ -91,10 +106,11 @@ struct NotificationSettingsPage {
   build() {
     NavDestination() {
       Column() {
-        IBestNavBar({
+        SecondaryHeader({
           title: I18n.text(this.language, '消息通知', 'Notifications'), isShowStatusBar: true,
           navBarBgColor: DesignTokens.surface(this.darkMode), onLeftClick: () => AppRouter.back()
         });
+          SecondaryFeedback({ errorKind: this.pageError, updating: this.loading, onRetry: () => this.load() });
         Scroll() {
           Column({ space: 16 }) {
             Row({ space: 12 }) {
@@ -109,7 +125,7 @@ struct NotificationSettingsPage {
                   'Enable only the alerts that matter to your business.'))
                   .fontSize(12).fontColor(DesignTokens.muted(this.darkMode)).width('100%').lineHeight(18);
               }.layoutWeight(1).alignItems(HorizontalAlign.Start);
-            }.width('100%').padding(18).backgroundColor(DesignTokens.card(this.darkMode))
+            }.width('100%').padding(16).backgroundColor(DesignTokens.card(this.darkMode))
               .borderRadius(DesignTokens.RADIUS_XL).border({ width: 1, color: DesignTokens.border(this.darkMode) });
             Column() {
               this.toggleRow(I18n.text(this.language, '推送通知', 'Push notifications'),
@@ -138,7 +154,7 @@ struct NotificationSettingsPage {
             Text(I18n.text(this.language,
               '系统级通知权限仍可在 HarmonyOS“设置 → 通知”中调整。',
               'System notification permissions can also be changed in HarmonyOS Settings.'))
-              .fontSize(11).fontColor(DesignTokens.muted(this.darkMode)).width('100%').lineHeight(18).padding({ left: 4, right: 4 });
+              .fontSize(12).fontColor(DesignTokens.muted(this.darkMode)).width('100%').lineHeight(18).padding({ left: 4, right: 4 });
           }.width('100%').constraintSize({ maxWidth: 680 })
             .padding({ left: 16, right: 16, top: 16, bottom: this.safeBottom + 24 });
         }.layoutWeight(1).align(Alignment.Top).scrollBar(BarState.Off);
diff --git a/native/harmony-merchant/entry/src/main/ets/features/profile/ProfilePage.ets b/native/harmony-merchant/entry/src/main/ets/features/profile/ProfilePage.ets
index 763e2770..a812052d 100644
--- a/native/harmony-merchant/entry/src/main/ets/features/profile/ProfilePage.ets
+++ b/native/harmony-merchant/entry/src/main/ets/features/profile/ProfilePage.ets
@@ -1,5 +1,9 @@
+import { SessionStore } from '../../core/storage/SessionStore';
+import { LeaveGuard } from '../../core/ui/LeaveGuard';
+import { PageReadScope, SecondaryPolicy } from '../../core/ui/SecondaryPolicy';
+import { SecondaryFeedback, SecondaryHeader } from '../../core/ui/SecondaryPage';
 import { common } from '@kit.AbilityKit';
-import { IBestActionSheet, IBestButton, IBestField, IBestNavBar, IBestToast } from '@ibestservices/ibest-ui';
+import { IBestActionSheet, IBestButton, IBestField, IBestToast } from '@ibestservices/ibest-ui';
 import { FileUploadService } from '../../core/file/FileUploadService';
 import { I18n } from '../../core/i18n/I18n';
 import { AppRouter } from '../../core/navigation/AppRouter';
@@ -15,6 +19,31 @@ export function ProfileBuilder(_: string) { ProfilePage(); }
 
 @Component
 struct ProfilePage {
+  private locationRequestToken: string = '';
+  private locationOwner: string = '';
+  private openLocation(): void {
+    this.locationOwner = SessionStore.shared().user?.id || '';
+    this.locationRequestToken = `${this.locationOwner}:${Date.now()}:${this.locationEpoch}`;
+    AppStorage.setOrCreate('locationRequestToken', this.locationRequestToken);
+    AppStorage.setOrCreate('pickedLocationToken', '');
+    AppStorage.setOrCreate('pickedLatitude', this.latitude);
+    AppStorage.setOrCreate('pickedLongitude', this.longitude);
+    AppStorage.setOrCreate('pickedLocationSet', this.hasLocation);
+    AppRouter.push('LocationPicker');
+  }
+
+  private handleBack(): void {
+
+    if (this.categorySheetVisible) { this.closeCategorySheet(); return; }
+    this.leaveGuard.request(this.language, this.saving || this.uploading, this.hasChanges(), () => AppRouter.back());
+  }
+
+  private leaveGuard: LeaveGuard = new LeaveGuard();
+  aboutToDisappear(): void { this.pageScope.dispose(); }
+
+  private pageScope: PageReadScope = new PageReadScope();
+  @State private pageError: string = '';
+
   @StorageProp('safeBottom') private safeBottom: number = 16;
   @StorageProp('windowWidth') private windowWidth: number = 390;
   @StorageLink('language') private language: string = 'zh-CN';
@@ -48,9 +77,11 @@ struct ProfilePage {
     I18n.contract('办公'), I18n.contract('户外')
   ];
 
-  aboutToAppear(): void { this.load(); }
+  aboutToAppear(): void { this.pageScope.open(); this.load(); }
 
   private onLocationChanged(): void {
+    if (!this.locationRequestToken || AppStorage.get<string>('pickedLocationToken') !== this.locationRequestToken || this.locationOwner !== (SessionStore.shared().user?.id || '')) return;
+    AppStorage.setOrCreate('pickedLocationToken', '');
     this.region = AppStorage.get<string>('pickedRegion') || this.region;
     this.address = AppStorage.get<string>('pickedAddress') || this.address;
     const latitude = AppStorage.get<number>('pickedLatitude');
@@ -65,10 +96,12 @@ struct ProfilePage {
   }
 
   private async load(): Promise<void> {
+    const pageTicket = this.pageScope.begin('load');
+    this.pageError = '';
     this.loading = true;
     this.loadError = false;
     try {
-      const data = await BusinessRepository.profile();
+      const data = await this.pageScope.read(BusinessRepository.profile(), pageTicket);
       this.original = data;
       this.shopName = data.shopName;
       this.contactName = data.contactName;
@@ -89,12 +122,16 @@ struct ProfilePage {
       AppStorage.setOrCreate<number>('pickedLongitude', this.longitude);
       AppStorage.setOrCreate<boolean>('pickedLocationSet', this.hasLocation);
     } catch (error) {
+      if (!this.pageScope.accepts(pageTicket)) return;
+      this.pageError = SecondaryPolicy.errorKind(error);
       this.loadError = this.original === null;
-      IBestToast.show(error instanceof ApiError ? error.message
+      IBestToast.show(error instanceof ApiError ? SecondaryPolicy.message(error, this.language)
         : I18n.text(this.language, '资料加载失败', 'Failed to load profile'));
     } finally {
+      if (this.pageScope.accepts(pageTicket)) {
       this.loading = false;
     }
+    }
   }
 
   private chooseAvatar(): void {
@@ -119,7 +156,7 @@ struct ProfilePage {
       const urls = await FileUploadService.chooseAndUploadImages(1, 'avatar');
       if (urls.length > 0) this.avatar = urls[0];
     } catch (error) {
-      IBestToast.show(error instanceof Error ? error.message
+      IBestToast.show(error instanceof Error ? SecondaryPolicy.message(error, this.language)
         : I18n.text(this.language, '头像上传失败', 'Failed to upload avatar'));
     } finally {
       this.uploading = false;
@@ -134,7 +171,7 @@ struct ProfilePage {
       const url = await FileUploadService.captureAndUploadImage(context, 'avatar');
       if (url) this.avatar = url;
     } catch (error) {
-      IBestToast.show(error instanceof Error ? error.message
+      IBestToast.show(error instanceof Error ? SecondaryPolicy.message(error, this.language)
         : I18n.text(this.language, '头像拍摄上传失败', 'Failed to capture and upload avatar'));
     } finally {
       this.uploading = false;
@@ -273,7 +310,7 @@ struct ProfilePage {
       IBestToast.show(I18n.text(this.language, '资料已保存', 'Profile saved'));
       AppRouter.back();
     } catch (error) {
-      IBestToast.show(error instanceof ApiError ? error.message
+      IBestToast.show(error instanceof ApiError ? SecondaryPolicy.message(error, this.language)
         : I18n.text(this.language, '保存失败', 'Save failed'));
     } finally {
       this.saving = false;
@@ -304,14 +341,14 @@ struct ProfilePage {
           Text(I18n.text(this.language, `商户号 ${this.original.merchantNo}`, `Merchant ID ${this.original.merchantNo}`))
             .fontSize(12).fontColor(DesignTokens.muted(this.darkMode)).maxLines(1);
           Text(I18n.text(this.language, `状态：${this.original.status || '--'}`, `Status: ${this.original.status || '--'}`))
-            .fontSize(11).fontColor(DesignTokens.brand(this.darkMode));
+            .fontSize(12).fontColor(DesignTokens.brand(this.darkMode));
         }
         Text(this.uploading ? I18n.text(this.language, '头像上传中', 'Uploading avatar')
           : I18n.text(this.language, '点击头像更换', 'Tap the avatar to change it'))
-          .fontSize(11).fontColor(DesignTokens.muted(this.darkMode));
+          .fontSize(12).fontColor(DesignTokens.muted(this.darkMode));
       }.layoutWeight(1).alignItems(HorizontalAlign.Start);
     }.width('100%').padding(16).backgroundColor(DesignTokens.card(this.darkMode))
-      .borderRadius(DesignTokens.RADIUS_LARGE).border({ width: 0.5, color: DesignTokens.border(this.darkMode) });
+      .borderRadius(16).border({ width: 0.5, color: DesignTokens.border(this.darkMode) });
   }
 
   @Builder
@@ -333,7 +370,7 @@ struct ProfilePage {
         placeholder: 'contact@example.com', type: 'email', maxlength: 60, hasBorder: false,
         radius: DesignTokens.RADIUS_FIELD });
     }.width('100%').padding(6).backgroundColor(DesignTokens.card(this.darkMode))
-      .borderRadius(DesignTokens.RADIUS_LARGE).border({ width: 0.5, color: DesignTokens.border(this.darkMode) });
+      .borderRadius(16).border({ width: 0.5, color: DesignTokens.border(this.darkMode) });
   }
 
   @Builder
@@ -345,14 +382,14 @@ struct ProfilePage {
         Column({ space: 4 }) {
           Text(this.categorySummary()).fontSize(13).fontColor(DesignTokens.text(this.darkMode)).maxLines(2).width('100%');
           Text(I18n.text(this.language, `已选择 ${this.categories.length} 项`, `${this.categories.length} selected`))
-            .fontSize(11).fontColor(DesignTokens.muted(this.darkMode)).width('100%');
+            .fontSize(12).fontColor(DesignTokens.muted(this.darkMode)).width('100%');
         }.layoutWeight(1).alignItems(HorizontalAlign.Start);
         MerchantIcon({ name: 'arrow', color: DesignTokens.muted(this.darkMode), iconSize: 17 });
       }.width('100%').constraintSize({ minHeight: 58 }).padding({ left: 14, right: 14, top: 10, bottom: 10 })
         .backgroundColor(DesignTokens.subtle(this.darkMode)).borderRadius(DesignTokens.RADIUS_FIELD)
         .onClick(() => this.openCategorySheet());
     }.width('100%').padding(16).backgroundColor(DesignTokens.card(this.darkMode))
-      .borderRadius(DesignTokens.RADIUS_LARGE).border({ width: 0.5, color: DesignTokens.border(this.darkMode) });
+      .borderRadius(16).border({ width: 0.5, color: DesignTokens.border(this.darkMode) });
   }
 
   @Builder
@@ -366,7 +403,7 @@ struct ProfilePage {
           MerchantIcon({ name: 'location-o', color: DesignTokens.brand(this.darkMode), iconSize: 15 });
           Text(I18n.text(this.language, '地图选址', 'Choose on map')).fontSize(12).fontColor(DesignTokens.brand(this.darkMode));
         }.height(38).padding({ left: 10, right: 10 }).borderRadius(DesignTokens.RADIUS_CONTROL)
-          .backgroundColor(DesignTokens.selectedSurface(this.darkMode)).onClick(() => AppRouter.push('LocationPicker'));
+          .backgroundColor(DesignTokens.selectedSurface(this.darkMode)).onClick(() => this.openLocation());
       }.width('100%').padding({ left: 10, top: 4 });
       IBestField({ value: $region, label: I18n.text(this.language, '所在地区', 'Region'),
         placeholder: I18n.text(this.language, '例：陕西省西安市', 'Example: Xi’an, Shaanxi'),
@@ -375,11 +412,11 @@ struct ProfilePage {
         placeholder: I18n.text(this.language, '请输入详细地址', 'Enter the detailed address'), maxlength: 100,
         hasBorder: false, radius: DesignTokens.RADIUS_FIELD });
       if (this.hasLocation) {
-        Text(`${this.latitude.toFixed(6)}, ${this.longitude.toFixed(6)}`).fontSize(10)
+        Text(`${this.latitude.toFixed(6)}, ${this.longitude.toFixed(6)}`).fontSize(12)
           .fontColor(DesignTokens.muted(this.darkMode)).padding({ left: 10, right: 10, bottom: 6 }).width('100%');
       }
     }.width('100%').padding(6).backgroundColor(DesignTokens.card(this.darkMode))
-      .borderRadius(DesignTokens.RADIUS_LARGE).border({ width: 0.5, color: DesignTokens.border(this.darkMode) });
+      .borderRadius(16).border({ width: 0.5, color: DesignTokens.border(this.darkMode) });
   }
 
   @Builder
@@ -393,10 +430,10 @@ struct ProfilePage {
         .placeholderColor(DesignTokens.muted(this.darkMode)).backgroundColor(DesignTokens.subtle(this.darkMode))
         .borderRadius(DesignTokens.RADIUS_FIELD).maxLength(160)
         .onChange((value: string): void => { this.description = value; });
-      Text(`${this.description.length} / 160`).fontSize(11).fontColor(DesignTokens.muted(this.darkMode))
+      Text(`${this.description.length} / 160`).fontSize(12).fontColor(DesignTokens.muted(this.darkMode))
         .width('100%').textAlign(TextAlign.End);
     }.width('100%').padding(16).backgroundColor(DesignTokens.card(this.darkMode))
-      .borderRadius(DesignTokens.RADIUS_LARGE).border({ width: 0.5, color: DesignTokens.border(this.darkMode) });
+      .borderRadius(16).border({ width: 0.5, color: DesignTokens.border(this.darkMode) });
   }
 
   @Builder
@@ -443,7 +480,7 @@ struct ProfilePage {
           Text(I18n.text(this.language, '选择经营品类', 'Select operating categories')).fontSize(20)
             .fontWeight(FontWeight.Bold).fontColor(DesignTokens.text(this.darkMode));
           Text(I18n.text(this.language, `已选择 ${this.categoryDraft.length} 项`, `${this.categoryDraft.length} selected`))
-            .fontSize(11).fontColor(DesignTokens.muted(this.darkMode));
+            .fontSize(12).fontColor(DesignTokens.muted(this.darkMode));
         }.layoutWeight(1).alignItems(HorizontalAlign.Start);
         Column() { MerchantIcon({ name: 'cross', color: DesignTokens.muted(this.darkMode), iconSize: 18 }); }
           .width(38).height(38).justifyContent(FlexAlign.Center).borderRadius(19)
@@ -485,7 +522,7 @@ struct ProfilePage {
           onBtnClick: () => this.confirmCategories() });
       }.width('100%');
     }.width('100%').height(this.windowWidth >= 600 ? 560 : 520)
-      .padding({ left: 20, right: 20, top: 20, bottom: this.safeBottom + 18 })
+      .padding({ left: 16, right: 16, top: 20, bottom: this.safeBottom + 18 })
       .backgroundColor(DesignTokens.elevatedSurface(this.darkMode))
       .borderRadius({ topLeft: DesignTokens.RADIUS_XL, topRight: DesignTokens.RADIUS_XL })
       .border({ width: 0.5, color: DesignTokens.border(this.darkMode) });
@@ -495,11 +532,12 @@ struct ProfilePage {
     NavDestination() {
       Stack({ alignContent: Alignment.Bottom }) {
         Column() {
-          IBestNavBar({
+          SecondaryHeader({
             title: I18n.text(this.language, '个人与店铺资料', 'Profile and store'), isShowStatusBar: true,
             navBarBgColor: DesignTokens.surface(this.darkMode), titleColor: DesignTokens.text(this.darkMode),
-            leftIconColor: DesignTokens.text(this.darkMode), onLeftClick: () => AppRouter.back()
+            leftIconColor: DesignTokens.text(this.darkMode), onLeftClick: () => this.handleBack()
           });
+          SecondaryFeedback({ errorKind: this.pageError, updating: this.loading, onRetry: () => this.load() });
           if (this.loading) {
             Column() { LoadingProgress().width(34).height(34).color(DesignTokens.brand(this.darkMode)); }
               .layoutWeight(1).justifyContent(FlexAlign.Center);
@@ -508,7 +546,7 @@ struct ProfilePage {
               MerchantIcon({ name: 'replay', color: DesignTokens.brand(this.darkMode), iconSize: 30 });
               Text(I18n.text(this.language, '资料暂时无法加载', 'Profile is temporarily unavailable'))
                 .fontSize(15).fontWeight(FontWeight.Medium).fontColor(DesignTokens.text(this.darkMode));
-              IBestButton({ text: I18n.text(this.language, '重新加载', 'Try again'), type: 'primary',
+              IBestButton({ btnHeight: 44, text: I18n.text(this.language, '重新加载', 'Try again'), type: 'primary',
                 color: DesignTokens.brand(this.darkMode), fontColor: DesignTokens.onBrand(this.darkMode),
                 btnBorderRadius: DesignTokens.RADIUS_FIELD, onBtnClick: () => this.load() });
             }.layoutWeight(1).justifyContent(FlexAlign.Center);
@@ -527,13 +565,6 @@ struct ProfilePage {
           this.categorySheet();
         }
       }.width('100%').height('100%');
-    }.hideTitleBar(true).onBackPressed((): boolean => {
-      if (this.categorySheetVisible) {
-        this.closeCategorySheet();
-        return true;
-      }
-      AppRouter.back();
-      return true;
-    });
+    }.hideTitleBar(true).onBackPressed((): boolean => { this.handleBack(); return true; });
   }
 }
diff --git a/native/harmony-merchant/entry/src/main/ets/features/profile/SettingsPage.ets b/native/harmony-merchant/entry/src/main/ets/features/profile/SettingsPage.ets
index 9299a106..d2543698 100644
--- a/native/harmony-merchant/entry/src/main/ets/features/profile/SettingsPage.ets
+++ b/native/harmony-merchant/entry/src/main/ets/features/profile/SettingsPage.ets
@@ -1,6 +1,8 @@
+import { PageReadScope, SecondaryPolicy } from '../../core/ui/SecondaryPolicy';
+import { SecondaryFeedback, SecondaryHeader } from '../../core/ui/SecondaryPage';
 import { MenuCopy, MenuHeading, MenuRow } from './LocalizedMenu';
 import { common } from '@kit.AbilityKit';
-import { IBestDialogUtil, IBestNavBar, IBestToast } from '@ibestservices/ibest-ui';
+import { IBestDialogUtil, IBestToast } from '@ibestservices/ibest-ui';
 import { AppConfig } from '../../core/config/AppConfig';
 import { PhoneService } from '../../core/device/PhoneService';
 import { I18n } from '../../core/i18n/I18n';
@@ -20,6 +22,11 @@ export function SettingsBuilder(_: string) { SettingsPage(); }
 
 @Component
 struct SettingsPage {
+  aboutToDisappear(): void { this.pageScope.dispose(); }
+
+  private pageScope: PageReadScope = new PageReadScope();
+  @State private pageError: string = '';
+
   @StorageProp('safeBottom') private safeBottom: number = 16;
   @StorageProp('windowWidth') private windowWidth: number = 390;
   @StorageLink('language') private language: string = 'zh-CN';
@@ -29,7 +36,7 @@ struct SettingsPage {
   @State private publicSettings: PublicSystemSettings | null = null;
   @State private loggingOut: boolean = false;
 
-  aboutToAppear(): void {
+  aboutToAppear(): void { this.pageScope.open();
     this.refreshCacheSize();
     this.loadPublicSettings();
   }
@@ -39,7 +46,11 @@ struct SettingsPage {
   }
 
   private async loadPublicSettings(): Promise<void> {
-    try { this.publicSettings = await PublicSettingsRepository.load(); } catch (_) {}
+    const pageTicket = this.pageScope.begin('loadPublicSettings');
+    this.pageError = '';
+    try { this.publicSettings = await this.pageScope.read(PublicSettingsRepository.load(), pageTicket); } catch (_) {
+      if (!this.pageScope.accepts(pageTicket)) return;
+      this.pageError = SecondaryPolicy.errorKind(_);}
   }
 
   private refreshCacheSize(): void {
@@ -165,7 +176,7 @@ struct SettingsPage {
           () => AppRouter.push('AccountSecurity'), false);
       }.width('100%').backgroundColor(DesignTokens.card(this.darkMode))
         .border({ width: 1, color: DesignTokens.border(this.darkMode) })
-        .borderRadius(DesignTokens.RADIUS_LARGE).clip(true);
+        .borderRadius(16).clip(true);
     }.width('100%');
   }
 
@@ -182,7 +193,7 @@ struct SettingsPage {
           () => AppRouter.push('NotificationSettings'), false);
       }.width('100%').backgroundColor(DesignTokens.card(this.darkMode))
         .border({ width: 1, color: DesignTokens.border(this.darkMode) })
-        .borderRadius(DesignTokens.RADIUS_LARGE).clip(true);
+        .borderRadius(16).clip(true);
     }.width('100%');
   }
 
@@ -201,7 +212,7 @@ struct SettingsPage {
           () => AppRouter.push('Agreements', new AgreementRouteParams('collect')), false);
       }.width('100%').backgroundColor(DesignTokens.card(this.darkMode))
         .border({ width: 1, color: DesignTokens.border(this.darkMode) })
-        .borderRadius(DesignTokens.RADIUS_LARGE).clip(true);
+        .borderRadius(16).clip(true);
     }.width('100%');
   }
 
@@ -220,7 +231,7 @@ struct SettingsPage {
           () => this.contactSupport(), false);
       }.width('100%').backgroundColor(DesignTokens.card(this.darkMode))
         .border({ width: 1, color: DesignTokens.border(this.darkMode) })
-        .borderRadius(DesignTokens.RADIUS_LARGE).clip(true);
+        .borderRadius(16).clip(true);
     }.width('100%');
   }
 
@@ -231,17 +242,18 @@ struct SettingsPage {
       Text(I18n.text(this.language, '退出登录', 'Sign out')).fontSize(14).fontWeight(FontWeight.Medium)
         .fontColor(DesignTokens.danger(this.darkMode));
     }.width('100%').height(52).justifyContent(FlexAlign.Center)
-      .backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(DesignTokens.RADIUS_LARGE)
+      .backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(16)
       .border({ width: 1, color: DesignTokens.border(this.darkMode) }).onClick(() => this.confirmLogout());
   }
 
   build() {
     NavDestination() {
       Column() {
-        IBestNavBar({
+        SecondaryHeader({
           title: I18n.text(this.language, '设置', 'Settings'), isShowStatusBar: true,
           navBarBgColor: DesignTokens.surface(this.darkMode), onLeftClick: () => AppRouter.back()
         });
+          SecondaryFeedback({ errorKind: this.pageError, onRetry: () => this.loadPublicSettings() });
         Scroll() {
           Column({ space: 16 }) {
             if (this.windowWidth >= 600) {
@@ -259,7 +271,7 @@ struct SettingsPage {
             Text(I18n.text(this.language,
               '设置更改会保存到本机；通知偏好会同步到服务端。',
               'Settings are saved on this device. Notification preferences sync with the server.'))
-              .fontSize(11).fontColor(DesignTokens.muted(this.darkMode)).textAlign(TextAlign.Center).width('100%');
+              .fontSize(12).fontColor(DesignTokens.muted(this.darkMode)).textAlign(TextAlign.Center).width('100%');
           }.width('100%').constraintSize({ maxWidth: 960 }).padding({
             left: this.windowWidth >= 720 ? 24 : 14, right: this.windowWidth >= 720 ? 24 : 14,
             top: 14, bottom: this.safeBottom + 24
diff --git a/native/harmony-merchant/entry/src/main/ets/features/profile/SharePage.ets b/native/harmony-merchant/entry/src/main/ets/features/profile/SharePage.ets
index 086a0ca2..94e1aaaa 100644
--- a/native/harmony-merchant/entry/src/main/ets/features/profile/SharePage.ets
+++ b/native/harmony-merchant/entry/src/main/ets/features/profile/SharePage.ets
@@ -1,7 +1,9 @@
+import { SecondaryPolicy } from '../../core/ui/SecondaryPolicy';
+import { SecondaryHeader } from '../../core/ui/SecondaryPage';
 import { common } from '@kit.AbilityKit';
 import { pasteboard } from '@kit.BasicServicesKit';
 import { systemShare } from '@kit.ShareKit';
-import { IBestButton, IBestNavBar, IBestToast } from '@ibestservices/ibest-ui';
+import { IBestButton, IBestToast } from '@ibestservices/ibest-ui';
 import { AppConfig } from '../../core/config/AppConfig';
 import { ImageSaveService } from '../../core/file/ImageSaveService';
 import { AppRouter } from '../../core/navigation/AppRouter';
@@ -24,7 +26,7 @@ struct SharePage {
       await pasteboard.getSystemPasteboard().setData(record);
       IBestToast.show(I18n.text(this.language, '下载链接已复制', 'Download link copied'));
     } catch (error) {
-      IBestToast.show(error instanceof Error ? error.message : I18n.text(this.language, '复制失败', 'Copy failed'));
+      IBestToast.show(error instanceof Error ? SecondaryPolicy.message(error, this.language) : I18n.text(this.language, '复制失败', 'Copy failed'));
     }
   }
 
@@ -42,7 +44,7 @@ struct SharePage {
       const controller = new systemShare.ShareController(data);
       await controller.show(this.getUIContext().getHostContext() as common.UIAbilityContext, {});
     } catch (error) {
-      IBestToast.show(error instanceof Error ? error.message : I18n.text(this.language, '分享失败', 'Sharing failed'));
+      IBestToast.show(error instanceof Error ? SecondaryPolicy.message(error, this.language) : I18n.text(this.language, '分享失败', 'Sharing failed'));
     }
   }
 
@@ -63,7 +65,7 @@ struct SharePage {
       );
       IBestToast.show(I18n.text(this.language, '分享海报已保存到图库', 'Share poster saved to Photos'));
     } catch (error) {
-      IBestToast.show(error instanceof Error ? error.message : I18n.text(this.language, '海报保存失败', 'Failed to save the poster'));
+      IBestToast.show(error instanceof Error ? SecondaryPolicy.message(error, this.language) : I18n.text(this.language, '海报保存失败', 'Failed to save the poster'));
     } finally {
       this.saving = false;
     }
@@ -94,18 +96,18 @@ struct SharePage {
   build() {
     NavDestination() {
       Column() {
-        IBestNavBar({
+        SecondaryHeader({
           title: I18n.text(this.language, '分享应用', 'Share app'), isShowStatusBar: true, navBarBgColor: DesignTokens.surface(this.darkMode),
           onLeftClick: () => AppRouter.back()
         });
         Scroll() {
           Column({ space: 16 }) {
             this.poster();
-            IBestButton({
+            IBestButton({ btnHeight: 44,
               text: I18n.text(this.language, '打开系统分享', 'Open system share'), type: 'primary', color: DesignTokens.brand(this.darkMode), fontColor: DesignTokens.onBrand(this.darkMode),
               btnWidth: '100%', onBtnClick: () => this.share()
             });
-            IBestButton({
+            IBestButton({ btnHeight: 44,
               text: I18n.text(this.language, '复制 AppGallery 链接', 'Copy AppGallery link'), plain: true,
               color: DesignTokens.brand(this.darkMode), btnWidth: '100%', onBtnClick: () => this.copyLink()
             });
@@ -120,8 +122,8 @@ struct SharePage {
             Text(I18n.text(this.language,
               '二维码和分享链接均指向正式 AppGallery 详情页。',
               'The QR code and share link both open the official AppGallery listing.'))
-              .fontSize(11).fontColor(DesignTokens.muted(this.darkMode)).textAlign(TextAlign.Center);
-          }.width('100%').padding({ left: 20, right: 20, top: 18, bottom: this.safeBottom + 24 });
+              .fontSize(12).fontColor(DesignTokens.muted(this.darkMode)).textAlign(TextAlign.Center);
+          }.width('100%').padding({ left: 16, right: 16, top: 18, bottom: this.safeBottom + 24 });
         }.layoutWeight(1).align(Alignment.Top).scrollBar(BarState.Off);
       }.width('100%').height('100%').backgroundColor(DesignTokens.page(this.darkMode));
     }.hideTitleBar(true);
diff --git a/native/harmony-merchant/entry/src/main/ets/features/shell/FilteredListRoutes.ets b/native/harmony-merchant/entry/src/main/ets/features/shell/FilteredListRoutes.ets
index d332e630..770793bd 100644
--- a/native/harmony-merchant/entry/src/main/ets/features/shell/FilteredListRoutes.ets
+++ b/native/harmony-merchant/entry/src/main/ets/features/shell/FilteredListRoutes.ets
@@ -1,15 +1,25 @@
+import { SecondaryBackHandler } from '../../core/ui/SecondaryPolicy';
 import { FilterRouteParams } from '../../core/navigation/RouteParams';
 import { OrderListPage } from '../order/OrderListPage';
 import { ProductListPage } from '../product/ProductListPage';
 
 @Builder
 export function FilteredOrdersBuilder(_: string, raw: Object) {
-  NavDestination() { OrderListPage({ initialStatus: (raw as FilterRouteParams).status, showBack: true }); }
+  NavDestination() { OrderListPage({ initialStatus: (raw as FilterRouteParams)?.status || '', showBack: true }); }
     .hideTitleBar(true);
 }
 
 @Builder
 export function FilteredProductsBuilder(_: string, raw: Object) {
-  NavDestination() { ProductListPage({ initialStatus: (raw as FilterRouteParams).status, showBack: true }); }
-    .hideTitleBar(true);
+  FilteredProductsRoute({ status: (raw as FilterRouteParams)?.status || '' });
+}
+
+@Component
+struct FilteredProductsRoute {
+  status: string = '';
+  private backHandler: SecondaryBackHandler = new SecondaryBackHandler();
+  build() {
+    NavDestination() { ProductListPage({ initialStatus: this.status, showBack: true, backHandler: this.backHandler }); }
+      .hideTitleBar(true).onBackPressed((): boolean => this.backHandler.closeOverlay());
+  }
 }
diff --git a/native/harmony-merchant/entry/src/main/ets/features/stats/StatsPage.ets b/native/harmony-merchant/entry/src/main/ets/features/stats/StatsPage.ets
index 3c730591..a018290a 100644
--- a/native/harmony-merchant/entry/src/main/ets/features/stats/StatsPage.ets
+++ b/native/harmony-merchant/entry/src/main/ets/features/stats/StatsPage.ets
@@ -1,3 +1,4 @@
+import { SecondaryBackHandler } from '../../core/ui/SecondaryPolicy';
 import { PrimaryNavigationMetrics } from '../shell/PrimaryNavigationMetrics';
 import { AppRouter } from '../../core/navigation/AppRouter';
 import { ApiError } from '../../core/network/ApiClient';
@@ -14,11 +15,13 @@ import { StatsTrendChart } from './StatsTrendChart';
 
 @Builder
 export function StatsFullBuilder(_: string) {
-  NavDestination() { StatsPage({ showBack: true }); }.hideTitleBar(true);
+  StatsFullRoute();
 }
 
 @Component
 export struct StatsPage {
+  backHandler: SecondaryBackHandler = new SecondaryBackHandler();
+  private back(): void { if (!this.backHandler.closeOverlay()) AppRouter.back(); }
   @Prop @Watch('onActivation') activationEpoch: number = 0;
   showBack: boolean = false;
   @StorageProp('safeBottom') private safeBottom: number = 16;
@@ -43,8 +46,8 @@ export struct StatsPage {
   private reloadPending: boolean = false;
   private readonly periods: string[] = ['today', 'week', 'month', 'year'];
 
-  aboutToAppear(): void { this.disposed = false; this.owner = SessionStore.shared().user?.id || ''; this.load(); }
-  aboutToDisappear(): void { this.disposed = true; this.requests.invalidate(); this.calendarVisible = false; }
+  aboutToAppear(): void { this.backHandler.closeOverlay = (): boolean => { if (!this.calendarVisible) return false; this.closeSheet(); return true; }; this.disposed = false; this.owner = SessionStore.shared().user?.id || ''; this.load(); }
+  aboutToDisappear(): void { this.backHandler.closeOverlay = () => false; this.disposed = true; this.requests.invalidate(); this.calendarVisible = false; }
   private current(epoch: number): boolean {
     return !this.disposed && this.loadEpoch === epoch && this.requests.current(epoch) && this.owner === (SessionStore.shared().user?.id || '');
   }
@@ -164,8 +167,8 @@ export struct StatsPage {
     Column() {
       Row({ space: 6 }) {
         if (this.showBack) Button({ type: ButtonType.Normal }) { MerchantIcon({ name: 'arrow-left', color: DesignTokens.text(this.darkMode), iconSize: 24 }); }
-          .width(44).height(44).backgroundColor(Color.Transparent).onClick(() => AppRouter.back());
-        Text(I18n.text(this.language, '经营数据', 'Analytics')).fontSize(22).fontWeight(FontWeight.Bold).fontColor(DesignTokens.text(this.darkMode)).layoutWeight(1);
+          .width(44).height(44).backgroundColor(Color.Transparent).onClick(() => this.back());
+        Text(I18n.text(this.language, '经营数据', 'Analytics')).fontSize(this.showBack ? 20 : 22).fontWeight(FontWeight.Bold).fontColor(DesignTokens.text(this.darkMode)).layoutWeight(1);
         Button({ type: ButtonType.Normal }) { MerchantIcon({ name: 'question-o', color: DesignTokens.muted(this.darkMode), iconSize: 22 }); }
           .width(44).height(44).backgroundColor(Color.Transparent).accessibilityText(I18n.text(this.language, '统计说明', 'About these figures'))
           .onClick(() => { this.sheetMode = 'info'; this.calendarVisible = true; });
@@ -211,3 +214,12 @@ export struct StatsPage {
         backgroundColor: DesignTokens.card(this.darkMode), onWillDismiss: () => this.closeSheet() });
   }
 }
+
+@Component
+struct StatsFullRoute {
+  private backHandler: SecondaryBackHandler = new SecondaryBackHandler();
+  build() {
+    NavDestination() { StatsPage({ showBack: true, backHandler: this.backHandler }); }
+      .hideTitleBar(true).onBackPressed((): boolean => this.backHandler.closeOverlay());
+  }
+}
diff --git a/native/harmony-merchant/entry/src/main/ets/features/update/UpdatePage.ets b/native/harmony-merchant/entry/src/main/ets/features/update/UpdatePage.ets
index 6a381681..59c37d53 100644
--- a/native/harmony-merchant/entry/src/main/ets/features/update/UpdatePage.ets
+++ b/native/harmony-merchant/entry/src/main/ets/features/update/UpdatePage.ets
@@ -1,6 +1,8 @@
+import { PageReadScope, SecondaryPolicy } from '../../core/ui/SecondaryPolicy';
+import { SecondaryFeedback, SecondaryHeader } from '../../core/ui/SecondaryPage';
 import { common } from '@kit.AbilityKit';
 import { updateManager } from '@kit.AppGalleryKit';
-import { IBestButton, IBestNavBar, IBestTag, IBestToast } from '@ibestservices/ibest-ui';
+import { IBestButton, IBestTag, IBestToast } from '@ibestservices/ibest-ui';
 import { AppConfig } from '../../core/config/AppConfig';
 import { AppRouter } from '../../core/navigation/AppRouter';
 import { ApiError } from '../../core/network/ApiClient';
@@ -18,6 +20,11 @@ export function UpdateBuilder(_: string) {
 
 @Component
 struct UpdatePage {
+  aboutToDisappear(): void { this.pageScope.dispose(); }
+
+  private pageScope: PageReadScope = new PageReadScope();
+  @State private pageError: string = '';
+
   @StorageProp('safeBottom') private safeBottom: number = 16;
   @StorageLink('language') private language: string = 'zh-CN';
   @StorageLink('darkMode') private darkMode: boolean = false;
@@ -34,17 +41,19 @@ struct UpdatePage {
   @State private storeUrl: string = '';
   @State private openingStore: boolean = false;
 
-  aboutToAppear(): void {
+  aboutToAppear(): void { this.pageScope.open();
     this.check();
   }
 
   private async check(): Promise<void> {
+    const pageTicket = this.pageScope.begin('check');
+    this.pageError = '';
     this.checking = true;
     this.errorMessage = '';
     this.genericError = false;
     this.noPublishedRelease = false;
     try {
-      const release: HarmonyRelease | null = await UpdateRepository.latest();
+      const release: HarmonyRelease | null = await this.pageScope.read(UpdateRepository.latest(), pageTicket);
       if (!release) {
         this.latestVersion = AppConfig.CLIENT_VERSION;
         this.latestCode = AppConfig.CLIENT_VERSION_CODE;
@@ -64,11 +73,15 @@ struct UpdatePage {
       this.forceUpdate = release.force;
       this.storeUrl = UpdatePolicy.storeUrl(release, AppConfig.APP_GALLERY_URL);
     } catch (error) {
-      this.errorMessage = error instanceof ApiError ? error.message : '';
+      if (!this.pageScope.accepts(pageTicket)) return;
+      this.pageError = SecondaryPolicy.errorKind(error);
+      this.errorMessage = error instanceof ApiError ? SecondaryPolicy.message(error, this.language) : '';
       this.genericError = !(error instanceof ApiError);
     } finally {
+      if (this.pageScope.accepts(pageTicket)) {
       this.checking = false;
     }
+    }
   }
 
   private visibleError(): string {
@@ -124,10 +137,11 @@ struct UpdatePage {
   build() {
     NavDestination() {
       Column() {
-        IBestNavBar({
+        SecondaryHeader({
           title: I18n.text(this.language, '版本中心', 'Update center'), isShowStatusBar: true,
           navBarBgColor: DesignTokens.surface(this.darkMode), onLeftClick: () => AppRouter.back()
         });
+          SecondaryFeedback({ errorKind: this.pageError, onRetry: () => this.check() });
         Column({ space: 16 }) {
           if (this.checking) {
             Column() { MerchantIcon({ name: 'upgrade', color: DesignTokens.brand(this.darkMode), iconSize: 34 }); }
@@ -140,7 +154,7 @@ struct UpdatePage {
             Text(I18n.text(this.language, '检查更新遇到问题', 'Unable to check for updates'))
               .fontSize(18).fontWeight(FontWeight.Bold).fontColor(DesignTokens.text(this.darkMode));
             Text(this.visibleError()).fontSize(14).fontColor(DesignTokens.muted(this.darkMode));
-            IBestButton({ text: I18n.text(this.language, '重新检查', 'Check again'), type: 'primary', color: DesignTokens.brand(this.darkMode), fontColor: DesignTokens.onBrand(this.darkMode), onBtnClick: () => this.check() });
+            IBestButton({ btnHeight: 44, text: I18n.text(this.language, '重新检查', 'Check again'), type: 'primary', color: DesignTokens.brand(this.darkMode), fontColor: DesignTokens.onBrand(this.darkMode), onBtnClick: () => this.check() });
           } else {
             Column() { MerchantIcon({ name: 'upgrade', color: DesignTokens.brand(this.darkMode), iconSize: 34 }); }
               .width(68).height(68).justifyContent(FlexAlign.Center)
@@ -164,7 +178,7 @@ struct UpdatePage {
             Text(this.visibleChangelog())
               .fontSize(15).fontColor(DesignTokens.text(this.darkMode)).lineHeight(24).width('100%')
               .padding(14).backgroundColor(DesignTokens.subtle(this.darkMode)).borderRadius(14);
-            IBestButton({
+            IBestButton({ btnHeight: 44,
               text: this.hasNewVersion()
                 ? I18n.text(this.language, '前往 AppGallery 更新', 'Update in AppGallery')
                 : I18n.text(this.language, '已是最新版本', 'Up to date'),
@@ -179,7 +193,7 @@ struct UpdatePage {
         .width('92%')
         .constraintSize({ maxWidth: 620 })
         .justifyContent(FlexAlign.Start)
-        .padding({ left: 24, right: 24, top: 24, bottom: this.safeBottom + 24 })
+        .padding({ left: 24, right: 24, top: 12, bottom: this.safeBottom + 24 })
         .backgroundColor(DesignTokens.card(this.darkMode)).borderRadius(DesignTokens.RADIUS_XL)
         .border({ width: 1, color: DesignTokens.border(this.darkMode) })
         .margin({ top: 18, bottom: this.safeBottom + 18 });
diff --git a/native/harmony-merchant/scripts/audit-mutation-safety.mjs b/native/harmony-merchant/scripts/audit-mutation-safety.mjs
index d0d36896..e90acfe6 100644
--- a/native/harmony-merchant/scripts/audit-mutation-safety.mjs
+++ b/native/harmony-merchant/scripts/audit-mutation-safety.mjs
@@ -14,13 +14,13 @@ const guardedContracts = [
   ['entry/src/main/ets/features/auth/SetPasswordPage.ets', ['if (this.submitting) return;']],
   ['entry/src/main/ets/features/auth/ChangePhonePage.ets', ['if (this.saving) return;', 'this.sendingOld || this.oldCountdown > 0', 'if (this.sendingNew || this.newCountdown > 0) return;']],
   ['entry/src/main/ets/features/product/ProductListPage.ets', ['if (this.processingBatch) return;', 'this.processingBatch = true']],
-  ['entry/src/main/ets/features/product/ProductEditPage.ets', ['if (this.saving) return;', 'if (this.uploading || this.saving) return;', 'if (this.skuUploading || this.saving) return;', 'if (this.uploading || this.skuUploading)']],
+  ['entry/src/main/ets/features/product/ProductEditPage.ets', ['if (this.saving) return;', 'if (this.uploading || this.saving) return;', 'if (this.skuUploading || this.saving) return;', 'if (!this.current() || this.loading || this.loadFailed || this.saving || this.uploading || this.skuUploading) return;']],
   ['entry/src/main/ets/features/product/CategoryPage.ets', ['if (this.saving || this.workingId) return;', 'if (this.workingId || this.saving) return;']],
-  ['entry/src/main/ets/features/order/OrderDetailPage.ets', ['if (this.submitting) return;', 'if (this.parsing || this.submitting) return;']],
+  ['entry/src/main/ets/features/order/OrderDetailPage.ets', ['if (this.submitting) return;', 'if (!this.current() || this.parsing || this.submitting) return;']],
   ['entry/src/main/ets/features/order/AfterSalePage.ets', ['if (!this.selected || this.submitting) return;']],
   ['entry/src/main/ets/features/order/AfterSaleDetailPage.ets', ['if (!this.item || this.submitting) return;']],
   ['entry/src/main/ets/features/business/CustomerPage.ets', ['if (this.processingId) return;']],
-  ['entry/src/main/ets/features/business/CommissionPage.ets', ['if (this.saving) {', 'this.dirty = true;']],
+  ['entry/src/main/ets/features/business/CommissionPage.ets', ['if (this.saving) {', 'this.dirty = this.fingerprint() !== this.savedRules;', 'if (!this.loaded || !this.dirty || this.verifying || this.loading) return;']],
   ['entry/src/main/ets/features/business/StorePage.ets', ['if (this.processingId) return;']],
   ['entry/src/main/ets/features/business/StoreAuthPage.ets', ['if (this.saving) return;']],
   ['entry/src/main/ets/features/business/StaffPage.ets', ['if (this.saving || this.actionId) return;', 'if (this.actionId || this.saving) return;']],
@@ -32,7 +32,7 @@ const guardedContracts = [
   ['entry/src/main/ets/features/plaza/AgencyPage.ets', ['if (this.saving) return;', 'if (this.actionId || this.saving) return;']],
   ['entry/src/main/ets/features/member/MembershipPage.ets', ['if (this.purchasing || this.restoring)', 'this.purchasing = plan.id', 'this.restoring = true']],
   ['entry/src/main/ets/features/profile/ProfilePage.ets', ['if (this.uploading || this.saving) return;', 'if (this.saving) return;', 'if (this.uploading) {']],
-  ['entry/src/main/ets/features/business/PriceRulePage.ets', ['if (this.saving || !this.dirty) return;', 'if (this.dirty) this.flushSave();']],
+  ['entry/src/main/ets/features/business/PriceRulePage.ets', ['if (this.saving || !this.dirty) return;', 'if (!this.loaded || this.verifying || this.loading) return;', 'SecondarySaveBar(', 'this.dirty = JSON.stringify(this.currentRule()) !== this.savedRule;']],
   ['entry/src/main/ets/pages/Index.ets', ['UpdatePolicy.canCheck(this.updateChecking', 'if (this.initializing) return;', 'if (this.updateOpening) return;']]
 ]
 
diff --git a/native/harmony-merchant/scripts/test-stats.mjs b/native/harmony-merchant/scripts/test-stats.mjs
index 12a9fe9e..e7944144 100644
--- a/native/harmony-merchant/scripts/test-stats.mjs
+++ b/native/harmony-merchant/scripts/test-stats.mjs
@@ -18,6 +18,7 @@ function compile(source, context = {}, dependencies = {}) {
   return exports
 }
 const models = compile(read('StatsModels'))
+const { SecondaryBackHandler } = compile(fs.readFileSync(path.join(base, '../../core/ui/SecondaryPolicy.ets'), 'utf8'))
 const { StatsSelection: Selection } = models
 const { StatsPolicy: Policy, StatsRequests: Requests } = compile(
   read('StatsPolicy'),
@@ -36,6 +37,7 @@ function controller(name, anchor, extra = {}) {
     StatsPolicy: Policy,
     StatsRequests: Requests,
     StatsSelection: Selection,
+    SecondaryBackHandler,
     Scroller: class {},
     ...extra,
   })[name]
