# 鸿蒙商品编辑与语言刷新

状态：代码及本地构建通过；真机验收外部阻塞。未推送 GitHub，未部署服务器。

## 本次改动

- 商品编辑改为图片、基础信息、价格库存、配送四个分区，固定草稿/审核操作栏。
- 分类通过搜索及完整分类路径选择，确认才应用；SKU、规格组、批量价格和库存通过独立面板编辑，取消不会修改父级数据。
- SKU 图片、启用/停用、零售价、批发价、会员价、库存、尺寸计价及全店价格规则入口均保留。
- 主图/详情图限制仍为 10/20 张。先预览本地图片，再逐张上传；部分失败保留成功项，失败项可单独重试或移除。
- 详情图片使用独立草稿，取消不应用；上传、页面生命周期与账号归属有失效保护。
- 保存失败保留内容；字段错误就地显示并定位对应分区。保留草稿校验、金额计算、审核状态和现有接口契约。
- 导航与系统返回统一处理未保存内容，面板正确阻挡下层点击；页面显示时采用键盘压缩布局，隐藏/离开时恢复原模式。
- 设置分组、菜单行及“我的”服务入口使用直接订阅语言的叶子组件，传递未翻译文案，不传递翻译后的快照。主题摘要、空账号提示和中英文标题按实时语言计算。
- 沿用当前品牌与图片图标，不增加 SVG/字体图标，不重置导航栈或业务数据。

## 证据与复核

1. `focused-tests.txt`：17 项相关检查通过，其中 8 项针对本次编辑逻辑/菜单绑定，9 项复用此前外观、图片和列表可靠性检查。菜单绑定检查是静态检查，不代表真机渲染验收。
2. 本机 DevEco 的最终调试构建通过。编译过程中修正了 ArkTS 类型、构造函数和组件必填属性问题；最终产物及校验和见 `verification.json`。仍有原有 AdaptiveMaterialSurface 等兼容性警告，未将其冒充零警告构建。
3. 外观审计、生成图标审计、`git diff --check` 通过。
4. 修改前的三个原文件备份在 `C:\Users\Administrator\AppData\Local\Temp\jingwei-product-editor-before-20260909`。
5. 源码已同步到 `D:\DevEcoProjects\jingwei-workspace\native\harmony-merchant`；保留该工程原有签名和本地配置。最初哈希差异确认仅为换行格式后才同步。
6. 小程序原有 `project.config.json` 未修改，SHA-256 保持 `12F3A5532F80082D4DED85074F78C91FD36089AD7CD3D48A5FC4F118D0DA4BEB`。

从主仓库根目录复核：

```powershell
node --test native/harmony-merchant/scripts/test-product-editor.mjs native/harmony-merchant/scripts/test-ui-reliability.mjs
node native/harmony-merchant/scripts/audit-appearance.mjs
node native/harmony-merchant/scripts/audit-generated-icons.mjs
git diff --check
```

## 真机待验收

开始检查时设备连接但锁屏；后续 HDC 返回 `[Empty]`。本轮未安装、未卸载、未清除手机数据，也未进行真实商品写入。

设备重新连接并解锁后，使用现有签名覆盖安装本次 HAP；如签名不一致应停止，不卸载重装。

1. 设置 → 主题与语言 → English → 返回一次，核对设置分组、全部菜单及外观摘要均为英文；再返回“我的”核对入口。反向切换与快速切换同样检查。
2. 使用专用测试商品验证新建/编辑、规格值增删、原 SKU 金额与库存保留、停用/启用和尺寸计价。
3. 分类、详情图、规格面板取消不回写；主页面未保存返回确认；保存失败不丢内容。
4. 多图中一张失败时其他成功项保留；重试仅处理指定图片；离开期间旧上传结果不修改新页面。
5. 英文、短屏和键盘弹出时，确认表单可滚动且底部操作可见。

业务保存闭环、真机语言返回与视觉体验仍为待验证；不得仅凭上述自动检查宣称这些项目已验收。
