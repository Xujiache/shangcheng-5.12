# 鸿蒙平台与本机调试配置

记录日期：2026-09-09。基于 `1d07d6dd87be61baddf8c1903fcfd714d51f3205` 及本文列出的本机修改。

## 1. 当前结果

- 已创建「经纬科技」项目和「经纬科技-商家端」HarmonyOS 应用。
- 已开通地图、推送，并将对应能力写入新的调试 Profile。
- 已切换企业签名团队，生成本机调试签名，完成真实本机构建、签名校验和手机安装。
- 已创建推送服务账号，凭证已保存到仓库外的受限目录；未发送推送、未部署后端、未发布应用。
- 应用内购买因商户资料未齐，按确认暂缓。当前不能宣称全部业务可用或达到正式上架条件。

## 2. 华为平台标识

- 团队：辽宁经纬建筑装饰有限公司。
- 项目名称：经纬科技。
- 项目 ID：`101653523864981814`。
- 应用名称：经纬科技-商家端。
- APP ID：`6917615897123628578`。
- Bundle Name：`top.ewsn.jingwei.merchant`。
- 开发者 ID：`10086000966185042`。

地图能力 `com.huawei.service.mapkit`、推送能力 `com.huawei.service.push.base_service` 已保存，并在签名 Profile 中核对。Profile 类型为 debug，关联一台设备，未添加额外 ACL 权限。

应用内购买尚未开通。平台要求商户入驻及相关协议，缺少银行、营业执照、法人、税务和联系人等资料；本次未代填或提交不确定资料，也未接受商户协议。手机上的定位、相机、通知等运行时授权仍需按实际场景由使用者同意，并非后台开通能力后自动拥有。

## 3. 在 DevEco 中继续开发

使用已经打开并完成配置的工程：

`D:\DevEcoProjects\jingwei-workspace\native\harmony-merchant`

DevEco Studio 版本：`26.0.0.621`。这是独立的英文路径工作副本，不是中文主仓库的自动镜像。连接本次已签名的手机，选中 `entry` 后使用 Run；新设备需要更新调试 Profile。

本机 `build-profile.json5` 已配置 `default` 签名，并在产品中指定 `signingConfig: "default"`。IDE 生成的证书、Profile 和密钥库位于：

`C:\Users\Administrator\.ohos\config\`

**该英文路径副本的 `build-profile.json5` 含本机签名材料路径和加密口令，不得提交 Git。** 中文主仓库没有写入这些签名材料。不要为消除工作区差异而直接覆盖本机配置。

未签名配置备份：

`D:\DevEcoProjects\jingwei-local-config\build-profile.unsigned.json5`

## 4. 本次修复和简要验证

修复四处 `ClickEvent.stopPropagation()` 导致的 ArkTS 编译错误，改为消费点击事件的空处理器，不阻断父容器滚动。以下两份源码已同步修改到中文主仓库和英文路径副本：

- `native/harmony-merchant/entry/src/main/ets/features/order/OrderListPage.ets`
- `native/harmony-merchant/entry/src/main/ets/features/order/OrderProductSummary.ets`

证据：

- 首次本机构建定位到四处编译错误；修复后第二次构建输出 `BUILD SUCCESSFUL in 51 s 640 ms`。
- 订单 UI 静态审计、`git diff --check` 通过；没有进行大规模回归或压力测试。
- 官方 `hap-sign-tool.jar verify-app` 输出 `verify-app success`。
- 调试 Profile 签名校验通过，并核对 APP ID、Bundle Name、地图及推送能力。
- 手机卸载、重新安装和启动命令分别返回成功。这里只证明安装成功、启动请求被接受，不代替前台页面及完整业务验收。
- 现有构建警告仍存在，不宣称零警告。英文副本的 IBest `library/BuildProfile.ets` 有构建生成差异，未升级或提交第三方依赖。

构建日志：

`D:\DevEcoProjects\jingwei-local-config\debug-build-fixed-20260909.log`

签名校验日志：

`D:\DevEcoProjects\jingwei-local-config\debug-signature-verification.log`

安装包：

`D:\DevEcoProjects\jingwei-workspace\native\harmony-merchant\entry\build\default\outputs\default\entry-default-signed.hap`

SHA-256：`91B65FB84EE8841F42440E7741AECAF33C1C25ED1E5F053E40B4491BD7509DD1`。

中文主仓库原有 `packages/ledger-mp/project.config.json` 修改保留，未覆盖。本次未提交或推送 GitHub。

## 5. 手机备份与重装

首次覆盖安装因旧版与新版签名不一致被拒绝；在确认允许备份后重装的前提下执行。

已备份可读取的应用目录，覆盖设备 EL1 至 EL4 的应用 base/database 目录；校验设备与本机归档 SHA-256 一致，归档可列出 65 项。

备份：

`D:\DevEcoProjects\jingwei-local-config\device-backup-20260909\app-readable-data.tar.gz`

SHA-256：`43cef16baa8da3ba3501fe65e06db3d3854494560e74bd9e940d781ba4e626c7`。

检查记录、卸载、安装和启动日志保存在同目录。排除了不可读取的系统 `hiappevent` 诊断及 `image_file_cache` 图片缓存；AssetStore 登录凭据不能保证恢复。因此这不是整机无损备份，新版可能需要重新登录。未将旧数据直接覆盖进新版，避免恢复旧会话或写坏当前状态。

归档校验不等于恢复演练，恢复仍需核对应用版本、目录权限和业务数据格式。

## 6. 推送凭证

服务账号：`jingwei-harmony-push`。华为页面显示启用、角色 `N/A`，没有授予管理、发布或财务角色。账号用于本项目推送，未验证平台具有逐密钥仅限 Push 的独立策略，不将用途说明等同于平台强制授权边界。

当前使用凭证：

`D:\DevEcoProjects\jingwei-local-config\huawei-push-service-account.json`

对应配置：

`D:\DevEcoProjects\jingwei-local-config\huawei-push.local.env`

配置使用 `HUAWEI_PUSH_PROJECT_ID`、`HUAWEI_PUSH_SERVICE_ACCOUNT_FILE`、`HUAWEI_PUSH_TEST_MESSAGE=1`。仅适用于服务端；不要把私钥放入 App、Git、聊天或前端环境变量。

当前凭证的项目 ID、平台登记的 Key ID 和本机 RSA 公私钥签名验签均已核对。文件关闭权限继承，仅允许本机 Administrator 和 SYSTEM 访问，默认下载目录中的两份 JSON 已移入受限目录。

下载延迟导致先后登记了两把同账号密钥，两把均保留在平台：当前使用本机生成的密钥；首次生成的凭证另存为 `huawei-push-initial-backup.json`，没有默默撤销。注册响应保存在 `huawei-push-registration-response.json`。如后续精简，应先确认实际使用方，再撤销备用密钥，不能只删除本机文件。

**尚未将凭证配置到运行中的后端，也未进行真实 OAuth 换令牌、设备推送或最终送达验证。** env 文件只是配置材料，不代表服务已自动加载。

## 7. 剩余事项

1. 补齐商户入驻资料和必要协议，再开通 IAP、配置测试商品并联调购买、恢复及最终会员权益。
2. 对齐当前仓库与配套后端的鸿蒙接口缺口。此前核对发现 11 个 HTTP 接口及 1 个 WebSocket 入口尚未完整合入当前仓库；不能通过已有服务器响应或本次 HAP 构建成功宣称缺口已解决。
3. 后端实现确认后，以受控方式配置推送凭证，进行鉴权及测试设备联调；本次没有自动部署或发送消息。
4. 真机人工验证登录、地图、定位授权、通知授权和核心业务。已存在的手机截图不是目标 App 的前台页面，未将其作为通过证据。
5. 上架前单独配置发布证书/Profile、隐私材料、备案和正式验收；当前仅为 debug 调试版，不是上架交付包。

## 8. 官方参考

- [DevEco 自动签名](https://developer.huawei.com/consumer/cn/doc/harmonyos-guides/ide-signing-auto)
- [Push 服务账号鉴权](https://developer.huawei.com/consumer/cn/doc/harmonyos-guides/push-jwt-token)
- [华为 API 凭证管理](https://developer.huawei.com/consumer/cn/doc/start/api-0000001062522591)

以上参考用于说明配置方式；实际完成状态以本文列出的平台页面、签名内容、构建及设备日志为准。
