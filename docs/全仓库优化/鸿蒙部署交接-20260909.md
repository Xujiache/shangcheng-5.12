# 鸿蒙部署交接补充

## 本轮授权与方式

用户要求将本次密钥文件上传 GitHub，并向服务器端 Codex 交接完整部署任务。核对当前仓库为公开仓库后，采用**全量相关文件加密备份、部署凭证经 SSH 单独交接**的方式；不把可使用的私钥放进公开 Git 历史。

本次备份包含 13 项推送及本工程 DevEco 调试材料，路径和恢复方式见 `deploy/huawei-key-backup/README.md`。不包含其他项目凭据、手机业务备份、既有服务器 `.env` 或 SSH 密钥。

## 服务器交接材料

- 推送 JSON：`/root/secure/jingwei-huawei-20260909/huawei-push-service-account.json`。
- 参考 env：`/root/secure/jingwei-huawei-20260909/huawei-push.server.env`。
- 部署任务：`/root/jingwei-huawei-handoff-20260909/DEPLOY_PROMPT.md`。
- 对应 Git 提交：同目录 `github-revision.txt`。
- 文件完整性及权限记录：同目录 `transfer-verification.json`。

敏感目录权限 700、文件 600。参考 env 不代表运行中的后端已经加载；部署任务要求核实当前实例、非 root 容器可读性和只读挂载后再接入。

## 启动服务器任务

在已连接该服务器的 VS Code Codex 对话中发送：

> 请读取 `/root/jingwei-huawei-handoff-20260909/DEPLOY_PROMPT.md`，结合同目录 `github-revision.txt` 和 `transfer-verification.json`，按其中任务完整执行后端整合与部署。直接使用已交接的私有推送凭证文件，不要输出密钥。先保护旧工作区和线上数据，做简要重点验证；IAP 商户资料尚缺，不要假报已开通。

**工具边界：**本机电脑控制工具不支持代操作 VS Code 的 Codex 扩展，当前任务接口也未提供目标服务器任务，因此上述文件准备与传输不等于提示词已自动发送、更不等于后端部署已完成。

本次没有直接覆盖服务器旧工作区、改写运行中的 `.env`、重启业务服务或执行数据库升级。是否已经部署，应以服务器端 Codex 的执行证据另行确认。
