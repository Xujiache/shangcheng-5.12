# TASK — 全范围任务登记

状态：待处理 / 进行中 / 待验证 / 已验收 / 外部阻塞。“已验收”只表示条目注明的验证层级，不意味着正式发布。

## P0 工程底座与风险边界

- ENG-01（待验证）：七包真实 lint/typecheck/test；根检查入口、失败日志、源码指纹和发布证据门禁。已落代码，最终结果见 ACCEPTANCE。
- ENG-02（待验证）：Node 24/pnpm 固定、构建排除测试源、生产入口统一、精简非 root 容器。容器启动属于独立未验收项。
- ENG-03（待验证）：CI 覆盖开发分支和所有 PR，记工测试接入；原生 SDK 构建需要可用 runner。
- AUTH-01（待验证）：三 uni 端刷新单飞、网络失败保留会话、旧账号响应拒绝及延迟跳转保护。
- AUTH-02（待验证）：后端写请求读取最新账号状态；移除旧 token 商家关联回退。
- ENV-01（待处理）：各前端正式地址回退、预发布与开发配置隔离尚未统一；仅数据库测试已加显式目标保护。
- AUTH-03（进行中）：已实现 Redis refresh 原子消费、旧令牌一次性消费、刷新核验最新账号归属与角色、撤销存储失败拒绝放行，相关模拟测试通过（见 QUICK_FIXES.md）。完整跨实例缓存失效、目标 Redis 实例联调、持久化部署及登出后的所有连接与存储验证仍待处理。
- OPS-01（待验证）：live/ready、traceId 一致性、内部错误不回传连接信息、helmet 固定依赖。
- DB-01（待验证）：记工 SQL 升级执行器、dry-run、adopt、结构漂移、事务恢复；已完成一组真实 PostgreSQL 实验，须保留完整证据。
- DB-02（待处理）：全历史 SQL 版本序列和空库基线、全量回填、索引执行计划、逐版本回退兼容。
- HARMONY-01（进行中）：已修复两个 Windows 中文路径审计问题，并接入四项源码检查；本地核对仍发现必需的后端测试与 smoke 命令缺失；禁止验证脚本默连正式域名。缺失业务实现不能以文档补齐代替。

## P1 业务完整性与结构优化

- CORE-01（待处理）：商家/平台/用户大服务按查询、命令、金额、状态和外部适配拆分，保留门面与契约。
- CORE-02（待处理）：订单、发货、取消、库存回补、部分/全额退款、佣金、提现的完整状态、并发与最终对账测试。
- CORE-03（待处理）：公共 DTO/权限/异常类型收敛，输入边界解析，领域算法关键分支覆盖≥90%，不以覆盖率代替业务断言。
- CORE-04（待处理）：WebSocket 对象归属、重连、旧连接隔离和通知策略；上传格式/归属、公开与私有文件分离的全域复核。
- WB-01（进行中）：保留已存在的本地免费闭环，并将计算、流程、落盘/恢复、图标和页面过渡测试接入门禁。
- WB-02（待处理）：记工全实体强类型、云快照回执分页、减少重复扫描/克隆；保留 JSONB 主存储和持久幂等。
- WB-03（待处理）：双设备冲突、到期历史取回、带凭证完整备份中断、CSV 公式注入、多页 PDF/图片与真实分享保存专项。
- UI-01（待处理）：依据 inventory 逐页核对正常/空态/加载/错误/无权限/离线，拆分大页面及统一表单/筛选/弹层。
- UI-02（待处理）：各端返回保留状态、按影响刷新、键盘/安全区、大字体/大金额、PC 焦点/窄屏、图标实际尺寸。
- HARMONY-02（待处理）：31 功能面契约与真实后端补齐，会员/IAP/Push/深链、签名及手机/宽屏验收。
- PERF-01（待处理）：压力数据与业务负载、N+1/索引/分页、内存与监听器泄漏；桌面领域 benchmark 不算真机性能验收。

## P0 正式发布门禁

- RELEASE-01（外部阻塞）：Docker daemon 当前不可用，干净容器启动/依赖验证未完成；不擅自重启桌面服务。
- RELEASE-02（待处理）：可观测指标、告警及手册、后台任务唯一执行、生产镜像/配置分离。
- RELEASE-03（待处理）：持续恢复、异地备份、附件对齐、RPO/RTO 实测和回退演练。
- RELEASE-04（外部阻塞）：设备、签名及第三方测试条件尚未核实可用，不能宣称全端真机或第三方联调完成。
- RELEASE-05（待处理）：隐私声明与真实权限/API/上传数据一致性，应用提审材料复核。

## 实施依赖

基线 → 真实检查/保护性测试 → 核心业务与迁移 → 结构/体验/性能 → 真机/联调/恢复 → 发布评审。P0 未解决不能放行；剩余工作不得被“完成第一批”掩盖。
