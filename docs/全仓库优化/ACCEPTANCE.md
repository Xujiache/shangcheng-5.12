# ACCEPTANCE — 实际验收记录

> **后续快速修复说明（2026-09-09）：** 下文四项全量检查属于快速修复前的历史源码指纹，不代表当前工作区已经重新全量验收。按最新要求，后续只执行相关测试及必要检查，结果见 [QUICK_FIXES.md](QUICK_FIXES.md)；旧证据原样保留，不继承为新版本的发布放行依据。

日期：2026-09-09（Asia/Tokyo）。原始提交：`461c097cb90ebbd77b8cdc91339343fa23a74fa6`；本轮改动尚未提交或推送。源码指纹及每个命令的退出码、耗时、日志校验和以 `evidence/*-report.json` 为准。

**结论：已实现并验证第一批工程底座和若干可靠性修复，不是完整方案已交付，也不是可以正式上线。** 本文件与 TASK 一起阅读；自动化通过只覆盖实际执行的测试，不能将全部业务或设备状态改成已验收。

## 1. 已落地的代码

1. 七个包均提供真实 lint、typecheck 和测试入口；PC 增加独立类型检查，后端语义 lint 与格式检查分离。移除原先输出文字的占位检查。
2. 根级新增质量、单元、集成、构建、源码清单与发布证据检查。报告绑定提交和工作区指纹，执行中改动源码会判为 `source-changed`；日志被替换、证据缺失或过期不能放行。
3. 固定 Node 24.19.0、pnpm 9.0.0 和本次新增工具版本；CI 覆盖全部开发分支及 PR。CI 的 uni-app 构建扩展到现有 H5、微信与 App 中间资源目标。
4. 新增 shared 会话协调器；三个 uni-app 请求适配器接入刷新单飞、一次重试、断网保留登录状态、账号切换拒绝旧响应、刷新结果不覆盖新登录。畸形响应、HTTP 500 不再作为成功数据交给页面。
5. 后端写请求重新校验数据库中的账号状态及商家归属，不从旧 token 复活已清空的商家关联；普通读取仍有 60 秒缓存，尚非全链路即时吊销。
6. 增加 `/health/live`、`/health/ready`，保留原 `/health`；统一请求与异常 traceId，未知内部错误不直接回传底层异常；helmet 正式列为依赖。
7. 统一后端构建产物与启动入口到 `dist/main.js`；Dockerfile 补共享包构建、OpenSSL/证书、运行时 Prisma 生成及非 root 运行。**镜像尚未实际启动验收。**
8. 新增记工版本化 SQL 执行器：默认 dry-run、显式备份确认、完整已有结构登记、字段/约束/索引检查、校验和、防并发锁、事务回滚和漂移拒绝。只完成记工试点，没有冒充全历史 SQL 基线。
9. 建立隔离 PostgreSQL/Redis 验证入口。拒绝正式环境、远程库、非测试库名和 Redis DB 0；全量集成缺少 Redis 会失败，不再跳过 Redis 套件取得绿灯。只启动、停止脚本自己的实例，保留日志与测试数据。
10. 条件编译分别检查 H5、微信与 App 分支，增加 122 个 Vue 模板检查；修正格式整理后四处多语句事件处理器的模板兼容写法。109 个文件的格式整理不算新增业务能力。
11. 鸿蒙 i18n 与编译警告审计修复 Windows 盘符、中文路径解析；四项源码审计纳入根质量检查，未修改 ArkTS 业务或 IBest 子模块。
12. 生成 243 个页面/视图、341 个 Controller 端点的源码清单；另列鸿蒙后端验证脚本缺失的 10 个测试文件及一个 smoke 命令。源码存在性不是接口联调结果。

## 2. 自动验证口径

最终冻结源码复跑：`quality:check`、`test:unit`、`build:verify`、`test:integration` 四个入口均为 passed，指纹完全一致。证据已逐份校验 SHA-256 后归档；发布检查仍为不可发布，阻塞来自完整范围及外部验收。后续代码变化必须重新执行，不能继承本轮结果。

本轮源码指纹：`9a91ab63215086de13e2c2c4c5682cc6a35d25117f32c1c6e454e89d1bbeb58d`。

- 七个包的类型检查、语义 lint、格式及记工计算镜像检查已经实际执行。
- 后端 Jest：28 个套件，329 个用例通过（相比基线增加 12 个用例）。
- shared、PC Vue 组件及三 uni 请求层：26 个 Vitest 用例通过，分别为 4、1、7、7、7。
- 记工已有领域、流程、下料、图标与 12 项页面过渡检查通过；没有据此声明 Android/iOS 真机通过。
- 发布证据、条件编译处理器与升级执行器：7 个 Node 测试通过。
- 隔离 PostgreSQL/Redis：5 个 Jest 集成套件、19 个用例通过；另有 1 个真实 PostgreSQL 升级场景，覆盖并发锁、失败回滚、重试、重复执行、校验和、既有数据及结构漂移。
- 鸿蒙源码审计：248 个源码文件的敏感边界检查、i18n、35 个声明路由与 27 个静态跳转目标、96 个 HTTP/WebSocket 调用契约检查通过。不是 31 功能的设备验收。
- 已实际完成的构建目标包括后端、PC、用户端 H5/微信/App、商家端 H5/微信/App、平台端 H5/App；原生利账使用官方 WXML/WXSS 编译器检查了 41 个路由、53 个 WXML、60 个 WXSS。

App 构建结果是 uni-app 中间资源，**不是签名 APK/IPA**。官方模板编译器成功不是微信开发者工具完整运行、更不是真机业务验收。全部构建已取得相同源码指纹的本地报告；签名包、运行时和设备层级仍未验收。

## 3. 环境与限制

- 本机 Windows、Node 24.19.0、pnpm 9.0.0；数据库实验使用 PostgreSQL 17.11，隔离缓存使用本机既有 Redis 3.0.504 二进制的独立进程及 DB 15。
- 本次没有升级或改动已存在的 PostgreSQL/Redis 服务。临时测试端口为 55448/16389，实例目录在 `.quality/pg-*`、`.quality/redis-*`。
- **本机版本不等于部署版本**：CI 当前定义 PostgreSQL 16、Redis 7，仍须在目标镜像中重新执行。旧版 Windows Redis 不作为部署推荐。
- Docker daemon 不可用；未构建、启动干净镜像，未证明容器依赖、生产启动与就绪检查可用。未擅自重启 Docker。
- CI 配置已经修改，但尚未推送触发 GitHub Actions；本地成功不能表述为云端 CI 成功。
- 保留已有 uni-app/Vite/Vue 依赖组合。构建中的 Sass legacy API、Rollup 注释和较大分包告警仍需后续治理；本轮没有一次性升级所有框架。
- 初次复跑期间出现过并行安装导致的依赖链接变化；对应结果未验收。Windows 原生命令继承输出句柄也曾造成测试等待，现将 PostgreSQL helper 输出重定向到文件并加超时，最终使用冻结源码重新验证。

## 4. 性能只形成桌面领域基线

合成记工 1千/5千/1万笔，各 20 次样本。Windows / Xeon E5-2673 v3，48 逻辑核心：领域操作 P95 为 10.56 / 47.53 / 102.29ms；汇总 P95 为 1 / 8.66 / 10.87ms。

这不是 4核8GB压力环境，不包含微信落盘、附件、真实设备、网络或服务端并发。不能据此宣布达到“万笔本机保存≤1秒”、3秒冷启动、50用户持续负载或 RPO/RTO 目标。原始结果见 `evidence/desktop-domain-benchmark.txt`。

## 5. 仍未完成的工作

- 订单/库存/退款/佣金/提现全状态、并发与最终对账治理；大服务、大页面拆分和全域类型收敛。
- 记工 JSONB 同步扫描与回执分页优化、全实体强类型、双设备冲突、带凭证备份与真实分享保存专项。
- 243 个页面的正常/空态/加载/错误/权限/离线逐页验收；本轮没有重新设计页面、生成图标或交付新的真机截图。
- 全历史 SQL 版本序列、生产基线、回填、执行计划索引、逐版本回退兼容。
- 鸿蒙缺失后端测试/命令与实际实现逐项对齐，31 项功能、Hvigor/Hypium、签名和真机验收。
- 完整环境分离尚未完成：部分前端仍保留既有正式地址回退逻辑；本轮测试均拦截传输或显式使用本机地址，不代表回退逻辑已治理。
- 支付/IAP/Push/短信/地图/私有文件/云同步真实联调；Android/iOS 微信、uni-app 安装包、鸿蒙手机及宽屏设备验收。
- 目标环境压力测试、完整监控告警、持续恢复、异地备份、附件对齐、RPO/RTO 和发布回退演练；隐私与实际数据流复核。

以上既有待实施工作，也有设备/签名/服务配置依赖，不能全部归因于外部阻塞。

## 6. 复测与数据保护

1. 固定源码和锁文件后，先完成 `pnpm install --frozen-lockfile`，不要让安装与测试/构建并行改写依赖目录。
2. 依次执行 `pnpm quality:check`、`pnpm test:unit`、`pnpm build:verify`。
3. 仅 PostgreSQL 验证：`powershell -File scripts/db/test-postgres.ps1`；本机有对应 Redis 二进制时，完整隔离验证：`powershell -File scripts/db/test-local-dependencies.ps1`。可通过参数指定安装目录；端口占用时直接拒绝，不停止原服务。
4. 执行 `pnpm quality:inventory` 和 `node scripts/quality/harmony-backend.mjs` 更新源码证据；后者缺项时应退出非零。
5. `pnpm release:check` 汇总自动证据及外部验收。当前必须拒绝发布，因为完整范围和外部门禁尚未完成；不得手工伪造 passed。

本机利账 `project.config.json` 原有改动已单独备份，SHA-256 保持 `12F3A5532F80082D4DED85074F78C91FD36089AD7CD3D48A5FC4F118D0DA4BEB`。没有重置数据库、重算历史台账、清理账号数据、覆盖工程配置、强推或发布应用。
