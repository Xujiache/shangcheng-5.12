# 鸿蒙订单详情重构

状态：代码、相关检查及本地调试构建通过；真机视觉与业务验收待验证。

## 页面与交互

- 延续商品编辑页的品牌色、生成图片图标、轻量卡片及固定底部操作栏。
- 顶部突出订单状态、下一步说明与订单金额。待付款显示“应付金额”，不再笼统标记为“实付”；退款订单明确保留的是原订单金额。
- 分区展示物流、收货信息、商品明细、金额明细、订单编号与时间、客户备注。仅展示服务端提供的时间，不虚构物流进度。
- 收货卡保留联系客户及复制完整收货信息；复制订单号、地址识别和刷新集中到更多工具。
- 商品图片沿用缓存、失败重试组件，订单详情支持点击查看大图；其他使用商品摘要的页面保持原点击行为。
- 发货入口仅在待发货状态出现；售后相关状态提供通用售后管理入口，不冒充某个具体退款单链接。
- 发货、分享、地址识别使用固定标题和操作栏的独立面板，内容区滚动，覆盖底层点击，支持返回关闭和键盘避让。中英文文案响应当前语言。

## 同步修复

- 地址识别仅用于预览、复制，不再直接覆盖页面上的订单收货地址而造成“已保存”的错觉。
- 分享配置加载期间阻止编辑；关闭后旧请求不再重新打开面板。客户资料默认隐藏，不再在默认分享简介中自动夹带金额。
- 分享字段、有效天数与简介有校验；保留创建、复制链接、系统分享、撤销旧分享。
- 发货和分享写操作有防重复保护；请求返回时检查页面及账号归属，失效结果不更新界面。
- 发货成功后重新读取订单；刷新失败保留上次记录并明确提示，禁用发货直到重新加载。
- 保持现有后端接口、金额、订单状态和签名配置；未修改服务器或数据库。

## 简要验证

8 项针对性测试通过：状态与发货入口、金额标签、分享校验、地址复制不改订单、旧分享请求取消、发货防重复与刷新、失败保留数据、请求代际隔离。

上述测试采用本地仓库接口替身，不代表真实发货、系统分享或真机业务验收。订单 UI、生成图标、外观审计及 `git diff --check` 通过。

本机 DevEco 最终调试构建成功；过程中已修正 ArkTS 类型错误。仍有原有材质组件 SDK 兼容警告，不宣称零警告。产物路径、校验和及源码同步结果见 `verification.json`。

源码已同步到 `D:\DevEcoProjects\jingwei-workspace\native\harmony-merchant`，保留本机签名。原文件备份位于 `C:\Users\Administrator\AppData\Local\Temp\jingwei-order-detail-before-20260909`。

复核命令（主仓库根目录）：

```powershell
node --test native/harmony-merchant/scripts/test-order-detail.mjs
node native/harmony-merchant/scripts/audit-order-ui.mjs
node native/harmony-merchant/scripts/audit-generated-icons.mjs
node native/harmony-merchant/scripts/audit-appearance.mjs
git diff --check
```

## 待真机确认

本轮 HDC 始终返回 `[Empty]`，未安装、未卸载、未清除手机数据。未调用真实发货或分享接口，未推送 GitHub、未部署服务器。

连接并解锁设备后，使用同签名覆盖安装，简测深浅色、中英文、长订单号/地址/大金额、图片预览、键盘弹出和面板关闭；再使用专用测试订单验证发货最终状态、分享可见字段和撤销结果。签名冲突时停止，不卸载重装。
