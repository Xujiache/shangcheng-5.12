# FlyingMouse Format 0.7.10 — Windows 公开版


2026-09-21。本次提供 **Windows 10/11 x64 完整版**：[下载安装包](#)。Lite、macOS、Windows 7 的 0.7.10 安装包未发布；本轮未执行 Microsoft Store 0.7.10 提交。各渠道状态与观察时间见[修复与发布记录](#)。

## 本次修复

- PDF 转 Excel 逐页检查覆盖，补处理混合文档中的扫描表格，无法恢复时明确报错；不再遗漏整页后显示成功。
- OCR 表格保留小数、符号和高置信度原值，避免用推测算式改错数字；PDF 密码完整保留首尾空格。
- 转换过程中切换语言保留进度；预览窗口约束键盘焦点，关闭后恢复焦点。
- 转换显示真实已耗时与当前阶段进度，完成/失败后冻结，保存时间不计入。未知总量显示静止条与无法估算，取消循环动画；阶段完成不等于整个任务完成，ZIP 写入不沿用已处理页数的满额进度。
- 修复约 4.5 MB 密集短段 TXT 转 EPUB 时章节和压缩任务暴涨、可能导致崩溃的问题，改为有界分章、顺序压缩。原生成品用同量级样本验证完整正文、空行、章节顺序和 ZIP 结构；未取得反馈者原文件，不承诺已复现其全部故障。断连或写入失败会停止并清理本次半成品。
- 文本转 EPUB 可选 UTF-8、GBK/GB18030、UTF-16LE/BE。默认自动模式支持 UTF-8 和带 BOM 的 UTF-16；不能按所选编码严格解码时明确报错，允许改选编码重试，不猜测所有无 BOM 编码或静默替换错误字符。
- 清空结果释放已丢弃的临时产物，同时保护保存中、传输中和仍可见的未保存结果。
- 高级结构识别在原生引擎初始化前限制线程数，同一应用中的高级请求排队执行。模型和输入分辨率保持；CPU 消耗降低可能增加转换时间，模型的物理内存峰值仍较高。
- 高级模型在排队结束及每批启动前检查当前可用物理内存，至少需要 5 GiB；不足时明确提示释放内存或使用普通文字提取，避免明知内存不足仍强行加载。该限制不影响 TXT 转 EPUB、普通文字提取等轻量转换。
- 图片、PDF 和上传采用有限资源预算，超额明确提示，上传断流清理本次半成品。
- 延续 Windows 启动入口、启动失败恢复、Office 后台准备与退出清理修复；避免退出任务长期占锁，保留沙箱和同一实例保护。

## 大小与验证范围

安装包 **1,515,803,399 字节，约 1.41 GiB**。完整展开布局 **4,036,049,597 字节，约 3.76 GiB**，较本次 0.7.9 对照布局净减少约 **6.2%**。移除经过转换对照的冗余资源，保留完整引擎和模型；并未缩成小型工具或改为按需下载。展开体积不包括运行缓存、临时结果或文件系统分配差异，也不是运行内存。

已在 Windows 11 验证原生成品启动、退出重开、图片/文档/媒体转换、大文本 EPUB、编码重试、保存和窄窗口交互。高级模型仍可能占用数 GiB 内存；至少 5 GiB 当前可用物理内存的准入检查不能保证任意文件都不超预算。Windows 10、其他硬件和商店签名版新装/升级仍需对应环境验证；不能保证所有电脑兼容或任意文档无损还原。

本地最终验证 **1,033 通过、0 失败、9 跳过**；[最终验证提交 CI](#) 为 Windows 1,024 通过，macOS ARM/Intel 各 1,016 通过，均 0 失败；分别有 13/21/21 项跳过，不计为通过。macOS CI 通过不代表本次提供 macOS 安装包。

安装器来自生产构建 `5d90f84`；`830c4d8` 仅补强两个未打包测试，后续合并和发布文档未改变包内源码。发布沿用已验收的同一安装器，没有将文档或测试提交描述为二进制重建。

文件名：`FlyingMouse-Format-Setup-0.7.10-x64.exe`

SHA-256：

```text
56c022bc5ec1904300c8ffe77f892d0bea09fe157fbbf402589a64decab2587b
```

Windows 安装器未签名，SmartScreen 可能提示。PDF 转 DWG 尚未实现。

音频仅支持普通音乐格式（MP3/WAV/FLAC/AAC 等），不支持其他音乐平台的加密特殊格式。请支持正版音乐，尊重创作者；本工具与各音乐平台无任何关联。
