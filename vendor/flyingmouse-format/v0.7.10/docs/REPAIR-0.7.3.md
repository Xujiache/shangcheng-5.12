# 0.7.3 本地修复与审查记录

2026-09-14，本候选针对不同版本/渠道的白屏、LibreOffice 初始化弹窗和设置迁移 EXDEV 报错，并检查桌面、转换、保存、设置、缓存、安全及打包链路。基于 0.7.2 的提交 `2750b4087f5b31cd8f516d632262ce88820770d0` 在隔离副本实现；源码版本不代表已安装或已上架。

## 已复现并修复

- 本地导航被代理阻断、渲染崩溃或脚本缺失时只有空窗口：应用会话显式直连；补充主进程原生错误提示、用户重试和就绪监测。加载错误页不再记为成功；恢复后的旧提示不重载已恢复工作。
- LibreOffice 配置初始化在深路径/不可写配置位置失败，原生弹窗可没有 stderr 并卡住：采用短、独占且实际可写的配置目录；启动超时结束本次进程树，Store 验证共用策略，失败仅影响 Office。
- EXDEV 回退复制中途失败会损坏设置：保留上一次完整配置供恢复；原有设置降级不阻断转换的行为保留。
- 转换中清空/重新选文件会混入旧请求；慢识别会覆盖新选择：冻结任务入口，按快照执行，并拒绝过期识别/扫描结果。
- 连续同名 Word→Markdown 会覆盖旧图片或继承旧附件：每个结果使用独立图片目录，保存仍保留用户文件名与相对链接。
- 图片 PDF/ZIP 写流错误会结束进程、损坏 ZIP 解压可一直等待：绑定完整流生命周期，将失败交回请求。
- qpdf 缺失时分组拆分忽略每组页数：回退遵循相同分组。加密 PDF 合并与无法解密的拆分回退明确拒绝，不生成空正文伪成功结果。
- 启动清理可能删除超过一天但仍存活实例的结果或同名前缀备份：只检查精确 PID 后缀与确已退出的进程。Office 不再自动删除其他代际缓存。
- 批量保存到不支持硬链接的目标会失败：添加排他复制回退，仍拒绝覆盖同名结果；诊断导出也使用独占临时文件发布，写入失败保留旧文件。
- 转换 API 允许其他 localhost 端口来源：收紧到当前监听的精确 127.0.0.1 origin，保留无浏览器 Origin 的 CLI。
- APPX 默认 PowerShell 5.1 打包时损坏 Node 内联参数：把目录校验写到本次独占脚本文件后执行，实际完成封包与校验。
- 首轮远程 macOS CI 发现 Office 超时清理未建立独立进程组：`execFile` 不转发 `detached`，改为实际创建进程组的进程启动方式，同时保持输出上限和 Windows 精确进程树清理。深 TEMP 冒烟测试也改为按平台检查是否使用过回退目录。

## 验证与发布状态

最终 `npm test`：前置结构测试 104/104，主测试 686 通过、0 失败、12 跳过；合计 790 通过、0 失败。最终 EXE 的 HTTP 转换矩阵 62 通过、0 失败、1 个引擎缺失场景不适用。生产依赖审计为 0 个已知漏洞。包内 64 项关键文件/manifest 内容与最终源码核对通过。

真实 Electron 复现并验证代理、主导航阻断、脚本缺失和渲染崩溃恢复；实际 Windows 原生对话框经点击 Retry 后恢复。真实 UI 验证异步选择竞争、转换冻结、两份 TXT→Markdown 原文、两种双页 PDF 合并。真实 LibreOffice 深 TEMP 配置回退产出 PDF，初始化超时后无本次子进程残留。最终包内 Store Worker 在接近日常用户目录长度的隔离根完成冷缓存（16.656 秒、51 文件校验与真实转换）、暖缓存（0.635 秒）及准备期间非 Office 转换；这不是 Store 容器安装测试。

本轮生成 Windows 10/11 x64 本地候选并分别记录 NSIS、APPX 的构建校验。随后按用户授权同步本地软件、清理旧包并上传 Git；源码同步以包含本记录的远程提交为准。未上传商店或公开发布。README 的版本文件名是待发布候选名称，公开可下载版本以实际 Release 资产为准。

2026-09-14 本地完整程序已更新至 0.7.3（便携目录部署，不是 NSIS 安装流程）：EXE 与 ASAR 和已测最终构建完全一致。使用原有用户配置启动，真实 TXT→Markdown、CSV→PDF 通过；设置文件更新前后 SHA256 一致。桌面快捷方式的程序、工作目录与图标均指向新版，实际通过快捷方式启动并确认界面就绪。旧版本目录仅在以上验证完成后清理，保留原始引擎、用户设置及复现证据。

首轮 GitHub CI 的 Windows 门禁通过（APPX 检查 14/14、源码测试 790 通过、生产依赖审计 0 已知漏洞）；两个 macOS 架构均在上述进程组与路径测试失败。该失败及修复后的 CI 应按各自提交分别核对，不能将首轮 Windows 成功记为跨平台成功。修复运行代码后必须再次整体构建并同步本地程序，交付文件的最终哈希、源码提交及验证结果记录在本轮交付产物清单与更新记录中。

APPX 独立复核覆盖全包 CRC、四段版本、Store Identity/Publisher、x64 架构、EXE/ASAR 与四张原鼠鼠图标。首轮均通过；重新构建后同样执行校验。此包未签名，尚未安装到正常 Store 容器或提交认证。

## 验证边界

- 用户截图来自不同版本/渠道；尚未在原反馈机器或正常 Store 安装容器复测。无法据此保证任意安全软件拦截、损坏系统或磁盘故障均可自动修复。
- 更深的自定义引擎缓存位置仍可能使 LibreOffice 原生程序退出：本次长根的缓存验证返回 3221226505，短根成功；不能仅据该错误码判定具体原生原因，不能将配置目录回退描述为解决所有引擎资源长路径。失败仍拒绝该 Office 引擎，非 Office 保持可用。
- 非 NTFS 保存通过真实文件复制与不支持硬链接错误注入验证，未使用实体 FAT/exFAT 设备；复制完成后才报成功，但断电时不保证新文件原子可见。
- 保留旧 Office 缓存有磁盘占用代价。未知锁、磁盘损坏和断电事务不作额外保证。
- 缺少的 OFD、Gutenberg、特定 OCR/PDF 样本列为跳过，不计通过。普通极大输入仍受既有引擎和内存约束，未把占位上限宣传为无限能力。
- Win7/macOS 未构建或做设备验收；保留原鼠鼠 UI、主题、中英切换与现有格式边界。

历史记录：[0.7.2](REPAIR-0.7.2.md)。本版不会回写历史测试结论。
