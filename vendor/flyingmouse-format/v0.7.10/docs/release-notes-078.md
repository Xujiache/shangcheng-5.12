# FlyingMouse Format 0.7.8 启动兼容性候选


当前渠道：公开功能版。本轮修复启动资源读取和 DLL 加载权限的兼容问题，改善日志不可写、早期配置错误提示和原生入口一致性；保留 GPU 与渲染器沙箱。两个渠道已通过受控目录权限故障下的真实 GUI 启动回归。

本机已取得 ICU 读取被拒绝导致 GPU 子进程崩溃的对照证据。它不证明客户现场根因已确认；新增程序及安装包须经过本轮独立验收。保留 0.7.6 的转换范围与质量检查，暂停的 0.7.7 PDF → DWG 不纳入本版。

详细实现状态、验证要求与未测边界见 [修复记录](REPAIR-0.7.8.md)。当前不是 GitHub Release 或 Microsoft Store 发布；Win7、macOS、Store 与客户现场结果不能从本候选版本推断。
