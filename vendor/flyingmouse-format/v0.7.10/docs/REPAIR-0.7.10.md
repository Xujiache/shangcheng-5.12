# 0.7.10 修复与发布记录


## 当前渠道状态（2026-09-21）

本页是现役版本、渠道状态与产物来源的统一记录。用户可见变化见[发布说明](release-notes-0710.md)，具体操作见[发布流程](RELEASE.md)。

| 事实面 | 已验证状态与边界 |
| --- | --- |
| GitHub 正式版 | [v0.7.10](#)，北京时间 2026-09-21 03:10:02 公开设为 Latest，非草稿、非预览。13:30 再次回读四个资产的名称、大小和服务端 SHA-256，均匹配已验收文件；版本徽章为 v0.7.10。 |
| 源码与 CI | PR #78 已合并为 `92f26a5`；发布标签固定为 `8eabd89a54c32997a2c3ecc6f21649171614859f`，[发布提交 CI](#) 三平台成功。main 可继续包含文档收尾提交，不因此移动发布标签或重写构建来源。 |
| 公开安装器 | Windows 10/11 x64 完整版 `FlyingMouse-Format-Setup-0.7.10-x64.exe`，未签名；本地原件和上传副本的完整摘要相同。03:10 的匿名下载核验确认 Latest 跳转、安装器前 1 MiB 和响应总大小，三个小资产完整下载哈希一致；不声称匿名重新下载了整份 1.5 GB 安装器。 |
| 本地 Store 候选 | `FlyingMouse-Format-0.7.10-x64-unsigned.msix`，`0.7.10.0` / x64，2,000,120,675 字节；ZIP/CRC、身份、鼠鼠图标、入口/Runtime/ASAR 与已测成品一致，尚未签名。此候选没有本轮上传、认证、公开推送或微软签名安装凭证。 |
| Microsoft Store 现场 | 本次 13:30 知识收尾无法连接后台，当前状态为待复核。最后有效总览快照为 2026-09-21 02:26:54：Submission 16 在认证，Submission 15 为 Rollout halted；此前包页读取为 0.7.9.0。不能将该快照当作持续有效的状态或据此推断客户已安装版本。 |
| 其他平台 | 本次未交付 0.7.10 Lite、macOS 或 Win7 安装器。macOS CI 通过不等于安装包或实体设备验收。 |

本地 MSIX SHA-256：`557d96d3938bfd79201beb4784e8ad6d69993dca984bb72edcb9fefcc45b5d70`。Identity 为 `488B6338.354574AC174AD`，Publisher 为 `CN=56D7A4A1-B653-4162-A5E2-7D9B3F3754AD`；每次商店提交仍需现场对照。

## 成品来源与验收边界

已在 Windows 11 完成原生成品启动、退出重开、实际转换、TXT/GBK EPUB 全文和结构、PDF 漏页/数值/密码、保存与清空、窄窗口交互及资源检查。安装器为 1,515,803,399 字节，SHA-256 `56c022bc5ec1904300c8ffe77f892d0bea09fe157fbbf402589a64decab2587b`。完整展开布局 4,036,049,597 字节（约 3.76 GiB），比本次旧 0.7.9 对照布局净减少约 6.2%；运行缓存与文件系统分配另计，不能称为轻型应用或运行内存降至此数值。

生产安装器基于 `5d90f84`。后续 `830c4d8` 只修改两个未打包测试，覆盖 Windows 文件流的 `_writev` 路径；其余 305 个冻结文件保持相同。合并提交 `92f26a5` 与该验证提交树相同，本次发布文档补记不改包内源码，不重新编译已验收安装器。本地验证 1,033 通过、0 失败、9 跳过；对应 CI Windows 1,024、macOS 两架构各 1,016 通过，均无失败，跳过项分别为 13/21/21。

待完成的是 Windows 10、低内存/其他硬件、微软签名版新装/升级及具体客户启动场景的对应验证；未取得反馈者原始 TXT，不承诺覆盖所有客户崩溃。CAD 功能仍暂停；本地专用音乐模块不属于公开交付。

验收、发布和知识收尾分别保留凭证；任务工作区入口见 [HANDOFF](HANDOFF.md)。生成记忆本轮只读，未改写旧快照；工作树、封存成品和唯一复现证据保留。清理对象仅做预览，清场另行确认。下文为候选阶段历史，“候选”“待验”“尚未发布”描述当时时点，不作为当前待办。

## 候选阶段记录（2026-09-20 至 2026-09-21）

2026-09-20，基于 0.7.9 r2 的真实审查反例修复。0.7.10 为新的本地候选，不能与已提交商店认证的 0.7.9 混用；本文件不表示已经发布。CAD 工作仍暂停，平台加密音乐模块不进入公开版。

### 内容与交互

- PDF→Excel 对每页检查原生提取质量和覆盖，保留已成功的表格，再处理缺失页；未能读出扫描页时明确失败，不能跳页后报告成功。六页原样本的扫描第六页现有可编辑表格，前五页原文保留。
- 旧 OCR 表格后处理保留小数、符号和原始数字语义，每个数值目标分别检查置信度，不用一格低置信度授权改写另一格高置信度数据。
- PDF 加密与解密均保留密码首尾空格，不静默修改密码。
- TXT→EPUB 大文本修复：旧分章判断达到 99 章后会将每个后续短段落单独成章，`yazl.addBuffer` 又同时启动各章压缩。约 4.5 MB、49,999 段的实际原生成品复现约两秒达到工作集 2,999 MiB / 私有提交 3,795 MiB，审计资源护栏主动停止其自有进程树；这不是已取得客户原文件的自然崩溃转储。候选改为按完整字符分段、惰性顺序生成与压缩 XHTML，保留正文、空行及目录顺序，报告实际已处理章节。HTML 保持标签结构、Markdown 保持原标题分章。写入失败与取消拒绝成功、释放自有半成品；HTTP 断开已接到 EPUB 转换的取消信号。最终资源和正文对照以 r2 成品验收为准。
- 语言刷新保留活动转换进度并翻译当前状态；预览约束键盘焦点。
- 同时修复文本转 EPUB 固定按 UTF-8 解码造成的 GBK 静默乱码。源文件编码默认为 UTF-8/UTF-16 BOM 自动模式，无法完整解码返回 422；用户明确选择 GBK/GB18030、UTF-16LE/BE 后按指定编码读取。输入采用可取消的流式累计预算（依据设备预算、上限 64 MiB），禁止边读边替换错误字符或截断内容；CSV/TSV 转 EPUB 使用相同解码策略。该选项不宣称自动辨别所有无 BOM 编码，也不改变其他转换目标的解码规则。
- 转换显示实时已耗时，并在成功或失败时保留本次耗时。进度条按实际上传量、已完成页数或媒体处理时间显示当前阶段进度；引擎不能提供总量时明确显示阶段和无法估算，不按计时器虚构百分比，不循环播放。批次完成数与当前阶段分开，输出验证及注册完成后才报告转换成功。
- 最终复核还修正输出打包阶段的计数：PDF 页面或图像帧处理完成后，ZIP 打包和输出写入仍属转换过程，不将页数或帧数的满额计数继续显示为验证完成。实际输出验证由服务端在文件写完后进入。
- 清空结果通过精确结果 ID 释放服务端产物；保护保存操作、正在传输的响应、共享资产和仍可见的未保存结果。仅删除登记时捕获且身份未变的自有文件，不递归清理用户新增内容。

### 资源预算

高级结构识别在启动子进程前设置经过实测的线程限制，并串行执行完整结构识别请求，避免同时加载多套模型。模型及输入分辨率保持，性能数据与耗时取舍见最终验收记录，不能宣称识别精度百分之百或模型驻留内存已消除。

高级模型另设加载前的可用物理内存门槛：至少 5 GiB。这是根据约 3.4 GiB 工作集实测并预留余量的最低准入检查，不是任意文档的峰值上限。队列出队及每批引擎启动前重新读取内存；不足时明确提示释放内存或选择已有的普通文字提取，不静默降低识别质量。4 GB 内存设备会被阻止加载此高级模型，普通文字提取和 TXT 转 EPUB 不受这个独立门槛影响。

普通图片、合并图像、通用 PDF 页数采用有限的设备内存预算，Sharp 保留像素保护；超预算会明确拒绝并提示拆分，不自动压缩、跳页或修改源文件。上传以流式累计量约束临时磁盘预算，并保留输出空间；超额和断流移除自有半成品。Windows Legacy 运行时缺少 `fs.statfsSync` 时使用实际磁盘空闲查询，不降级成无限制上传。

Office 泄漏测试按本次独立 profile 归属检查进程，枚举异常必须使测试失败；路径预算测试在宿主解析后把相对布局投影到 Windows，不混用 POSIX 与盘符路径。

### 验收与分发边界

保留审查红样本和修复输出，使用相同真实样本比较页数、单元格、密码、源文件摘要及 UI 状态。最终全量测试、成品 EXE/ASAR、资源摘要、启动/退出、资源采样与包体积须由当次构建回执确认，定向测试通过不代替整包验收。

2026-09-20 现场读取商店：Submission 16 有 0.7.9.0 x64 包，仍在认证；渐进更新起始比例 0%，Submission 15 为 Rollout halted。本机商店版仍为 0.6.10.0。此状态会变化，0.7.10 不能从上述状态推断已提交或已向用户分发。

2026-09-21（北京时间）再次读取总览，Submission 16 仍在认证，Submission 15 仍为 Rollout halted；本机 `Get-AppxPackage` 仍为微软商店签名的 0.6.10.0。用户原始 `startup-17148.log` 显示本地音乐 bootstrap 0.7.4、GPU 子进程反复退出及最终 0x80000003；它不是可用于证明商店 0.7.9 启动失败的版本日志。保留所有已复现的启动、Office 准备、权限和退出修复，客户故障仍需按实际版本及错误分类，不能把上传、认证、推送和客户端升级合并为一个状态。

上述本机 0.6.10 查询记录为 00:07 时点；随后本轮再次查询当前账号时已没有该应用注册，旧安装路径也不存在，原因未确认，本轮没有执行安装或卸载。两次观察分别保留在 `startup-followup-20260921.json` 和 `store-local-registration-recheck.json`，不能将先前版本记录当作持续有效的当前安装状态。

首次 0.7.10 成品与提交 `9f563d2` 已通过对应测试和构建，但尚未包含后来反馈的 TXT→EPUB 修复，不能交付为本轮最终修复。加入 EPUB 修复后使用新的独立源码快照与 `FlyingMouse-0.7.10-Public-r2` / `FlyingMouse-0.7.10-Store-r2` 产物；完整测试、进程启动、真实大文本和其他转换必须针对这一份重新验证。

本地审查证据：`work/product-audit-20260920/`；修复与最终验证证据：`work/product-repair-20260920/`。真实微软签名新版本安装/升级、Windows 10/低内存/其他硬件仍需相应环境验证，不能由本机成功启动替代。
